/-
Copyright (c) 2026 The Salt project contributors. Released under the Apache
License, Version 2.0; see `Salt/Entropy/LICENSE-PFR-Apache-2.0`.

# λ-BV wave 2-S, step F5 (β) — THE GRADED SIBLING LANE: `StridePairReceipt` regenerated at the
# road's threaded pin `1/(838400·2^11·h²)`, the door handed out at `ρ ≤ 1/(837782·2^11·h²)`

THE DEMAND (F5 price §1, at the landed object): the affine head composes the crown's door
`a·Zr·ρ + E` with the entropy half's `∀ ρ' ≤ δ₀_aff, door ρ' → ¬ fails` only if
`a·Zr·ρ + E ≤ δ₀_aff`; with `ρ ≤ 1/(837782·k²)`, `Zr ≤ 1.02`, `δ₀_aff ≥ 1/(838400·k²)`, `k = a·h`,
that is MISSED by `1.0208·a`.  `GradedAffHeadAt` (`StridePrize.lean:166`) states the head at
`ρ ≤ 1/(837782·2^11·k²)`, where it CLOSES (`log_chowla_aff_composed_of_headG`); its producer is
this file's crown fed into the graded Entropy head (`StrideShellG`, `StrideGradeReceipt`).

THE MECHANISM (the ruling's β, 2026-09-04 19:18, after α′/α″/α‴ were withdrawn): the landed lane
mints its door at the head's own `δ₀ = cD3/(16C)·ε/4 = 1/(128000·h²·(1+8·log 2))` and threads that
`δ₀` — a bound variable of every form — down the road; the road's grade is PAID at the envelope
`doorRhoOfDelta (s12DeltaSock δ₀ Kc)`, linear in `δ₀`.  The pin `1/(838400·h²) ≤ δ₀` is an EXPORT
CONJUNCT of the six forms and the door-head's own mint; the replays READ it at exactly two names
(`s15ArmH_log_le`, `s15_sel''_L_gk_witness_flat_bumped_win_h`) and forward it everywhere else.
So the graded lane is the landed text with FOUR substitutions and NOTHING landed moved:
  (i)   the six forms `FlatXFormHG`: `838400 * (h : ℝ) ^ 2 ↦ 838400 * 2 ^ 11 * (h : ℝ) ^ 2`;
  (ii)  the door-head `flat_door_head_xceil_h_g`: the mint `δ₀ ↦ δ₀ / 2 ^ 11`, its two numerals at
        `838400 * 2 ^ 11` (floor) and `837782 * 2 ^ 11` (ceiling);
  (iii) `MRTDoorReceiptSetG` and the receipts/crown: `837782 ↦ 837782 * 2 ^ 11`;
  (iv)  the two consumed names ↦ their graded twins (`StrideGradeWalls`), the numeral walls the
        census of the envelope found (`2^581 → 2^592`, `403 → 411`, `417 → 425`, the bump at `2^11`,
        the arm's `c`-ceiling at `2^11·1201216`) each re-cut by substitution with its slack stated.
Every declaration below is the landed one (`StridePairReceipt.lean`, salt `021c22c1`) with the
substitution printed in its docstring; the five replays' bodies are the landed bodies VERBATIM
except the two name swaps; the crown's body is the landed crown's verbatim.  ⟦WHICH FORMS THE
CROWN READS⟧ (the helm's 18:38 question, answered at the object): the crown reads THIS file's six
forms, five replays and door-head through `mrtUniformityXiL2AffSet_holds_flat_floor` →
`mrtUniformityXiL2Set_holds_flat_floor` → `flat_chain_generic_h`; it does NOT call the three
`S16FlatTerminalLinearLH` hop forms, `S16FlatTerminalExitH`'s exit, `S16UniformLH` or any of
`S16ComposeLH`'s export sites — those are the `h`-lane's OWN terminals (the `h`-prize
`logChowla2_v7_rated_h` reads them), from whose STATEMENTS the six forms were generated, and the
replays re-prove their bodies at a generic `P`.  The five `S16FlatTerminalLinearLH` helpers the
replays call (`s13_smallGradeFits_of_halfWindow_L_gk_h`, `s13_gate8_L_gk_h`,
`doorBaseFrame_at_socket_LH`, `s15_gRows_const_at_socket_flat_doorLH_gk`,
`s13_g2_jfloor_of_MSelect'_L_gk_shift28`) are `δ₀`/`ρ`-generic (no `838400`/`403`/`417` in any of
their statements).  `V7RatedH`/H3 are untouched by construction: nothing here is imported by them.

HONEST LABEL.  A second terminal that the crown alone reads: the landed lane keeps serving the
`h`-prize at `1/(837782·h²)`, this lane serves the affine composition at `1/(837782·2^11·h²)`.
The price is `+7.62` nats on every `ρ`-charge (`403 + 2·log h ≤ 417` becomes `411 + 2·log h ≤
425`) against cap lines with `≥ 449×` of room and one binding window line with `4·10⁻⁴·e^{3.2A}`
of room; the door itself gets 2048× finer for free through `_mono`.  Statement-only at the
freeze; NO executor fires before the helm's refuter verdict.  ⛔ MERGE FENCE (iron rule 2).
Nothing here bears on twin primes: the prize this lane feeds (`StrideGradeReceipt`) is `z`-rough
+ odd `Ω(n(n+2))` at `primorial z ≤ 548`, Tao Thm 2.3 at one class, nothing on the apex.

Degenerate values: as at `StridePairReceipt` — `a = 0` excluded by `1 ≤ a`; `h = 0` by `0 < h`;
at `a = 1`, `Xi := bigXiH h` the graded anti-drift instance `mrtUniformityXiL2H_holds_flat_g` is
the plain `h`-door 2048× finer; `K = 0` trivialises the counts.  At `h = 1` the graded pin is
`1/(838400·2048) = 5.82·10⁻¹⁰`, still `≥ 1/2^31 = 4.66·10⁻¹⁰` (the walls' floor).
-/
import Salt.MR.StridePairReceipt
import Salt.MR.StrideGradeWalls
import Mathlib

-- The same three `open private` lines as `StridePairReceipt.lean:87-97` — the door-head and the
-- road-exit replay are the same bodies and reach the same private names.
open private flatCapH_shuffle from Salt.Entropy.Chowla.HloExportFlatH
open private xceil_flat_P xceil_flat_step from Salt.MR.XCeil
open private flatRootCapH_arc_k from Salt.MR.S16ComposeLH

noncomputable section

open scoped BigOperators
open MeasureTheory
open Salt.Entropy.Chowla

set_option exponentiation.threshold 4000

namespace Salt.MR

/-! ## §4 — THE FORMS, GRADED: the six `StridePairReceipt` forms with the pin at `838400 · 2^11`. -/

/-- **⟦H0 FORM, GRADED⟧ (def).** `FlatHeadFormH` (StridePairReceipt.lean:902, 021c22c1) with the ONE
substitution `838400 * (h : ℝ) ^ 2 ↦ 838400 * 2 ^ 11 * (h : ℝ) ^ 2` on the export conjunct — the
road's threaded pin at the graded grade; every other byte identical. ⟦SUBSTITUTED LINES⟧ source 904.
-/
def FlatHeadFormHG (h : ℕ) (Xi : XiFamily) (P : ChowlaRegime → Prop) : Prop :=
    ∃ (ε : ℚ) (K δ₀ β : ℝ) (Hopq : ℕ), 0 < ε ∧ 0 < K ∧ K ≤ 2 ^ 539 ∧ 0 < δ₀ ∧
      1 / (500 * (h : ℚ)) ≤ ε ∧ 1 / (838400 * 2 ^ 11 * (h : ℝ) ^ 2) ≤ δ₀ ∧ 0 < β ∧
      ∀ A : ℝ, 26 ≤ A → budgetAFlat (ε : ℝ) β ≤ A →
        ∃ Hcap : ℕ,
          Hcap = max (flatDesignFloor A)
            (max (max Hopq (budgetFloorFlat (ε : ℝ) β A)) (4 * ⌈(1 / ε : ℚ)⌉₊ ^ 4)) ∧
          ∀ (a extraFloor U1floor : ℕ) (g : ℕ → ℕ → ℕ), 1 ≤ a → a ≤ 1096 →
            XCeilRider ε (fun Hhi ω => a * g Hhi ω) → ∃ R : ChowlaRegime,
            R.eps = ε ∧ extraFloor ≤ R.Hlo ∧ U1floor ≤ R.Hlo ∧ a * g R.Hhi R.ω ≤ R.x ∧
            StrideScale a R ∧
            Real.log ((R.x : ℕ) : ℝ) ≤ 31 / (ε : ℝ) * ((R.Hhi : ℕ) : ℝ) ∧
            (∀ H : ℕ, ∀ [NeZero H], R.Hlo ≤ H → H ≤ R.Hhi →
              ((Xi R.eps H).card : ℝ) ≤ K) ∧
            (50 ≤ Real.log (Real.log (R.Hlo : ℝ)) →
              Real.log (Real.log (R.Hhi : ℝ))
                ≤ Real.exp (Real.log (Real.log (R.Hlo : ℝ)) / 2)) ∧
            R.Hlo ≤ max Hcap (max extraFloor U1floor) ∧
            ∀ ρ : ℝ, 0 < ρ → ρ ≤ δ₀ → MRTUniformityXiL2Set Xi R ρ →
              P R

/-- **⟦H1 FORM, GRADED⟧ (def).** `FlatRoadExitFormH` (:927) with the pin at `838400 * 2 ^ 11` ;
nothing else moves. ⟦SUBSTITUTED LINES⟧ source 930. -/
def FlatRoadExitFormHG (h : ℕ) (P : ChowlaRegime → Prop) : Prop :=
    ∃ (Cg : ℝ) (ε : ℚ) (Kb δ₀ β : ℝ) (Hopq : ℕ), 1 ≤ Cg ∧ Cg ≤ 2 * 10 ^ 12 ∧
      0 < ε ∧ 0 < Kb ∧ Kb ≤ 2 ^ 539 ∧ 0 < δ₀ ∧ 1 / (500 * (h : ℚ)) ≤ ε ∧
      1 / (838400 * 2 ^ 11 * (h : ℝ) ^ 2) ≤ δ₀ ∧ 0 < β ∧
      ∀ (K : ℕ) (A : ℝ), 162 ≤ A → budgetAFlat (ε : ℝ) β ≤ A →
        ∃ Hcap : ℕ,
          Hcap ≤ max (flatDesignFloor A)
            (max (max Hopq (budgetFloorFlat (ε : ℝ) β A)) (4 * ⌈(1 / ε : ℚ)⌉₊ ^ 4)) ∧
          ∀ (a U1floor : ℕ) (g : ℕ → ℕ → ℕ), 1 ≤ a → a ≤ 1096 →
            XCeilRider ε (fun Hhi ω => a * g Hhi ω) →
            ∃ R : ChowlaRegime, R.eps = ε ∧ U1floor ≤ R.Hlo ∧ a * g R.Hhi R.ω ≤ R.x ∧
              StrideScale a R ∧
              Real.log ((R.x : ℕ) : ℝ) ≤ 31 / (ε : ℝ) * ((R.Hhi : ℕ) : ℝ) ∧
              (50 ≤ Real.log (Real.log (R.Hlo : ℝ)) →
                Real.log (Real.log (R.Hhi : ℝ))
                  ≤ Real.exp (Real.log (Real.log (R.Hlo : ℝ)) / 2)) ∧
              R.Hlo ≤ max Hcap U1floor ∧
              ∀ (δ Bceil : ℝ) (RS : ℕ → ℕ → ℝ) (RSan RStr Braw : ℕ → ℝ) (M k j₀ : ℕ),
                M4DoorGates_L_gk K Cg R M k δ → 1 ≤ M →
                (∀ H : ℕ, 0 ≤ RSan H) → (∀ H : ℕ, 0 ≤ RStr H) → (∀ H : ℕ, 0 ≤ Braw H) →
                (∀ j H : ℕ, j₀ ≤ j → RS j H ≤ RSan H) →
                (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi → ((h : ℝ) * arcDen 12 H) ^ 7 ≤ RStr H) →
                (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi →
                  44 * RSan H + 87 * ((h : ℝ) * arcDen 12 H) ≤ (4 / 3 : ℝ) ^ j₀) →
                (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi →
                  128 * ((h : ℝ) * arcDen 12 H) ^ 3 ≤ (H : ℝ)) →
                (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi →
                  (h : ℝ) * arcDen 12 H < ((calP (AdoorL M) (s13GK K M) 1 : ℕ) : ℝ)) →
                (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi →
                  96 * (1 + 2 * Real.pi) ^ 2 * strataResidualH h H ^ 2
                      * m4BclGraded j₀ (fun H => 2 * RSan H) (fun H => 2 * RStr H) H
                    ≤ Braw H) →
                (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi → Braw H ≤ Bceil) →
                2 * Kb * Bceil + δ / 2 + 8 * 2 ^ k / (R.x : ℝ) ≤ δ₀ →
                M4ChiSummedFreeRowH_L_gk h K R M RS →
                  P R

/-- **⟦H2 FORM, GRADED⟧ (def).** `FlatCapstoneFormH` (:967) with the pin at `838400 * 2 ^ 11` . ⚠
the `constPool (doorRhoOfDelta (s12DeltaSock δ₀ Kc))` antecedents are UNCHANGED as text: they are
discharged inside the graded H2→H3 replay from the register's `gP1` /`lvl`/`rho` fields, whose
graded witnesses (`StrideGradeWalls`) pay the `+8` nats at the cap lines. ⟦SUBSTITUTED LINES⟧ source
970. -/
def FlatCapstoneFormHG (h : ℕ) (Awin : ℝ) (P : ChowlaRegime → Prop) : Prop :=
    ∃ (Cg : ℝ) (ε : ℚ) (Kc δ₀ β : ℝ) (x₀ Hopq Mfl : ℕ),
      1 ≤ Cg ∧ 0 < ε ∧ 0 < Kc ∧ 0 < δ₀ ∧ 1 ≤ Mfl ∧
      Cg ≤ 2 * 10 ^ 12 ∧ 1 / (500 * (h : ℚ)) ≤ ε ∧ 1 / (838400 * 2 ^ 11 * (h : ℝ) ^ 2) ≤ δ₀ ∧
      Kc ≤ 2 ^ 539 ∧
      (∀ A : ℝ, 162 ≤ A → Awin ≤ A → Mfl ≤ flatDoorM A) ∧
      0 < β ∧
      ∀ K : ℕ, ∃ Ct : ℝ, 0 < Ct ∧ Ct ≤ 2 ^ 23 ∧
      ∀ A : ℝ, 162 ≤ A → budgetAFlat (ε : ℝ) β ≤ A →
        ∃ Hcap : ℕ,
          Hcap ≤ max (flatDesignFloor A)
            (max (max Hopq (budgetFloorFlat (ε : ℝ) β A)) (4 * ⌈(1 / ε : ℚ)⌉₊ ^ 4)) ∧
          ∀ (Cp : ℝ), 0 ≤ Cp →
            ∀ (a U1floor : ℕ) (g : ℕ → ℕ → ℕ), 1 ≤ a → a ≤ 1096 →
            XCeilRider ε (fun Hhi ω => a * g Hhi ω) →
              ∃ R : ChowlaRegime, R.eps = ε ∧ U1floor ≤ R.Hlo ∧ a * g R.Hhi R.ω ≤ R.x ∧
              StrideScale a R ∧
                Real.log ((R.x : ℕ) : ℝ) ≤ 31 / (ε : ℝ) * ((R.Hhi : ℕ) : ℝ) ∧
                (50 ≤ Real.log (Real.log (R.Hlo : ℝ)) →
                  Real.log (Real.log (R.Hhi : ℝ))
                    ≤ Real.exp (Real.log (Real.log (R.Hlo : ℝ)) / 2)) ∧
                R.Hlo ≤ max Hcap U1floor ∧
                ∀ (M : ℕ), Mfl ≤ M → K ≤ 170000000 * M →
                  ∃ C' : ℝ, 0 < C' ∧
                    8 * C' ≤ (Real.log 2 * ((doorRowFloorL M : ℕ) : ℝ))
                        ^ (s13Aexp + (-(1 : ℝ) / 2 + 1 / 1000)) ∧
                    ∀ (C₁ M₀ _epsf epsrf : ℕ → ℝ) (Kf : ℝ) (k : ℕ),
                      -- ⟦A⟧ THE SPINE ARITHMETIC
                      M4DoorGates_L_gk K Cg R M k δ₀ →
                      8 * 2 ^ k / (R.x : ℝ) ≤ δ₀ / 4 →
                      (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi →
                        4 * Real.log (263 * (h : ℝ) * max 1 (arcDen 12 H))
                          ≤ ((doorRowFloorL M : ℕ) : ℝ)) →
                      (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi →
                        (h : ℝ) * arcDen 12 H
                          < ((calP (AdoorL M) (s13GK K M) 1 : ℕ) : ℝ)) →
                      (∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi →
                        m4SmallGradeFits (doorRowFloorL M)
                          (fun H => 2 * RSanDoorRhoH (doorRhoOfDelta (s12DeltaSock δ₀ Kc)) h H)
                          (fun H => 2 * ((h : ℝ) ^ 7 * rStrWitness H)) H) →
                      -- ⟦B1'⟧ THE FUSE'S OWN DEMANDS AT THE CONSTANT POOL
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        DoorBaseFrame (A + s) j) →
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        374784 * Ct * Real.exp 3 * (1 / ((calP (AdoorL M) (s13GK K M) 1 : ℕ) : ℝ))
                          ≤ constPool (doorRhoOfDelta (s12DeltaSock δ₀ Kc)) R.Hhi) →
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        GRowsZeroGate'''_L_gk K M (A + s) Cp
                          (constPool (doorRhoOfDelta (s12DeltaSock δ₀ Kc)) R.Hhi)) →
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        14 * Real.log (Real.log ((R.Hhi : ℕ) : ℝ)) + Real.log 376266
                            + (-Real.log (doorRhoOfDelta (s12DeltaSock δ₀ Kc)))
                          ≤ (theta293 - epsrf (A + s))
                              * Real.log (Real.log (((A + s : ℕ)) : ℝ))) →
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        (Real.log (((A + s : ℕ)) : ℝ)) ^ (-theta293)
                          ≤ constPool (doorRhoOfDelta (s12DeltaSock δ₀ Kc)) R.Hhi) →
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        (4096 : ℝ) ≤ (Real.log (((A + s : ℕ)) : ℝ)) ^ (1 - (1 : ℝ) / 500)
                          * constPool (doorRhoOfDelta (s12DeltaSock δ₀ Kc)) R.Hhi) →
                      -- ⟦THE εr/ε SPLIT⟧ the absorption exponent's own window
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        0 ≤ epsrf (A + s) ∧ epsrf (A + s) ≤ theta293 - 1 / 500) →
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        calQK (AdoorL M) (s13GK K M) M 2 ≤ A + s ∧
                          Real.log ((calQK (AdoorL M) (s13GK K M) M 2 : ℕ) : ℝ)
                              ≤ Real.sqrt (Real.log (((A + s : ℕ)) : ℝ)) ∧
                          (100 : ℝ) ≤ Real.sqrt (Real.log (((A + s : ℕ)) : ℝ)) ∧
                          (4 : ℝ) ≤ ((2 ^ j : ℕ) : ℝ) ∧
                          ((calQK (AdoorL M) (s13GK K M) M 1 : ℕ) : ℝ) ≤ ((2 ^ j : ℕ) : ℝ)) →
                      -- ⟦B4 RAW⟧ the crossing bound, carried
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        ∀ χ : DirichletCharacter ℂ q, ∀ T : ℝ,
                          (((A + s : ℕ)) : ℝ) / ((2 ^ j : ℕ) : ℝ) ≤ T →
                          2 * T ≤ (((A + s : ℕ)) : ℝ) → TannGate (((A + s : ℕ)) : ℝ) (2 * T) →
                          5 ≤ Real.log (Real.log (2 * T)) →
                          (∫ t in seamAnn (((A + s : ℕ)) : ℝ) (2 * T),
                              ‖spoly (2 * (A + s))
                                (winCutH (A + s) (doorChiCoeff_L_gk K χ M)) t‖ ^ 2)
                            ≤ 8 * (0 : ℝ) ^ 2
                              + (∫ t in (seamAnn (((A + s : ℕ)) : ℝ) (2 * T)
                                    \ seamBall (((A + s : ℕ)) : ℝ) 0)
                                  ∩ seamTtotG (chiBarCoeff q χ liouvilleC)
                                      (calP (AdoorL M) (s13GK K M))
                                      (calQK (AdoorL M) (s13GK K M) M) (calH (H1doorL M))
                                      (mrAlpha (1 / 12)) 2,
                                  ‖spoly (2 * (A + s))
                                    (winCutH (A + s) (doorChiCoeff_L_gk K χ M)) t‖ ^ 2)
                              + 2 * ((2 * T / (((A + s : ℕ)) : ℝ) + 1)
                                  * (Real.log (((A + s : ℕ)) : ℝ))
                                      ^ (-theta293 + epsrf (A + s)))) →
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        DoorBandBase_L_gk K x₀ C' s13Aexp M (A + s) q (C₁ (A + s)) (M₀ (A + s))) →
                      (∀ H L q j A s : ℕ, SocketBaseLH h R M H L q j A s →
                        DoorArithFrameRho_L M H j (((A + s : ℕ)) : ℝ) (C₁ (A + s)) (M₀ (A + s)) Kf
                          (doorRhoOfDelta (s12DeltaSock δ₀ Kc))) →
                        P R

/-- **⟦H3 FORM, GRADED⟧ (def).** `FlatConditionalFormH` (:1067) with the pin at `838400 * 2 ^ 11` ;
the register slot `S15Sel''_L_gk K Cg δ₀ Ct (doorRhoOfDelta (s12DeltaSock δ₀ Kc)) …` is
text-identical — the STRUCTURE does not move, its witness does. ⟦SUBSTITUTED LINES⟧ source 1070. -/
def FlatConditionalFormHG (h : ℕ) (Awin : ℝ) (P : ChowlaRegime → Prop) : Prop :=
    ∃ (ε : ℚ) (Cg Kc δ₀ β : ℝ) (x₀ Hopq Mfl : ℕ),
      0 < ε ∧ 1 ≤ Cg ∧ 0 < Kc ∧ 0 < δ₀ ∧ 1 ≤ Mfl ∧
      Cg ≤ 2 * 10 ^ 12 ∧ 1 / (500 * (h : ℚ)) ≤ ε ∧ 1 / (838400 * 2 ^ 11 * (h : ℝ) ^ 2) ≤ δ₀ ∧
      Kc ≤ 2 ^ 539 ∧
      (∀ A : ℝ, 162 ≤ A → Awin ≤ A → Mfl ≤ flatDoorM A) ∧
      0 < β ∧
      ∀ K : ℕ, ∃ Ct : ℝ, 0 < Ct ∧ Ct ≤ 2 ^ 23 ∧
      ∀ A : ℝ, 162 ≤ A → budgetAFlat (ε : ℝ) β ≤ A →
        ∃ Hcap : ℕ,
          Hcap ≤ max (flatDesignFloor A)
            (max (max Hopq (budgetFloorFlat (ε : ℝ) β A)) (4 * ⌈(1 / ε : ℚ)⌉₊ ^ 4)) ∧
          ∀ (a U1floor : ℕ) (g : ℕ → ℕ → ℕ), 1 ≤ a → a ≤ 1096 → XCeilRiderStrict ε g →
            max Hcap (max arcFloor36 loglogFloor50) ≤ U1floor →
            ∃ R : ChowlaRegime, R.eps = ε ∧ R.Hlo = U1floor ∧ a * g R.Hhi R.ω ≤ R.x ∧
              StrideScale a R ∧
              Real.log ((R.x : ℕ) : ℝ) ≤ 31 / (ε : ℝ) * ((R.Hhi : ℕ) : ℝ) ∧
              (50 ≤ Real.log (Real.log (R.Hlo : ℝ)) →
                Real.log (Real.log (R.Hhi : ℝ))
                  ≤ Real.exp (Real.log (Real.log (R.Hlo : ℝ)) / 2)) ∧
              ∀ M : ℕ, K ≤ 170000000 * M →
                S15Sel''_L_gk K Cg δ₀ Ct (doorRhoOfDelta (s12DeltaSock δ₀ Kc)) x₀ Mfl R M →
                S15CrossingBound_LH_gk h K R M → P R

/-- **⟦H4 FORM, GRADED⟧ (def).** `FlatKswinFormH` (:1096) with the pin at `838400 * 2 ^ 11` .
⟦SUBSTITUTED LINES⟧ source 1099. -/
def FlatKswinFormHG (h : ℕ) (Awin : ℝ) (P : ChowlaRegime → Prop) : Prop :=
    ∃ (ε : ℚ) (Cg Kc δ₀ β : ℝ) (x₀ Hopq Mfl : ℕ) (Cq cs T₀ Kq Ks C : ℝ),
      0 < ε ∧ 1 ≤ Cg ∧ 0 < Kc ∧ 0 < δ₀ ∧ 1 ≤ Mfl ∧
      Cg ≤ 2 * 10 ^ 12 ∧ 1 / (500 * (h : ℚ)) ≤ ε ∧ 1 / (838400 * 2 ^ 11 * (h : ℝ) ^ 2) ≤ δ₀ ∧
      (∀ A : ℝ, 162 ≤ A → Awin ≤ A → Mfl ≤ flatDoorM A) ∧
      0 < β ∧
      0 < Cq ∧ 0 < cs ∧ Real.exp (-100) ≤ cs ∧ 3 ≤ T₀ ∧ 0 < Kq ∧ 0 < Ks ∧ 0 < C ∧
      Real.log C ≤ 40 ∧
      ∀ K : ℕ, ∃ Ct : ℝ, 0 < Ct ∧
        ∀ A : ℝ, 162 ≤ A → Awin ≤ A → budgetAFlat (ε : ℝ) β ≤ A →
          K ≤ 170000000 * flatDoorM A →
        (Hopq ≤ flatDesignBase A → flatWitFloor ε β A Hopq = flatDesignBase A) ∧
        ((x₀ : ℝ) ≤ Real.exp (Real.exp (3.2 * A) / 10) →
          Hopq ≤ flatDesignBase A →
          T₀ ≤ Real.exp (Real.sqrt ((flatWitFloor ε β A Hopq : ℕ) : ℝ) / 2) →
          Real.log (1 / Ks) ≤ 3 * Real.exp (3.2 * A) / 16 →
          ∀ (U1floor : ℕ), flatWitFloor ε β A Hopq ≤ U1floor →
            Real.log (Real.log ((U1floor : ℕ) : ℝ)) ≤ 3.2 * A + Real.log 2 →
          ∀ (a : ℕ) (g : ℕ → ℕ → ℕ), 1 ≤ a → a ≤ 1096 → XCeilRiderStrict ε g →
            ∃ R : ChowlaRegime,
            R.eps = ε ∧ R.Hlo = U1floor ∧ a * g R.Hhi R.ω ≤ R.x ∧ StrideScale a R ∧
            Real.log ((R.x : ℕ) : ℝ) ≤ 31 / (ε : ℝ) * ((R.Hhi : ℕ) : ℝ) ∧
            (50 ≤ Real.log (Real.log (R.Hlo : ℝ)) →
              Real.log (Real.log (R.Hhi : ℝ))
                ≤ Real.exp (Real.log (Real.log (R.Hlo : ℝ)) / 2)) ∧
            3.2 * A ≤ Real.log (Real.log (R.Hlo : ℝ)) ∧
            Real.log (Real.log ((R.Hhi : ℕ) : ℝ)) ≤ 2 * Real.exp (3.2 * A / 2) ∧
            (S16CofactorSupply_LH_gk h K Cq R (flatDoorM A) →
              S16BaseScaleCap96_LH_gk h K R (flatDoorM A) →
                P R))

/-- **⟦H5 FORM, GRADED⟧ (def).** `V7RatedFormH` (:1131) with the pin at `838400 * 2 ^ 11` .
⟦SUBSTITUTED LINES⟧ source 1136. -/
def V7RatedFormHG (h : ℕ) (P : ChowlaRegime → Prop) (A₀ : ℝ) : Prop :=
    ∃ (ε : ℚ) (Cg Kc δ₀ Ct A β : ℝ) (Mfl : ℕ) (Cq cs T₀ Kq Ks C : ℝ),
      0 < ε ∧ 1 ≤ Cg ∧ 0 < Kc ∧ 0 < δ₀ ∧ 0 < Ct ∧ 1 ≤ Mfl ∧
      0 < Cq ∧ 0 < cs ∧ Real.exp (-100) ≤ cs ∧ 3 ≤ T₀ ∧ 0 < Kq ∧ 0 < Ks ∧ 0 < C ∧
      Real.log C ≤ 40 ∧ Cg ≤ 2 * 10 ^ 12 ∧ 1 / (500 * (h : ℚ)) ≤ ε ∧
      1 / (838400 * 2 ^ 11 * (h : ℝ) ^ 2) ≤ δ₀ ∧
      Mfl ≤ flatDoorM A ∧ 0 < β ∧ 162 ≤ A ∧ A₀ ≤ A ∧
      ∀ (U1floor a : ℕ) (g : ℕ → ℕ → ℕ), flatDesignBase A ≤ U1floor →
        Real.log (Real.log ((U1floor : ℕ) : ℝ)) ≤ 3.2 * A + Real.log 2 →
        1 ≤ a → a ≤ 1096 → XCeilRiderStrict ε g →
      ∃ R : ChowlaRegime,
        R.eps = ε ∧ R.Hlo = U1floor ∧ a * g R.Hhi R.ω ≤ R.x ∧ StrideScale a R ∧
        (50 ≤ Real.log (Real.log (R.Hlo : ℝ)) →
          Real.log (Real.log (R.Hhi : ℝ))
            ≤ Real.exp (Real.log (Real.log (R.Hlo : ℝ)) / 2)) ∧
        3.2 * A ≤ Real.log (Real.log (R.Hlo : ℝ)) ∧
        Real.log (Real.log ((R.Hhi : ℕ) : ℝ)) ≤ 2 * Real.exp (3.2 * A / 2) ∧
        P R

/-! ## §5 — THE REPLAYS, GRADED: bodies verbatim from `StridePairReceipt`, two name swaps. -/

/-- **⟦H0→H1 REPLAY, GRADED⟧ (class A).** Statement: `flat_roadExit_generic_h` (:1161) at the graded
forms. BODY: StridePairReceipt.lean:1167-1184 VERBATIM — it forwards `hδpin` opaquely (no numeral is
read); the road at `Xi` (`m4_second_road_L2_Set_gk_flatRoot_L_khoist`) is δ₀-generic. ⟦SUBSTITUTED
LINES⟧ source none (names only). -/
theorem flat_roadExit_generic_h_g (h : ℕ) (hh : 0 < h) (_hh7 : Real.log (h : ℝ) ≤ 7)
    (Xi : XiFamily)
    (harcXi : ∀ eps : ℚ, 0 < eps → ∃ H₀ : ℕ, ∀ H : ℕ, ∀ [NeZero H], H₀ ≤ H →
      ∀ ξ ∈ Xi eps H, NearRatTight ((h : ℝ) * arcDen 12 H) H (-(ξ.val : ℝ) / (H : ℝ)))
    (P : ChowlaRegime → Prop) (hhead : FlatHeadFormHG h Xi P) :
    FlatRoadExitFormHG h P := by
  sorry

/-- **⟦H1→H2 REPLAY, GRADED⟧ (class B).** Statement: `flat_capstone_generic_h` (:1189) at the graded
forms. BODY: :1193-1332 VERBATIM — `hδpin` is forwarded; the share table `δ₀/(8·Kc)` , the ceiling
`m4_arith_rs_ceiling_met_of_deltaH` and gate 10b (`2·Kc·(δ₀/(8Kc)) = δ₀/4`) are all stated in the
threaded `δ₀` , which is now the graded one. ⟦SUBSTITUTED LINES⟧ source none (names only). -/
theorem flat_capstone_generic_h_g (h : ℕ) (hh : 0 < h) (hh7 : Real.log (h : ℝ) ≤ 7) (Awin : ℝ)
    (hband : S16BandLaneCBoundedLH_winU h Awin) (P : ChowlaRegime → Prop)
    (hroad : FlatRoadExitFormHG h P) :
    FlatCapstoneFormHG h Awin P := by
  sorry

/-- **⟦H2→H3 REPLAY, GRADED⟧ (class B).** Statement: `flat_conditional_generic_h` (:1406) at the
graded forms. BODY: :1410-1601 VERBATIM with ONE name swap:
`s15ArmH_log_le hh hh7 hδ₀ hδpin hKc hKcb hH4 hll` (:1479) ↦ `s15ArmH_log_le_g …` (same arguments;
`StrideGradeWalls` ), because `hδpin` now reads `1/(838400·2^11·h²) ≤ δ₀` . Every other use of
`hδpin` in the body is a forward. The register fields `hsel.half/rho/gP1/lvl` are consumed at
symbolic `ρ` exactly as landed. ⟦SUBSTITUTED LINES⟧ source none (names only). -/
theorem flat_conditional_generic_h_g (h : ℕ) (hh : 0 < h) (hh7 : Real.log (h : ℝ) ≤ 7)
    (Awin : ℝ) (_hband : S16BandLaneCBoundedLH_winU h Awin) (P : ChowlaRegime → Prop)
    (hcap : FlatCapstoneFormHG h Awin P) :
    FlatConditionalFormHG h Awin P := by
  sorry

/-- **⟦H3→H4 REPLAY, GRADED⟧ (class B).** Statement: `flat_kswin_generic_h` (:1613) at the graded
forms. BODY: :1617-1711 VERBATIM with ONE name swap:
`s15_sel''_L_gk_witness_flat_bumped_win_h hA26 K hKw hh hh7 hδ₀ hδpin hKc hKcb hCt hCtb hCgle
(hMflb A hA26 hAwin) hx0win heps hlo hwin` (:1693)
↦ `s15_sel''_L_gk_witness_flat_bumped_win_h_g …` (same arguments; `StrideGradeWalls` ).
⟦SUBSTITUTED LINES⟧ source none (names only). -/
theorem flat_kswin_generic_h_g (h : ℕ) (hh : 0 < h) (hh7 : Real.log (h : ℝ) ≤ 7) (Awin : ℝ)
    (_hband : S16BandLaneCBoundedLH_winU h Awin) (P : ChowlaRegime → Prop)
    (hcond : FlatConditionalFormHG h Awin P) :
    FlatKswinFormHG h Awin P := by
  sorry

/-- **⟦H4→H5 REPLAY, GRADED⟧ (class B).** Statement: `flat_v7_generic_h` (:1721) at the graded
forms. BODY: :1725-1841 VERBATIM — `hδpin` is forwarded at :1801 only; the eight-arm design
constant, the rated supply and the base-scale cap read no pin. ⟦SUBSTITUTED LINES⟧ source none
(names only). -/
theorem flat_v7_generic_h_g (h : ℕ) (hh : 0 < h) (hh7 : Real.log (h : ℝ) ≤ 7)
    (P : ChowlaRegime → Prop)
    (hk : ∀ Awin : ℝ, S16BandLaneCBoundedLH_winU h Awin → FlatKswinFormHG h Awin P) (A₀ : ℝ) :
    V7RatedFormHG h P A₀ := by
  sorry

/-! ## §6 — the door-head, the chain, the receipts, GRADED -/

/-- **⟦THE RECEIPT PREDICATE, GRADED⟧ (def).** `MRTDoorReceiptSet` (:1859) with the door's ceiling
`837782 * (h : ℝ) ^ 2 ↦ 837782 * 2 ^ 11 * (h : ℝ) ^ 2` ; the ε-pin equality and the count ride the
slot unchanged. ⟦SUBSTITUTED LINES⟧ source 1863. -/
def MRTDoorReceiptSetG (h : ℕ) (Xi : XiFamily) (R : ChowlaRegime) : Prop :=
  R.eps = 1 / (500 * (h : ℚ)) ∧
  (∃ K : ℝ, 0 < K ∧ K ≤ 2 ^ 539 ∧ ∀ (H : ℕ) [NeZero H], R.Hlo ≤ H → H ≤ R.Hhi →
    ((Xi R.eps H).card : ℝ) ≤ K) ∧
  ∃ ρ : ℝ, 0 < ρ ∧ ρ ≤ 1 / (837782 * 2 ^ 11 * (h : ℝ) ^ 2) ∧ MRTUniformityXiL2Set Xi R ρ

/-- **⟦THE DOOR-HEAD, GRADED⟧ (class B).** Statement: `flat_door_head_xceil_h` (:1881) at
`FlatHeadFormHG … (MRTDoorReceiptSetG h Xi)` . BODY: :1886-1968 VERBATIM with the mint moved by
`2^11` : the witness `cD3 / (16 * C) * (ε : ℝ) / 4` (:1946) ↦
`cD3 / (16 * C) * (ε : ℝ) / 4 / 2 ^ 11` (its positivity gains one `div_pos … (by positivity)` );
`hval` (:1913-1917) gains `/ 2 ^ 11` on the left and `* 2 ^ 11` inside the right denominator
(`field_simp; ring` unchanged); `hδ₀ge` (:1920-1924) at `838400 * 2 ^ 11` with the same
`hnum : 128000 * (1 + 8 * log 2) ≤ 838400` and `nlinarith [hnum, hsq0]` now multiplying through
`2 ^ 11` (state `hnum'` at `× 2 ^ 11` if `nlinarith` needs the product); `hδ₀le` (:1925-1929) at
`837782 * 2 ^ 11` likewise; the discharge `le_trans hρ hδ₀le` (:1971) unchanged. The regime build,
the count hook, the hoist and the cap read NO `δ₀` . ⟦SUBSTITUTED LINES⟧ source none (names only).
-/
theorem flat_door_head_xceil_h_g (h : ℕ) (hh : 0 < h) (_hh7 : Real.log (h : ℝ) ≤ 7)
    (Xi : XiFamily)
    (hcount : ∃ C : ℝ, 0 < C ∧ C ≤ 2 ^ 539 ∧ ∃ H₀ : ℕ, 2 ≤ H₀ ∧ ∀ (H : ℕ) [NeZero H], H₀ ≤ H →
      ((Xi (1 / (500 * (h : ℚ))) H).card : ℝ) ≤ C) :
    FlatHeadFormHG h Xi (MRTDoorReceiptSetG h Xi) := by
  sorry

/-- **⟦THE CHAIN, GRADED⟧ (class A).** `flat_chain_generic_h` (:1973); BODY :1978-1982 VERBATIM at
the `_g` replays. ⟦SUBSTITUTED LINES⟧ source none (names only). -/
theorem flat_chain_generic_h_g (h : ℕ) (hh : 0 < h) (hh7 : Real.log (h : ℝ) ≤ 7) (Xi : XiFamily)
    (harcXi : ∀ eps : ℚ, 0 < eps → ∃ H₀ : ℕ, ∀ H : ℕ, ∀ [NeZero H], H₀ ≤ H →
      ∀ ξ ∈ Xi eps H, NearRatTight ((h : ℝ) * arcDen 12 H) H (-(ξ.val : ℝ) / (H : ℝ)))
    (P : ChowlaRegime → Prop) (hhead : FlatHeadFormHG h Xi P) (A₀ : ℝ) :
    V7RatedFormHG h P A₀ := by
  sorry

/-- **⟦THE RECEIPT AT `h` , GENERIC SET, GRADED⟧ (class A).** Statement:
`mrtUniformityXiL2Set_holds_flat_floor` (:1994) with the ceiling at `837782 * 2 ^ 11` . BODY:
:2013-2029 VERBATIM at `MRTDoorReceiptSetG` , `flat_chain_generic_h_g` , `flat_door_head_xceil_h_g`
. ⟦SUBSTITUTED LINES⟧ source 2012. -/
theorem mrtUniformityXiL2Set_holds_flat_floor_g (h : ℕ) (hh : 0 < h) (hh7 : Real.log (h : ℝ) ≤ 7)
    (Xi : XiFamily)
    (harcXi : ∀ eps : ℚ, 0 < eps → ∃ H₀ : ℕ, ∀ H : ℕ, ∀ [NeZero H], H₀ ≤ H →
      ∀ ξ ∈ Xi eps H, NearRatTight ((h : ℝ) * arcDen 12 H) H (-(ξ.val : ℝ) / (H : ℝ)))
    (hcount : ∃ C : ℝ, 0 < C ∧ C ≤ 2 ^ 539 ∧ ∃ H₀ : ℕ, 2 ≤ H₀ ∧ ∀ (H : ℕ) [NeZero H], H₀ ≤ H →
      ((Xi (1 / (500 * (h : ℚ))) H).card : ℝ) ≤ C)
    (A₀ : ℝ) :
    ∃ (ε : ℚ) (A : ℝ), 0 < ε ∧ 1 / (500 * (h : ℚ)) ≤ ε ∧ ε = 1 / (500 * (h : ℚ)) ∧
      162 ≤ A ∧ A₀ ≤ A ∧
      ∀ (U1floor a : ℕ) (g : ℕ → ℕ → ℕ), flatDesignBase A ≤ U1floor →
        Real.log (Real.log ((U1floor : ℕ) : ℝ)) ≤ 3.2 * A + Real.log 2 →
        1 ≤ a → a ≤ 1096 → XCeilRiderStrict ε g →
      ∃ R : ChowlaRegime,
        R.eps = ε ∧ R.Hlo = U1floor ∧ a * g R.Hhi R.ω ≤ R.x ∧ StrideScale a R ∧
        3.2 * A ≤ Real.log (Real.log (R.Hlo : ℝ)) ∧
        Real.log (Real.log ((R.Hhi : ℕ) : ℝ)) ≤ 2 * Real.exp (3.2 * A / 2) ∧
        (∃ K : ℝ, 0 < K ∧ K ≤ 2 ^ 539 ∧ ∀ (H : ℕ) [NeZero H], R.Hlo ≤ H → H ≤ R.Hhi →
          ((Xi R.eps H).card : ℝ) ≤ K) ∧
        ∃ ρ : ℝ, 0 < ρ ∧ ρ ≤ 1 / (837782 * 2 ^ 11 * (h : ℝ) ^ 2) ∧ MRTUniformityXiL2Set Xi R ρ := by
  sorry

/-- **⟦THE ANTI-DRIFT INSTANCE, GRADED⟧ (class A).** The plain `h` -door on the flat family at the
graded ceiling `1/(837782·2^11·h²)` — the conservativity control: at `h = 1` it is
`mrtUniformityXiL2_holds_flat` (DoorReceipt) 2048× finer. Statement: `mrtUniformityXiL2H_holds_flat`
(:2038) with the ceiling substituted; BODY :2044-2053 VERBATIM at `_g` . ⟦SUBSTITUTED LINES⟧ source
2043. -/
theorem mrtUniformityXiL2H_holds_flat_g (h : ℕ) (hh : 0 < h) (hh7 : Real.log (h : ℝ) ≤ 7)
    (A₀ : ℝ) :
    ∃ (ε : ℚ) (A : ℝ), 0 < ε ∧ 1 / (500 * (h : ℚ)) ≤ ε ∧ 162 ≤ A ∧ A₀ ≤ A ∧
      ∃ R : ChowlaRegime, R.eps = ε ∧ R.Hlo = flatDesignBase A ∧
        3.2 * A ≤ Real.log (Real.log (R.Hlo : ℝ)) ∧
        ∃ ρ : ℝ, 0 < ρ ∧ ρ ≤ 1 / (837782 * 2 ^ 11 * (h : ℝ) ^ 2) ∧ MRTUniformityXiL2H h R ρ := by
  sorry

/-- **⟦THE AFFINE-SET INSTANCE, GRADED⟧ (class A).** Statement:
`mrtUniformityXiL2AffSet_holds_flat_floor` (:2064) with
`837782 * ((a * h : ℕ) : ℝ) ^ 2 ↦ 837782 * 2 ^ 11 * ((a * h : ℕ) : ℝ) ^ 2` . BODY :2079-2088
VERBATIM at `mrtUniformityXiL2Set_holds_flat_floor_g` . ⟦SUBSTITUTED LINES⟧ source 2077. -/
theorem mrtUniformityXiL2AffSet_holds_flat_floor_g (a b h : ℕ) (ha : 0 < a) (hh : 0 < h)
    (hah7 : Real.log ((a * h : ℕ) : ℝ) ≤ 7) (A₀ : ℝ) :
    ∃ (ε : ℚ) (A : ℝ), 0 < ε ∧ 1 / (500 * ((a * h : ℕ) : ℚ)) ≤ ε ∧
      ε = 1 / (500 * ((a * h : ℕ) : ℚ)) ∧ 162 ≤ A ∧ A₀ ≤ A ∧
      ∀ (U1floor a' : ℕ) (g : ℕ → ℕ → ℕ), flatDesignBase A ≤ U1floor →
        Real.log (Real.log ((U1floor : ℕ) : ℝ)) ≤ 3.2 * A + Real.log 2 →
        1 ≤ a' → a' ≤ 1096 → XCeilRiderStrict ε g →
      ∃ R : ChowlaRegime,
        R.eps = ε ∧ R.Hlo = U1floor ∧ a' * g R.Hhi R.ω ≤ R.x ∧ StrideScale a' R ∧
        3.2 * A ≤ Real.log (Real.log (R.Hlo : ℝ)) ∧
        Real.log (Real.log ((R.Hhi : ℕ) : ℝ)) ≤ 2 * Real.exp (3.2 * A / 2) ∧
        (∃ K : ℝ, 0 < K ∧ K ≤ 2 ^ 539 ∧ ∀ (H : ℕ) [NeZero H], R.Hlo ≤ H → H ≤ R.Hhi →
          ((bigXiAffD a b h R.eps H).card : ℝ) ≤ K) ∧
        ∃ ρ : ℝ, 0 < ρ ∧ ρ ≤ 1 / (837782 * 2 ^ 11 * ((a * h : ℕ) : ℝ) ^ 2) ∧
          MRTUniformityXiL2Set (fun eps H _ => bigXiAffD a b h eps H) R ρ := by
  sorry

/-! ## §7 — THE CROWN, GRADED: the affine door at Tao's range at `ρ ≤ 1/(837782·2^11·(ah)²)` -/

/-- **⟦THE CROWN, GRADED⟧ (class B) — `mrtUniformityXiL2AffW_holds_flat_stride_g` .** Statement: the
landed crown (:2121) with `ρ ≤ 1 / (837782 * 2 ^ 11 * ((a * h : ℕ) : ℝ) ^ 2)` — the spelling
`GradedAffHeadAt` (StridePrize.lean:166) and `log_chowla_aff_of_door_g` 's `hcrown` read. BODY:
:2132-2259 VERBATIM at `mrtUniformityXiL2AffSet_holds_flat_floor_g` — the receipt's `hρle` is
forwarded through the transport and `mrtUniformityXiL2AffW_mono` untouched; no line of the crown's
proof reads the ceiling's numeral. ⟦SUBSTITUTED LINES⟧ source 2127. -/
theorem mrtUniformityXiL2AffW_holds_flat_stride_g (a b h : ℕ) (ha : 0 < a) (hh : 0 < h)
    (hba : b < a) (hah7 : Real.log ((a * h : ℕ) : ℝ) ≤ 7) (A₀ : ℝ) :
    ∃ (ε : ℚ) (A : ℝ), 0 < ε ∧ 1 / (500 * ((a * h : ℕ) : ℚ)) ≤ ε ∧
      ε = 1 / (500 * ((a * h : ℕ) : ℚ)) ∧ 162 ≤ A ∧ A₀ ≤ A ∧
      ∃ Ra : ChowlaRegimeAff, Ra.a = a ∧ Ra.b = b ∧ Ra.eps = ε ∧
        flatDesignBase A ≤ Ra.Hlo ∧ 3.2 * A ≤ Real.log (Real.log (Ra.Hlo : ℝ)) ∧
        ∃ (ρ Zr E : ℝ), 0 < ρ ∧ ρ ≤ 1 / (837782 * 2 ^ 11 * ((a * h : ℕ) : ℝ) ^ 2) ∧
          1 ≤ Zr ∧ Zr ≤ 1.02 ∧ 0 ≤ E ∧
          E ≤ 2 ^ 539 * (a : ℝ) / (((a : ℝ) * ((Ra.x / Ra.ω : ℕ) : ℝ) + 1)
              * (Real.log (Ra.ω : ℝ) - 1)) ∧
          MRTUniformityXiL2AffW h Ra ((a : ℝ) * Zr * ρ + E) := by
  sorry

end Salt.MR

end
