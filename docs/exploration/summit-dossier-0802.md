# THE STATEMENT-HONESTY DOSSIER — for the Captain's read, before his word
### 2026-08-02, composed at 73d02e6 (the bar-2 done-state), ceremony in flight

*This is council v5's item C3(d): one document holding what stands, what
it says, what it assumes, who checked it, and what a summit word would
and would not claim. Nothing here is new; every line cites the ledger.*

---

## 1. What stands (the named objects, all 3-axiom, all pushed)

- **THE PEARL** — `logChowla2_witnessed_scale_flat_L_v2`
  (S16FlatFinal.lean): at every A ≥ 162 a Chowla regime with base
  pinned at ⌈e^{e^{3.2A}}⌉, width exported (loglog H₊ ≤ 2·e^{1.6A}),
  the S15 register supplied, the crossing paid.
- **THE COROLLARY CHAIN** — `logChowla2_ineffective` (v1, S16Uniform)
  → `_v2` (band rider discharged) → **`_v3` (S16Compose) — THE FINAL
  OBJECT**: the classical ∃A form with the Siegel pair, the band-lane
  constant, and the Kc/Ct/Kq numerals ALL discharged inside the proof.
- **THE TOLL** — `towerFlat_width_ge`/`_le` (TowerFlat.lean): the flat
  tower's width bracketed in [4^{(20/21)A}, (21/20)·4^A].
- **THE SHAPE-FREE TOLL** — `towerShape_width_ge` (TowerShape.lean):
  ANY threshold schedule with floor A_b crossing the entropy budget
  pays ≥ 4^{(20/21)A_b}; the flat tower is extremal within (21/20).

## 2. What v3 says, plainly

> For every depth A₀ there exist a design constant A ≥ max(162, A₀)
> and a Chowla regime whose window base equals ⌈e^{e^{3.2A}}⌉ — depth
> unbounded — such that IF three facts hold about three constants the
> theorem itself produces (e^{-100} ≤ cs, T₀ ≤ e^{e^100},
> e^{-100} ≤ Ks), THEN for the witnessed scale: given the two supply
> predicates, the log-averaged two-point Chowla correlation does not
> fail there. The price of removing every effectivity hypothesis is
> that A is chosen classically — the theorem names no computable A.

## 3. The survivors, each with its nature named

**Inner riders (three):**
- `e^{-100} ≤ cs` — a lower bound on a min-expression whose leaves
  (K₄, Cκ) are compactness existentials of our own port. NOT Siegel,
  NOT a field wall — an unread trace. Post-summit route: trace or
  floor the leaves (open; my earlier "cs likely dischargeable" remains
  a guess, not a result).
  > **ERRATUM (RIDER-TRACE, 2026-08-02).** K₄ and Cκ are NOT compactness
  > existentials — they are closed-form `noncomputable def`s
  > (`K₄ = 2^{3/4}·16`, PortAssembly:637; `Cκ = 2^{3/4}·8`,
  > HalaszPrimesCore:3456). Every cs leaf is effective: `c_vk = 1/10^8`
  > literal, `c₀ = 1/10^9` literal (PowRegion:365). cs = 3.716e-11
  > against e^{-100} = 3.72e-44 — **33 orders of room**, kernel-pinned at
  > `MR/RiderTrace.cs_closed_form_ge_exp_neg_hundred`. The discharge is
  > conjunct-carry, no new analysis.
- `T₀ ≤ e^{e^100}` — equivalent to T₀z ≤ e^{e^100} over three
  compactness/VK leaves with no landed bound. (ERRATUM banked: I once
  called this "dischargeable by construction" — wrong.)
  > **RIDER-TRACE (2026-08-02): UNREACHABLE at the corpus's own witness.**
  > T₀ = max T₀e T₀z; the twisted row's T₀e = e^{e^100} EXACTLY (meets the
  > rider with equality), but the ζ row's T₀z inherits
  > `zeta_zero_free_region_pow`'s own threshold `e^{e^{A}} + t₀ + 3`,
  > A = 8·log(20000K) + 1100 ≥ 1100 — so **T₀ ≥ e^{e^{1100}}** (at the
  > corpus's own K: e^{e^{1251.2}}). A second, worse floor `e^{e^{Cbig}}`,
  > Cbig = 9000·c_vk + c_vk/(2δ₀) + 1, rides the opaque δ₀ at height
  > e^{e^{A}}. NOT a wall: the numeral is MIS-SIZED (the hband-at-40
  > genre) — the consumer's real need is `capfloor_T0_Tann_sharp`'s
  > `T₀ ≤ e^{√H/2}` ≈ 10^{10^{225}} at A = 162, ALREADY LANDED. Repair =
  > rethread the sharp form (6 hops, class B) + floor ε₀ (the CMU-HUNT
  > class-C stone).
- `e^{-100} ≤ Ks` — the Siegel-genre remnant: a fixed numeral against
  a fixed constant with Siegel-ineffective content. The honest wall's
  last echo inside the statement. Consumer-side A-windowing is a
  design item, not a trace (DISCHARGE route 6 finding).

**Conclusion-side predicates (two):**
- `S16BaseScaleCap96_L_gk` — kernel-verified STRICTLY WEAKER than the
  landed cap (DELTA-REF probe; converse fails).
- `S16CofactorSupply_L_gk` — a NEW ask at the linear door,
  incomparable to the landed one (DELTA-REF D1) — but COFK-L proved
  the intended supplier is anchor-general: ONE shared unbuilt debt at
  both doors (RULING 9), the L-delta class A, cushion 10^219× at
  A = 162 and growing with A. Honest holes banked: K_vt symbolic
  unbounded (headroom 4.3e223); the μ-floor/gP1-p2 cap collision
  survives the linear re-cut (watch only if μ ever lowers).

**Disclosure (DELTA-REF):** cs/T₀/Ks bound constants v3's own
∃-prefix produces, and nothing in the tree bounds them from the needed
side — the conclusion is contingent on three facts about named opaque
constants. This is inherited from the landed v3 terminal's genre,
disclosed in the statement, and is exactly what the post-summit
strengthening ladder addresses.

## 4. What was discharged this sitting (removed-because-proven)

| item | was | now |
|---|---|---|
| x₀ Siegel window | hypothesis | discharged (quantifier hoist + max-trick, S16Uniform) |
| Hopq/K_Chen arm | hypothesis | discharged (same device) |
| band lane log C ≤ 40 | hypothesis, UNSATISFIABLE at the corpus's own witness (true value ≈ 22 661 — CMU-HUNT) | A-windowed (log C ≤ 0.64A − 20) and discharged off the landed hypothesis-free ∃C |
| Kc ≤ 2^539 | rider | proven at ≈ 2^228 (317 bits room) |
| Ct ≤ 2^23 | rider | proven at 6e^14 (86% of budget) |
| Kq ≤ e^100 | rider | proven at 1.27e-3 (46 orders) |

## 5. The verification record

- **Refuters (all ruled):** HONEST (not vacuous; repairs landed),
  SAT (satisfiable modulo named opaques), DELTA-REF (supply chain
  kernel-clean; D1+D2 caught and repaired).
- **Scouts:** CMU-HUNT (the band constant traced to every leaf —
  effective, ceiling 22 661, Siegel enters only the threshold);
  COFK-L (cofactor transfers); HB-CENSUS (aside: the repulsion banner
  confirmed closed since 7/18).
- **Builds:** every wave sealed at exit 0 with the warning set
  byte-identical to baseline; every new declaration at
  [propext, Classical.choice, Quot.sound].
- **The ceremony:** re-running now at 73d02e6 — 1100 modules through
  leanchecker, the gold --fresh replay of S16Compose, Brun/Maynard
  solo on the quiet tail. Result lands async; the word should wait
  for it.

## 6. What a summit word could honestly say

"In the kernel: an effective conditional Chowla-regime schema (the
Pearl) whose classical limit is an unconditional-in-the-Siegel-sense,
ineffective theorem (v3) carrying exactly three numeric riders on its
own constants and two named supply predicates; plus an unconditional
companion law (the Toll, shape-free): crossing the entropy budget
costs 4^A in width, extremally." — Every clause above is a theorem
citation. What it must NOT say: "unconditional" simpliciter; "log-
Chowla proven"; anything omitting the predicates.

## 7. The post-summit ladder (priced, none gating the word)

(1) cs/T₀ leaf traces; (2) the Ks/fixed-rider consumer-side A-window
redesign; (3) C_mu full effectivity (~4-5 waves, one class-C stone:
the low-height zero-free strip); (4) the cofactor supplier build
(anchor-blind bulk, both doors at once); (5) the ε²/m structural door
on the Toll; (6) T-BAL-BUDGET (gated on the fulcrum's consuming node).

**Recommendation:** the bar-2 state is met. When the ceremony returns
green, the dossier is complete and (e) is ripe.

---

## ADDENDUM (2026-08-02 evening) — THE CAP VACUITY: (e) IS NOT RIPE

**CAP-SCOPE (read-only, mpmath probes + kernel reads) proved
S16BaseScaleCap96_L_gk FALSE at the very regime the flat terminals
produce** — the socket's own conjuncts pin the scale AT the endpoint
(A+s ∈ [x/(16ω(log H)^12), 3x], and the landed vacuity-fence witness
sits at A+s = 2·R.x exactly), while the Toll's landed LOWER bound
forces loglog(endpoint) ≈ 10^(1.76·10^95) at A = 162 against the
cap's RHS ≈ 10^(9.6·10^6). Two independent refutations (interface
via the caller's g; arithmetic at g ≡ 0).

**Consequence: the conclusion-side implication of the FLAT-lane
terminals — the Pearl (v2) and logChowla2_ineffective v1/v2/v3 — is
currently VACUOUS** (false antecedent at the produced regime). The
non-vacuous standing objects today: the WIDE v3 terminal at the
landed tower (the cap is TRUE there: 10^44 vs 10^(9.6e6)), the
door-only reduction, the budget head, and the Tolls. The refuters
missed it because the falsity is manufactured by the Toll's lower
bound — landed in the same wave the refuters were ruling on.

**The repair is priced and ruled (composite iii-B + iii-A, class B):**
re-cut the cap onto the endpoint form (a THEOREM modulo the g-rider —
already the intermediate line of s16_baseScaleCap96_L_of_xceil), move
the door-vs-endpoint numeral into the budget field as an explicit
hypothesis, raise the lever to Klev := ⌈4·e^{1.6A}⌉ (admissible with
137× uniform margin inside the landed 1.7e8·M ceiling; ~25 signature
weakenings, mechanical), add the g-rider (log(g H₊ ω) ≤ (31/ε)·H₊ —
the builder's own x sits four orders inside it), then the
already-written s16_baseScaleCap96_L_supplied discharges the cap.
**GATED on one probe: the band lane's K-uniformity** (S16BandLane
reads K through the mass gate; at K ≈ e^{1.6A} the window discharge
s16_bandLaneWinL_holds must survive — BAND-K-PROBE in flight).

**(e) waits for the repaired v4.** The summit word must not cite the
flat-lane conditionals until then.

## ADDENDUM 2 (2026-08-02 night) — v4 MINTED; BOTH VACUITIES REPAIRED

**`logChowla2_ineffective_v4` (S16ComposeV4.lean:1002, 3-axiom,
d103766) is the standing summit object.** v1/v2/v3 are marked
SUPERSEDED in RESULTS.md with the vacuity reason on the row.

- **The cap vacuity: REPAIRED** — S16BaseScaleCap96_L_gk is GONE,
  discharged by `s16_baseScaleCap96_L_at_klevF` at the raised lever
  KlevF A (dial kcap := 4, budget 547, margin 1.386× named).
- **The T₀ vacuity: REPAIRED** — the rider reshaped IN the terminal
  to the sharp landed form `T₀ ≤ e^{√(flatDesignBase A)/2}`,
  satisfiable at the corpus witness with two exponential levels of
  clearance (e^{10^543} vs e^{½·e^{6.4e224}}).
- **A quantifier finding en route** (the mint's own catch): K := KlevF A
  closed a circle at v3's prefix order; repaired by hoisting ∀K inside
  the ∃ of the K-free constants — kernel-verified that only Ct and the
  crossing constants move with K.

**v4's exact surviving list**: outer NOTHING. Inner riders (3, all
satisfiable-or-named): cs (clears by 33 orders, kernel-pinned),
the sharp T₀ (above), Ks (Siegel-genre, the field's caveat).
Conclusion-side (2): the x-ceiling rider
`log R.x ≤ (31/ε)·R.Hhi` — the builder's OWN landed law, one
threading wave (~800 ln, one true-by-a-mile estimate) from being a
theorem — X-THREAD in flight; and `S16CofactorSupply_L_gk (KlevF A)`
— RULING 9's known debt, kept as ONE named predicate rather than
traded for two carried hypotheses (the register inhabitation + the
symbolic K_vt cushion): the honest bookkeeping.

**(e) ripens** on the Captain's read of v4 — either as-is (the two
conclusion-side items named plainly) or after X-THREAD lands (one
named predicate only). The g-rider coefficient correction also banked:
the builder sits NINE orders inside the budget, not four.
