# THE CLOSING-WAVE BRIEFS — preserved verbatim for the next session's first dispatch

*Written at the 200/200 session boundary, 2026-07-28 afternoon. The next
session dispatches these two as Opus executors (model: "opus",
general-purpose), byte-faithfully — they encode REFUTE-RESIDUE's
corrections and the authorized in-place edit lists. Base commit at
freeze: a926c2e. Read docs/exploration/m4-residue-design-0728.md (v2
supersedes v1) first if anything needs re-derivation.*

---

## BRIEF 1 — W-EXECUTOR (the err rewire)

You are the W-EXECUTOR of the M4 closing wave in
/Users/jyh/projects/claude/salt (Lean 4 + mathlib, branch main). IRON
RULES: no git; ~3 attempts per obstacle then STOP+report; no
sorry/native_decide/new axioms. Authorized in-place edits are LISTED
below (all day-old wave artifacts, the repair precedent,
flags-recorded); everything else additive (new file
Salt/MR/M4ErrRewire.lean).

READ FIRST: docs/exploration/m4-residue-design-0728.md WHOLE (v1's Part
W + v2's W1/W2 verdicts are your spec, with v2 superseding); then the
cited bytes as you go (the re-grep law).

DELIVERABLES:

1. THE p²-MASS STONE at ramP2domMR [~30–60 ln]: the max-half port of
ramP2coeff_norm_div_le (SeamCalibrationK.lean:759–825) with the
by_cases hmem replaced by the fibre-emptiness split reading the window
from ramHonMR membership (RamareMR.lean:668–670;
RamareErr.lean:743–745); ramP2_term_norm_le supplies the 2; compose
with the landed Σ-half (SmallStones' ramP2mass_le route, the 8/P) →
ramP2massMR_direct : ∑‖ramP2coeffMR …‖²/n² ≤ 16·logb 2 (2X)/(X·P)-
shaped. NO hwin anywhere.

2. THE ERR RE-ROUTE: err_at_witness_mr — the twin of
FrameWitness.err_at_witness consuming ramErr_moment_split_mr_windowed
(SeamRowWindowed.lean:340–361; binders SeamCoefW + hasupp) +
seam_rows_grade (RamareMR.lean:863–867, at the Xd slot with
hXd : X ≤ Xd — the glyph resolution the refuter verified) + your stone.
The grade fit: 4·(520·g + EP2') ≤ 3·(720·g + EP2) at g ≥ 0 — carried as
the witEP2-bumped inequality. AUTHORIZED IN-PLACE: re-route
a2Frame3_witness's err field to consume the twin (FrameWitness.lean —
the err supply line only; the OTHER ten fields untouched); redefine
witEP2 with the 4/3 factor (FrameWitness.lean:613–614) and NOTE the
silent strengthening at its FOUR sites (FrameWitness.lean:640, :758;
M4MeanSq.lean:418, :696 — REFUTE-RESIDUE Correction 1: the hypothesis
text is unchanged but the carried demand strengthens; verify each site
still discharges/threads). The 12·EP2 gate: verify the bumped slack per
Correction 2 (free from log X ≥ 1.6·10⁴ — state the threshold lemma if
a numeral is needed, symbolic).

3. THE BALL-WEIGH RE-PROOF [~10 ln]: AUTHORIZED IN-PLACE: ThmA2Rows'
private a2_term3_ball_weigh (:249–260) re-proved as one step
(8·Sb² + 5760·(RS + Cst·(2/Mr)) ≤ 5760·(RS + Ccc·(2/Mr)) reduces to
8·Sb² ≤ 5760·Sb² under hCcc — Correction 4; the docstring at :226
already carries the 720-th note). The ×4 itself is ALREADY BANKED
(a2Mrow's 1920/5760 — touch NOTHING else there).

4. THE PIN-CHAIN b-SLOT GENERALIZATION (W2 AS RESCOPED — the pin/err
chain P = Q ONLY, the band chain is OUT per v2): the err-side binders
of m4_meansq_per_chi_gen re-stated at generic 1-bounded b with the pair
law at the repaired window range — AUTHORIZED IN-PLACE in M4MeanSq.lean
(the err-supply binders only; the refuter verified genericity at every
link — E_priced/ramP2mass/err_at_witness read only ‖b‖ ≤ 1 + the pair
law). Land the door-datum inhabitation lemma:
cf P := liouvilleC P · conj(χ P),
b(m) := memSCoeff-at-Pm · liouvilleC m · conj(χ m) · e((αP)·m)
satisfies the binder set (verified byte-honest, ALL m including P ∣ m).

BUILD: full lake build RC 0, no new warnings; axiom-check headliners
incl. re-audited a2Frame3_witness + m4_meansq_per_chi_gen via a
scratchpad scratch (delete); wire the new file into Salt/MR/All.lean
(a C-sibling flies concurrently in M4BridgePhase + a new class file —
preserve; retry once on out-of-cone breakage). TRAPS: the Xd/X glyph at
seam_rows_grade's slot; H := H83 X theta293 throughout the frame (never
bare H); the four log scales; conclusion byte-identity for every
in-place edit's UNCHANGED parts (diff-verify); strict gates. Class C,
500–800 ln total. REPORT: names, the four witEP2 site verdicts, the
in-place diffs' summaries, build RC, axioms, drifts, deviations.

---

## BRIEF 2 — C-EXECUTOR (the class pricing)

You are the C-EXECUTOR of the M4 closing wave in
/Users/jyh/projects/claude/salt (Lean 4 + mathlib, branch main). IRON
RULES: no git; ~3 attempts per obstacle then STOP+report; no
sorry/native_decide/new axioms. ONE authorized in-place edit (below);
everything else additive (new file Salt/MR/M4ClassPrice.lean).

READ FIRST: docs/exploration/m4-residue-design-0728.md WHOLE — v2
SUPERSEDES v1 (the induction is DELETED; the depth is 1). Then the
cited bytes (re-grep law).

DELIVERABLES:

1. THE q-GRADED SOCKET REPAIR (v2's "real kill" — AUTHORIZED IN-PLACE
in Salt/MR/M4BridgePhase.lean, day-old artifacts, flags-recorded):
re-cut M4SievedDoorSqSup's conclusion to
≤ Braw' H · (q:ℝ)² · (H:ℝ)² and m4_sievedDoorSq_of_sup's hgrade to the
q-graded form (1 + 2π·arcDen 12 H/q)² · ((q:ℝ)²·Braw' H) ≤ Braw H at
0 < q, (q:ℝ) ≤ arcDen 12 H — KEEP the q-graded factor from hpt (delete
the hden/nlinarith demotion); the closing arithmetic
(q + 2πA)² ≤ (1+2π)²·A² under q ≤ A. m4_sievedDoorSqSup_trivial
survives (H² ≤ q²H² at q ≥ 1 — re-verify). M4BridgeCover's
m4_cover_assembly_sup consumes the socket — re-thread its call site if
the shape moved (additively-minimally, report).

2. THE TWO-CASE CLASS LEMMA (NO induction — depth 1): m4_class_price —
for a class r mod q of the sieved-λχ̄ window sum at a tight-major
rational b/q: case d₀ = gcd(r,q) = 1 →
M4BridgeResidue.norm_sum_windowClass_liou_le_of_uniform (the 1/φ(q)
cancellation — re-grep its exact shape) → the row's mean-square
hypothesis; case d₀ > 1 → m4_class_dilate_exit (the EQUALITY, zero
loss) → m4_class_dilate_coprime → the SAME coprime branch at (q₀, r₀)
on the dilated window (the dilated frequency b/q₀ is constant on
classes mod q₀ — no second split; cite the general nearRatTight_dilate,
NEVER the door-pinned nearRatTight_dilate_door, per K3(i)'s
correction). The assembled per-q statement: the full window sum ≤
(q classes) × (the per-class grade) with the q² landing in the socket's
q-graded slot — THE SAME q as the drift witness (M4BridgePhase:71–74
documents the identity).

3. THE ENDPOINT-DROP LEMMA (K3(iii)): the dilated block
(⌊X_{i+1}/d₀⌋, ⌊X_i/d₀⌋] fails the general fit by ≤ 3 units against the
TIGHT doorLadder_fit — land the drop lemma (discard the top ≤ 3
indices; each costs H'²/A' to the harmonic sum; A' ≥ x/(ω·W)-grade
makes it negligible — state symbolically) and the fitted-block
corollary feeding sum_Ioc_absWindowSum_sq_div_le
(M4BridgeIntegral:448–454, the free-A,B form).

4. THE GRADE SIBLING (U3 — mandatory): m4_gradeGate_direct —
M4GradeGate discharged from √(Braw H) ≤ mrtDeliveredGrade (C/2) H
directly (the Prop-socket needs NO statement edits — a sibling of
m4_gradeGate_of_pricing that does not route through Braw ≤ m4Saving).
Plus (U1) the decay-retaining pricing lemma: summand 1 priced from
cfbM0 K q X directly — 8448·(cfbC₁ X C₁)²·exp(−cfbM0 K q X/e) ≤ (a
named grade with the honest (log X)^{1/15−7/(30e)} exponent, symbolic)
— NEVER through m4_quality_summand_le (its g1 is unsatisfiable, U1;
work the exponent arithmetic honestly: 1/15 − 7/(30e) < 0 strictly —
state the strict negativity lemma).

5. THE ORDER PIN (U2 — in the file header, load-bearing
documentation): Part C is a RE-CUT of M4RowMeanSq between the door and
the capstone — the drift + split + expansion REMOVE the phase; the
capstone instantiates at the UN-phased datum. State the target shape of
the re-cut M4RowMeanSq (the un-phased per-block form) as a def + the
bridging statement to M4Join's phased original, so the final compose
has its interface pinned.

BUILD: full lake build RC 0, no new warnings; axiom-check headliners
via a scratchpad scratch (delete); wire the new file into
Salt/MR/All.lean (a W-sibling flies concurrently in
FrameWitness/M4MeanSq/M4ErrRewire — preserve; retry once). TRAPS: the
four log scales; the enlarged cap (B-3's rule) at the dilation;
liouvilleC/liouChi only; the two q's identity; strict gates; conclusion
byte-identity on the socket re-cut's unchanged parts. Class C, 500–800
ln. REPORT: names, the socket re-cut's diff summary, the closing
arithmetic's exact form, build RC, axioms, drifts, deviations.

---

## AFTER BOTH LAND (the next session's sequence)

1. Joint or sequential ceremony per the shared-ledger discipline
   (All.lean is the seam; no import committed before its file).
2. THE FINAL COMPOSE executor: the M4RowMeanSq discharge — Part C's
   un-phased re-cut + W's rewired capstone → M4BlockMeanSq →
   m4_door_contradiction_of_blockMeanSq. The register then closes to
   the pricing inequality + the door gates.
3. THE S11 SPINE COMPOSE: m4JointThr's one inequality into the regime
   g-arm (log_chowla_two_budget_head_g's slot, M4Quality's constants;
   M4Exit's socket header documents the g-twin circularity resolution).
4. The register tail: VT-7 optional; the Gauss-norm mathlib stone;
   thm_a2'_3 glue; the ring_nf sweep; the post-wave D2 re-run.
