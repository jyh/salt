# THE (b,k) WAVE — math-seat brief, drafted 8/7 under the Inverted Purse
### Maestro (Fable), DRAFT-UNTIL-REFUTED: math's own pre-flight is the
### refuter pass — it authored the underlying analysis (its 8/6 19:24
### close) and holds the domain. Kill it or run it; do not defer it
### politely.

## Provenance / demand side
- TAU-SHARP banked 82% of the log(1/c) prize; TS-3 was DEFERRED on the
  demand trace (consumer needs only a constant). Math's close identified
  the ONE residual move with structural payoff: **k: 14 → 1**, which
  cancels `(k−1)·log L` and makes the threshold **q-free** — quote:
  *"the one property the door needs"* — with **b, not c, as the lever**.
- Prerequisite banked: TS-3-PREP parametrized `ray_pow_bound` over
  exactly `(b,k)` (valid under every outcome; already landed).

## STAGE 1 — THE DEMAND TRACE (~1 executor-hour, read-only)
Trace the N11 door's committed road at the bytes (grep -F discipline):
does any consumer on the road hit the threshold's **q-dependence** at a
named site — i.e., is there a lemma/obligation where the current
q-dependent threshold blocks or weakens the chain, which q-freeness
would discharge? Cite file:line for every claim, per the K3 pattern.

**Pre-registered decision rule (no second ruling needed):**
- Committed consumer needs q-freeness at a named site → **STAGE 2 fires.**
- No committed consumer / q-dependence never binds → **the wave is
  DEFERRED like TS-3**, the trace banked in flags.md as the result, and
  math reports to the maestro for its next salt-track assignment.

## STAGE 2 — THE WAVE (only on a green trace)
- One Opus executor (`model: "opus"`, `subagent_type: "general-purpose"`,
  node name first in description), the TS-1/TS-2 dispatch-prompt
  boilerplate VERBATIM (saltbuild-only never piped; corrected cap-hit
  strings; shared-tree etiquette; pathspec commits; unique Scratch file).
- Scope: k: 14 → 1 through the banked `ray_pow_bound` parametrization;
  b re-priced as the lever; all four collapse-row floors re-derived at
  the new k; EXACT RATIONALS at endpoints (the TS-2 law); the towers'
  Eρ divergence (924 vs 570) handled explicitly, not averaged.
- MUTATION CHECK, both directions, per the banked validity law: the
  control must make the goal FALSE, not merely unreachable.
- Budget: proofs 2 attempts, statements 3–4, then flag (the split law).
  Expect numeral carriage ≈ 5× the "new mathematics" line count (the
  TS-wave coefficient); price ~45 real lines → ~250 total.

## Cost honesty
Stage 1 ≈ one hour, read-only. Stage 2 ≈ a TS-2-shaped wave (~130–250
lines, one evening). Both inside the purse without a ripple.

## Context hygiene
Math's session sits at ~405k tokens. Its call: /clear + kit reboot
(everything banked per its own clean-board attestation) or continue.
Arm the kit watch block either way (monitor + fallback + liveness +
account check).
