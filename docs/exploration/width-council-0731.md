# THE WIDTH COUNCIL — 2026-07-31 evening
## The three-faced wall, the escapes, and what The Witness gets to say

*Convened by JYH on the dossier of 180b7fc. The ledger of record:
flags.md from "B4-SUPPLY SCOPE" through "REF-REPAIR lands 7/7". In the
kernel: `logChowla2_witnessed_scale_final'` (K = 32M; ε/δ₀/Cg proven;
the empty rider pair NAMED in its header). The seal: not granted.*

---

## C1 — THE WALL, and the four exits

**The wall, in its final honest form (all three faces kernel-backed):**
(i) the entropy pigeonhole hands the cap bundle an *uncontrolled*
H ∈ [H₋, H₊] (`entropy_decrement`'s ∃H — WIDTH-SCOPE's dead-at-source
verdict stands); (ii) at an H₊-socket the headroom forces
loglog X_d ≥ e^{λ₊}/2 with the tower's λ₊ ≥ λ₋³ (kernel theorem);
(iii) the budget ceiling is K-linear with K capped twice — by the
frame (1.7·10⁸) and by the lever's own recoil (the band mass gains
2^{1.05K}, landing on M, capped by the window at K ≲ 0.55·λ₋).
λ₋³ versus λ₋: the squeeze is structural.

**Exit (a) — THE LEVEL-2 BAND PAIR (unpriced; the council's discovery
of the day).** The lever's recoil exists because the band chain's
mass ratio reads log 𝒬₂ / log 𝒫₁ — level 2 over level 1 — so 2^K
survives in the ratio. Instantiated at the level-2 pair (𝒫₂, 𝒬₂),
the ratio is K-free and the recoil vanishes; face (iii)'s second cap
dies, leaving K free up to the frame's 1.7·10⁸ — which covers
λ₋³ up to λ₋ ≈ 492, comfortably above every working point.
**Unknowns**: does the band/T₀ chain's P *have* to be the level-1
cutoff (the ram window's lower edge)? What re-prices in
`windowMassConst`'s c-exponent (c = 1 + 1/(𝒫−1) — at 𝒫₂ the c is
*closer to 1*, which helps)? Does the T₀-datum discharge survive the
re-based window?
**RECOMMENDATION: scope it now** — one scoper + refuter pass, the
B4-SUPPLY genre, before anything else is decided. If (a) lives, the
wall dies at faces (ii)+(iii) simultaneously and the unconditional
road reopens on the existing lever machinery.

**Exit (b) — the old three, re-priced with today's knowledge**: narrow
the ∀-H (dead at the pigeonhole — unchanged); re-price the thinBundle
tail (buys ≤ 2× — unchanged); the polynomial-window regime (deletes
the spine — unchanged). Held, not re-scoped.

**Exit (c) — accept the conditional** (see C3). Not a surrender: a
strategic sequencing of what gets claimed when.

## C2 — THE COMPANION CERTIFICATE + THE R.x EXPOSURE (statement law)

The refuters' structural findings, proposed as standing law:
1. **Any sealed final object carries a companion certificate**: the
   dischargeable riders proven at the actual witnesses; the opaques
   NAMED with their provenance (the ∃-site) and their generous
   ceilings justified. An `∃c, P c ∧ (Q c → R c)` with Q unexamined
   is not sealable — that is the lesson of all four catches.
2. **The R.x exposure**: the regime builder's own R.x construction is
   carried as an upper conjunct through the road (the HLO-EXPORT
   genre, fourth use), so the base-scale cap converts from an
   unverifiable assumption into a checkable one (REF-FINAL-SAT's
   item W).
**RECOMMENDATION: ratify both as law; execute when C1's road settles.**

## C3 — WHAT THE WITNESS SAYS (the strategic item)

The object that exists today: *a machine-checked reduction of
windowed log-averaged two-point Chowla at witnessed scales to a
short, fully named hypothesis list — every item effective except
Siegel's own constant, every wall mapped in the kernel, every
false road refuted by the program's own machinery.* That is a real,
publishable theorem-shaped contribution — arguably the most honest
artifact of its kind anywhere: the paper would not claim the
unconditional result; it would claim the reduction, the method, and
the walls, with the kernel refereeing all three.

**The fork**: (α) The Witness states the conditional object as its
theorem now, and the unconditional becomes the gold-road's next
mountain (possibly falling to exit (a) while the paper is in
review); or (β) The Witness waits on C1's scoping — if exit (a)
lives on a weeks-scale, the paper states the stronger form.
**RECOMMENDATION: decide after C1's scoper reports** (days, not
weeks) — the fork's price is small and the information is coming.
Either way, the name still fits perfectly: the theorem *is* about
witnesses.

## C4 — THE RIDER CLEANUP (do regardless)

Kc / Ct / Hcap traces + the five constant riders: the EPSPIN/CgPin
genre, one at a time, cheap, parallel with everything. Each trace
either proves the rider (removed-because-proven) or finds its honest
bound. **RECOMMENDATION: authorize as a background lane.**

## C5 — HYGIENE (the standing queue)

The duplicate private decl (M4ArithRho); the unclosed-namespace
pattern (7 files — close them in one sweep or ratify the style); the
full leanchecker ceremony (owed by BOTH the summit protocol and the
Pi prep — dual-purpose, compute-only). **RECOMMENDATION: the
ceremony fires this weekend as background compute; the rest rides
the next quiet hour.**
