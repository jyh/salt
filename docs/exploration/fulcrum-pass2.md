# THE SUPPLY LEDGER — Pass 2 verdict (2026-07-19 02:45 PT; 9/9 agents; both syntheses survived 0-refutations)

## THE ¬F SUPPLY LEDGER

THE ¬F SUPPLY LEDGER (judge-final, 2026-07-19). ¬F(C⋆) ≡ ∃Q₀ ∀q>Q₀, ∀ primitive quadratic χ≠1 mod q, ∀ρ: L(ρ,χ)=0 → ‖1−ρ‖·C⋆·log q > 1 (correct de Morgan of FulcrumQualityMin, GROUNDED Salt/Fulcrum/Basic.lean:61-64, judge-read). Calibrated regime C⋆ ≥ 2/c₀ throughout. Every entry judge- or refuter-verified live; MEMORY demotions explicit.

TIER K — kernel-composable TODAY (zero new Lean beyond one contrapositive):
K1. ¬F(C⋆) ⟹ ¬IMSZ ⟹ NoSiegelZeros. Route: contrapositive of imsz_gives_fulcrum_witnesses (GROUNDED Gadget.lean:128-131 — takes ONLY IMSZ; the gadget siegel_zeros_isolated_below is a landed sorry-free THEOREM at :93-96, induction, consumed internally at :133) + noSiegelZeros_iff_not_infinitely (GROUNDED SiegelTwin.lean:129-131). NO propositional debt: SiegelModulusUnbounded (Basic.lean:73, a def) is consumed ONLY by the superseded IMSZ→F artifact at Basic.lean:194 (judge grep). E1 catch-3 ruled STALE — catch #239 fossil, resolved against E1. Caveats: the ∃c in NoSiegelZeros is bare; below-Q₀ radius c_iso(Q₀) is nonconstructive (ContinuousAt.eventually_ne).

TIER B — B-class glue on landed legs:
B1. Uniform no-Siegel standoff, ALL q: β ≤ 1 − c/log q with c = min(c_iso(Q₀), 1/C⋆) [E3-D1].
B2. Three-zone effective ZFR: ∀ primitive χ, zeros with Re ρ ≥ 1/2 obey Re ρ ≤ 1 − c₁/log(q(|Im ρ|+2)), c₁ = min(c₀, 1/C⋆, c_iso(Q₀)) — legs: zero_free_region_all (GROUNDED ZeroFreeReal.lean:605-608), gadget below Q₀ (real form suffices), ¬F above [E3-D2, case-checked]. Complex-ball-all-q variant needs one A/B node (gadget is real-only — catch #224).

TIER C — C-class re-tracing (obstruction removed; constants NOT traced today):
C1. THE HEADLINE — effective Siegel–Walfisz. The landed chain (siegelWalfisz_holds, GROUNDED Gate.lean:150) has exactly ONE ineffective input: Cₛ from siegel_theorem, sole consumer Fold.lean:167 (judge-read), applied at conductor level (:282-283). Under ¬F: 1−β₁ > 1/(C⋆·log f) closes the same e^{−c5√log x} kill; K(A,C) effective in (A,C,C⋆,Q₀,c₀,c₃,c_iso). Source: MNT Cor 24.24/24.25 effectivity remark (GROUNDED-source montgomery3.txt:7576-7614). Scope: SW quantifies all characters; the ZFR carve-out tiles (B2) and imprimitives reduce via psi1_transfer (Fold.lean:154) — no gap [E2 §1].
C2. Super-SW error x·exp(−c·log x/loglog x) + extended range q ≤ exp(log x/(c₂·loglog x)) — GROUNDED-source MNT Thm 24.22 (montgomery3.txt:7504-7537; E₁ term the sole obstruction) + Ex. 28.4.3.1 (:20087-20109). DEMOTION: the conductor-divides-q bridge is MEMORY-routine [E4 §0b]. Latent: no landed consumer demands it.
C3. Effective Gallagher/Linnik: Thm 28.20 fed with κ = max(κ₀, C⋆) (GROUNDED-source montgomery3.txt:23113-23124, κ₀ at :22945-22948); Linnik Thm 28.21 no-zero case (:23128-23140). Effective-for-q>Q₀. DEMOTION: numerical A values (Xylouris 5.18 etc.) MEMORY.
C4. Effectivized consumers (eat list): bounded_gaps_unconditional K (Gate.lean:376), BV haircut, Maynard thresholds, Chen op_floors (TheHeadline.lean:55), Goldbach A1, effective Linnik. ALL truth values unchanged — every consumer already landed unconditional; ¬F buys effectivity ONLY, invisible in kernel Props [E2 §6, E4 §1].

TIER N — needs ONE new C-class node (the Hecke converse: "no real zero in (1−δ,1) ⟹ L(1,χ) ≫ δ"; corpus holds only the cap direction LFunction_one_re_le_mvt_sharp; positivity substrate landed, DHRepulsion.lean:36-41):
N1. Effective L(1,χ) ≫ 1/(C⋆·log q), q > Q₀. DEMOTED: conditional on the unbuilt node; the classical route is MEMORY (MV vol. 1 — montgomery3.txt's 28.62 is a POINTER, not a staged proof). Class-number corollaries: MEMORY, consumer-free in Salt.
N2. Effective non-pretense (anti-surrogate). GROUNDED half: the zero-route supplier of small PretenseSum (TransferFull.lean:183-185) is definitionally cut — η < C⋆ ∀q > Q₀. DEMOTED half: "PretenseSum ~ ½·log N large" needs N1 + a MEMORY Mertens-for-χ step. Precision (R3): hres (hb_lemma2, :205-214) becomes satisfiable-but-VACUOUS beyond Q₀ (RHS exceeds x-scale), not unsatisfiable.

ANTI-SUPPLY (what ¬F deletes — load-bearing for Z):
A1. HB engine starved: HeathBrownDichotomy (SiegelTwin.lean:97) resolves RIGHT via K1 with zero twin yield; SiegelSequence false (siegelSequence_implies_infinitely); the surviving S1 ≤ S2 (GROUNDED Transfer.lean:189-192, termwise) is an UPPER bound only.
A2. DH repulsion subsumed above Q₀: dh_repulsion_ordered RHS ≤ 2^{−250}/(log q)^14 < 1/(C⋆·log q) at calibrated C⋆ ≈ 2^18 (arithmetic refuter-re-done) — F-side artillery only [E3-D5].
A3. The exceptional-character Möbius surrogate — the only known unconditional family parity oracle (GROUNDED-source H-B p.196 via E4, PDF outside working dirs) — certified absent at effective quality for q > Q₀.

HONEST ZEROS: character sums (Polya–Vinogradov unconditional; Burgess absent) [E1-D7]; twin_almost_prime (no character/L input; Ω≤20 sieve-combinatorial) [E2 §3]; the M₂/twin_bar door (no distributional hypothesis in its statement — nothing to feed) [E2 §4]; the entropy spine (MRT-gated, character-free at the def, dichotomy-invariant) [E3-D9]; VK t-aspect chain (disjoint aspect; family-uniform pow-in-q unreachable) [E3-D7].

SCOPE NOTES (catch #224): marginal content = REAL zeros of quadratic χ only at calibrated C⋆ (fulcrum_zero_real hC : 2 ≤ C·c₀, judge-read — the complex ball is already landed ZFR; below 2/c₀ the reality derivation FAILS and accounting changes); SW kill runs at CONDUCTOR (Fold.lean:282) — instantiate ¬F at conductors, finite debt = conductors ≤ Q₀; Q₀ is a bare ∃ — every "effective" is effective-for-q>Q₀, in-principle below; c₀ = 1/126848 docstring-grade, numeral extraction owed (Basic.lean:169).

MEMORY DEMOTION REGISTER (do not treat as grounded): Hecke L(1) route (MV vol.1); Mertens-for-χ; class numbers h(−d); Xylouris/Akbary–Hambrook shapes; conductor-divides bridge; TPC ⟺ B(x)-unbounded-mod-dust bridge (A/B, unlanded); general-weight parity theorem (Selberg 1949/Bombieri — kernel holds only the k=2 instance); "no published no-Siegel⟹twin-adjacent" null sweep (E4 §5, deliberate but MEMORY).

## Z — THE GAP STATEMENT

Z — THE GAP STATEMENT (refuter-survived form; repairs R1-R5 applied). Fix C⋆ in the calibrated regime C⋆ ≥ 2/c₀. Let 𝒟(¬F) be the supply ledger's cone (K1-N2). Let 𝑻(θ) be the Type-I twin sieve system: data {|A_d(x)| = g(d)x + r_d}_{d≤x^θ}, g the twin singular-series density, Σ_{d≤x^θ}|r_d| ≤ x(log x)^{−A}, θ ≤ 1. E is parity-invariant (E ∈ ParityInv) iff E holds in every completion of 𝑻(θ) — in particular in the twin-free completion 𝒜⁻: identical Type-I data, pair-support biased onto λ(n)λ(n+2) = −1 (weight 1 − λ(n)λ(n+2)); since twin pairs carry λ(p)λ(p+2) = (−1)(−1) = +1, 𝒜⁻ contains no twins and B(x) := Σ_{n≤x}Λ(n)Λ(n+2) = prime-power dust there [R1 fix applied; existence of the twin-free Type-I-consistent completion is MEMORY-formal (Selberg 1949/Bombieri asymptotic sieve) with the kernel k=2 instance as in-house witness].

Z: TPC requires an estimate E, usable as a LOWER-bound input for B(x), with E ∉ ParityInv — and 𝒟(¬F) ⊆ ParityInv.

The gap is two-sided: (Z1) ORIENTATION — twins need a lower bound on a binary correlation; 𝒟(¬F)'s lower-bound control is linear-only (ψ(x;q,a), single L-values, zero locations), and its ONE binary object, S1 ≤ S2 (GROUNDED Transfer.lean:189-192), is an upper bound; upper bounds hold in 𝒜⁻ (Brun's landed track already lives there). (Z2) PARITY — the crossing from the landed ceilings (chen_headline: Prime ∧ IsP2 2 (p+2), GROUNDED ChenTheorem.lean:32, whose Ω(p(p+2)) ≤ 3 form is an UNLANDED A-class corollary [R4]; Ω(n(n+2)) ≤ 20, TwinInstance.lean:771) down to Ω = 1 twice is invisible to 𝑻(θ) at every θ ≤ 1. Kernel witness at k=2: twin_bar J₁F+J₂F ≤ 2log2·I₂F (GROUNDED Impossibility.lean:173), twin_gate_fails ∀θ∈(0,1] (:262), no_twin_weight (:276), atom 2log2<2 (:161) — judge-read, hypotheses are continuity + θ≤1 ONLY, so no ¬F consequence can enter. General-weight form: MEMORY-formal. (Z3) EFFECTIVITY-INERTNESS — 𝒜⁻ satisfies every 𝒟(¬F) statement with the SAME effective constants; parity ambiguity is not a constants deficit, so ¬F's entire cash-out (effectivization) has measure zero against Z; twin_gate_fails' θ-quantifier prices and rejects every distributional strength up to θ=1 (full EH grade — itself beyond ¬F's reach: level 1/2 is large-sieve-limited).

RELATION TO THE PARITY BARRIER. (i) Z is parity-hard with the necessity direction EXACT: every E witnessing Z must break parity-invariance. (ii) Z is NOT parity-complete: no theorem, landed or classical, says parity-break + corpus ⟹ twins; the sufficient shape of E is open (FI a²+b⁴ needed specific bilinear structure). Z is a DEMAND SPECIFICATION, not an impossibility theorem — a future bilinear estimate in the twin configuration enters through Z's front door. (iii) NO ¬F deliverable carries parity-breaking information — element-exact audit, two near-misses priced: (a) S1≤S2 + effective evaluation: genuinely new vs classical no-Siegel analyses, parity-inert by ORIENTATION; (b) effective non-pretense: parity-RELEVANT with NEGATIVE sign — the grounded half (zero-route supplier cut for q>Q₀) certifies the oracle's absence; the strong form (PretenseSum provably large, hres vacuous) is CONDITIONAL on the unbuilt Hecke node + MEMORY Mertens-for-χ [R3]. The corpus's bilinear artillery is not ¬F-fed: Weil/Kloosterman bank oracle-gated (sole external consumer Salt/HB/All.lean, refuter grep); entropy spine λ-side and non-transporting (Tao verbatim, chowla.txt:196-200); MRTUniformity Siegel-insensitive.

THE LOCALIZATION. Over F(C⋆) ∨ ¬F(C⋆): F-horn — twins reduce to constants engineering (C⋆ vs C⁽¹⁾ = exp exp(2A/𝔖(α)), GROUNDED-source H-B p.223 via E4, + the unbuilt HB-ENGINE). ¬F-horn — twins REQUIRE (one-directional [R2]; sufficiency unpriced) a parity-break, lower-bound-oriented, in the (n,n+2) configuration, with no exceptional-character oracle: the bare parity problem, posed in the strongest linear world in the ledger — where "certified" attaches ONLY to K1 (kernel-composable today); all effectivity below Q₀ or global-in-q carries the c_iso-nonconstructive and bare-Q₀ asterisks [R5]. ¬F simultaneously maximizes the linear-effective cone AND deletes the one mechanism classical work knew could break parity family-uniformly — it pays entirely in a currency to which Z is insensitive. Refuter's missed-combination hunt (6 probes: Chen-minus-companions, S1≤S2 complementarity, hb_lemma2 reverse, super-SW+circle, k≥3 pair-selection, log-Chowla transport) all negative; judge re-grounded the load-bearing anchors independently. Certification grade: cone inclusion verified element-by-element; necessity kernel-instanced at k=2; ParityInv is sketch-precision (Lean-grade definition = real design work).

## THE SEAM CANDIDATES

RANKED SEAMS FOR PASS 3 (refuter-survived; repairs F1-F4 baked in; honest odds; kill-checks first).

S1 — SURROGATE-EXCHANGE (rank 1). Keep the HB transfer half, swap the evaluation half. Geography (F1-corrected grounding, judge-re-verified): pair-sieve half (PairSieve/MixedCount/PairSieveMixed/PairInstance) has ZERO DirichletCharacter usage — its periodicity lemmas are divisor-condition D-periodicity in n, character-free; star half consumes χ only pointwise (chiRe); the genuinely periodic piece is the evaluation half (QuadCharSum.lean Jacobi sums, HB p.217). The original "grep EMPTY" citation was FALSE and is replaced by this per-file audit. KC1 (pair-sieve periodicity) is thereby ALREADY DISCHARGED — in S1's favor. Mechanism: manufacture the engine's inputs from a non-character real multiplicative f, f²=1, with small pretense AND evaluable S2(f). Boundary datum (grounded, arithmetic refuter-verified): at f = λ the transfer is TAUTOLOGICAL (Λ̃_λ = μ∗log = Λ via TwistChain.lean:98), so the parity barrier sits at BOTH poles — the seam question is whether any f strictly between beats both. Remaining KC2 (half-day): state the exchange-rate conjecture, test at the three poles (λ; characters-under-¬F; trivial χ with pretense = log N). Preconditions: B-class re-typing to pointwise f; stage Hoffstein–Lockhart (MEMORY) before touching GL(2) surrogates. ODDS: ~2% a twin-producing mechanism survives refutation; ~20-25% a formal exchange-rate WALL theorem (pretense × evaluability) — ledger value either way.

S2 — ENGINE-GRADE DIAL (rank 2). C⋆ = max(C⁽¹⁾, 2/c₀) is SHARED between horns; lowering C⁽¹⁾ (ShiuCore, Weil bank, T-BAL) weakens F-horn fuel demand AND strengthens the ¬F harvest — no wasted spend either way. SATURATION CLAUSE [F3, judge-verified at fulcrum_zero_real hC : 2 ≤ C·c₀]: harvest improvement caps at C⋆ = 2/c₀; below it the reality derivation fails, the limit-shape dichotomy must be restated on REAL zeros, and ¬F(2/c₀) no longer pins η < C⁽¹⁾ — calibration-dependent bookkeeping (upside corner: both-horn engine overlap; understates, never overstates twin content). KC (one session, no Lean): freeze the C⁽¹⁾ ledger from fulcrum_audit_source.md; mark each η ≥ C⁽¹⁾ consumption in HB pp.198-224 absorbable/not; window edges NOT in play (both bind, :166/:177 — note the internal 250-vs-300 lower-edge discrepancy, line 166 vs 183, before reusing numerals). Forced exp-exp shape = kill. ODDS: ~20-30% the dial moves ≥ exp-order; ~0% ¬F-side twin content (F-horn only — honest).

S3 — QUANTITATIVE-NONPRET (rank 3). ¬F's unique unmatched supply: via the missing Hecke node, L(1,χ) ≫ 1/(C⋆ log q) at polylog moduli (loglog-grade floors vs unconditional q^{-1/2}). Feed χ-legs of a QUANTITATIVE spine (S5 split form ∀Q ∃x₀, Salt/MR/NonPret.lean:114-123 — path corrected). Needs: Hecke node (C-class), quantitative entropy-decrement (D-class), a λ→Λ bridge — NONEXISTENT, Tao's disclaimer verbatim (chowla.txt:196-200). KC (half-day, arithmetic, decisive): the q-range exchange — likely kill mode is "no growing-q demand exists yet" (the landed spine is already fixed-q). ODDS: <1% twins; ~15% a ¬F-conditional quantitative-Chowla node worth landing; ~50% same-day death (if anything generous to survival).

S4 — FLOOR + DEFLATION AUDIT (rank 4; non-twin; near-certain payoff). Floor: C1/C2 headline + Q₀ audit + Hecke node + c₀ numeral ⟹ first fully explicit SW→BV→Maynard→Chen chain (explicit12 synergy). ~85% floor survives (modulo c_iso). DEFLATION RISK (run FIRST): MNT Thm 24.22 carries the exceptional term EXPLICITLY; an explicit-secondary-term re-plumb might deliver effective AVERAGE-form BV unconditionally (Linnik precedent; Akbary–Hambrook shape MEMORY), deflating the ¬F headline to per-q effective SW only (per-q CANNOT deflate — that IS the Siegel wall). KC split [F4]: (a) mechanical — DETERMINATE, the landed chain consumes the per-q SW Prop (DispersionClose.lean:439, AbelCore.lean:757); (b) design — classify each per-q use inside the dispersion proof as summable-over-exceptional-moduli or not; pre-stated criterion; UNDECIDED ⟹ deflation stays priced. ODDS: deflation real for BV-grade consumers ~35-40%.

PRICED OUT (do not respend): the M₂/twin_bar door (no distributional hypothesis exists in its statement — judge-read); T-BAL R8 as ¬F amplifier (subsumed above Q₀, arithmetic verified); HB window-stretch (both edges bind); ¬F-to-spine (dichotomy-invariant); both-horn composites for TWINS (¬F branch parity-capped; F-horn twins live only in [q^250, q^500]-type windows — composites yield effectivity only, absorbed into S4).

## THE PASS 3 BRIEF

PASS 3 TARGETING BRIEF (attack pass; JYH-attentive on the deep tank; kill-check-first discipline BINDING — no target gets design spend before its cheapest kill-check has run and been recorded).

STANDING RULES (binding): GROUNDED/MEMORY labels on every claim; honest-shape law — none of S1-S3 may be presented as a twin route (priced lottery tickets only); every dichotomy-composite claim must beat the H-B C⁽³⁾ vacuity benchmark (p.196); catches #224 (scope-diffs: real-zeros-only marginal content, conductor-vs-modulus, C⋆-regime) and #239 (fossils: E1 catch-3 now resolved-stale, BV/Defs.lean:31-33 stale comment) are checklist items; Z is a demand specification, not an impossibility theorem. The banked D1/D2 texts are the REPAIRED versions (R1-R5, F1-F4) — the unrepaired forms are dead.

TARGET ORDER (cheapest-decisive first, refuter-side first; ~3 sessions total pre-commitment, no Lean):
T1. S4 DEFLATION AUDIT (first — it can re-price the program's headline before anything is built on it). KC(a) is already determinate (per-q consumption at DispersionClose.lean:439/AbelCore.lean:757). Run KC(b): walk the dispersion proof, classify each per-q SW use as summable-over-exceptional-moduli or not, against MNT Thm 24.22's explicit E₁ term. Outcomes: DEFLATES → ¬F's C1 headline re-prices to per-q effective SW only, program value concentrates in S1-S3, and the explicit12 floor becomes partially unconditional (still a win, different owner); HOLDS → C1 stands as ¬F's flagship cash-out; UNDECIDED → risk stays priced at 35-40%, floor work may proceed with the asterisk.
T2. S3 Q-RANGE KILL-CHECK (half-day, arithmetic, decisive). Compute the modulus range a rate-version 2-point log-Chowla actually demands; if polylog-in-the-rate where unconditional q^{-1/2} floors suffice, S3 dies same-day — record and close.
T3. S1 EXCHANGE-RATE CONJECTURE (KC1 already discharged in S1's favor — do not re-run). Half-day: state the rate (pretense(f) × evaluability(S2(f)) trade), test at the three poles (λ-tautology; ¬F-starved characters; trivial-χ). Survival ⟹ commission the B-class pointwise re-typing + the interior-f hunt as a design block; death ⟹ bank the exchange-rate WALL theorem candidate (~20-25% — the likely real payoff) into the wall series.
T4. S2 C⁽¹⁾ LEDGER FREEZE (one session, no Lean). From fulcrum_audit_source.md (resolve the 250-vs-300 edge discrepancy first); mark each η ≥ C⁽¹⁾ consumption absorbable/not; state the F3 saturation clause (cap at 2/c₀) in the frozen ledger. Forced exp-exp = kill; movable ⟹ price a WP2 run.

STAGING DEBT (before any target leans on these — MEMORY until staged): Hoffstein–Lockhart (T3 GL(2) kills); Akbary–Hambrook explicit BV + Xylouris (T1); MV vol. 1 Hecke L(1) route (T2, N1); Opera de Cribro ch. 16 + Selberg 1949/Bombieri asymptotic sieve (upgrades Z's Z2 from kernel-instance+folklore to formal-theorem grade); re-stage H-B PDF pages 193-198/221-224 into working dirs if any T-target cites them load-bearing.

JYH DECISION POINTS (surface early, ripe vs ripens-when): (D1) land K1's contrapositive as an A/B Lean node now? (cheap; makes the ¬F horn kernel-real; also lands the Ω≤3 A-class corollary while there). (D2) fund the Hecke-converse C-class node? (unlocks N1 + N2-strong + T2's supply side; substrate landed). (D3) after T1: re-ratify the program valuation on the deflation outcome. (D4) is a Lean-grade ParityInv/Z formalization worth a Fable design block? (C-class+, real definitional risk — the model-theoretic boundary could shift; per the Fable workflow gate this needs JYH approval with an honest token estimate BEFORE starting). (D5) Q₀-witness finite-audit campaign (retires the bare-∃ and c_iso asterisks; pairs with explicit12).

WHAT PASS 3 MUST NOT DO: respend on the priced-out list (M₂ door, T-BAL amplifier, window-stretch, ¬F-to-spine, both-horn twin composites); treat any MEMORY register item as grounded; write Lean inside the attack pass without a ratified node; present effectivity yields as twin progress (Z3: effectivization has measure zero against Z).

## OPEN RISKS

- General-weight parity theorem is MEMORY-formal (Selberg 1949 / Bombieri asymptotic sieve): the kernel holds only the k=2 instance (twin_bar at every theta <= 1). If the classical statement has unremembered scope conditions, Z2 drops from formal-theorem to kernel-instance+folklore grade. Stage Opera ch. 16 + Selberg before Pass 3 leans on it.
- ParityInv is defined at sketch precision (completions of the Type-I system); the boundary could shift under formalization — admissible remainder norms, and whether bounded-coefficient Type-II forms can be smuggled into Type-I data. The cone inclusion was checked element-by-element against the CURRENT cone, not a closed definition.
- c_iso(Q0) is nonconstructive (ContinuousAt.eventually_ne) and Q0 is a bare existential: every effectivity claim below Q0 or global-in-q is in-principle only; the finite audit is possible but unlanded. 'Certified' attaches only to K1.
- C-star regime dependence: the whole audit ran at calibrated C-star >= 2/c0 (fulcrum_zero_real needs 2 <= C*c0, judge-verified); below that the reality derivation fails and marginal-content accounting changes shape. C(1) is UNFROZEN — C-star = max(C(1), 2/c0) may be C(1)-dominated, making E3's 2^18 a floor, not a value.
- S4 deflation risk (~35-40%): an explicit-exceptional-term average-form BV could deflate the C1 headline to per-q effective SW only. Must run FIRST in Pass 3; until then no valuation built on C1 is stable.
- MEMORY staging debt: Hoffstein-Lockhart, Akbary-Hambrook, Xylouris, MV vol. 1 (Hecke L(1)), Opera ch. 16; H-B page cites (p.196/221/223) rest on E4's session read of a PDF outside the working dirs — re-stage before load-bearing reuse.
- c0 = 1/126848 is docstring-grade only (ZeroFreeReal.lean:604, Basic.lean:169 numeral extraction owed); every constant-grade statement inherits this debt.
- TPC <-> B(x)-unbounded-modulo-dust bridge is MEMORY-routine and unlanded; the repo TPC (Salt/Basic.lean:25-26) is the set-infinitude form while Z is stated against B(x). A/B-class to land, but currently a seam in Z's own statement.
- No independent #print axioms run this pass (no-Lean-writing): sorry-greps only; axiom posture accepted on the project's iron-rule discipline and the 9136-green main. A hidden axiom in K1's import chain would need re-audit (mitigated by the explicit proof-term reads).
- Z is a demand specification, not an impossibility theorem: 'no FI-type bilinear structure for (n,n+2)' is MEMORY — a future bilinear estimate in the twin configuration would satisfy Z's demand without contradicting anything banked. Do not let the localization ossify into a no-go claim.
- S3's 50% same-day-death estimate may be generous to survival: the landed spine shape is already fixed-q, so the likely kill mode ('no growing-q demand exists yet') is structural, not arithmetic.
- Fossil watch (catch #239): E1 catch-3 (SiegelModulusUnbounded as shared debt) is now RESOLVED-STALE — any future doc citing it must cite the resolution; BV/Defs.lean:31-33 stale comment still in-tree; fulcrum_audit_source.md carries an internal 250-vs-300 window-edge discrepancy (line 166 vs 183) to resolve before numeral reuse.
