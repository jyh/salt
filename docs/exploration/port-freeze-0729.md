# THE KMT PORT — design freeze v1 (2026-07-29, the maestro's hand)

*The P-0 deep read: three readers over KMT arXiv:1909.12280v5 (§§1–9
divided along the paper's own seams), full reports in the session
record, essences banked in flags. Status: v1 DRAFT → refuter pass +
SPECTRUM-SCOPE → JYH authorization with the refuted plan and honest
price. The C2/D-8 gate stands: nothing below fires until JYH rules.*

## The headline (the price collapsed)

The 8–18k estimate was priced against "two genuinely new stones + a
port of KMT's §§6–9." The deep read dissolved most of it:

1. **Stone 1 (the hybrid χ-aspect MVT, KMT Lemma 6.2) is a WRAPPER**:
   the landed `MVHilbertUniform` (arbitrary Finset, arbitrary
   frequency map, arbitrary δ) + the AP gap lemma (per-class spacing
   δ = q/2N — one A-tier lemma) + the orthogonality fold (extract
   from CharLS's proof-internal haves). Class B/C, no new mathematics.
2. **Stone 2 (the χ-aspect large-value count, Lemma 6.5) is a T → qT
   SUBSTITUTION** into the landed `large_value_count` — the k-th-power
   / k! combinatorial core is character-free and ports verbatim; only
   the spine re-runs at the hybrid input.
3. **The χ₁ apparatus DELETES WHOLESALE** (Reader B's table): our
   datum is sieved-Liouville and the corpus's all-χ floor stack
   (capFreeFloor3, uniform in ALL χ, no excluded character) replaces
   the minimizer/subtraction/weak-row machinery with a two-step proof
   (floor → halasz_ball_decay). The deletion is also what makes the
   FOUR-FOLD decomposition legal (per-datum minimizers would leave
   four undominated weak rows — fatal at the raw target).
4. **The exceptional-modulus/zero-density block is VACUOUS at
   q ≤ (log H)¹²** by the paper's own definitions (three independent
   textual witnesses); the typicality hypothesis likewise (Lemma
   9.1's second clause). Every theorem is exception-free and
   unconditional at our corner.
5. **The Parseval/dx step is the LANDED Lemma 14** + the orthogonality
   prefix (the paper's own proof says "follows [32, Lemma 14] almost
   verbatim").
6. **Shiu is landed for exactly KMT's g** (`shiu_moment_sq` at
   blockDiv; absolute constant); **the Ramaré ∫r^ω device is landed
   byte-for-byte** (RamWeight); the parameter cascade at tiny q runs
   MR's own cascade with h in place of h/Q (Q₁ = h^{1−o(1)}).
7. **THE ECONOMICS (Reader C)**: the paper's §3.5 remark — for
   μ-genre data, Siegel–Walfisz in place of Halász buys
   (log)^{−2+o(1)} — moves the port's Hlo demand from
   lnln Hlo ≈ 5.25·10⁴ (my corrected number; the docs' 4.5·10⁴
   dropped the kernel prefactor) to **lnln Hlo ≈ 125**, under the
   ALREADY-RULED 2³⁶ anchor with ~26× margin. The remark is unproved
   in the paper — the port executes the adaptation;
   `siegelWalfisz_holds` is the landed tool. **THE SW ROUTE IS THE
   CENTERPIECE.**
8. **Ineffectivity: exactly one constant** (Siegel, via the χ₁-row
   discharge), entering as a single ∃-H₀ gate (A := 13 makes Cor
   1.7's exceptional-multiple clause vacuous at q ≤ (log H)¹²); the
   H₀red fold pattern. [Reader C's aside, banked not consumed: the
   effective-Landau route caps the modulus exponent at 2−o(1) — the
   arc's 12 is what forces Siegel; B₅ = 12 is the S7 summit's pin,
   not slack.]

## THE ONE OPEN SEAM — the large-spectrum case (the port's real risk)

KMT's 𝒰-case needs Lemma 6.4 (Halász–Montgomery hybrid — the one
statement with NO proof in the paper, a Montgomery LNM 227 pointer)
+ Prop 8.5 (the (a_p)-arbitrary large-values count — the one place
no pretentious floor helps; Brun–Titchmarsh absent from the corpus).
Reader A's criterion: 6.4 is avoidable iff h ≫ q·X^ε — FALSE at our
scales (our windows are polylog-vs-X) — so on KMT's own route 6.4 is
required. BUT two escapes exist and the corpus may own the second:
- (i) footnote 7's calculus: the (log X)^{O(1)} lossy count (Lemma
  6.5 alone) is "fatal" only against KMT's 1/log-x-thin savings; our
  budget is (loglog H)^{−2}-thick — the numerology must be re-run
  honestly at our corner (it may afford the lossy route and delete
  6.4 + 8.5 outright).
- (ii) THE CORPUS-ARCHITECTURE ROUTE: at q = 1 the corpus's own
  thm_a2' arc already closed the identical large-spectrum case with
  its landed USet/spectrum machinery + the Vinogradov–Korobov region
  — and the VK-TWIST campaign (7/28) threaded the χ-twist through
  that very massif at exactly conductor ≤ polylog
  (capFreeFloor3_lamChi_unconditional). The port may run the
  corpus's 𝒰-leg per-χ + Σ_χ instead of KMT's §8.
**SPECTRUM-SCOPE (dispatched with this freeze) settles (i) vs (ii)
vs honest-6.4-port before wave P-5 is priced.**

## The architecture (D-P1): THE CORPUS-ARCHITECTURE HYBRID

Run the corpus's OWN landed window/seam machinery (the MR-track
five-summand genre, already kernel-checked at our exact window shape)
at the χ-twisted four-fold datum, χ-SUMMED, with KMT's §6 stones as
the suppliers and the all-χ floors as the χ₁-killer. KMT's §9 is the
map, not the code. Iron rules for every wave: the four-fold split
applies ONLY to the f-slot, never the g-slot (Reader B step 2); the
ellLin prime-power discrepancy checked per pairing (step 5); the
N1–N7 normalization dictionary (Reader A §4) carried in every brief;
the φ(q)/q sharpness DROPPED everywhere (cost ≤ logloglog H, budgeted
once — Reader A §3(f)).

## The waves

| # | wave | content | est. | class |
|---|---|---|---|---|
| P-1 | THE SOCKET AMENDMENT | the A-floor antecedent (Reader C f1: the socket is unsuppliable at A = 1); the piecewise-RS design (trivial grade at j < j₀ per the register's own han shape) | 100–200 | B |
| P-2 | THE HYBRID MVT | MVHilbert's Finset generalization + the AP gap lemma + the orthogonality fold + Lemma 6.2's assembly (explicit constants: 2φ(q)T + 13φ(q)N/q) | 400–700 | B/C |
| P-3 | THE COUNT + THE FOLD | 6.3 (Gallagher, the corpus's own written-out q=1 template) + 6.5 at T → qT (the spine re-run; the corpus's product-form weakening accepted if the budget check passes) | 300–600 | B |
| P-4 | THE MOMENTS | 6.6 (Shiu landed + 6.2) + 6.7 (the Ramaré bridge χ-lift; the third term = the sieve remainder, discharged by the landed density bound) | 400–700 | B/C |
| P-5 | THE SUP-NORM ARM | the "new Prop 8.3": capFreeFloor3 (landed) → the q-adapted Halász (the corpus's halasz_ball_decay at the χ-datum; φ(q)/q dropped; the c = 1/e grade carried honestly) + THE SW ROUTE (the §3.5 adaptation at the λ-datum — the centerpiece; SW landed) | 500–900 | C |
| P-6 | THE SPECTRUM CASE | per SPECTRUM-SCOPE's verdict: (i) the lossy-count route / (ii) the corpus 𝒰-leg χ-lift / (iii) the honest 6.4 port from Montgomery | 400–1500 | C (D-risk if (iii)) |
| P-7 | THE ASSEMBLY | the χ-summed window mean square at the four-fold datum through the corpus's seam architecture; the ε→ε^1.1→ε^6 bookkeeping (Reader C flag 5); the socket discharge + m4_second_road's item-11 consumption; the ∃-H₀ Siegel gate folded | 800–1500 | C |

**Honest total: ~3–6k ln** (against the pre-read 8–18k) with the
5.3× token coefficient applied to the NEW estimate: **~6–15M tokens
over 2–4 days.** The refuter pass may move P-6's band.

## The refuter questions (with the freeze, before JYH)

R-P1: the SW-route arithmetic (lnln Hlo ≈ 125 — re-derive; the §3.5
adaptation's actual proof obligations at the λχ̄-datum, listed).
R-P2: the four-fold × χ-summed composition at the raw target — no
step anywhere needs a per-datum minimizer (Reader B's step-1 break
verified closed by the floors at EVERY wave, not just P-5).
R-P3: P-2's δ-shift arithmetic (the AP gap q/2N; the constants).
R-P4: the ε-cascade bookkeeping (Reader C flag 5) at our ε.
R-P5: the socket amendment's shape (A-floor vs A-graded RS) against
every landed consumer.
