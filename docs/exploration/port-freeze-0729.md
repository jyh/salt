# THE KMT PORT — design freeze v2 (2026-07-29, post-refuter; supersedes v1 in full)

*Inputs: the three deep reads + SPECTRUM-SCOPE (P-6) + the refuter
pass (REF-P-MATH: R-P1 AMEND / R-P3 SAFE; REF-P-SHAPE: R-P2 AMEND
w/ two sub-kills / R-P4 the cascade KILLED + re-derived / R-P5
AMEND). All banked in flags. Status: v2 → JYH authorization. The
honest price is stated last and plainly.*

## What the refuters changed (the load-bearing corrections)

1. **KMT cannot be cited as a black box AT ALL.** The printed
   theorems' own hypothesis ε ≥ (log(h/Q))^{-1/200} = 0.42 at our
   windows excludes our demand by 109 orders. The §3.5 SW adaptation
   is load-bearing for SATISFIABILITY, not economics. Likewise the
   paper's ε-cascade (ε → ε^{1.1} → ε⁶) is inadmissible at any ε of
   ours — **the port runs its OWN cascade** (P₁ ≈ log(h/Q); the
   named repairs: the T-split at T₀ ≈ (X/h)·ε^{-1}; the ladder's
   j = 2 justification re-based; the H/Z parameters at t-genre;
   uniform-in-j throughout — J ≈ 1.3·10²¹, no finite unfolding).
2. **The supply that survives is the architecture ceiling**
   ((loglog t / log t)² at t = log h): delivered exponent α = 1.941
   vs required 1.449–1.706 — **headroom 0.23–0.49 of exponent; the
   "−2+o(1)"'s o(1) may not exceed ≈ 0.29** (the P-5 executor's
   standing gate). Margin at the honest floor: 10³⁷.
3. **The centerpiece re-named:** not "SW, landed" but **a χ-twisted,
   t-uniform μ-rate**, of which the landed `siegelWalfisz_holds` is
   the Λ/t=0/long-sum corner. The obligations: O1 Λ→μ rate LANDED
   (mmuRate_holds; the stale LambdaRate:20 comment flagged); O2 the
   μ→λ bridge LANDED; O3 the χ,t-twisted bridge mechanical
   (complete multiplicativity distributes over the convolution;
   250–400); **O4 the χ-twisted μ-rate (the MobiusRateClose re-run
   at L(s,χ), 700–1400); O5 the t-aspect = the χ-VK/Landau–Page
   region (800–2000) — THE SAME STONE as P-6's B**; O6 the g_r
   perturbation (μ·g_r = μ ∗ w_r, a genuine new convolution
   argument, 400–700); O7 settled by the cascade kill (the port's
   own cascade has no ε⁶ slot).
4. **The scales adjudicated — THE X-SCALE LADDER (R-P4(iv) ×
   R-P5):** the supply ladder's last rung needs loglog A ≳ 285,
   unreachable at the H-scale without breaking the 2³⁶ gate-8
   sandwich (an Hlo bump to 285 ⟹ tower 1.9·10¹² > the 8.2·10¹¹
   cap). THE RESOLUTION: the socket's bases ARE x-scale (the door
   ladder), so the socket carries R-P5's three antecedents — 2^j ≤ A,
   √H ≤ A, AND x/(4ω) ≤ A (suppliable: doorLadder ≥ x/2^i + hcount;
   an antecedent, not a register constraint — F5-census hygiene
   noted) — and the ladder + the floor thresholds read loglog A ≈
   loglog x ≫ 285. **No Hlo bump, no anchor movement: the ruled 2³⁶
   stands at its own [173.3, 241.5] sandwich, margin 5.25× — the
   port adds ZERO H-demand.** (v1's "125, ~26×" struck: 125 was
   non-binding, the 26× was not the port's margin.) If the capFree
   floor arm is used anywhere, the spine's g-arm value bumps
   10²⁴ → ≥ 2.2·10⁴⁴ — g is consumer-chosen at instantiation, FREE.
5. **The four-fold × χ-summed composition is SAFE** (no per-datum
   choice of the fatal genre anywhere: M_range's t-inf is uniformized
   by the ∀v floors; t₀ is a parameter; the cost is CS-diagonal 4,
   16 worst, never 4^J) — **with two floor-leg repairs**: the
   margin/pieceDatum floor forms still carry the crude q-debit and
   the e^{e^100} block (F4/VT-7's sharp family did not cover these
   two) — `capFreeFloor3_margin_all_chi_sharp` + `_pieceDatum_sharp`
   (~150–250, class B) join wave P-1; the pieceDatum floor at the
   four data is LANDED (the D-slot carries the 2·ln2 deviation) —
   v1's citation corrected.
6. **The socket amendment (P-1) re-shaped:** all three antecedents
   stated at once (they only weaken the socket; the trivial
   witnesses survive); the piecewise-RS needs NOTHING (han reads
   only j ≥ j₀; below it the trivial witness inhabits). ~250–400 ln,
   12 statements, none frozen-tier. **The wave order fix: P-1 lands
   AFTER P-5's route is pinned in this freeze (done — the x-scale
   ladder), so P-1 can fire first in execution.**
7. **P-6 as scoped** (SPECTRUM-SCOPE): the 𝒰-leg χ-lift; Lemma 6.4
   provably unnecessary (the 𝒯_S razor); the one new analytic stone
   = the χ-VK region (stones A–E; the D-risk = stone B's 3-4-1
   lower-bound leg at VK width — **PROBE FIRST**, it is also O5's
   engine). KR-2 (the q^{3/2} in the region's width) checked, not
   assumed, at P-6b.

## The waves (v2 — execution order)

| # | wave | content | honest est. | class |
|---|---|---|---|---|
| P-0′ | **THE B-PROBE** | stone B's feasibility: the 3-4-1 lower-bound leg for L(s,χ) at VK width from the landed classical-width template (ZeroFreeReal §2) + PowRegion's shape — a scoper-executor pair, ~3 attempts, STOP-and-report genre | 300–600 (or a STOP verdict) | **C/D — the port's go/no-go** |
| P-1 | the socket antecedents + the two sharp floor forms + O3's twisted bridge | 650–1050 | B |
| P-2 | the hybrid MVT (the Finset re-cut MANDATORY — the naive route loses a factor q, fatal; the fold ≈150–200; the assembly) | 700–1000 | B/C |
| P-3 | 6.3 + 6.5 at qT | 300–600 | B |
| P-4 | 6.6 + 6.7 (Shiu landed; the Ramaré bridge χ-lift) | 400–700 | B/C |
| P-5 | **THE TWISTED RATE** (O4 + O5-via-P-6b + O6) + the port's own cascade | 2200–4500 | C |
| P-6 | the 𝒰-leg χ-lift (stones A, C, D, E; B from P-0′) | 2550–5000 (1750–3000 if P-7 absorbs the pins) | C |
| P-7 | the assembly at the x-scale ladder + the socket discharge + the ∃-H₀ Siegel fold (now covering P-5 AND P-6) | 800–1500 | C |

**THE HONEST TOTAL: ~7–13k ln** — the price walked back up from
v1's 3–6k (the windmill was partly real after all): the two "new
stones" stayed dissolved, but the SW centerpiece is a real campaign
(the twisted rate), and P-6's honest band was 2× v1's. Against the
original 8–18k the port is still cheaper, and it is now FULLY
MAPPED: every stone named, every input's corpus status verified,
one D-risk isolated with a cheap probe in front of it. Tokens at
the 5.3× coefficient: **~15–30M over 3–6 days.**

## The authorization asks (JYH)

1. **Fire P-0′ (the B-probe) now** — cheap, and it de-risks or
   kills the whole campaign before the big waves.
2. **Authorize P-1..P-7 contingent on the probe** (the full price
   above), OR hold after the probe for a second look.
3. Note: the ruled 2³⁶ anchor and the spine conjunct are UNTOUCHED
   by every route in this freeze; the port adds zero H-demand and
   one ∃-H₀ ineffective gate.
