# V5-0 — THE WINDOW-SIDE DECISION (JYH-tier; the gate to H-EXIT)

*Consult brief, 2026-07-22 ~17:05. One decision requested. Everything
else in the v5 ladder is maestro/executor-tier and HIGH-confidence.*

## The decision

Change `prop21_analog`'s frozen CONCLUSION (HalaszRepAsm:488, and the
hfactor hypothesis :485): the seam's carrier `windowIndicator y (X/y)`
becomes the trivial indicator `fun _ => 1` — i.e. H-EXIT delivers the
identity for the FULL twisted hat-smoothed Σ f(n), exactly GHS (2.2).
One token-family, two mention sites, same statement otherwise.

## Why it is forced (three independent facts, all verified today)

1. **The windowed conclusion is UNSATISFIABLE.** prop21RHS's collapsed
   main term is the full Σ f(n) (GHS-faithful, per P21-3K); the
   out-of-window difference is Θ(X·log y), undamped — REC-V4-REF
   proved no budget of MS-EXIT grade can ever absorb it, and
   CARRIER-SCOPE's Q4 audit confirmed no windowed-side rescue exists
   (the joint-support argument windows the P-legs only; the main
   index is never pinned).
2. **GHS never windows its main term.** Prop 2.1 = the full sum; the
   only window in GHS is on the Λ-legs (which we keep — it is exact
   there). The window on f was OUR carrier addition at the seam.
3. **Nothing downstream needs it.** The consumption audit traced
   every link (contour leg → S2′ head → hhead/T1_decay → prop_A3′):
   each needs only 1-boundedness and the DISTANCE — and the distance
   converts windows at loglog-grade cost (T-1's landed machinery),
   or, at the trivial indicator, EXACTLY (the unwindowed distance
   identity has no out-of-window mass at all — simpler than what T-1
   had to prove).

## What it unlocks / costs

- UNLOCKS: the v5 ladder — the v4 collapse core (refuter-verified
  SOUND) + the joint-support un-truncation (FTC-V4-REF's specified
  repair; GHS's own §2.2 argument) + a reconciliation that SHRINKS
  (the out-of-window term becomes zero by construction). ~1.4-1.6k
  lines, ~2 waves, GO-confidence HIGH. H-EXIT stated at the
  unwindowed shape per route (i).
- COSTS: one genuine new residual, independent of this decision
  either way: the log-grade desmooth for the winCoeff ceiling sliver
  (V5-5; hat_desmooth's 1-bounded hypothesis provably excludes
  lambdaLin — a B/C stone). Nothing landed is touched: DistWindow's
  T-1 chain stays (heritage + still used by the branch floors);
  prop21RHS unchanged; all H-0/H-1/H-2/H-4 stones unchanged.
- PRECEDENT NOTE: unlike P21-2X/3K (definition-interior,
  maestro-tier), this edits a frozen CONCLUSION — hence your tier,
  flagged the moment it was classified.

## The recommendation

Approve. It is the GHS-faithful shape; it kills the last wall by
construction rather than by pricing; and the audit shows the window
was never load-bearing on the coefficient side — it was scaffolding
that wandered into the statement.

On your word: the statement surgery executes, the v5 block finalizes,
one targeted refuter pass runs (V5-2/V5-4/V5-5 specifics), and the
wave flies tonight.
