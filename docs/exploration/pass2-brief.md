# PASS 2 BRIEF (frozen 2026-07-19 00:50 PT; launch ~02:15 on math-acct's reset window; PRE-AUTHORIZED)

## The mission

THE FULCRUM HUNT, Pass 2 — the supply ledger. Pass 1 delivered
F_min (`FulcrumQualityMin C⋆`, kernel-checked in Salt/Fulcrum/)
and its negation brief. Pass 2 walks ¬F FORWARD: enumerate
exactly what the negation delivers, against the corpus's full
consumer inventory, and output THE HONEST GAP STATEMENT — what
¬F gives vs what twins need vs the exact missing bridge Z.
Pass 3 (the seam attack, JYH-attentive) consumes the output.

## ¬F, stated (from fulcrum-pass1.md's negation brief — read it)

¬FulcrumQualityMin C⋆: ∃ Q₀ such that EVERY primitive quadratic
χ mod q > Q₀ has NO zero in the ball ‖1−ρ‖ ≤ 1/(C⋆·log q) —
a UNIFORM EFFECTIVE fixed-quality zero-free ball at s=1 for the
entire family beyond Q₀. Consequences to enumerate: effective
L(1,χ) lower bounds (the ball forces L(1,χ) away from 0 at an
effective rate — the classical zero-vs-value link; our corpus
has L1_lower_siegel's CONVERSE direction and the ζ·L machinery),
effective Siegel–Walfisz at fixed polylog quality, effective
error terms in APs for quadratic moduli, character-sum
improvements, and the repulsion contract's own dichotomy
(dh_repulsion_ordered holds UNCONDITIONALLY — under ¬F its
hypothesis window empties beyond Q₀, so its content
concentrates below Q₀ — trace what that gives).

## The workflow shape (mirror Pass 1's structure; ~9 agents)

Phase ENUMERATE (4 parallel enumerators, by consumer family):
- E1 "effective-L": ¬F → effective L(1,χ)/SW/AP-error
  consequences; ground in Salt/SW (the Siegel apparatus,
  L1_lower_siegel, the effective machinery) + the classical
  zero-value link (cite the staged sources); label
  GROUNDED/MEMORY per the standing law.
- E2 "sieve artillery": what does the landed sieve inventory eat
  from ¬F's regularity — Salt/Chen (the keystones),
  twin_almost_prime (Salt/BrunLower), the BV chain
  (bounded_gaps_of_siegelWalfisz — CHECK its exact SW-grade
  demand vs what ¬F delivers), Salt/TwinBar (what stays
  EXCLUDED — the dead second-moment door).
- E3 "the fresh artillery": the 2026 corpus nobody has priced
  against ¬F — the power region (Salt/Vk), zeta_lower_all_t +
  lambda_nonpret_of_bridge (Salt/MR), the fulcrum machinery
  itself (Salt/Fulcrum's gadget — the isolation lemma was
  pre-noted as the ¬F-side weapon), ShiuCore, the H-B WP1
  transfer (which consumes NO Siegel input — the audit's
  finding; under ¬F it still runs — against WHAT?).
- E4 "the literature's ¬F attempts": the staged sources +
  memory (LOUDLY labeled) — what is known about
  "no Siegel zeros ⟹ ?" (effective Linnik, the
  Heath-Brown-style dichotomies, Chen-with-effective-SW);
  which routes died at parity and exactly where.

Phase SYNTHESIZE (2 designers on the combined enumeration):
- D1 "the gap statement": the honest Z — state formally what
  twins need that ¬F's deliverables don't provide; locate Z
  against the parity barrier (is Z parity-hard in the formal
  sense? partially?); the sharpest-ever localization.
- D2 "the seam candidates": the 2-3 most promising overlap
  regimes for Pass 3 — places where ¬F's effective regularity
  + the fresh artillery MIGHT combine on a twin-producing
  mechanism nobody has priced (the unharvested slack from
  Pass 1's structure candidate is one input; the below-Q₀
  concentration of the repulsion is another; genuinely new
  combinations encouraged — this is the lottery-ticket half,
  honesty about odds mandatory).

Phase REFUTE (2 refuters, one per designer): the standing
hardened checklist — every claimed deliverable of ¬F traced to
a grounded implication (no wishful effectivity); every seam
candidate's overlap arithmetic priced at the worst corner;
uncertain-on-load-bearing ⟹ refuted.

Phase JUDGE (1): THE SUPPLY LEDGER — ¬F's deliverables (each
grounded), the gap statement Z (refuter-survived), the ranked
seam candidates with honest odds, and the PASS 3 BRIEF (what
the attack should target, with JYH attentive).

## Standing laws (bind them into every agent's prompt)

GROUNDED (file:line / page:eq) vs MEMORY labels; the honest-shape
law (no unprovable compressions); catches #224/#239 (scope-diffs,
fossils); anti-spiral working style (incremental, draft files,
fields < 8000 chars); flags.md is the sole catch authority;
NO Lean-writing this pass (analysis only); budget ~1.1M total.

## Outputs

Bank the judge's ledger verbatim to
docs/exploration/fulcrum-pass2.md + the enumerator maps to
docs/exploration/; ledger + commit [skip ci]; then STOP —
Pass 3 waits for JYH (attentive, per the plan).
