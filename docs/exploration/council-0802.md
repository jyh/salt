# COUNCIL v5 — 2026-08-02 morning
## The done state: what stands, what it is, and the summit seal

*Sealed state: 2040ee5 (build 9674 exit-0, 3-axiom audit corpus-wide).
In the kernel: `logChowla2_witnessed_scale_flat_L_v2` — the terminal
with the register supplied, the width exported as a fact
(loglog H₊ ≤ 2·e^{1.6A}), and the crossing bound PAID. Both summit
refuters ruled (HONEST: not vacuous, repairs landed at 112d07d; SAT:
satisfiable modulo the named opaques, the joint certificate green).
The ledger: flags from "LADDER-L" onward.*

## C0 — What the theorem says (the plain form, for the record)

> At every design constant A ≥ 162 there is a Chowla regime whose
> window base is pinned at e^{e^{3.2A}} — arbitrarily deep, since A
> is unbounded — and for that regime the log-averaged two-point
> Chowla correlation does not fail:
> |Σ_{x/ω<n≤x} λ(n)λ(n+1)/n| ≤ ε·log ω at the witnessed (x, ω).

Conditional on, exactly: the band-lane constant (log C_mu ≤ 40-genre,
no corpus bound); Kc/Ct (true at their closed forms 2^538.42 and
2^22.78, unreadable from the proof's own ∃-witnesses); the Siegel
pair (the x₀ window at 414 double-log units of room; the Hopq/K_Chen
arm — the field's wall, twice, honestly); four sign-bounded constant
riders; and two carried predicates weaker than their landed
counterparts. **Companion result, unconditional**: the
entropy-decrement width law (the flat tower extremal) — publishable
in its own right.

## C1 — THE DELTA-REFUTER (the last protocol step; ask: fire now)

Both refuters sat BEFORE the final supply chain landed
(S13CapGateLinear 1025 ln + S16FlatFinal 207 ln + HONEST-FIX's
amendments). The delta pass: the new 37-field structure and grid at
the L door (the five door-reading fields), the discharge's
byte-fidelity (is the paid crossing the same bytes the terminal
carried?), the v2 statement vs its docstring, and the two
weaker-than-landed predicates' direction claims. Light — one
refuter, ~150-250k tokens.

## C2 — WHAT WE CALL IT (the discussion you asked for)

The honest taxonomy of what stands:
- It is NOT "the unconditional theorem" simpliciter — the band
  constant and the unread numerals are ours to close, not the
  field's.
- It IS arguably "unconditional modulo Siegel" once those close:
  the Siegel pair is the standard caveat the entire literature
  carries (every effective-constant paper says "ineffective by
  Siegel"). The remaining OUR-side items are all priced:
  Kc/Ct/cs/T₀/Kq/Ks = the EPSPIN/CgPin conjunct-carry genre
  (traceable, each either provable or boundable); hband/C_mu = the
  one genuinely opaque analytic constant (MmuChiRate's witness — a
  trace wave of the same genre, outcome unknown).
- THE PEARL QUESTION: is this object the Pearl? My reading: the
  Pearl was christened as the witnessed-scale theorem — this is it,
  in its strongest form. The gold remains the twin primes. Your
  call, at your leisure.
- THE WITNESS (paper #2): its content now exists — the conditional
  theorem + the width law + the method story (the three windmills,
  the true wall named). Drafting can begin when you rule.

## C3 — THE SUMMIT SEAL CHECKLIST (before any word)

(a) the delta-refuter (C1); (b) the constant-trace wave (Kc/Ct reads
+ cs/T₀/Kq/Ks + the C_mu hunt — the EPSPIN genre, one wave,
~300-500k); (c) the full leanchecker ceremony re-run (the corpus
grew ~12k lines since 1029/1031; + Maynard's solo retry); (d) the
statement-honesty dossier for your read (the unfolded terminal, the
survivors, the refuter verdicts — one document); (e) YOUR WORD.
Recommendation: fire (a)+(b) in parallel now, (c) as background
compute, (d) composed when (a) returns.

## C4 — THE WEEK (unchanged, plus the news)

Today (Sunday): your CV voice pass. Monday: the two filings. Tuesday:
the endorsement decision. Friday: the outside reviewer recording. The Fulcrum
waits on endorsement only; The Witness waits on C2's ruling.

## C5 — HOUSEKEEPING

The tank: silicon-acct mid-tank after the night (~2.6M subagent tokens
spent on LADDER-L alone); jason resets on its weekly cycle. The
in-flight-file coordination held perfectly across three concurrent
executors (noted for the method ledger).
