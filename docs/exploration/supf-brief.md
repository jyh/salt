# THE MORNING BRIEF — the supF campaign (the last mountain of S8's Part 3)

*Prepared overnight 2026-07-22→23. Decision requested: greenlight the
supF design block (Fable-tier) and its shape. Everything else on the
board is landed, in flight, or waiting only on this.*

## Where the campaign stands at dawn

- **Part 1 COMPLETE**: prop21_unconditional_final + _clean (HExit) —
  the S1′ representation, unconditional, manifest-audited.
- **Part 2**: A-1 landed; A-2 COMPLETE + A-3 in flight on the cloud
  branch (bring-it-home ceremony pending its report).
- **Part 3**: assembled to ONE input — hRHS (the ball-head bound on
  ‖prop21RHS‖), reachable through four legs:
  [A] the t-integral diagonal — LANDED (contour_A13_A14_head_sharp,
      k4_plan_le_diag_sharp; supplies the 1/σ weight, log²-grade).
  [ii] the integral factoring — LEG-II executor in flight overnight.
  [σ] sigma_cutoff [B/C] — the (1+M) two-regime split (GHS Cor 1.2
      p.13; M-BRIDGE-SCOPE verified the mechanism numerically to
      ratio 1.000; the (1+M) is genuine and necessary — both
      a-fortiori escapes proven to fail; grade_EM's 2 load-bearing).
  [i] **supF_pretentious — THE LAST MOUNTAIN**: sup over the ball of
      ‖𝒮·𝓛‖ ≤ C·e^{−M}·log X (+ F_trivial [B, partial] ≤ 1/σ).

## The tier disagreement to adjudicate (the design block's first job)

- SUPF-SCOPE: leg [i] = D-tier, 800–2000+ lines, "the K4′-twin
  campaign" — the corpus's own R2.3 fail-fast (HalaszCore:126-129)
  flags the Perron/Plancherel A.10 + contour A.13–A.14 route as
  unreachable; only the algebraic seeds (halasz_cosh_ineq,
  halasz_sinh_bound) are landed.
- M-BRIDGE-SCOPE: the needed form may be C-tier — the sup at the
  CENTER via halasz_cosh_ineq_complex (:179) + the log-derivative ↔
  distance identity, with the ball-sup absorbed by the kernel's
  concentration (the hatKernel centering) rather than a full A.10
  Plancherel port.
- The adjudication needs: reading halasz_cosh_ineq_complex's exact
  reach, the Dist residual (S1's "twisted Re log L identity" — the
  MR-W1 flag), and GHS (2.7)'s max-modulus step against what the
  exact hat kernel already gives us. Precedent worth weight: EVERY
  wall this campaign priced as D has dissolved or shrunk when the
  exact-kernel dividend was applied (Lemma 2.5 dissolved; the grain
  fell in one dispatch; the Shiu port went elementary).

## The recommendation

Greenlight the design block as the day's first Fable work: adjudicate
the tier, freeze the supF ladder ([i] + [σ] + F_trivial + the LEG-II
interface), refuter pass, then the wave. If the C-tier reading holds,
S8's Part 3 could be whole within days; if D confirms, we scope the
A.10 port honestly as its own rung (the K4′ pattern: multi-wave,
JYH-visible budget).

## Also on the morning docket

- LEG-II's landing (ceremony + the (1+M) docstring correction queued
  behind it — the attribution bug M-BRIDGE-SCOPE found).
- The cloud: A-3/L11 bring-it-home; the two trials' reports (note
  the branch-sharing wrinkle); the runbook lesson (per-run branches).
- The week's fixed points: Zulip return + walkthrough Thursday (the
  mathlib adaptation executor must run tomorrow), PR Friday,
  SaltBench Saturday, the the approval review Sunday.
