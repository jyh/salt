# THE S8 RESCOPE FREEZE — thm_A2′ (2026-07-27)

Council-ratified GO (decision 1). Synthesis of S8-INV (the landed-corpus
supply map + gaps G1–G7) and S8-SRC (the MRT pp. 20–28 skeleton S0–S11 +
demands D-1..D-15 + defects D1–D12/X1–X6) — both banked in flags/pilot
2026-07-27. SUPERSEDES the old s8-freeze.md where they conflict (notably:
D1's E(M) convention, the S8-row's D-declaration, and the 𝒮-scope).
STATUS: awaiting the refuter pass (dispatched with this freeze); the wave
fires on REPAIR-THEN-FIRE or better. H1-INHAB (the G6 correctness fix) is
aloft and is a hard prerequisite of A2-7.

## THE VERDICT THAT REPLACES "CLASS D, 8–20k"

thm_A2′ is a **C-class assembly campaign, band ≈ 2900–5300**, against a
corpus that already owns: the difference-form Parseval/contour layer
(N-uniform, Egap → 0), the 𝒯-leg rows calibrated at the K-family, the
graded 𝒰-leg unconditional at (log X)^{−1/500}, the ball/station
M-machinery at exp(−M/e)-grade, the renormalisation with the correct sign,
the moment rows, and the frame apparatus (CalFrameK + the parametric door
inhabitant). One item carries D-risk (A2-5, the linearization seam) and is
FAIL-FAST-FIRST. Two feared gaps are already closed (the hMsup single-row
collapse; the mid-range via the tall row).

## THE INTERFACE (revising Amendment B's line — the landed grades govern)

`thm_A2'` : for f : ℕ → ℂ 1-bounded multiplicative, the K-family 𝒮 at the
CORRECTED door inhabitant (Adoor M, 3072M, M, Jb = 2, **H₁ := H1door M**
— the G6 fix; NEVER the P₁^{1/6} pin), η = 1/12, SINGLE-h:

(1/X)∫_X^{2X} ‖(1/h)Σ_{x≤n≤x+h, n∈𝒮_K} f(n)‖² dx
  ≪ C₁·exp(−M/e)·(M-shaped factor honest to the landed station)
   + C₂·(log Q₁)^{1/3}/P₁^{1/6−η}
   + C₃·(log X)^{−1/500}

with M := the landed M-object (see A2-4 for the window), all constants
explicit. THE EXPONENT RULINGS: the third term is the LANDED −1/500 (the
printed 1/50 fails at our θ = 3/4 — T-1; and the corpus already weakened
further); the M-term is the LANDED exp(−M/e) (s8-freeze D1's exp(−M/2) is
STALE — the ball arm's 1/(2e) squared; consumers recalibrated: M4-6 at
(5e/2)·log W ≈ 6.8·log W). Downstream re-checks at these grades: M4-7's
close (the W-powers dominate — clears), the S11 δ-chain (doorGrade
untouched — clears; the C_MRT gate per the M4-9 three-step spec).

**The side-condition list (COMPLETE — three amendments folded):** f
1-bounded multiplicative; the frame gates (A.1)+(A.2) at j = 2 (landed as
CalFrameK fields — cite, don't restate), Q₁ ≤ exp(√log X₀), √X ≤ X₀ ≤ X,
[P₁,Q₁] ⊆ [1,h] (consumed ONLY at the S1b join, three times — log
identity via log_calQK_door_one), h ≥ 4 (the AS-2 MVT guard — NOT 3),
TannGate X Tann at Tann = 2X/h₁, X > X(η) as an explicit ∃-threshold, and
the V9c law (calP/calQK symbolic always).

**The truncation license (RULED — by ownership):** the corpus never
delegates the 𝒯₁ leg to MR Prop 1 — the landed seam campaign IS that leg
rebuilt natively at Jb = 2, kernel-checked (seam_row_number + TLeg_bound +
hUG34_unconditional at the calibrated frame). MR's J-to-h coupling (p. 8)
constrains THEIR proof, not ours. Refuter cross-check assigned: the
soft-landing observation (A.3 consumed only at T ≍ X/h) + no landed row
secretly consumes J-maximality.

## THE LADDER (ids A2-*; classes and bands from the joined maps)

| id | content | class | ln |
|---|---|---|---|
| A2-0 | **Lemma A.8** (self-contained: e^α + e^{−α} − 2cosθ ≤ e^{√(α²+θ²)}; the θ = 0 MVT-degeneracy edge; the S8-SRC-verified proof chain) + the S1b JOIN (D-2: the three [P₁,Q₁]⊆[1,h] consumptions, ~30 ln) + the sup→inf reading pin (defect D2) | B | 100–200 |
| A2-1 | **THE SINGLE-h PARSEVAL** (G1 = D-1). NOT a transcription — a re-derivation with the mechanism PINNED by S8-SRC: drop the U/V split entirely, run MR's V-argument from t = 0 (the kernel min(h/x, 1/\|t\|) is valid at t = 0; the smooth-g double integral and the 1/(\|t₁−t₂\|²+1) kernel untouched; NO log loss — verified). Blocks 1/2/4/5 of the landed five-way split de-difference free (hSC is ∀αβ; vtail single-h variants at half the constant); ONLY the T₀-block changes character (the leading term survives — paid via A2-3). Reuse Lemma14Bridge/PerronMeanSq/KernelCarry internals; the exit in the :1153 shape at ONE h | C | 700–1100 |
| A2-2 | **The composition spine** (G2): join the seam-supplied calibrated contour exits (lemma14_contour_seam_supplied_calibrated / seam_row_number) to A2-1's single-h shortSum form; the file that finally imports both SeamLemma14 and lands a shortSum statement | B/C | 250–400 |
| A2-3 | **The T₀-band** (G3) — THE ONE DESIGN CHOICE, kill-check-gated: (option W) widen the recentring radius to T₀ = (log X)^{1/15} (re-run the station's ball at the wider radius — dissolves S8-INV trap 2's covering cost AND the band question at once; the cost: the renormalise error at \|t−t₁\| ≤ T₀ is (log X)^{1/15}-scaled — re-derive the 7/8-page at the wider window per S9a's β-argument, which S8-SRC verified is Q_J-ceiling-driven, not radius-driven); (option C) cover [−T₀,T₀] by (log X)^{1/240} balls at seamRad — costs the covering factor in the M-demand (M ≳ 0.0113·log H, closes at ~29× — real but priced). THE KILL-CHECK (fail-fast, one scoper-hour): does the 7/8 bound survive the T₀-radius? If yes → option W; if no → option C with the priced demand | C | 400–700 |
| A2-4 | **The M-window bridge** (G4 + defect D10): inf_{\|t\| ≤ X} vs the offered M(f;X,W); the [W, (log X)^{1/16}] region via dist_one_floor_pow + pretDist_triangle; AND the D10 repair — the max-modulus reach \|t'\| ≤ X + T₀ (restate the inf at 2X or bound T₀; CHECK the landed M_range's own \|t\| ≤ X cap against its consumers — the same defect class) | C | 350–600 |
| A2-5 | **THE LINEARIZATION SEAM** (G5) — FAIL-FAST FIRST, dispatch order 1: (i) 1_𝒮·f → Σ_𝒥 (−1)^{\|𝒥\|} g_𝒥·f is landed-algebraic (Sec9Glue lemma5, 4 terms at Jb = 2; the g_𝒥 DEFINITION exists — ours, D-14 closed); (ii) g_𝒥·f (general multiplicative) → the ellLin datum: the L3′ analytic Euler-form half with explicit C_sq — declared seam-coupled (HalaszLambda :378–387), NO landed exit. THE KILL-CHECK: one scoper derives the exact statement the station needs at g_𝒥·f and tests whether smoothPart_factorization + cSq_corner_summable close it; if NOT, the fallback is re-running the pointwise arc (CenterSupply→SupStation, ~2500 ln of landed chain) at g_𝒥·f — a PORT not a discovery, and the band's upper end | C, D-risk | 600–1600 |
| A2-6 | G6 — **in flight** (H1-INHAB: H1door + calFrameK_satisfiable_doorH1 + the level-1 decay certificate) | B | 150–250 |
| A2-7 | **The frozen statement** (G7): thm_A2' verbatim as above, the COMPLETE side-condition list, the 𝒮_K = MemS (calP …) (calQK …) 2 identification (memS_calFamily), consuming A2-0..A2-6; + the mrt_extract repair (X1/X5/X6) in the same ceremony | C | 300–500 |

Band: **2850–5350.** Dispatch order: A2-5's kill-check + A2-3's kill-check
FIRST (both scoper-scale, both gate design choices); then A2-0 + A2-1 in
parallel; A2-2 after A2-1; A2-4 parallel; A2-7 last. The E(M)/exponent
recalibrations propagate to Amendment B's M4-6/M4-9 rows (annotated at
G7's ceremony, not silently).

## TRAPS CARRIED (the joined set — executors read BOTH banked reports' trap
sections in flags/pilot 2026-07-27 plus these)

The 𝒯₁-glyph collision (MRT's vs MR's — the corpus's TLeg* names are MR's
ladder; a naming note in every A2 brief); the Eq26Compose thm3_* DECOY
(real f, carried hL4, subtracted comparand — NOT the single-h form); the
CenterSupply live guard (use the SupStation/SupplyGeneric twins); ellLin
is squarefree-supported (trap 5); seamRad < seamT0 (the G3 geometry);
h₁ ≥ 4 not 2; TannGate at instantiation; the closed/half-open drift (D6)
pinned at the g_𝒥 join; the η self-collision (D8) — our η is CalFrameK's,
never 1/log y (rename the [9]-machine's to ηy in any port); U normalised
vs not (D3); the sup→inf reading (D2); no ∃x₀-shaped stone inside anything
f-uniform; π ∈ (3.125, 3.2) gates via Real.pi_gt_3141592/pi_lt_315; the
V9c law; the margin-hygiene law.
