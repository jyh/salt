# HALASZ-INFRA FINAL FREEZE (judge-final, 2026-07-19 ~15:50 PDT; wf_648a4200; 7/7, ~708k; the K1 wall designed)

## CHOSEN

minimal-seam (DES-B) + REF-B repairs R1-R7 + two DES-A grafts (grade_EM stone; 0<c/summability-led statement freezing per REF-A F1 discipline). KILL ADJUDICATION: REF-A F1 (abscissa c'=c0-a-b<1 vs frozen 1<c) CONFIRMED, repairable but exposes the sharp-Perron toolkit's consumption fragility; REF-A F2 (T2.1c is C not B, Si-boundedness absent from mathlib) CONFIRMED +200; REF-A F3 (eta-large corner) CONFIRMED; REF-A F4 (MVHilbertUniform conditional socket) CONFIRMED and VERIFIED this session (MVHilbert.lean:134/:269); REF-B F1 (tail at T0=(logX)^2 overruns grade by (logX)^2) CONFIRMED, repaired via Tsplit=(logX)^4 + explicit tail ledger; REF-B F2 (full-log leak not eps-absorbable) CONFIRMED, ledger-gate adopted; REF-B F3 (R2.4 [D,700] silently inherited; latent GS[10] Lemma 7.1) CONFIRMED, demotion argument + named gate-check adopted; REF-B F4 (R1.1 re-priced C/D) CONFIRMED against flags.md tail THIS SESSION; F5-F7 minor, applied. DECIDERS (all independently re-verified this session by opening files): s8-freeze.md:25 exit stone is center-t0/infimum-free (Wall 3 vacuous — DES-A's MBall is unneeded scope); kappa=1 disarmed BY CONSTRUCTION via Lambda_{ell'} (|Lambda_{ell'}| <= Lambda all n) vs DES-A's carried 2^{k+1} budget; hat kernel = two instances of landed kernel_identity (SW/Kernel.lean:195 verified) so DEBT-3 is deleted not discharged; staging debt on critical path = GHS [9] only; quota +~2.3k vs +~3.2k against 18k. New judge catch: DES-B's kernel-split 'T0' renamed Tsplit — it collided with parent-freeze N1's T0=(logX)^{1/16} (ball radius), a live numeral-confusion hazard.

## FREEZE

HALASZ-INFRA FINAL FREEZE (judge, 2026-07-19; all cites GROUNDED this session). 3 NEW files + I-3 same-file resumption; ZERO class D in the dispatched ladder; freeze PARTIAL with single named residual S1' until GHS [9] staged. Exit stone halasz_ball_decay (s8-freeze.md:25,37) served VERBATIM; R3.1/prop_A3' untouched. All new kernel stones frozen at 0<c with load-bearing summability (REF-A F1 discipline); consumers instantiate c0=1+1/logX.

== FILE A Salt/MR/HalaszKernel.lean (~800; s abbreviates (c:C)+(t:C)*I) ==
K1' [B,120] noncomputable def hatK (X h : R) (n : N) : R := (max (X+h-n) 0 - max (X-n) 0)/h
theorem hat_desmooth (a : N->C) (ha : forall n, ||a n|| <= 1) {X h : R} (hX : 1 <= X) (hh : 0 < h) (hhX : h <= X) : ||(Sum n in Finset.Icc 1 (floor X), a n) - tsum n, a n * (hatK X h n : C)|| <= h + 1
[name hat_desmooth NOT desmooth_identity — clash with SW/MobiusRateClose.lean:866]
K2' [B/C,220] theorem hat_contour_rep (a) (ha0 : a 0 = 0) {X h c : R} (hX : 1 <= X) (hh : 0 < h) (hc : 0 < c) (hsum : Summable fun n => ||a n|| * ((X+h)/n)^c) : tsum n, a n * (hatK X h n : C) = (1/(2*Real.pi)) • integral t : R, (tsum n, a n / (n:C)^s) * ((((X+h:R):C)^(s+1) - ((X:R):C)^(s+1)) / ((h:C)*(s*(s+1)))). EXACT, full-line, no truncation. Route: two kernel_identity instances (SW/Kernel.lean:195, verified) at y=(X+h)/n and X/n + kernel_sum_swap (:254) + linearity; (X+h)*((X+h)/n)^s = (X+h)^{s+1} n^{-s}.
K3' [B,120] theorem hat_mellin_bound {X h c} (1<=X)(0<h)(0<c)(t) : ||kernel(t)|| <= (X+h)^c * min (2/||s||) (2*(X+h)/(h*||s||^2)); corollary hat_tail: T >= 1 -> integral over |t|>=T of branch-2 <= 4*(X+h)^{c+1}/(h*T). TSPLIT LEDGER (REF-B R1, binding): at Tsplit := (logX)^4, sup-integrand <= C(logX)^3 [S_J*L <= ClogX x Lambda-window double-sum <= (log(x/y))^2] x tail mass -> X(logX)^{-1/2}, AT grade, zero eps borrowing. Tsplit is the KERNEL-SPLIT height — distinct from parent N1's T0=(logX)^{1/16} ball radius; never conflate.
K4' [C,340] theorem cos_int_pair {c th : R} (hc : 0 < c) : integral t : R, Real.cos (th*t)/(c^2+t^2) = Real.pi/c * Real.exp (-(c*|th|)) [mathlib ABSENT, verified; route SW/ContourShift rectBI-CIF (:104/:165) + R->infty limit; fallback direct residue/parity computation, +200 priced];
theorem dirichlet_plancherel (F : Finset N) (b) {c} (hc : 0 < c) (hF : forall n in F, 1 <= n) : integral t : R, ||Sum n in F, b n/(n:C)^s||^2/(c^2+t^2) = Real.pi/c * Sum m in F, Sum n in F, (b m * starRingEnd C (b n)).re / ((m*n:N):R)^c * Real.exp (-(c*|Real.log m - Real.log n|)). [Plancherel leg — does NOT consume MVHilbert; the (T+N) shape is killed by N=x/y; no new K2 load. MVHilbertUniform conditionality (MVHilbert.lean:134/:269, verified) stays on its own track.]

== FILE B Salt/MR/HalaszLambda.lean (~650) ==
L0' [B,120] defs: ellLin (g : N->C) : N->C := fun n => if n = 0 then 0 else if Squarefree n then Prod p in n.primeFactors, g p else 0; ellLinInv g := fun n => if n = 0 then 0 else Prod p in n.primeFactors, (-(g p))^(n.factorization p); theorem ellLin_mul_inv : (ellLin g) conv (ellLinInv g) = delta (per-Euler-factor telescope).
L1' [A/B,80] lambdaLin g := fun n => if IsPrimePow n then (-1)^(n.factorization n.minFac - 1) * g n.minFac ^ n.factorization n.minFac * (Real.log n.minFac : C) else 0; theorem lambdaLin_norm_le (hg : forall p, p.Prime -> ||g p|| <= 1) (n) : ||lambdaLin g n|| <= ArithmeticFunction.vonMangoldt n — ALL n (kappa=1 disarmed by construction).
L2' [B,130] theorem lambdaLin_convolution (g)(n) : (Real.log n : C) * ellLin g n = ((lambdaLin g) conv (ellLin g)) n — unconditional algebraic identity (two-term telescope per factor).
L4' [B,120] theorem ellLin_lseries_deriv (hg) {s : C} (hs : 1 < s.re) : -deriv (LSeries (ellLin g)) s = LSeries (lambdaLin g) s * LSeries (ellLin g) s. Route: LSeries_deriv (Deriv.lean:86, verified; abscissa <= 1 from ||ellLin|| <= 1) + L2' + LSeries_convolution' (Convolution.lean:148, verified; lambdaLin dominated by Lambda). PRODUCT FORM — F-ZERO-SET EXPLICITLY DEAD: no division by any L-series anywhere in the ladder, no 1/L evaluated at any point, nonvanishing never needed.
L3' [C,200-300] smooth-part split (internal shapes executor's per s8-freeze.md:37; frozen OUTPUT contract): fg_J = sPart conv ellLin(g*1_{p>y}) with sPart := fg_J conv ellLinInv(...); support(sPart) in y-smooth x squarefull; ||LSeries sPart (sigma+it)|| <= (A.11 Euler form) * C_sq with C_sq an EXPLICIT O(1) carried in-statement (p=2 k->infty corner: Sum (k+1)2^{-k c0} = O(1) — never silently absorbed); plugs into landed halasz_cosh_ineq_complex (HalaszCore.lean:177, verified).

== FILE C Salt/MR/HalaszSeam.lean (~800; WAVE I-2, GATED on staging GHS [9]) ==
S1' [C-with-staged-source,450] prop21_analog: GHS Prop-2.1 hat-kernel analog — representation of the smoothed Sum f g_J n^{-it1} K_{X,h}(n) via the alpha,beta double integral on the EXACT vertical line (K2'), lambdaLin replacing Lambda_ell, desmooth O(h) replacing truncation; Lambda-poly windows (y, x/y) via psi_le_const_mul_self (mathlib Chebyshev.lean:457, verified) + L1'. Exact statement frozen AT I-2 DISPATCH against the staged text (honest-shape: cannot freeze verbatim a port of an unstaged source). THE NAMED RESIDUAL until staged.
S2' [C,350] contour_A13_A14: sup-split + Cauchy-Schwarz with K4' + L3' => (A.9)-analog at center t0; K1' desmooths to the shape R2.4 consumes; Tsplit=(logX)^4 ledger (K3') carried. LEDGER GATE (REF-B R2): complete log-power ledger as TEXT ARTIFACT before any Lean; full-log leak = RUNG FAILURE (grade headroom < half a log); only (logX)^{o(1)}/loglog leaks eps-absorbable.

== WAVE I-3 (HalaszCore.lean resumption, ~1240; GATED on R1.1 + I-2) ==
grade_EM [B,40] (DES-A graft): 0 <= M -> (1+M)*Real.exp(-M) <= 2*Real.exp(-M/2) (interior max 1.213 — genuine constant); ball_mvt remainder [B/C,200] (Sum_{m/=n} 1/|log(m/n)| << NlogN on offdiag_int_bound :193); R2.4 DEMOTED to [C,700]: with S1'+S2' landed, A.6/A.7 reduces to ball integration + recentering packaging; LATENT [10]-Lemma-7.1 GATE-CHECK: declared replacement = center-t0 exit + R1.1 triangle recentering; if irreplaceable -> flags STOP, [10] joins staging debt or second named residual R2.4-Lipschitz; R2.5 [C,300] assembles exit stone VERBATIM (desmooth cost inside frozen (logX)^{-1/2+eps}).

== COVER/DEGEN (enumerated FIRST per split) ==
n-coordinate: n=0 (ha0), n=1, n<=X (hatK=1 exact), X<n<=X+h ramp (count <= h+1), n>X+h (0) — total, integer-X allowed (hat continuous; no x-not-in-N hypothesis needed, unlike sharp Perron). h: h->0 excluded in-consumer (h = X(logX)^{-1/2} frozen, GROWING #253), h<=X carried. c: 0<c + hsum; c0=1+1/logX-dependent constants GROWING, in-statement. g(p)=0 corner: lambdaLin term 0, factor 1 — covered; f=1: (-1)^{k-1}logp, <= Lambda ✓. Squarefull: C_sq carried. M: M=0 (E=1, trivially true), slow-M Fei-Wei corner UNTOUCHED (all seam errors additive + M-free); grade_EM M->0/M->infty pass. 2^J multiplicity: desmooth per-J, 2^J=(logX)^{o(1)} carried explicitly (REF-B R7).

== CONSUMPTION CERTIFICATES ==
R2.3: Lambda-identity = L2'/L4' at Re=c0>1 ✓; contour rep = K2' at c0, hsum from 1-bdd ✓; truncation DELETED (exact rep), desmooth (h+1)*2^J <= X(logX)^{-1/2+o(1)} inside frozen error ✓; Euler <= 1 = halasz_cosh_ineq_complex ✓; typical-n = TuranKubilius.lean:288. Residual: S1' internals ([9, Prop 2.1] + [9, proof of Thm 1.1], via 1503.05121v3 pp.25-27; NOT 'Cor 1.2' — REF-A F5b applied).
R2.4: S2' + grade_EM + K4' (MVHilbert-free) + Tsplit ledger; R1.1 recentering per flags 07-19 (Koukoulopoulos 13.1 port, C, queued).

== WAVES (single-writer by FILE) ==
I-1: 2 parallel Opus executors — E1 HalaszKernel.lean, E2 HalaszLambda.lean. ZERO staging debt; dispatchable on JYH funding go. I-2: 1 executor HalaszSeam.lean, after GHS [9] staged; day-1 fail-fast on S1' + ledger gate. I-3: 1 executor HalaszCore.lean (same-file resumption), after R1.1 lands + I-2. Nobody touches: SW/*, DistHalasz.lean, NonPretClose.lean, MVHilbert.lean, MVCore.lean, L2MVT.lean, TypicalDensity.lean (live untracked, A4a in-flight), All.lean (ceremony, s8-freeze D6). Gate per file: lake build Salt.MR.<File> green, no new warnings, #print axioms in [propext, Classical.choice, Quot.sound] per stone.

## BRIEFS

ALL BRIEFS CARRY VERBATIM: "Model: opus MANDATORY. You write ONLY Salt/MR/<YourFile>.lean. NO-GIT LAW (catch #244): you run NO git commands — no commit, no push, no branch, no checkout; the watching session lands files serially. Never edit Salt/MR/All.lean, any landed file, blueprint statements, or CLAUDE.md. Statement shapes in your brief are FROZEN (iron rule 1); internal helpers yours; a helper harder than your rung's class -> STOP, flags.md. 3 serious attempts per rung -> flags.md entry -> next rung. Gate: lake build Salt.MR.<YourFile>, no new warnings + scratch #print axioms per stone (must be within [propext, Classical.choice, Quot.sound]). Growing quantities (h, X/h, Tsplit, 1/logX-dependent constants) carried in-statement, never absorbed into C (#253)."

E1 (wave I-1, Salt/MR/HalaszKernel.lean, budget ~800 ln): rungs K1'->K2'->K3'->K4' in order. K1' hat_desmooth [B]: counting on the ramp window (X, X+h]; name is hat_desmooth (desmooth_identity exists in SW/MobiusRateClose.lean:866 — do not clash). K2' hat_contour_rep [B/C]: two instances of SW/Kernel.lean:195 kernel_identity (y=(X+h)/n, X/n) + :254 kernel_sum_swap + linearity; summability for the X-instance follows from hsum by (X/n)^c <= ((X+h)/n)^c. K3' [B]: MVT-on-segment branch (derivative (s+1)u^s) + trivial branch; ship the hat_tail corollary and the Tsplit=(logX)^4 ledger DOCSTRING (numbers in freeze). K4' [C, fattest]: cos_int_pair first (SW/ContourShift.lean:104/:165 rectBI route + R->infty limit; fallback: parity + direct primitive, priced +200 — if BOTH resist after 3 attempts, flags + move on; dirichlet_plancherel still lands as a Finset bilinear expansion consuming cos_int_pair as a stated hypothesis if needed, flag the split). dirichlet_plancherel: expand ||Sum||^2, swap finite sum with integral (finite — no domination subtlety), pair off; sin-part vanishes by oddness. Do NOT import MVHilbert, DistHalasz, or any SW file beyond Kernel/ContourShift.

E2 (wave I-1, Salt/MR/HalaszLambda.lean, budget ~650 ln): rungs L0'->L1'->L2'->L4'->L3'. L0': ellLin/ellLinInv/delta-identity — per-prime telescope then multiplicative glue by coprime induction on primeFactors. L1': lambdaLin def + norm_le vs ArithmeticFunction.vonMangoldt, ALL n (case split n=0/1, not-prime-power [both sides 0], p^k [<= log p]); enumerate before proving. L2': pointwise convolution identity, unconditional. L4': product-form -L' = L_Lambda * L_ell via mathlib LSeries_deriv (Deriv.lean:86) + LSeries_convolution' (Convolution.lean:148); abscissa <= 1 from 1-boundedness; NEVER introduce division by an L-series — if a route needs 1/L, it is the wrong route, STOP. L3' [C]: sPart split; support y-smooth x squarefull; the O(1) squarefull constant C_sq is EXPLICIT and in-statement (p=2 tail Sum(k+1)2^{-kc0} — carry it); output must compose with HalaszCore.lean:177 halasz_cosh_ineq_complex (import HalaszCore read-only). Internal window/g_J parametrization is yours (s8-freeze.md:37) — but the fg_J = sPart conv ellLin factorization shape is frozen.

E3 (wave I-2, Salt/MR/HalaszSeam.lean, budget ~800 ln; DISPATCH ONLY after docs/sources contains GHS 1706.03749): DAY-1 FAIL-FAST on S1' (K1-pattern: 3 attempts -> flags.md -> STOP before any S2' spend). BEFORE ANY LEAN: produce the complete S2' log-power ledger as a text artifact (Plancherel logX x sup-split x eta-normalizations x Tsplit tail -> exit U << X*E(M) + X(logX)^{-1/2+eps}); a full-log leak means the rung FAILS — do not attempt to absorb it into eps; ledger fails -> flags, zero further quota. S1': port [9, Prop 2.1] with the three seam substitutions (hat kernel: truncation error deleted, desmooth O(h) via K1'; lambdaLin for Lambda_ell using |lambdaLin| <= Lambda; window truncation via mathlib Chebyshev.lean:457). Verify against the STAGED text which internal GHS steps use the sharp indicator; each such step must reduce to a ramp-count <= h+1 — any step that cannot is a flags entry, not an improvisation. Desmooth applied per J-subset: carry 2^J = (logX)^{o(1)} explicitly. Tsplit = (logX)^4 everywhere; it is NOT the parent freeze's T0=(logX)^{1/16}.

E4 (wave I-3, Salt/MR/HalaszCore.lean resumption, budget ~1240 ln; DISPATCH after (a) R1.1/Koukoulopoulos-13.1 port lands and (b) I-2 green): you EXTEND the landed file — keep all 6 landed theorems byte-identical. Order: grade_EM [B] (route add_one_le_exp; max 1.213) -> ball_mvt remainder [B/C] (harmonic Sum 1/|log(m/n)| << NlogN over offdiag_int_bound :193 + L2MVT.dirichlet_poly_l2_diagonal — consume, never rebuild) -> R2.4 [C] ball integration + recentering (GATE-CHECK first: confirm the Lipschitz/renormalization step of MRT (A.8) is replaced by the center-t0 exit + R1.1 triangle; if NOT replaceable, flags.md STOP — do not port GS [10] from memory) -> R2.5 [C] assemble halasz_ball_decay: statement VERBATIM from s8-freeze.md:25 (iron rule 1; any deviation is a bug even if 'equivalent').

## SCOPE_DIFFS

SCOPE-DIFF LEDGER vs s8-freeze.md HALASZ SUB-FREEZE (lines 22-39): (1) R2.3 [D,FAIL-FAST,400] -> REPLACED by the seam ladder K1'-K4'/L0'-L4' (all <= C, zero staging) + S1' [C-with-staged-source] — the D-mass is reduced to a single named residual (S1' = GHS [9, Prop 2.1 + proof of Thm 1.1] internals), gated on staging, NOT eliminated. (2) R2.4 [D,700] -> DEMOTED to [C,700] conditional on S1'/S2'; latent GS [10] Lemma-7.1 dependency NAMED with declared replacement route (center-t0 exit + R1.1 recentering) and a flags-stop if irreplaceable — parent freeze had it silently inside the D. (3) K1's declared fallback (classical Montgomery route, 'also D') RETIRED as primary contingency; the seam IS the funded route; the fallback remains on the books only as a campaign-gate option (Fable/JYH). (4) NEW FILES: HalaszKernel/HalaszLambda/HalaszSeam (+~2,250 ln, band 2.1k-2.9k) OVER the H2 1800 budget; S8 total re-priced ~17.7-18.1k of the 18k quota — explicit JYH pricing item. (5) Sharp truncated Perron DELETED from H2 (hat-kernel exact rep); DEBT-3 (MNT-I Thm 5.1 / Saffari-Vaughan) no longer an H2 debt — A3a/ParsevalSL's own Perron need is UNAFFECTED and out of scope. (6) kappa=1 caveat (MRT p.25) discharged BY CONSTRUCTION (|lambdaLin| <= Lambda, all n) — no longer an assumed-against-staged-text risk. (7) Seam kernel-split height named Tsplit=(logX)^4 — NEW numeral, distinct from N1's T0=(logX)^{1/16} (ball radius), rename recorded to prevent conflation. (8) R2.5's gate GREW: now (R1.1 Koukoulopoulos-13.1 port [flags 2026-07-19 re-price C/D] + I-2) — parent freeze said only 'waits for H1'. (9) FROZEN INTERFACES UNTOUCHED: halasz_ball_decay verbatim, R3.1 conclusion, prop_A3', grade E(M)=exp(-M/2), H3 rungs R3.1-R3.4 unchanged. (10) Wall-3/M-infimum infra: NONE built (exit stone is center-form; M_range landed at DistHalasz.lean:262 serves H3/A2/A3b read-only).

## AMENDMENTS_PENDING

A1 [PENDING-JYH, staging, load-bearing]: add GHS [9] Granville-Harper-Soundararajan, 'A new proof of Halász's theorem, and its consequences', Compositio Math. 155 (2019), arXiv:1706.03749 to docs/sources BEFORE wave I-2 — required for the S1' PORT itself (REF-B R6 honest form: not merely confirmation), and it EXTENDS the D1 source-pin set (s8-freeze.md:10), which JYH should ratify explicitly. Wave I-1 is NOT gated on it. A2 [PENDING-JYH, quota]: the seam adds ~2,250 ln (band 2.1k-2.9k) over H2's 1800 budget; S8 projected total ~17.7-18.1k against the 18k quota — approve the pricing or direct a re-scope. A3 [PENDING-JYH, workflow gate]: this freeze is the designed-Fable-panel deliverable for the HALASZ-INFRA funding decision (pilot.md:9931-9934); wave I-1 (2 Opus executors, zero staging debt) dispatches on your go — Opus executors themselves are not gated per the workflow-gate rule. A4 [PENDING-JYH, option, NOT primary]: DES-B's route-option — one-iteration f*log = lambdaLin conv fg_J deleting the alpha,beta double integral (~-300 ln, swaps DEBT-1 for staging Tenenbaum III.4.3) — is a freeze-level change against D1's 'MRT App-A inline' pin; offered for decision, unvalidated interaction with the (A.11) alternating Euler product noted by its own designer. A5 [record]: GS [10] remains an H3-side staging debt outside this seam UNLESS the E4 gate-check finds the Lipschitz step irreplaceable, in which case it escalates per the E4 brief.

## OPEN_RISKS

1. S1' bespoke-port risk (HIGHEST): GHS's internal manipulations could use the sharp indicator at a step the hat kernel cannot mimic; REF-B's audit says every candidate step is a ramp-count <= h+1, confirmable only against the staged text. Mitigation: A1 staging gate + day-1 fail-fast + the freeze stays PARTIAL with S1' as THE named residual until it lands. 2. S2' log-power bookkeeping: grade headroom < half a log; the pre-Lean text-ledger gate converts a silent leak into a cheap flags-stop, but the ledger itself is the C-hard mass. 3. K4' cos_int_pair: mathlib-absent (verified); rectBI route is landed corpus but the improper-integral limit is a slog (+200 priced); split-landing contingency in the E1 brief. 4. I-3 double gate: R1.1's Koukoulopoulos-13.1 port (re-priced C/D, flags 2026-07-19) AND I-2 — the exit stone's landing date rides on the slowest of the two; a stall surfaces in flags, not in silent quota burn. 5. Latent GS [10] Lemma-7.1 in R2.4: replacement route declared but unproven until E4's gate-check; escalation path named (A5). 6. MVHilbert off-diagonal residual (Beurling-Selberg; pilot 07-19 MVCore entry): NOT a seam dependency (K4' is MVHilbert-free) but remains the parent freeze's K2 wall for waves 4-6 — the seam neither helps nor hurts it. 7. Concurrency: TypicalDensity.lean (A4a) is live-untracked; the seam's file set is disjoint; watcher lands serially per D6. 8. Numeral-conflation hazard Tsplit vs T0: renamed and briefed, but reviewers of I-2/I-3 output should check no (logX)^{1/16} appears where (logX)^4 is meant.

## SUMMARY_FOR_JYH

1. CHOSEN: minimal-seam (DES-B repaired) — hat-kernel Perron (two instances of the landed SW kernel_identity, exact, truncation deleted) + squarefree-linearized Lambda_{ell'} (kappa=1 disarmed by construction) + NO Wall-3 infra (exit stone verified center-form, infimum-free); DES-A's grade_EM stone and 0<c statement discipline grafted in.
2. WAVE I-1 IS DISPATCH-READY NOW: 2 Opus executors, 2 new files (HalaszKernel, HalaszLambda), zero staging debt, all rungs <= C on surfaces I re-opened and verified this session — awaiting only your funding go (A3).
3. YOUR THREE DECISIONS: A1 stage GHS arXiv:1706.03749 before wave I-2 (it is THE named residual); A2 approve +~2.3k ln (S8 lands ~17.7-18.1k of the 18k quota); A4 optional route-swap deleting the double integral (not primary).
4. REPAIRS BINDING: Tsplit=(logX)^4 (refuter-caught tail overrun, renamed to avoid colliding with N1's T0), S2' pre-Lean log-ledger gate (full-log leak = rung failure), R2.4 demoted D->C with the latent GS[10]-Lipschitz gate-check, I-3 gated on the R1.1 Koukoulopoulos port (flags 07-19).
5. FROZEN INTERFACES UNTOUCHED: halasz_ball_decay verbatim, R3.1, prop_A3', grade exp(-M/2); freeze is PARTIAL with exactly one named residual (S1' = the GHS Prop-2.1 analog) until staging lands.

dispatch_ready: True
