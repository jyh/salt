# COUNCIL v6 — 2026-08-03 morning (THE FLEET MEETING)
## The ruling session over fleet-meeting-0803-brief.md

*Sealed state: cdedcda — the board slept exactly as closed; nothing
moved overnight. Companion documents: the meeting brief (the four
roads priced), summit-dossier-0802.md + two addenda (the v5 read),
docs/RESULTS.md (68 trophies, lint-green).*

## C0 — THE STATE, one paragraph + the OOM note

v5 stands: outer nothing; riders cs/T₀-sharp/Ks + the caller-side
g-rider; conclusion-side ONE named predicate (the cofactor at the
raised lever). The ceremony: 1097/1097 content + the gold fresh
replay + Brun solo, zero rejections; the two OOM asterisks
(Salt-root, Maynard solo) reproduced across three environments.
THE OOM, read honestly: leanchecker replays a module's ENTIRE import
closure in one process — the root module = the whole corpus in RAM —
and the corpus has outgrown what 64GB gives a single replay process.
It is a milestone wearing an error code: the corpus is now too large
to hold in one head, which is precisely why we built the Trophy Room
and the search engine the same week. Fix: a large-memory cloud
container replay, registered, hygiene-not-doubt.

## C1 — THE READ + (e)

Your read: the dossier + addenda (one page + two updates). The
question C1 puts to you: does (e) — the word — wait for the last
stone (the cofactor register inhabitation + the K_vt trace), or is
v5-with-one-named-predicate the citable done-state? Sancho's counsel:
**let the stone land first** — the wave is priced (register
inhabitation likely free at the terminal's scale; K_vt the honest
open trace), and "one predicate, and here is its discharge" reads
better than "one predicate, trust us." But the Pearl + the Toll are
word-ready today if you wish to split the announcement.

## C2 — THE ALLOCATION (the brief's §6, for ratification)

1. **Road-B residue fires today**: REGISTER-INHABIT (the CofactorBulkL
   instances at the terminal's scale) + the K_vt trace; then the ε₀
   floor (ONE stone, THREE lanes: cs-genre leaves, T₀z, band
   effectivity) as the week's class-C item.
2. **Road A = the next campaign** (the fulcrum → the HB dichotomy
   crown), gated on C3's ruling + the N0 budget audit.
3. **Road C = the design frontier in parallel**: C5 (sign-function
   arithmetic certificates, cheapest) then C1 (the Type-II Completion
   class — Bombieri's asymptotic sieve as a formal question).
4. **Lanes standing**: MINE (fulcrum-method sweeps) + SEARCH (the
   --full embed pass runs today on the free machine); mathlib staging
   opens after the Fulcrum submission.

## C3 — THE ROSSER RULING (the one design decision gating Road A)

N5 needs the dimension-4 Rosser–Iwaniec sieve; no corpus analogue;
the import-vs-re-derive decision has been open since 7/20. Options:
(a) re-derive the needed fundamental-lemma consequence over our
Selberg machinery (self-contained, unknown breadth); (b) formalize
the RI λ± construction proper at dimension 4 (bigger, reusable, a
first anywhere — and PARITY-RECON found we already hold the RI
*linear* sieve vocabulary in Salt/Chen/LinearSieve.lean); (c) fire
ROSSER-SCOPE first — a read-only pricing scout over both routes
against the landed LinearSieve assets, verdict in hours. **Counsel:
(c) today, rule on bytes tomorrow.**

## C4 — THE WEEK (the human arc)

- **Today (Mon 8/3)**: the two filings (jas request + the salt
  reapplication naming Claude); the approval re-surface if quiet.
- **Tue 8/4**: the endorsement decision (nudge + third endorser
  together) — the Fulcrum's arXiv gate.
- **Fri 8/7**: the outside reviewer demo recording. The design doc draft is owed
  this week — the outside reviewer now holds three memos (spec doctrine, curation,
  the four returns); the facts skeleton is ready when you want to
  voice it.
- **The Witness**: the Toll chapter stands; the Pearl chapter can
  draft against v5's list now, or wait for the stone with §5 — your
  call rides on C1.
- the scheduled meeting + the second reviewer: week of 8/11.

## C5 — HOUSEKEEPING

The tank after the greatest single day (~4M+ subagent tokens across
~20 dispatches); jason resets on its cycle today — plenty for the
week. Queued hygiene: the CAMPAIGNS.md refresh + the stale-doc sweep
(MRT-ROAD's list), the SEARCH --full embed, the flagship's C⋆-vs-C⁽¹⁾
narrative note (before HB lands). The in-flight coordination lessons
of 8/2 (catch #104; the All.lean writer-slot law; /-! before
#audit_axioms) are all banked.
