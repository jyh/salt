# ⟦THE KNOT-2 CLOSURE⟧ — freeze v1 (2026-07-30 16:25 PDT; JYH-granted: THE QUANTIFIER — ∀x ∃M)

*The program's last design. Inputs: the KNOT2/LINK1 dossiers (v1+v2)
+ KNOT1-REF's ceiling + the maestro erratum (all banked). Sequence:
this freeze → K4-CENSUS (the refuter) → on PASS the three waves.*

## The grant

**M is chosen after x** (∀x ∃M): the ladder tracks the window, as
MR's own calibration does (Q₁ := h — mr_extract:471). The door's
absolute anchor was the formalization's artifact; every μ-wall since
was its shadow.

## The three waves

**R1 — THE p²-ROW L² RE-PRICING** (~300-500 new, C + ~10 sites B):
the Hölder split's sup-half (ω(n) ≤ log₂n) replaced by the direct L²
fibre count r(n) = #{p : p²|n}: ∑‖c‖²/n² ≤ 24/(X_d·P) — X_d-FREE.
Diagonal landed (cardMR_ramP2_cofactors_le); off-diagonal = the
p²q²-divisibility count (one line — the refuter checks it). The
re-thread through the a2RowsSum DEFINITION (text of thm_a2' verbatim;
hgRows WEAKENS — the S8 summit STRENGTHENS: the benign rule-1,
GRANTED). The endpoint twin's (log₂2X_d)²/(A²X_d) decaying — the
1.5× absorption re-checked (K2).

**R2 — THE CONSTANT POOL** (~2900, 60 decls, priced + mechanics
byte-verified by KNOT2-SCOPE; its KILL RETRACTED — contingent on the
p2 numerator R1 retires): the pool a free real π₀; hgU/hgBand stated,
hεwin/hL4096 dropped, ε FREED; the five summands constants-or-
decaying — the door's constant target δ₀/4 needs no decay.

**R3 — THE QUANTIFIER RE-CUT** (statement-tier, GRANTED): the
capstone's ∀M ∃C'x₀ ... ∃R prefix re-cut so M (hence Adoor, j₀, the
ladder) is chosen AFTER R. THE BRAID (the design content): g reads
x₀(M); M reads x; R reads g — resolve by EITHER a uniform g-majorant
over the m-ladder OR the band's ∃C'x₀ re-hoisted past R (the (P,Q)
are M-only — the S0-HOIST precedent, reversed); the K4-CENSUS
refuter adjudicates which. THE BILL: S13-A's numerals go m-SYMBOLIC
(the λ-window, the one-bit hMδ margin, smallGradeFits — the
secondary question: is m = 66 load-bearing anywhere?).

## The target picture (post-R1+R2+R3)

No μ-cap survives (gP1/level1/p2 μ-free); the window
[1,1280]·log2·M·Adoor M scale-free; the m-step 2.03 ≪ 1280 covers
every μ with 630× room; H/Q₁/P₁/A/M constants per instance; x → ∞
freely. The capstone-conditional's residue re-symbolized; the frames
close at the moving working point.

## The refuter mandates (K4-CENSUS, before any wave)

K1 ⟦THE DECAY CENSUS⟧ — the sharpest question: walk EVERY gate field
in DoorFuseFrame/DoorRowZeroBase/DoorBandBase/DoorArithFrameRho/
DoorCapBase/DoorCapErrWS/SocketBase/S13CapGate + the road-side gates
for ANY right side decaying in X_d (post-R1/R2 forms). ONE surviving
decay = the next wall — name it or certify none.
K2 ⟦R1's ARITHMETIC⟧ — the off-diagonal count; the endpoint twin's
absorption; the 24/(X_d·P) constant chain.
K3 ⟦THE BRAID⟧ — the R3 quantifier order: derive the consistent
prefix (the g-majorant vs the re-hoist) and verify against the head's
∃R payload + the band supplier's M-dependence; name the winning form.
K4 ⟦THE m-SYMBOLIC BILL⟧ — S13-A's pages: which numerals are
m-load-bearing (the one-bit margin!) and the re-derivation shapes.

FIRE RULE: on K1+K3 PASS (K2/K4 amendments foldable), the three
waves fire under the standing grant.
