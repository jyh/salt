# THE S8 MR-CORE FREEZE v2 (judge-merged, 2026-07-19 00:45 PT; both candidates refuted 2/2, all repairs applied; EXECUTION-COMPLETE)

## VERDICT/RATIONALE

halasz-first (repaired), with wave-architecture grafts. Rationale (follows the verdicts, not the prior): all four verdicts converge on ONE shared deepest flaw — the un-landed sigma=1 (log t)^{3/4}(loglog)^4 zeta UPPER bound — which has a fully specified cheap repair (new rung H1.0, both routes grounded on surfaces I re-verified this session). Beyond that, halasz-first's fatals are numeral/citation-grade (T0 revert, halving cascade, M_range cap, P3 artillery swap) with verified repairs, and its wave topology + 13-file partition explicitly SURVIVED both refuters. The wave-architecture candidate's fatals are baked into its binding decisions: the W6 character-bearing socket needs the never-landed S4 L-CHI-POW (+4-8k un-designed D-work or a JYH slot renegotiation — unavailable tonight), and the D-iii merge is provably vacuous at f=1 (M(1;X)=0). The halasz-first skeleton structurally avoids both (general-f terminal thm_A1', f=1 via capped M_range) — a scope split refuter-C explicitly ratified as legitimate. Every verified repair from all four verdicts is applied.

## THE FREEZE

S8 MR-CORE FREEZE v2 (judge-merged). Target thm_A1' — general-f MR short-interval mean-square at theta=3/4, qualitative fixed-delta grade; S9 consumes at f=lambda*chibar. Gate per rung: `lake build Salt.MR.<Module>` green, no new warnings, axioms in [propext, Classical.choice, Quot.sound] via scratch #print.
DECISIONS (amended): D1 pointwise Halasz port mandatory (MNT-III 23.15 qualitative-only, mr_extract sec 4.2); source MRT App-A pp.22-28 (1503.05121v3) inline; ALL A-chain conclusions at E(M):=exp(-M/2) [(1+M)e^{-M} <= 2e^{-M/2}, max 1.213]. D2 LOG-EULER-OSC SUBSUMED by H0 (flags.md:11448's two atoms); bridge TWO-SIDED, range x >= e (matches NonPret.lean:115); zeta-corollary discharges NonPret.lean:114 -> unconditional lambda_nonpret (S5 zeta-shadow closed). D3 extreme-t floor GROUNDED via NEW rung H1.0 (the flaw all 4 verdicts found): dist_one_floor_pow: D(1,p^{i b};x)^2 >= loglog x - (3/4)loglog(|b|+3) - 5*logloglog(|b|+16) - C, 1<=|b|<=x^A (-5 provable under BOTH H1.0 routes; -4 achievable under primary; consumers need only o(loglog)). MRT (A.5)/(A.6) ET machinery NOT ported — contingent on H1.0 (kill-check K3). D4 f=1 via M_range with domain CAPPED: M_range(f;X,T) := min D(f,n^{it};X)^2 over ((range [(logX)^{1/15},T] widened by (logX)^{1/16}) INTERSECT [-X,X]) — canonical def, NOT union-ball; M_range >= M(f;X) unconditional; M_range(1) >= (1/4-o(1))loglogX, consumed as exp(-M_range/2) <= (logX)^{-1/8+o(1)}. Fallback: separate real-Thm-3 port (+1.5k, C). D5 source pins: MR arXiv v4 ONLY (Sedunova-Wang 1_{(p,m)=1}), MRT v3 ONLY (corrected slow-M Prop A.3); pins in docstrings. D6 Salt/MR/All.lean CEREMONY-OWNED: executors never touch it; watcher appends imports + audit entries serially at landings.
REPAIRED NUMERALS (binding; S5 law: no logloglog absorbed into C): N1 T0 = (logX)^{1/16} (1/64 retune REVERTED: at A2's P,Q the exponent is 1-1/64-47/48 = +1/192, diverges; 1/16 gives -1/24, converges). N2 R3.1 conclusion D(fg_J,p^{it};X)^2 >= (1/32)loglogX - 5logloglog(2X+16) - C (halving via R1.1 now CARRIED; threshold stays (1/16)loglog; branch-b f-floor (1/2-1/4)^2 = 1/16, halved 1/32). N3 R3.2 T1 pointwise << X(logX)^{-1/64+o(1)}; T1 mass (logX)^{-1/32} <= C exp(-M/2) for M <= (1/16)loglog (threshold tie absorbed by <<-constant + existential c0). N4 c0 existential everywhere; middle term (480 loglog h/log h)^2.
WAVES (single-writer: 15 disjoint NEW files under Salt/MR/; landed files import-only, never edited).
WAVE 1 (t=0, 5 executors): [H0] PrimeSigmaShift.lean 900: R0.1 wrapper on LANDED sum_log_div_prime_le (Salt/Maynard/Mertens.lean:152, judge-verified) [A,50]; R0.2 prime_tail_shift Sum_{p>x}p^{-1-1/logx}=O(1) via mertens_second_sharp (Second.lean:216) + dyadic Abel — NOT ChebyshevInterval (wrong direction) [B,250]; R0.3 sigma_shift [A/B,120]; R0.4 euler_osc_truncation, general 1-bdd g, TWO-SIDED [C,300]; R0.5 two-sided bridge |Sum_{p<=x}cos(t logp)/p - log||zeta(1+1/logx+it)||| <= K, x >= e, via log_norm_zeta_eq_re_tsum (NonPret.lean:189) + prime_power_tail_le (Dist.lean:120); lambda-side >= and D(1)-side <= corollaries [B,180]. [H1.0] OneLinePowGrowth.lean 700 [C]: one_line_pow_growth: ||zeta(1+d'+it)|| <= C(log|t|)^{3/4}(loglog|t|)^4, 0<=d'<=1, |t|>=T0. PRIMARY route: cut J0=(log t)^{3/4}(loglog t)^4; low blocks harmonic; mid blocks vk_window_mid_prefix (GrowthPow.lean:451, verified) + Abel to sigma=1+d' (geometric tail exp(-Theta*J0)-grade = o(1)); high blocks k=12 branch of vk_dirichlet_block_le + Abel; n>t^2 tail zeta_sub_dirichlet_bound (Growth.lean:30). FALLBACK (declared): sigma-integration of a zeta'/zeta region bound (zeta_zero_free_region_pow GrowthPow.lean:1044 + near_norm_logDeriv_Zc_le ZetaPowLower.lean:301 + SW/ContourShift rectangle-CIF) to sigma1=1+(log|t|)^{-3/4}(loglog)^{-5} — gives (loglog)^5, consumers tolerate. [P1] MVHilbert.lean 1200 [C/D]: mv_hilbert + dirichlet_poly_l2_mvt: int_{-T}^{T}||dpoly N a t||^2 <= (2T+C_MV*N)Sum||a_n||^2, C_MV<=20; SHAPE-MANDATORY (E1 log-intolerant at fixed h — refuter-confirmed; T+NlogN is a REFUTED substitution); consumes L2MVT.lean:97/127; houses l2_duality (L10,B). MV-HILBERT closed. [P2] ShiuMoment.lean 700 [C]: shiu_moment_sq Sum_{Y<=n<=2Y}g(n)^2 << Y; route 1 adapt sum_tau_in_ap_le (ShiuS5b.lean:628 — corrected pointer); route 2 Rankin Euler-product (preferred fallback). [H2] HalaszCore.lean 1800 [D, THE keystone, moved to t=0]: see halasz_freeze; order R2.2 -> R2.3 (FAIL-FAST day 1) -> R2.1 -> R2.4; R2.5 waits for H1.
WAVE 2: [H1] DistHalasz.lean + NonPretClose.lean 900 (after H0,H1.0): R1.1 pretentious triangle + halving dist_mul_half [B,450]; R1.2 dist_one_floor_pow per D3 (consumes H1.0 + R0.5's <=-side; NOT zeta_lower_all_t — that is the LOWER bound, lambda-side only) [C,350]; R1.3 unconditional lambda_nonpret [B,100]; R1.4 M_range def (capped) + Mrange_one_floor (1/4-o(1)) [B/C,150]. [P3] LargeValues.lean 2000 [C, repriced] (after P1): L7 wellspaced_l2 via gallagher_pointwise (LS/Gallagher.lean:38, general C^1, verified) + NEW line-disjointness helper [B, in-file] + dirichlet_poly_l2_mvt on BOTH int||F||^2 and int||F'||^2 (coeffs a_n log n); LS/Spacing pair DROPPED (circle-carrier, wrong shape); L8 large_value_count (theta-free); L9 halasz_integers; L11 halasz_primes_pow at theta=3/4: contour sigma=1-c/((logT)^{3/4}(loglogT)^4), artillery = ZetaPowLower.lean:301 disc core + SW/ContourShift.lean + Vk zero-free region (NOT "SW/Landau" — does not exist), decay exp(-logP/((logT)^{3/4}(loglogT)^4))(logT)^2. [A1] Decomp.lean 1000 [C, repriced] (after H0): T_j/U defs + Ramare/Buchstab with v4 1_{(p,m)=1} correction (NAMED checklist line) + Lemma-12. [A3a] ParsevalSL.lean 1000 [C, repriced] (after H0): Lemma-14 continuous Perron + Saffari-Vaughan (Parseval.lean:76 is integer-frequency — new analytic work). [A4a] TypicalDensity.lean 450 [C] (independent, may run wave 1): Lemma-2.2 << (logP1/logQ1)X; Brun scaffolds are twin-specific — Rankin fallback declared, any positive power serves.
WAVE 3: [H3] PropA3Core.lean 1600 [C] (after H1,H2): R3.1/R3.2/R3.3/R3.4 with N1-N3 — see halasz_freeze.
WAVE 4: [A2] Prop1Assembly.lean 1600 [C/D] (after H3,P1,P2,P3,A1): E1 exact T+N; E_j moments (T/X+2^l Y1)(l+1)!^2 theta-free; int_U sec 8.3 at P=exp((logX)^{1-1/48}), Q=exp(logX/loglogX), H=(logX)^{1/48} (margin 11/48; first term (logX)^{-1/24}/loglog with N1); PLUS large-T sub-rung [200]: T > X regimes via trivial MVT + prefactor absorption (M_range cap makes prop_A3' T-uniform). TERMINAL prop_A3' (M_range form, capped domain).
WAVE 5: [A3b] ThmA2.lean 500 [C] (after A2,A3a): thm_A2' f-arm + f=1-arm (via R1.4). WAVE 6: [A4] ThmA1.lean 300 [B] (after A3b,A4a,R1.4): TERMINAL thm_A1' (S9 interface, general f): (1/X)int_X^{2X}|(1/h)Sum_{x<=n<=x+h}f(n)|^2 dx <= C(exp(-M(f;X)/2) + (480 loglog h/log h)^2 + (logX)^{-c0}), P1=(log h)^{480}, Q1=h, eta=1/12.
SCOPE-DIFFS (#224/#239): (1) real Thm 3+L4 dropped (D4 fallback declared); (2) A.6 ET replaced by D3 — contingent on H1.0/K3; (3) E(M)=exp(-M/2); (4) DELETED — T0 unchanged at (logX)^{1/16}; (5) A.4 threshold 1/8->1/16, conclusion 1/32; (6) c0 existential (no theta=2/3-tied numerals); (7) f=1 via capped M_range, floor 1/4-o(1) NOT 14/15; (8) v3 Prop A.3 + v4 Sedunova-Wang only; (9) twisted log-L bridge + lambda*chibar-quality floor = S1/S4/S5 deliverable consumed by S9, NOT S8 (thm_A1' is general-f; the character gap that killed the rival socket stays outside S8 by construction).
KILL-CHECKS (wave-1 exit): K1 H2/R2.3 fail-fast (3 attempts -> flags; fallback classical-Montgomery, also D — campaign gate, Fable/JYH decision). K2 P1 MV-Hilbert (stall blocks waves 4-6). K3 H1.0 (gates D3 compression; stall -> un-descope MRT (A.6) ET +1.5-2k or VK-PNT partial summation — flags + Fable ping, NOT executor discretion).
LINES: W1 900+700+1200+700+1800; W2 900+2000+1000+1000+450; W3 1600; W4 1600; W5 500; W6 300 = 14,650 + ceremony ~ 15.5k (refuter-honest band 15-17.5k, inside extract's 14-22k; quota budget 18k). Critical path max(H2, H0->H1)->H3->A2->A3b->A4 ~ 7.6k.

## THE HALASZ SUB-FREEZE

HALASZ BLOCK SUB-FREEZE (H2 HalaszCore.lean + H3 PropA3Core.lean — the dominant D-component). Executors read docs/sources/1503.05121v3.pdf pp.22-28 DIRECTLY (v3 pin: corrected slow-M route). NO textbook shortcut exists (MNT-III 23.15 is qualitative-equivalence only) — the pointwise GHS "[9]" engine must be ported. Frozen grade everywhere: E(M)=exp(-M/2) [(1+M)e^{-M} <= 2e^{-M/2}; buys sqrt-slack at each Cauchy-Schwarz].
EXIT STONE (frozen interface, consumed by R3.2/R3.4/A2): halasz_ball_decay: f 1-bdd mult, t0 real: the ball |t-t0| <= (logX)^{1/16} contributes U << X(exp(-D(f,p^{it0};X)^2/2) + (logX)^{-1/2+eps}).
H2 RUNGS (single writer, 1800 lines, dispatch t=0, internal order FIXED):
R2.2 halasz_cosh_ineq [B,80] FIRST (day-1 warm-up, pure series): 2cosh(a)-2cos(t) <= e^{sqrt(a^2+t^2)}; k=0 terms cancel, a^{2k}+t^{2k} <= rho^{2k} termwise, sum <= 2cosh(rho)-2 <= e^rho.
R2.3 lambda_ell_decomp [D, FAIL-FAST, 400] DAY 1: the s_J/ell/Lambda_ell Plancherel decomposition of pp.24-25; TuranKubilius.lean:288 supports typical-n bookkeeping. 3 serious attempts -> flags.md + STOP (kill-check K1); fallback = classical Montgomery-route Halasz (also D; Fable/JYH decision, not executor discretion). This IS the freeze's irreducible risk.
R2.1 ball_mvt [C,300]: from landed dirichlet_poly_l2_expand/_diagonal (L2MVT.lean:97/127) + elementary off-diagonal; T+NlogN grade ACCEPTABLE here (short prime blocks only — the log-intolerance lives in E1, not the ball).
R2.4 A.6/A.7 chain [D,700]: iterated moment/decay chain at exp(-M/2) grade.
R2.5 halasz_ball_decay [C/D,300] LAST (needs H1's R1.1 triangle): assemble R2.1-R2.4 into the exit stone.
H3 RUNGS (1600 lines, after H1+H2; REPAIRED NUMERALS BINDING):
R3.1 dist_split_A4 [B/C,300]: threshold (1/16)loglogX. Branch a (M >= (1/16)loglog): halve via R1.1 dist_mul_half -> (1/32)loglog - O(1). Branch b (|t-t1| > (logX)^{1/16}/2): recenter D(f,p^{it}) >= D(1,p^{i(t-t1)}) - D(f,p^{it1}) >= (sqrt(1/4-o(1)) - sqrt(1/16))sqrt(loglog) — the 1/4-coefficient is EXACTLY what H1.0 grounds (underivable from zeta_lower_all_t, which is the LOWER bound serving only the lambda-side) -> f-floor (1/16)loglog -> halved (1/32). FROZEN CONCLUSION (both branches): D(fg_J,p^{it};X)^2 >= (1/32)loglogX - 5logloglog(2X+16) - C. The -5logloglog is o(loglog), carried IN-STATEMENT, never absorbed into C (S5 law); any executor "simplification" absorbing it is a bug.
R3.2 T1_pointwise_decay [C,500]: T1 pointwise << X(logX)^{-1/64+o(1)} (= exp(-(1/2)(1/32)loglog) grade); integrated T1 mass (logX)^{-1/32} <= C exp(-M/2) for all M <= (1/16)loglog; the tie at threshold is absorbed by the <<-constant + existential c0.
R3.3 halasz_R_poly [C,500]: complex A.5-analog; T0 = (logX)^{1/16} — MRT's own choice, REVERTED from the refuted 1/64 (at A2's P,Q the 1/64 version made sec-8.3's first term DIVERGE, exponent +1/192; 1/16 gives (logX)^{-1/24}/loglog -> 0). Distance threshold (logX)^{1/16}/2 unchanged; ball/range separation 1/16 < 1/15 intact.
R3.4 ball_integrated [B/C,300]: T0-ball integrated packaging via R2.5 + R3.3.
INTERFACE CONTRACT (extract-granularity discipline): pdf-level mismatches in R2.3/R2.4 INTERNAL lemma shapes do NOT propagate — only halasz_ball_decay, R3.1's frozen conclusion, and prop_A3''s statement are frozen interfaces; executors adjust internals within their own file.
CONSUMES (all judge- or double-refuter-verified): L2MVT.lean:97/127; Dist.lean:50/120; TuranKubilius.lean:288; NonPret.lean:65 loglog_height_le (+ trivial x^A variant as in-file helper); H1.0's one_line_pow_growth via R1.2; R0.5's two-sided bridge. FEEDS: A2 (sec 8.3 + T0/T1 split), A3b, thence thm_A1'.
RISK POSTURE: R2.3/R2.4 is the irreducible D-mass (mr_extract sec 4.1: Halasz-from-scratch 4-7k dominant; our 1800+1600 sits low in band because ball-MVT + cosh atoms are landed/elementary and the ET machinery is not ported — overrun priced in the 18k quota budget). K1 protocol: an R2.3 stall reaches flags.md within day 1 of dispatch, before any wave-2+ quota is spent.

## GRAFTS

- H1.0 OneLinePowGrowth rung — closes the FATAL all four verdicts share; primary route from the halasz-first refuter (vk_window_mid_prefix GrowthPow.lean:451, judge-verified), fallback sigma-integration route from the wave-arch refuters (near_norm_logDeriv_Zc_le ZetaPowLower.lean:301 + SW/ContourShift, judge-verified)
- sum_log_div_prime_le grounding (Salt/Maynard/Mertens.lean:152, wave-arch verdicts' verified upgrade of the LOG-EULER-OSC flag's 'presence UNVERIFIED') — R0.1 collapses to a class-A wrapper
- two-sided bridge form + x >= e range for R0.5 (wave-arch refuter repair; the D(1)-side <= corollary is what R1.2 consumes — the lambda-side alone was direction-insufficient)
- D5 source pins as binding decision (MR v4 ONLY / MRT v3 ONLY, docstring-carried) — from wave-arch's D-iv
- D6 All.lean ceremony-owned protocol + module-target build gates (both refuter pairs found the shared-write hazard independently)
- kill-check discipline at wave-1 exit (wave-arch W1 pattern): K1/K2/K3 named, incl. H2 moved to t=0 with day-1 fail-fast on R2.3
- E1 log-intolerance audit result (wave-arch refuter's back-of-envelope) — P1's shape-mandatory posture confirmed, audit-first branch deleted
- -5 logloglog safety grade on all floor statements (wave-arch refuters' route arithmetic) — provable under BOTH H1.0 routes, free for consumers
- scope-diff (9): explicit character-gap exclusion (wave-arch refuter-C's checklist finding, ratifying halasz-first's general-f terminal) — S4/L-CHI-POW + twisted log-L bridge named as S1/S4/S5 deliverables
- ShiuS5b.lean:628 corrected pointer for P2 (halasz-first cited ShiuFinal.lean; judge-verified the actual location)

## OPEN RISKS

- H2 R2.3/R2.4 (the GHS Lambda_ell core) remains class-D with zero corpus and a D-grade fallback — the freeze's irreducible risk; mitigated by t=0 dispatch + day-1 fail-fast (K1), not eliminated
- P1 MV-Hilbert is shape-mandatory with no substitute (E1 log-intolerant, refuter-confirmed); a stall blocks waves 4-6 (K2)
- H1.0 is new analysis (the sigma=1 (log)^{3/4}(loglog)^4 growth): both routes are specified from judge-verified surfaces but neither is landed; a stall triggers K3 (un-descope MRT A.6 ET +1.5-2k, Fable ping required — the D3 compression and c_T1=1/16 both die without it)
- A4a sieve-density adaptation: Brun scaffolds are twin-specific; Rankin fallback declared; one-sided (pure additive term) risk
- Sedunova-Wang v4 1_{(p,m)=1} correction must survive every Buchstab step in A1/A2 — named checklist line, Benli-flavored silent off-by-one risk
- S9 must supply the lambda*chibar-quality floor externally (scope-diff 9); if S9's slot in mr-freeze.md expected a character-bearing S8 socket, JYH should confirm the general-f thm_A1' interface at next contact — decision-register item, does NOT block wave-1/2/3 dispatch (only A4's final statement wording could shift) [RESOLVED 2026-07-26, confirmed against source: MRT §4 p. 15 applies Thm A.2 to g·χ̄ as a general-f black box; the χ-quality genuinely lives outside S8, supplied by the S9 ladder's M4-6 off LandauL1; see s9-freeze-0726.md]
- Line total 15.5k is refuter-honest but H2+H3 sit low in the extract's dominant Halasz band — overrun to ~17.5k priced in; recommend quota budget 18k
- M_range large-T absorption sub-rung in A2 (~200 lines) is judge-added from refuter repair R4 and is the least battle-tested piece of the assembly wave (T > X via trivial MVT + prefactor absorption); if it leaks, prop_A3' falls back to T <= X^2 stated range, which every downstream consumer satisfies (Lemma-14's max is at T = X/h1 <= X)

## EXECUTOR NOTES (wave-1 briefs)

DISPATCH PROTOCOL (all waves): subagent_type = the rung file's stone name (agent-naming memory); model: "opus" MANDATORY on every dispatch (executor-model memory — omitted model inherits Fable and burns the quota cliff); work on the MR track branch, NEVER commit to main without JYH approval (commit-policy memory); [skip ci] on routine pushes. Per-rung gate: `lake build Salt.MR.<Module>` + scratch `#print axioms <stone>` (<= propext/Classical.choice/Quot.sound); executors NEVER edit Salt/MR/All.lean, any landed file, blueprint statements, or CLAUDE.md — the watching session wires All.lean imports + audit entries serially at each landing and runs full `lake build` there. Iron rule 4: 3 serious attempts -> flags.md entry -> next rung. All frozen numerals are BINDING (iron rule 1); the -5logloglog terms are carried in-statement, never absorbed into C.
WAVE-1 BRIEFS (5 parallel executors, t=0):
[E1: PrimeSigmaShift] File Salt/MR/PrimeSigmaShift.lean. R0.1 [A,50]: wrap sum_log_div_prime_le (Salt/Maynard/Mertens.lean:152) as mertens_first_upper; grep mathlib first (cheap either way). R0.2 prime_tail_shift [B,250]: Sum_{p>x} p^{-1-1/logx} = O(1) via mertens_second_sharp (Salt/Mertens/Second.lean:216) + dyadic Abel; do NOT use ChebyshevInterval (its primes_in_interval_ge is a lower bound — wrong direction). R0.3 sigma_shift [A/B,120]: Sum_{p<=x}(p^{-1}-p^{-1-delta}) <= delta*Sum(logp)/p at delta=1/logx, then R0.1. R0.4 euler_osc_truncation [C,300]: TWO-SIDED, all 1-bounded g: |Sum'_p Re(g(p))p^{-1-1/logx} - Sum_{p<=x}Re(g(p))/p| <= C. R0.5 [B,180]: two-sided bridge |Sum_{p<=x}cos(t logp)/p - log||zeta(1+1/logx+it)||| <= K for Real.exp 1 <= x, via log_norm_zeta_eq_re_tsum (Salt/MR/NonPret.lean:189) + k>=2 peel with prime_power_tail_le (Salt/MR/Dist.lean:120); export lambda-side >= corollary (exact hbridge shape of NonPret.lean:114) and D(1)-side <= corollary. Exit stones: log_euler_osc_zeta (closes the LOG-EULER-OSC flagged node) + euler_osc_bridge_le.
[E2: OneLinePowGrowth] File Salt/MR/OneLinePowGrowth.lean [C,700]. Stone one_line_pow_growth: ||zeta(1+d'+it)|| <= C(log|t|)^{3/4}(loglog|t|)^4 for 0<=d'<=1, |t|>=T0. Primary route (pinned): dyadic cut J0=(log t)^{3/4}(loglog t)^4; blocks logN <= J0 harmonic (<= 1+J0); mid blocks (log2 <= Theta*j, 10j < log t) via vk_window_mid_prefix (Salt/Vk/GrowthPow.lean:451) + Abel to sigma=1+d', geometric tail Sum_j exp(-Theta j log2) <= exp(-Theta*J0)/(Theta log2) = o(1) since Theta*J0 = c*loglog t; high blocks (10 logN >= log t) via the k=12 branch of vk_dirichlet_block_le + Abel; n > t^2 tail via zeta_sub_dirichlet_bound (Salt/Vk/Growth.lean:30). Declared fallback (if 3 attempts fail): sigma-integrate a zeta'/zeta region bound built from zeta_zero_free_region_pow (GrowthPow.lean:1044) + near_norm_logDeriv_Zc_le (Salt/MR/ZetaPowLower.lean:301) + Salt/SW/ContourShift.lean rectangle-CIF, to sigma1 = 1+(log|t|)^{-3/4}(loglog|t|)^{-5} — lands (loglog)^5, which every consumer tolerates (statements frozen at -5logloglog). Both routes exhausted -> K3: flags.md + STOP, ping the watcher (Fable decision on un-descoping MRT A.6). Do not edit GrowthPow.lean or ZetaPowLower.lean.
[E3: MVHilbert] File Salt/MR/MVHilbert.lean [C/D,1200]. Port the Montgomery-Vaughan generalized Hilbert inequality |Sum_{r != s} x_r conj(x_s)/(lambda_r - lambda_s)| <= C_H Sum |x_r|^2/delta_r (delta_r = min gap), then dirichlet_poly_l2_mvt: int_{-T}^{T}||dpoly N a t||^2 <= (2T + C_MV*N) Sum||a_n||^2, C_MV <= 20, from dirichlet_poly_l2_expand/_diagonal (Salt/MR/L2MVT.lean:97/127; frequencies log n, gaps ~1/n). The (T+N) SHAPE is mandatory — T+NlogN is a refuted substitution (E1 in wave 4 is log-intolerant); do not weaken. Also house l2_duality (MR L10, B-grade) for P3. Closes the MV-HILBERT flagged node. Stall = K2: flags + ping.
[E4: ShiuMoment] File Salt/MR/ShiuMoment.lean [C,700]. shiu_moment_sq: Sum_{Y<=n<=2Y} g(n)^2 << Y for the block-divisor g. Route 1: adapt sum_tau_in_ap_le (Salt/Maynard/ShiuS5b.lean:628, the ShiuCore export). Route 2 (preferred fallback): Rankin Euler-product <= Y*exp(Sum_{[Y1,2Y1]} 3/p). Feeds A2's L13 moments with admitted C^l slop (C <= 8).
[E5: HalaszCore] File Salt/MR/HalaszCore.lean [D,1800]. Follow the halasz_freeze verbatim: R2.2 (day-1 warm-up) -> R2.3 (FAIL-FAST, day 1: 3 attempts -> flags + STOP, K1) -> R2.1 -> R2.4; R2.5 deferred until H1 lands. Read docs/sources/1503.05121v3.pdf pp.22-28 directly. Internal lemma shapes are yours to adjust; the exit stone halasz_ball_decay's statement is frozen.
WAVE-2 BRIEF SKELETONS (dispatch on wave-1 gates): [DistHalasz+NonPretClose | after H0+H1.0] R1.1 triangle + dist_mul_half; R1.2 dist_one_floor_pow = loglog x - (3/4)loglog(|b|+3) - 5logloglog(|b|+16) - C via H1.0 + R0.5's <=-side (NOT zeta_lower_all_t); R1.3 unconditional lambda_nonpret (discharge NonPret.lean:114's hypothesis, closes the S5 residual); R1.4 capped M_range + Mrange_one_floor (1/4-o(1)). [LargeValues | after P1] L7 via gallagher_pointwise (Salt/LS/Gallagher.lean:38) + new line-disjointness helper + l2_mvt on F and F' (coeffs a_n log n); L8/L9 theta-free; L11 contour at sigma = 1-c/((logT)^{3/4}(loglogT)^4) with ZetaPowLower.lean:301 + SW/ContourShift + zeta_zero_free_region_pow. [Decomp | after H0] Buchstab/Ramare with the v4 1_{(p,m)=1} correction on EVERY step — named checklist item. [ParsevalSL | after H0] continuous Perron + Saffari-Vaughan; Parseval.lean:76 is a pattern only (integer-frequency). [TypicalDensity | independent] Lemma-2.2; check Brun scaffold adaptability first, Rankin fallback. WAVE 3+ (H3, A2, A3b, A4): dispatch from the freeze's rung lines + halasz_freeze's H3 section; the A2 executor additionally reads 1501.04585v4.pdf sections 4-8 and carries the large-T sub-rung.
