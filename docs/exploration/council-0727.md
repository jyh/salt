# FLEET COUNCIL — first sitting, 2026-07-27 09:00 PT

Format (JYH-ratified 7/25): maestro-seat sitting; per-seat briefs drafted by
the maestro; decisions banked here and mirrored to the private archive at close-of-board;
JYH carries jas-side ratifications; JYH adds non-confidential outside-lane plans
verbally (never written here — the firewall).

## Per-seat briefs

### salt — PERSONAL FLAGSHIP (this seat; the maestro's home)

**State**: the strongest 48 hours of the campaign. **S7 CLOSED UNCONDITIONAL**
(`bigXiArcTight_twelve` — the full minor-arc arc, 3806 ln, zero unstaged
sources, sealed f501db2) after the 7/26 marathon: seventeen landings in one
day, including the eq-26 arc (both waves), the B₅ = 12 consequence chain, the
parametric door inhabitant, and **hsieve retired** under the new
no-hypotheses law (it was already owned — `typical_density_le` IS MR
Lemma 2.2). The refuter cascade caught and repaired errors at every level
(the maestro's included) same-day. Census: 9459 jobs green, all landings at
the three axioms.

**The remaining door road** (the register, decisions 1–4 below): the
S8/thm_A2′ arc (THE big remaining object; interface pinned in Amendment B),
the Landau production (odd-χ FE cheapest; even-χ the isolated residual), the
M4/S9 wave (gated on both), Lemma 4/GS (carried, owned, unscheduled), S11's
compose, the eq-26 mechanical close.

### jas — the vector-illustration editor (PUBLIC; JYH carries ratifications)

From the maestro's read-only reach (branch `arc2-prototypes`): the seat is
mid **arc-2 prototype instrumentation** — the BINDVALUE arc (the bind-value
pass with its two gates, landed for the reference + Swift + Rust ports, with
a counted-residue record of what closed and what did not), POLYAREA (a true
even-odd area metric + a family that gates the instruments, one copy per
port), EVALBOUNDS (doc-precision narrowing). The pattern mirrors salt's:
per-port gates, honest residue counts, statement-first discipline — the
bribeable-referee space run with unbribeable bookkeeping. No cross-seat
blockers visible from here. JYH to relay anything the jas seat wants raised.

### jacobian-verify — PUBLIC (github.com/jyh/jacobian-verify + mathlib PR #42116)

PR state this morning: OPEN; JYH's own-words reply to felixpernegger's
duplication challenge posted last night (the graceful close-offer included).
**ocfnash (maintainer) engaged constructively**: an inline suggestion to
merge master and use the new `eval_det` tactic — the PR is being reviewed on
its merits. The maestro has an experiment aloft preparing (NOT pushing) the
merge + tactic test; results to JYH for the decision and the own-words
follow-up. Posture question for the sitting: decision 5.

### Heritage trio (metaprl / libmojave / omake) — PUBLIC, Road 5

Unchanged per the Road-5 triage: banner-first; libmojave the modernization
demonstrator; omake points to ocaml-omake; metaprl frozen crown jewel. No
urgency; no action before salt's publication gate. (Standing: salt public
only behind the history purge + SaltBench wave boundary.)

### mandelbrot — the first toy. Dormant, fondly.

### seat — PRIVATE FOREVER

The mirror receives this council's decisions at close-of-board (the
push-to-cloud command set from the 7/22 protocol). Last sync: pre-marathon;
today's sync carries the S7-closed state + this document.

## Cross-seat standing items

- **The lanes/firewall**: outside-lane material stays verbal and non-confidential
  at council; nothing enters the written record. (Ratified 2026-07-21.)
- **The writeup plan**: venues ratified (arXiv → Pi flagship; Notices A′);
  the flagship skeleton at papers/flagship/main.tex. S7's closure makes the
  minor-arc section writable. SaltBench SILENCE RULE stands.
- **the copyright-waiver filing sequencing**: jas files only after salt's.
- **The cloud bridge**: TODAY from the jason seat — environment + firewall
  config + re-fire the drafted salt-bridge-lean4checker routine (the full
  prompt drafted 7/24; the GitHub-app grant for jyh/salt is done). The
  lean4checker replay doubles as the D2 pre-arXiv gate.
- **Spend posture**: last night's marathon was subagent-heavy (~2.5M
  subagent tokens). The results justified it (S7 whole); flagging for JYH's
  quota awareness going into the week. Fable-workflow gate law unchanged.
- Housekeeping: caffeinate sentinel 67161 killed this morning; the repo-root
  montgomery3.pdf is an MNT-III duplicate (JYH: keep or drop).

## THE DECISION TABLE (for the sitting)

1. **The S8/thm_A2′ design block** — the big remaining analytic object.
   Maestro recommendation: run the design block TODAY (fresh, post-marathon;
   the H-block seam machinery is largely landed — the block scopes what
   remains between the landed seam/kernel exits and the pinned single-h
   interface). Ratify: go/hold + the token budget.
2. **The Landau production** — the odd-χ FE stone [C, ~400] closes the
   L1LowerEffective production for HALF the characters; the even-χ side is
   the campaign's one open Siegel-flavored residual. Options: (a) dispatch
   odd-χ now + a design scoper for even-χ (regulator/CNF territory);
   (b) hold both for the effectivity-audit discussion (the deferred
   final-theorem-effectivity stance ties in here). Maestro leans (a)+scoper.
3. **eq-26 mechanical close** — cheap, gated on hMsup + Lemma 4; no action
   needed today beyond noting the order.
4. **The Lemma-4/GS arc** — the last carried analytic external with no
   schedule. Wants a pricing scoper this week (GS [12] is staged as
   gs9911246.pdf). Ratify: scoper now vs after S8.
5. **#42116 posture** — the close-offer is on the table in JYH's reply; the
   maintainer engagement suggests the PR may live. Decide: hold the
   Counterexamples-home line (with the eval_det improvement as the good-faith
   response) vs actively steer to the FC-canonical close. Maestro leans:
   apply eval_det, reply in JYH's words, let the maintainers decide homes.
   **⟦EXPERIMENT RESULT, pre-sitting⟧ eval_det WORKS, better than asked:**
   master merged clean (55 commits, zero conflicts, toolchain unchanged);
   the bridge is `Matrix.eta_fin_three` (J is a fun, not a `!![...]`
   literal); `eval_det` closes the goal ALONE (the trailing `ring` deleted);
   the `set_option maxRecDepth 8000` workaround is GONE (pderiv distributes
   over nine small entries pre-expansion); file build 6.5s → 1.5s (4.3×);
   net −2 lines; axioms unchanged on all 13 theorems; lint clean. Local
   commit a505176, 57 commits unpushed — THE PUSH IS JYH'S. Two judgment
   calls to confirm/revert: (a) the docstring's "normalizing with ring"
   was factually stale and was updated — an isolated hunk, droppable;
   (b) the now-redundant Determinant.Basic import kept as the honest
   direct dependency. Reply facts for JYH's own words: merged master;
   eval_det works here via eta_fin_three; closes the goal outright;
   removed our maxRecDepth workaround; 4× faster; thanks for the pointer.
6. **The cloud bridge** — confirm today's jason-seat session and the
   firewall checklist.

*(Close-of-board: bank rulings inline below each item, sync to the private archive.)*
