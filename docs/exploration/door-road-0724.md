# THE DOOR ROAD — Prop 1 → MRTUniformityXi → unconditional log-Chowla
## (DOOR-SCOPE recon, 2026-07-24 evening; read-only; maestro-verified where noted)

*The pricing of everything between the S8 campaign's imminent Prop 1 and the
discharge of the MRT door — the single hypothesis of the landed spine. GO
HIGH (structural) / MEDIUM (schedule). Total ≈ 12.5k–21k lines; exactly ONE
item carries D-risk, and the arithmetic points at a rescope that removes it.*

## The twist-mechanism answer (the brief's critical question)

**`e(αn)` is NOT a Mellin/costwist object, and MRT never converts it — they
ELIMINATE it** (MRT 1503.05121 §4, pp.14–16; mrt_extract.md:240–254):
(a) Dirichlet-approximate α = a/q + θ, q ≤ W; kill the archimedean drift
`e(θn)` by integration by parts **in the sub-window length H′** (eq. (4.2))
— the freeze's S9 "phase-freezing subdivision"; NO multiplicative Abel
absorption (refuted long ago). (b) Kill the rational part `e(an/q)` by the
residue split + Dirichlet-character orthogonality (eq. (4.4)) → the twisted
function g·χ̄, χ mod q₀ ≤ W — needs COMPLETE multiplicativity (λ has it).
(c) The quality bridge M(gχ̄;X′) ≥ M(g;X,W) − O(1) + Thm A.2 + CS + dyadic-X
⇒ (4.1): ≪ HX/(d·W^{1/4}).
The t₀-generic seamCoeff machinery supplies step (c)'s QUALITY input (it is
exactly the M_range variable) — but the additive frequency never becomes
n^{it}. ⚠ mr_map_sources.md:198–201's "[M] partial summation in n^{it}"
guess is half-wrong (characters right, n^{it} wrong) — superseded by this.

## The object map (door L¹/logMeasure ↔ S8 L²-in-x): B1 phase re-basing [A];
B2 λ-instantiation via the CM extension = ArithmeticFunction.liouville
identity [B]; B3 integer log-sum ↔ real integral (±2 window terms, 2/H) [B/C];
B4 dyadic split of (x/ω,x] [B]; B5 L²→L¹ Cauchy–Schwarz per block (door δ ⇒
mean-square δ²) [A/B]; B6 re-sum against Z ≈ log ω (harmonic_window_bounds
landed) [B]; B7 the ∀α-OUTSIDE invariant carried through every step (named
guard; moving it = Tao (4.1), OPEN); B8 = the MRT §4 elimination above [C/D
→ the S9 block]. B3–B6 = the freeze's "logMeasure glue"; none exists yet.

## Regime findings (the important ones)

1. **The door's H-range pinned**: H ≤ H₊ ≈ log x/(2.77ε²) — H = O_ε(log x).
   MR Theorem 1 needs only 2 ≤ h ≤ X; our H sits at the extreme small end,
   exponentially clear of h₂ = X/(logX)^{1/5}. **Free savings**: MR Thm 3's
   h > h₂ branch (eq. (26), Lemmas 4+5) and §9's large-h case NOT needed.
2. **⚠ THE BINDING DEFECT IS INSIDE S8, not at the door**: the landed
   annular rows gate at **T ≤ log X** (AnnHead :202/:304/:341/:400,
   Prop1Assembly :404) but the Lemma-14 consumer needs T up to X/h₁ ≫ log X.
   Fix = the morning-brief's deferred re-grade: the (T/X+1)·e^{−cM} socket
   form (Prop 1's own (T/(X/Q₁)+1) grade). Class C, 300–500 ln —
   **T-RESHAPE dispatched (additive variants; no landed line edited).**
3. **⚠ THE TERMINAL TO CONSUME IS `log_chowla_two_budget_head` at
   extraFloor := H₀door(δ₀)** — NOT `log_chowla_two_door_only_xi` (its
   extraFloor := 0 witness allows R.Hlo ~ 4·10⁶ where the MR grade
   (log H)^{−5/4} ≈ 0.02 sits ~47 orders above δ₀). The ∀-extraFloor
   interface exists precisely for this (mr-freeze S11).
4. **δ₀ numerics close one-sided**: δ₀ ≈ 2·10⁻⁴⁹; H₀door ≈ exp(9·10³⁸);
   x ≈ exp(exp(9·10³⁸)) — double-exponential, Siegel-ineffective posture
   already registered; S10a's threshold satisfied with 34 orders of headroom;
   W = log⁵H vs Tao's (log X)^{1/125} ✓.

## The ladder (Prop 1 → door)

Landed already: regime_W_headroom_of_floor (S10a); Mrange_one_floor (R1.4);
the Xi seam + implication; bigXi_bounded; **MR-A = dirichlet_poly_l2_mvt_final
LANDED**; **MR-C = turan_kubilius LANDED** (both registered openers done; TK
is typical-n bookkeeping, not the decomposition — that's Ramaré/Decomp).

| # | stone | class | ~ln | path |
|---|---|---|---|---|
| 1 | eq-(24) seam hsplit (𝒰/𝒯ⱼ partition + eq-(16) join) | C | 400–700 | DESIGN then executor — critical path |
| 2 | hfloor (R3.1 branch supply) | C | 300–500 | executor after design |
| 3 | T-range re-shape → (T/X+1) form | C | 300–500 | executor — DISPATCHED |
| 4 | thm_A2′ (f-arm + f=1) | C | 500 | executor |
| 5 | thm_A1′ | B | 300 | executor |
| 6 | MR §9 / MRT A.1 glue (small-h branch only; §9 params un-transcribed — the gap) | B/C | 1000–2000 | executor |
| 7 | S7 ARC bigXi_arc (not landed; Vinogradov contrapositive) | C | 2500 | executor |
| 8 | S9 MRT-A1 (§4 (a)+(b)+(c) + B3–B6 glue) | C/D→C | 4000 | design block first (template grounded) |
| 9 | λχ̄-quality arm (scope-diff 9) | C (D-risk) | 1500–8000 | THE LONG POLE — the lever below |
| 10 | S10b CHOICE-JOIN g-builder | B/C | 900 | executor |
| 11 | S11 COMPOSE at extraFloor := H₀door(δ₀) | B/C | 600 | executor |

## 🔑 THE LEVER (stone 9's likely rescope — the design-block kill-check)

Demand: 3·log W with W ≪ (logX)^{1/125} ⇒ (3/125)·loglog x. The landed
ζ-side θ=3/4 coefficient 1/4 beats it by 125/12 = 10.4× (matches the ledger's
"10.4×" verbatim — independent confirmation). ⇒ **any χ-power-region with
θ ≤ 122/125 ≈ 0.976 suffices** — exactly POLE-2's residual clause ("VK's 2/3
is sufficient, not necessary; any θ < 0.98 region would do"). The q-aspect
is a non-issue (q ≤ W ≤ (logloglog x)⁵; the unconditional L(1,χ) ≫ q^{−1/2}
floor overserves by two iterated exponentials; the real-χ Siegel branch
priced at 0.041 ≪ 1/4). **KILL-CHECK for the design block: does an
unconditional θ ≈ 0.97 t-aspect region for L(s,χ), q ≤ (logX)^{1/125},
discharge stone 9 — retiring S4a's Vk phase-parametricity D-risk entirely?
If yes, NO D-grade node remains in the whole campaign.**
Secondary kill-check (same block): re-derive MRT (4.4)'s character-expansion
cost at OUR δ₀ — the W^{1/4} saving must survive (a naive Gauss-sum triangle
costs √q = (logH)^{B5/2} against the L¹ grade (logH)^{−1+o(1)}, which FAILS
for B5 ≥ 2 — verify against 1503.05121 §4 and S7's B5 consistency).

## NOT needed (retired explicitly)

The minor-arc Kátai/BSZ ~2M package (Ξ_H weakening); Tao (4.1) sup-inside
(OPEN, never on the path — the standing guard holds); MR §10–§11; MR Thm 3's
h > h₂ branch; any Weyl/Vinogradov machinery beyond S7's contrapositive.

## Order

1. The eq-(24) seam + hfloor (critical path; the seam design block first).
2. Parallel, off-path: T-RESHAPE (dispatched); S7 ARC; the stone-9
   kill-check design block (the only item that can change the campaign's
   grade — start early).
3. thm_A2′ → thm_A1′; the §9 glue (transcribe §9's two-case params first).
4. S9 with the MRT §4 template.
5. S10b → S11 at extraFloor := H₀door(δ₀). THE DOOR DISCHARGES; log-Chowla
   is UNCONDITIONAL.

## Housekeeping done same-session

- mr_extract.md Thm-1 exceptional set corrected `δ²h^{1/25}` → `δ²h^{δ/25}`
  (BOTH sites; maestro PDF-verified pp.1–2; flags.md entry; the h-saving is
  δ-dependent and degrades as δ→0 — load-bearing for any δ-ledger).
- mr_map_sources.md's [M] twist-mechanism guess: superseded (see above);
  correct at the next Route-M touch.

## ⟦CORRECTIONS BLOCK — the night scouts, 2026-07-24 ~21:00⟧

Per SEAM-SCOPE (seam-scope-0724.md) + CHI-CHECK (chi-check-0724.md), both
maestro-audited (one scoper error corrected: L11 IS landed —
halasz_primes_pow, HalaszPrimesCore:3641):

1. **Stone 1 re-priced and re-specified**: the current hsplit binder is
   UNSATISFIABLE at the intended semantics (annHead ≍ T vs Itot ≍ T/X+1 —
   the object finding); the honest seam = SHAPE 0 (≤-weakening) + SHAPE A
   (the MRT ball/far split on the ANNULUS; the partial-summation bridge
   replaces the line re-pin; the head leg becomes T-FREE) + SHAPE B (the
   literal partition, B-tier) as the far-leg interior. The re-pin of the
   head object is JYH-TIER (the original annHead pin was JYH-ratified).
2. **Finding #2's repair superseded**: not the socket's T-shape — the OBJECT.
   (T-RESHAPE's polyT chain stands as landed shelf stones; the _of_floor
   variants stay do-not-wire.)
3. **NEW ladder item**: the §8.3 interior (post-L11-correction): L3 for the
   dyadic Ramaré co-factor + L9's hZ vdC socket + L12's herr ≈ 1–2k, C.
4. **Stone 9 RESCOPED — no D-risk remains anywhere**: Route A (the k-th-power
   pretentious-triangle elimination; H1 via the landed dist_one_floor_pow at
   b = kt through the landed pretDist_triangle; H2 via the landed
   zero_free_region_all' + Siegel branch) = **C, 1200–1800 ln, no χ-power
   region needed**. The θ-numerals corrected: the generic-corner threshold is
   θ < 0.9346 (0.976 false as stated); the door's ACTUAL threshold is any
   fixed θ < 1 via S10a's headroom (cite S10a, not 1/125). S10b's
   "triple-exp arm" is NOT dead — it is Route A's price (soften mrt_extract §0).
5. **The shared critical-path debt named**: the χ-Euler bridge inherits the
   BLOCKED q=1 λ-Euler bridge (the flags "single residual") — on the ladder
   explicitly, shared with S5.
6. **Numerals**: "5.175·log q" has no corpus witness (actual: ≈3.7–4.7·log q
   + an ineffective additive constant — design-grade flag); W at the door =
   (loglog x)⁵ not (logloglog x)⁵ (overservice ONE iterated exponential);
   H₀door = exp(9·10³⁸) is implied-constant-free — a shape, not a pin; the
   Gauss-sum sentence at :92-94 replaced by the uniformity-cancellation
   mechanism (φ(q₀) cancels; the cost is Σ_b = q; the naive failure is the
   wrong per-χ input).

7. **(EULER-SCOPE, same night): the "shared blocked debt" item (#5 above) is
   VOID** — the q=1 λ-Euler bridge closed 2026-07-19 (flags supersession
   pointer banked); the χ-side re-prices to ~165–230 ln all-B (mathlib's
   χ log-Euler form + the landed general-g truncation). **NOTHING on the
   ladder is blocked.**
