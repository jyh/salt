# THE BAND WAVE + THE NIGHT CAMPAIGN — the jason-seat handoff (2026-07-28 evening)

*Written at the silicon-acct→jason changeover. The fresh session's FIRST
ACT: dispatch the BAND-WAVE executor below (Opus, general-purpose,
verbatim), then run the night sequence. Base at freeze: 1bab8e3 + this
doc's commit. BAND-SCOPE's full census is the session record; its
verdict is distilled here — the wave is ONE flight, no new analysis.*

## THE BAND-WAVE BRIEF (dispatch verbatim)

You are the BAND-WAVE executor in /Users/jyh/projects/claude/salt
(Lean 4 + mathlib, branch main). IRON RULES: no git; ~3 attempts per
obstacle then STOP+report; no sorry/native_decide/new axioms. Your
in-place authorizations: the FrameWitness/M4MeanSq/CofactorSupplier/
CaseAWide wave artifacts per the point-vs-band ruling (flags 1bab8e3);
new content additive (Salt/MR/M4Band.lean where it doesn't belong
in-file).

READ FIRST: docs/blueprints/flags.md's last entry (the wall) + this
brief's edit list. THE DESIGN: the capstone chain re-cut from the
POINT ramI(H83, P, P) to the BAND ramI(H83, P, Q) with **Q PINNED :=
⌊Q83 X⌋₊** (the unique corner where C4's h-ceiling charge
30/loglog X vanishes while the tail grade is minimal — do NOT leave
Q free). The chain beneath is (P,Q)-general; the socket
(m4_supplier_complete) is already band-stated; witEP2 and its gate DO
NOT MOVE (Q-independent).

THE ORDERED EDITS:
W-1 the ramQbase sandwich (FrameWitness, replaces the §2 singleton
block): ramQbase_ge_bot (le_max_left); ramQbase_le_top (j ≤ ⌊H log Q⌋
⇒ ⌈e^{j/H}⌉₊ ≤ Q); ramI_index_ge (the C1 band form). ~15 ln.
W-2 the band h-ceiling (FrameWitness §3): h_ceiling_gate_band —
log h + 30·(log X/loglog X) ≤ log X → log Q ≤ log X/loglog X →
30·log Q ≤ log(2(X/h)); c4_at_height_band through ramQbase_le_top;
log Q ≤ log X/loglog X from Q ≤ Q83 (the m4_tail_gate_at_pins step).
~20 ln.
W-3 the FrameWitness band re-index (:298–971, ~250 ln touched, zero
new content): tlBlockGates34_at_witness via the sandwich — C2/C5/C6
transport UP from P (C6 is monotone in the base — the landed binder
at P suffices); C3/C4 transport DOWN from Q; everything else P P →
P Q. blocks/thin/Tstar2_box/row_ladder/err_at_witness_mr (its
supplier E_priced_mr_row_scale is ALREADY P Q — a binder rename)/
a2Frame3_witness (12 slots).
C-1 the socket shift decoupling (CofactorSupplier:222, CaseAWide:746/
807): P's double duty (band-bottom vs the shift in doorCofactor0)
split — fresh binder Ps with 1 ≤ Ps; conclusion at doorCofactor0 χ
Pseq Qseq J Ps; plus doorCofactor0_at_one : doorCofactor0 … 1 =
memSCoeff Pseq Qseq J (liouChi χ) (3 ln — one_mul + the if-algebra).
Instantiate the band at Ps := 1. ~10 ln.
C-2 the band pair law (new, ~50 ln — the UPWARD mirror of
M4Residue:145–225, same proofs with < flipped):
blockPrimeDivs_eq_empty_of_large → blockOmega_shift_up →
memS_shift_up → indicator_mul_shift_up →
**doorChiCoeff_seamCoefW_band** : (∀ j ∈ Icc 1 2, calQK … j < P) →
SeamCoefW Xd P Q (winCut Xd (doorChiCoeff χ M)) (doorChiCoeff χ M)
(liouChi χ) — b IS the datum, cf = λχ̄, no coprimality, no m = 1
exception; the band gate from SeamCalibrationK.ladder_below_stationK
(:465) + P83 ≤ P (the hJdef gate is already a capstone binder).
⚠ Do NOT generalize M4ErrRewire's doorDatum/doorCofactor (:514/:521/
:563) — the phased pair is genuinely point-only (e(αpm) does not
factor at varying p); the band sibling lives at the UN-PHASED
doorChiCoeff per the forced order.
C-3 the capstone band re-cut (M4MeanSq:482 + :770, ~120 ln touched):
Q enters; hPhigh → hQhigh : (Q:ℝ) ≤ Q83 X; the 9 ramI P P → P Q; new
binders hPQ, hQbot : 0 < Q, hhceilBand, hdomBand (the Ramaré-band
error-domination gate — (√Xd+1)·∏_{p∈band}(1+3/p) ≤ Xd·(log P/log Q),
trivially clear at the band, carried named), the Mtail threshold
(C-4's). Downstream is le_rfl.
C-4 the band tail supply (M4RowSupply §4, ~60 ln): Mtail := C·r/Xd +
1/Xd² at P := ⌈P83⌉₊, Q := ⌊Q83⌋₊; compose m4_tail_mass_at_band +
m4_tail_gate_at_pins + m4_tail_grade_at_pins into 12·EP2 ≤
(log X)^{−θ₂₉₃+ε} under **THE ONE NEW NAMED THRESHOLD:
1344·C·loglog X ≤ (log X)^ε** (the exact constant — the 4/3 bump
makes it 672·C·r doubled into the gate; ⟹ loglog X ≳ 3.3·10⁴,
regime territory, same genre as the landed 6412.6/9.1e3 gates),
instantiated INSIDE blockfree_sum_le's ∃C scope (the K6 pattern).

BUILD: full lake build RC 0, no new warnings; axiom-check the
headliners via scratchpad scratch (delete); census in Salt/MR/All.lean.
TRAPS: the four log scales; Q pinned not free; the un-phased pin;
liouChi/liouvilleC; strict gates; conclusion byte-identity where the
ruling doesn't move statements; maxHeartbeats as the originals.
Class B/C, ~550–800 ln net. REPORT: per-edit verdicts, the threshold's
final bytes, build RC, axioms, drifts, deviations.

## THE NIGHT SEQUENCE (after the band wave lands + its ceremony)

1. **W2-DOOR**: the door instantiation at R1's dyadic family — the
   capstone (band form) instantiated at (length 2^j ≤ H, scale
   X_{i+1}+s, s ≤ H) per M4Maximal's M4ChiDyadicRowMeanSq target;
   the winCut bridge; the socket at Ps := 1; the pair law C-2;
   the tail C-4 → **M4ChiDyadicRowMeanSq discharged**.
2. **THE ADAPTERS**: R2's M4CoprimeBlockMeanSq (interval/length-
   general) from the capstone family (the capstone is scale-general —
   more instances, no new analysis) + the slack-4 supplier fix (one
   more dropped index in the sum_Ioc_drop_top consumer).
3. **THE COLLAPSE**: m4_wave_closed_of_dyadicRow's chain fires with
   R1+R2+R3 ALL closed → the final theorem at the register =
   regime-absorbable + witnessed ONLY. Bank + ceremony + Hero chime.
4. **THE S11 SPINE COMPOSE**: m4JointThr (M4Quality) + the new
   thresholds (the 3.3e4 tail gate, the 9.1e3 CASE-A gate, the harc
   gate 2·arcDen ≤ H) joined into ONE g-arm demand; instantiated at
   the g-slot of log_chowla_two_budget_head_g (landed, the REGIME-CUT
   lever; the Bfun/cffK constants per M4Quality's m4QualityB); the
   M4 arm's MRTUniformityXi supply = m4_exit_socket's hbd from the
   closed wave → **the M4/S9 road joined to the door**. This is the
   road's final act — take it carefully, ceremony it loudly.
5. Close-of-board: memory + seat sync + the morning report.

## Register riders (if the night has room)
- the lossy m4_wave_exit_sup retirement decision; the vacuous
  hUG34_unconditional_nocap3 retire-or-keep; the ring_nf sweep
  (DHExtract:118); VT-7; the fresh D2 run at the post-wave HEAD
  (needs the Captain's console — morning item).
