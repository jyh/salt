# THE INTERFACE FREEZE v1 — 2026-07-31 night
## The wall, fully decomposed; the one road out; the claims for the refuters

*Author: the maestro (Fable), from the four scope reports
(iface-scope-reports-0731.md, verbatim; their probes in
scratchpad/iface/). Status: FROZEN FOR REFUTATION — nothing fires
until the refuter pass and the Captain's council v3.*

## A. The wall, in its final decomposed form (all kernel-backed)

1. **THE CANCELLATION (S1, P1)**: the height-3 demand is ONE summand —
   κ = H/(log H·logloglog H) in budget_facts' fact (iv) — and in
   κ/gcap the H and log H cancel EXACTLY (gcap = ε⁶H/(36·log4·log H)
   − log2), leaving 576·log4/(ε⁶β²) ≤ logloglog H, forced by the
   frozen contract itself (P2). Every other budget fact needs only
   height 1 (log H ≥ 2.2·10³¹ — trivially inside every ceiling, P7).
   All constant repair is dead: the sharpest conceivable chain still
   demands logloglog H ≥ 4·10⁷ (P6).
2. **THE WINDOW FLOOR (S2)**: the tower's exponent 3.7436 is an
   artifact (the multiplier choice; a k = 2 tower gives an ADDITIVE
   window) — but the design-independent floor is
   λ₊ − λ₋ ≥ 0.9·logloglog H₋ (probe_window_floor). The tower and
   the budget are ONE wall: the width is proportional to the very
   quantity the budget inflates.
3. **THE REGISTER CEILING (S3)**: honest maxima λ₋ ≤ 20048 (register)
   / 13381 (with the cap); the frame ceiling SCALES with log₂M
   (K ≤ 1.79·10⁸·(L+1) — the corpus's 1.7e8 is the M = 1 pin); the
   flags' "6.24 in the triple log" was an under-read (honest ≈ 9.9);
   AND the ceiling is ONE definition — Adoor M := 2³⁶·(log₂M+1),
   re-cuttable to polynomial-in-M.
4. **THE X-COUPLING (S4)**: of the 37 cap-gate fields exactly ONE
   reads H (q_arcDen, free); the whole coupling is SocketBase's
   x-scale line against hPHheadroom, whose exponential enters at the
   ω factor of 8·P_H²·ω ≤ x; the cap is the chain's only x-ceiling;
   the collision closes without the tower (K ≥ 1.44·e^{budgetX});
   the S16 decoupling buys exactly one exponential.

## B. THE ROAD (the design bet, stated for refutation)

**D1 — THE BENNETT RE-PROOF (the crown; C/D-tier, FBridge).**
Replace the Hoeffding range-square (fBridge_varTerm's ((hi−lo)/2)² =
(H/p+1)²) by a Bennett/Bernstein variance bound. S1: g then gains a
full log H, the κ-cancellation breaks, the budget charges in the
SINGLE log — where the demand (2.2·10³¹-genre) sits inside the
register's window (e^{513} ≈ 10²²³) with ~190 orders of room.
budgetFloor collapses from height 3 to height 1.

**CLAIM 1 (for the refuters)**: with D1 landed, λ₋ ≈ 72–110-genre
suffices for every budget fact, and the S2 window floor
(0.9·logloglog H₋ ≈ 4) fits INSIDE the register's landed 7.1448
window law — faces 1+2 close SIMULTANEOUSLY with no tower re-cut
(the shallow tower is optional insurance, not required).

**CLAIM 2**: at λ₋ ≈ 110, λ₊ ≈ λ₋ + 4-genre (post-D1 the tower can
stay as landed? NO — the landed tower still forces λ₊ ≥ λ₋^{3.74};
the k = 2 re-cut IS required unless λ₊ ≈ λ₋^{3.74} ≈ 10⁷·⁶ stays
inside the register: anchor caps λ₊ ≤ 2.79e8·(L+1) — at L+1 ≈ 155
that is 4.3e10 ≥ 10⁷·⁶ ✓. THE REFUTERS DECIDE which branch is
cheaper: (a) tower as landed + the register at λ₊ ≈ 4e7 (the cap
face then needs K ≈ e^{λ₊}?? — NO: that face is the S4 coupling,
see CLAIM 3); (b) the k = 2 shallow tower re-cut (~Regime/Tower/
TowerExport twins) + additive window.

**CLAIM 3 (the residual face)**: post-D1, the S4 x-coupling still
demands K ≥ 1.44·e^{budgetX_height1}? — NO: with the κ-summand
gone, budgetFloor ≈ e^{4X} with X ≈ 10³¹ i.e. log Hlo ≈ 4·10³¹,
λ₋ ≈ 73; the socket floor is loglog X_d ≥ e^{λ₊}/2; at branch (a)
λ₊ ≈ 4e7 → e^{λ₊} — ASTRONOMICAL, dead; at branch (b) λ₊ ≈ 77 →
e^{77} ≈ 3e33 → K ≈ 4e33 vs frame 1.79e8·(L+1) ≈ 2e10 — STILL DEAD
BY ~23 ORDERS. ⟹ the S4/S3 face requires its OWN repair even
post-D1: either the Adoor re-cut (S3: polynomial-in-M — but the
maestro's hand-walk suggests c/(1+c) < 1 makes it asymptotically
short; REFUTE OR CONFIRM) or the ω-factor repair in hPHheadroom
(S4's entropy_residueWindow_ge re-proof) or the L2-band-pair-style
per-block reprice of the CAP's own demand. THE REFUTERS MUST SIZE
THIS FACE HONESTLY — it may be the true residual mountain.

## C. The refuter mandates

R1 (CLAIM-JOINT): the full post-D1 working point at symbolic
numerals — all four faces + the eleven register lines + the cap
chain, jointly, at branch (a) and (b); kernel-probe the decisive
inequalities; deliver the honest verdict: which faces close, which
remain, at what λ₋/λ₊/M/K.
R2 (D1-SHAPE): the Bennett re-proof's actual mathematics — read
FBridge's fBridge_varTerm/fBridge_var_le_sharp and the Transport
chain; state the variance the Bennett form needs (the TRUE variance
of the bridge variable vs the range-square), whether the corpus's
own objects supply it, the honest est (ln/class/risk), and whether
the ε-powers improve too (ε⁶ → ε⁴?).
R3 (THE RESIDUAL FACE): CLAIM 3's arithmetic adversarially — the
Adoor-re-cut asymptotics, the ω-factor's true role, and any FOURTH
route (the cap demand repriced per-block like L2-PROD?); if the
face is real post-D1, price the three routes honestly.

## D. What does NOT move (the fences)

The frozen statements of budget_facts (any change is
statement-tier, the Captain's); the landed tower laws (twins only);
the register's landed lines (S15Sel''-genre re-cuts are selection-
tier, cheap); the summit protocol (nothing announces).
