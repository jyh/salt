# SALTBENCH — the design sitting (2026-07-25, Saturday morning council)
## JYH + maestro; all three decisions RATIFIED ("I like all 3 Ds")

*Builds on the 7/21 architecture (roadmap.md Road 6 + the council-brief-tue21
notes): the four seat-mirroring tiers, the honesty axis, the pipeline-not-
corpus leakage model, the own-thin-harness lane discipline, the
build-after-flagship sequencing. This sitting sharpens with the week's data
and pins three decisions.*

## THE WEEK'S SHARPENINGS (adopted)

1. **Tier-S is TWO-SIDED** (JYH's windmill/giant duality, coined 7/25):
   - *Windmill arm*: dissolve the fake giant (ground truth: the 11+ ledgered
     windmills — obstructions that dissolved on contact, each with the
     dissolution recorded).
   - *Giant arm*: certify the real giant — refuse a wrong dissolution
     (ground truth: the R2.4 case — "GS 7.1 not needed" ruled dissolved,
     later retracted; the benchmark asks whether a scoper re-derives the
     load-bearing factor or rubber-stamps the old verdict).
   - Rationale: one-sided measurement rewards overconfident dissolvers; the
     giant arm punishes exactly that failure mode.
2. **Tier-V, the fifth family — verification cells** (the Jacobian/DGG
   pattern): given a claimed artifact (counterexample, certificate,
   transcript), produce the kernel-checked verdict INCLUDING statement
   fidelity against the primary source. Worked cells already in hand:
   jacobian-verify; DINITZ-VERIFY (in flight 7/25); the graded
   transcription-fidelity tasks (the h^{δ/25} and (X/h₁)/T defects — byte-
   level ground truth; the pdftotext trap as an instrument).
3. **The honesty axis gains its exemplar corpus**: the DGG transcript
   (three honest refusals with no-go production before the find) = the
   positive pole; the week's three vacuity catches (annHead's hsplit;
   ball_leg_of_sup; lemma12's window row) = the subtle-dishonesty probes —
   kernel-valid-but-vacuous is the shape the axis must detect.

## THE THREE DECISIONS (RATIFIED)

- **D-1 — the wave boundary**: the flagship paper may describe the method
  and name theorems freely; **pre-state commits and node briefs of
  designated bench waves stay private until their release**; the
  bench-reserved history ranges are DESIGNATED BEFORE THE PAPER SHIPS.
  (This instantiates the fleet map's "salt public later behind a hard gate:
  history purge + the SaltBench wave boundary.")
  → NEXT STEP (maestro drafts, JYH ratifies): the reservation list —
  candidate first waves: the L14 five-stone day (7/24), the Route-A arc,
  the seam campaign (freeze+refuters+wave), a windmill/giant Tier-S set,
  and 3–5 Tier-E replay nodes per the 7/21 starred rec. Draft due before
  the paper's arXiv submission.
- **D-2 — the release parameter**: **N = 6 months, rolling** (the
  FrontierMath pattern; the pipeline is fed by ongoing campaigns).
- **D-3 — the family structure**: **five tiers** — E (executor replay),
  S (scoper, two-sided), D (refuter), F (maestro/freeze authorship),
  V (verification) — with the honesty axis cross-cutting all five.

## Standing (unchanged from 7/21)

Lane: SaltBench = personal lane; its harness is its OWN, reimplementing
only field-standard methodology (pre-registration, memory-reset
counterfactuals, mutation-tested oracles, admission probes); no outside-lane
code ever; Tier-E cells stay salt-side always; the outside benchmark and
SaltBench are siblings that cite each other once public. Judge = the
kernel (lake build + #print axioms + statement-hash), no autorater.
Sequencing: BUILD waits for the flagship arXiv + the outside window;
the design (this doc) is banked; the reservation list (D-1) is the one
pre-arXiv obligation.

## ⟦D-1 LIST RATIFIED + THE SILENCE RULE (JYH, 2026-07-25 11:11 PDT)⟧

The wave-1 reservation list ratified as drawn. **THE SILENCE RULE (JYH's
prudence, maestro concurring and extending): NO public mention of SaltBench
— name, design, or existence — until (a) the flagship's publication approval
lands AND (b) JYH decides whether SaltBench's eval-adjacency to outside-lane
territory warrants its own the copyright-waiver filing/clearance BEFORE any announcement.** The
sequencing already held the BUILD for the arXiv + the 6-week window; the
silence rule extends that to the NAME. Verified clean: main.tex, the
dinitz-verify artifacts, and the facts sheet carry zero mentions. The
design + reservation docs live only in the private repo + the seat.
