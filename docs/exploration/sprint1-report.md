# THE SPRINT-1 REPORT (FINAL)

*Registered 2026-07-15 (commit 7ddeb491665ac2cc82055f0552f467587ee1c494);
closed 2026-07-16 with chen_goldbach kernel-checked (d68c855). This
report discharges the registration's reporting clause: all outcomes,
all costs, nothing omitted for being unflattering. Amendments A1/A2
and the Q1 checkpoint extension were ratified in-ledger; the full
audit trail is docs/exploration/pilot.md.*

## 1. The outcome table

| Q | Registered question | Outcome | Cost (exec. tokens) |
|---|---|---|---|
| Q1 | Chen-2: the reuse coefficient | **chen_goldbach KERNEL-CHECKED** (the frozen statement byte-exact); the coefficient MEASURED: **5.3×** the 2.9M estimate, anatomy documented | ≈ 15.5M, ~50 landings |
| Q2 | The M_k atlas | **least_k_theorem**: k = 2,3,4 closed (M₄ < 2 UNCONDITIONAL — four_bar proved), M₅ > 2 by the ℚ certificate; crossover ∈ (2.0025, 2.0118]; Dirichlet bridge = registered debt | ≈ 1.9M (incl. the 6-node assembly) |
| Q3 | GEH_min | The interface extraction: gaps_le_twelve_of_hasLevel (HasLevel(3999/4000), one-line diff from the landed chain); the GEH route's obstruction map recorded | ≈ 190k |
| Q4 | The windowed-BV variant | Stated standalone; the literature verdict: the conjunction is UNRECORDED | ≈ 136k |
| Q5 | The class boundary | (a) the constrained no-go + cs_bound_ignores_constraint (the obstruction as a theorem) + the c(δ)→2 map — the full form identified as Tao's open problem; (b) twin_razor_deficit (GAP-U/GAP-E named) + deficit_floor_of_certs + heavy_semiprime_obstruction + **alpha_half_optimal** (½ exactly optimal); Axis A = debt | ≈ 800k |
| Q6a | The parity wall | parity_wall + no_parity_beating_certificate + rosser_floor_undershoot (unconditional core; the budget conditional on MmuRate = registered debt, the Rb-4 obstruction named exactly) | ≈ 1.9M (incl. 317k of route-killing recons) |
| Q6b | The door | TwinB_min stated (the gate-corrected Λ-weighted main term); **twinB_min_implies_twins PROVEN**; **wall_or_door** — the dichotomy | ≈ 430k |

Wall-clock: the six questions closed in ~8.5 hours on day 1; Chen-2
ran ~30 hours total. All work Opus-executed except two ratified
Fable forks at the Chen-2 close.

## 2. The headline structural finding

**The razor's dimensionless ledger is a property of the method, not
the problem.** Every certified constant of Chen's argument — the
main value 9779/10000, the switch bound 243/100, c̄ with its
600-panel certificate, the 1/100 margin — is provably independent of
whether the target is twins or Goldbach: the problem enters only
through the normalization X_W = totalMass·W, and the singular series
rides inside W where it cancels in every consumed ratio. The formal
form is as strong as the language permits: the twin and Goldbach
totalMass are **definitionally identical (`rfl`)**. One numeric
certification, run once, proved both of Chen's theorems. This is the
proof-as-object claim with a checkable exhibit: the corpus factored
a classical proof into its problem-invariant and problem-covariant
parts, and the factorization is now a theorem rather than an
aesthetic impression.

## 3. The reuse coefficient, honestly

The registered quantity. The census predicted 63% verbatim / 35%
mirror / 1–2% re-derive; the landing cost 5.3× the census-based
estimate. The anatomy:

- **The verbatim layer held perfectly** (the numeric certificates,
  the sieve values, the BV engine, the abstract razor — zero
  re-certification, exactly as the day-one gate proved).
- **The mirror layer ran BELOW estimate** (waves W0–W2: est 960k,
  actual 787k, all first-attempt — the reflection often SIMPLIFIED:
  no equidistribution crumb in the count seam, divisor_cases
  dissolved, the odd-d casework vanished).
- **The overrun is entirely the scale-coupled machinery**: the
  bottom-half product window (the reflection's one structural
  novelty) collided with hidden global-window dependencies in the
  twin's bilinear pricing chain — the cutoff-difference encoding,
  the dyadic interval, the D0-window, the level cap, the annulus
  re-absorption. Five structural tears, each invisible at signature
  level, each surfacing only at instantiation. The band chain
  re-derived at ~4× its single-node budget.

The measured law: **interfaces mirror cheaply; mechanisms bound to a
window SHAPE re-derive at 2–4×, and no declaration-level census sees
shape-binding in advance.** This is itself the transferable finding.

## 4. The catch ledger delta

Sprint catches: **6 designer-tier catches** (the executors/gates
refuting HOUSE designs: the Q6b main-term log-off; the DSplit
head-cap/conductor conflation; the count·max absorption; the M-side
tail bound; plus the two pre-execution gate catches at Q6a/N4-ASM)
and **1 house-refutes-executor** (the y-floor arithmetic overturning
a "provably fails" flag — later formalized and kernel-checked). The
audit is bidirectional and settled by arithmetic, never authority.
Zero wrong proofs merged; the kernel discovered zero defects (the
sprint-1 pattern: discovery lives at instantiation, confirmation at
the kernel).

## 5. Method findings (codified during the sprint)

1. **METHODS III.3″ — witness the slots at the operating point**
   (user-ratified mid-sprint): every rate/budget-shaped hypothesis
   slot freezes WITH a numerically-evaluated worst-case instance.
   The three escapes that motivated it each cost a node-wave; the
   check costs a few thousand tokens. After adoption: five
   consecutive first-attempt landings on the arc's hardest nodes.
2. **The Zeno iteration** (named, diagnosed, fixed): a decomposition
   whose case space is discovered branch-by-branch instead of
   enumerated at the freeze regenerates a "one more supplier"
   residual at every landing. Fix: case-space enumeration as a
   freeze artifact + the gate checking enumeration COMPLETENESS
   (now a sprint-2 registration clause).
3. **The fork experiment**: two Fable forks (full-context, ratified)
   closed in ~2 hours what projected as 8+ briefed nodes, resolving
   in-flight five would-be flag-return cycles (the puncture catch,
   the Kc unification, the Even-N corner, the window swap, the
   boundary indicator trick). The datum: on integration-shaped
   tails, context beats briefing; on derivation-shaped work, briefed
   Opus executors remain the efficient tier.
4. **The one new mathematics of the arc**: the ten-prime Bertrand
   chain (the punctured modulus breaks the twin's two-step depth
   argument; ≤ 8 of ten chained Bertrand primes can divide N). Plus
   the arc's minor new tools: the min-side trivial bound via carrier
   symmetry, the annulus telescope, the y-floor engines.

## 6. Registered debts (carried forward, discharge conditions named)

| Debt | The exact obligation | Route |
|---|---|---|
| MmuRate (Q6a) | The effective local zero-density (Rb-4's statement) | **Sprint-2 Track A — in flight** |
| The Dirichlet bridge (Q2/A1) | ∫_{Δ₅} = the ℚ DInt identity + the 5-D real assembly | Post-sprint |
| Axis A (Q5b/A2) | The θ-crossover of the razor | Post-sprint |
| The λ-chain end-to-end (Q6a) | Mlambda_rate unconditional | Track A stretch (A2d) |

## 7. Compliance statement

All six registered questions + the checkpoint-ratified extension
report final outcomes above. The registration text was never edited;
amendments A1/A2 were appended transparently and user-ratified. No
frozen statement was weakened anywhere in the sprint (the honest-
weakening protocol was exercised 4 times on EXPLORATION files, each
recorded at its ceremony). The pilot remains disclosed-and-excluded.
Every keystone audits to [propext, Classical.choice, Quot.sound];
the full build (9016 jobs) was green at every one of ~60 ceremonies.

*— drafted by the Fable house session, 2026-07-16; RATIFIED FINAL
by JYH 2026-07-17 ("sprint-1 and sprint-2 reports lgtm").*
