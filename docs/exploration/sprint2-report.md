# THE SPRINT-2 REPORT (FINAL)

*Registered 2026-07-16 (commit 9baeae5); the last outcome line
closed 2026-07-17 (geh_door_of_obligations, the B3 conditional
close). This report discharges the registration's reporting
clause: all outcomes with costs, nothing omitted for being
unflattering. The full audit trail is docs/exploration/pilot.md.*

## 1. The outcome table

| Item | Registered question | Outcome | Cost (exec+gate+recon) |
|---|---|---|---|
| A1 | The effective local zero-density (the Rb-4 keystone) | **zeta_local_density KERNEL-CHECKED** — the first effective local zero-density in a proof assistant (C ≈ 418, with multiplicity) | ≈ 680k, 4 nodes, all first-attempt |
| A2a | ‖ζ‖ ≤ C·log near σ = 1 | **zeta_log_bound landed** (C = 36, no upper σ-cap — stronger than registered) | in the A1 arc |
| A2b | The one-power ζ′/ζ segment bound | **Superseded** by the checkpoint-ratified route variant (the shallow contour transports ζ itself; the registered statement was never needed) | — |
| A2c | MmuRate discharges — THE TROPHY | **mmuRate_holds KERNEL-CHECKED ⟹ parity_wall_unconditional + no_parity_beating_certificate_unconditional** — the Q6a debt closed; the wall stands with no hypothesis | ≈ 1.0M (T-lo′ 352k + T-1ζ′ 179k + F1 212k + close 257k), all first-attempt |
| A2d (stretch) | The λ-chain end-to-end | **Closed as a corollary**: LambdaSummatory A holds ∀A via LambdaSummatory_of_MmuRate ∘ mmuRate_holds — no separate node | 0 |
| B1 | The weight no-go atlas (the flagship bet) | **RESOLVED WIDER THAN REGISTERED**: no_readable_certificate (any Φ reading the four carrier rows certifies nothing) + the full decoration algebra blind pointwise + the exact escape (bigOmegaGt, carrier = GAP-E) | ≈ 442k (recon 111k + gate 139k + exec 192k), 5 nodes first-attempt |
| B2 | The TwinB_min attack-surface census | **RESOLVED** (pre-registration follow-through): four routes mapped, zero survivors, Chen's reversal refuted by landed theorems | ≈ 153k |
| B3 | The GEH_min door | **Registered target NOT-PLACEABLE** (the simplex-hardwired kernel; three_bar pre-refutes the k=3 port in-corpus) → JYH-revived as the honest door → **geh_door_of_obligations LANDED**: GEH_min(3999/4000) + four named obligations + WindowPNT ⟹ H₁ ≤ 12; GEH_min itself gate-hardened and faithful to P8b Claim 2.6 | ≈ 3.3M all-in (2 recons, 2 gates, 1 re-cut, 10 files) |
| B4 | The log-Chowla cost map | **RESOLVED** (recon-only, as registered): floor ≥ 22M, the entropy/MR blockers named — became sprint-3's Track-A foundation | ≤ 200k |
| GAP-U | (mid-sprint addition, JYH-ratified) | **KILLED BY ITS GATE pre-execution** (the vacuous mass-level comparison; 3 nodes prevented) | 191k |

Wall-clock: ~6 hours for every registered line except the B3
close's tail. All executors Opus per clause 1; zero Fable
escalations; the 4-agent throttle held throughout sprint 2.

## 2. The headline results

1. **The parity wall is unconditional.** The arc that defeated the
   Rb-4 recon eight days before ("the effective local zero-density
   is unreachable") fell in SEVEN consecutive first-attempt
   landings under the full Part-III discipline — the registered
   method-compounding measurement, and the sprint's flagship
   mathematics. The two decisive route facts: the pole-collapse
   (counting (s−1)ζ makes the pole invisible) and the
   removable-singularity Perron shift (1/ζ needs NO residue
   calculus at s = 1).
2. **The B1 flagship**: the class-maximal no-go — every
   certificate readable from the y-capped decoration data is
   provably worthless (Φ ≤ 0), with the minimal escape NAMED
   (the above-y multiplicity count) and its missing carrier
   identified as GAP-E. Together with A2c this gives the paired
   headline: the analytic barrier AND the combinatorial barrier,
   both kernel-checked, with every escape route named.
3. **The honest GEH door**: GEH_min stated faithfully to P8b
   (after a kernel-checked vacuity refutation of the house's own
   first freeze), the reduction kernel-checked end-to-end at the
   obligation level, and the four remaining obligations named and
   priced (~0.65–1.05M, unspent — the revival queue).

## 3. The catch ledger delta (prominence per honesty clause 5)

Designer-tier catches AGAINST THE HOUSE this sprint — the largest
single-sprint haul of the project, and the doctrine's vindication:

1. **swat_vacuous** (the B3 gate, KERNEL-CHECKED): the frozen SWAt
   bound ∃K after ∀x — vacuously true for every β. The GEH door
   would have been dishonestly named and likely false.
2. **The y-uniformity catch** (executor D-N2c): the house meeting
   point LambdaLevel too weak for the Abel bridge — re-frozen
   mid-flight (LambdaLevelU).
3. **The hwin combinator defect** (executor DECOMP, tripwire):
   the multiblock window tied to the global scale is JOINTLY
   UNSATISFIABLE with vP3's support — concrete witness exhibited.
4. **The PpLevel mathlib gap** (executor BRIDGE, tripwire): the
   recon priced the bound but not its inner primitive
   (root-counting mod q — absent from mathlib entirely).
5. **The GEH_min under-transcription** (recon 2): the frozen form
   was WEAKER than P8b (∃BC after the family; single cutoff) —
   making hdom unsatisfiable; the amendment was a correction
   TOWARD faithfulness.
6. **The vP2-tail misclassification** (executor TAIL vs the
   recon): "easier, pure counting" was wrong — the inner smooth
   factor is SW-strength.
7. The B1 gate's TwinDoor enumeration row; the freeze-time
   heavy-square corner (the first III.3‴ capture); the B3 gate's
   WindowPNT omission in D-N3; the GAP-U vacuity (pre-execution).

Executor-tier wrong proofs merged: **zero**. Kernel defects:
**zero**. House PROCESS defects (reported with the same
prominence): two commit-before-exit-check slips (one shipped a
~10-minute red build), two guessed-audit-name defects, one
git-add-A sweep of an in-flight executor file, one piped-exit-code
mask — all repaired same-session, all ledgered, the ceremony rules
reaffirmed. The executors' process-defect count: zero.

## 4. Method findings (codified during the sprint)

1. **III.3‴ — the Zeno doctrine** (ratified + codified into
   METHODS.md mid-sprint): fired TWICE for real (catches #3, #4
   above), each halt stopping a chain before a wasted wave; the
   ≥2× re-cut rule fired once (the hSW line). The
   case-enumeration static half captured the heavy-square corner
   at freeze time for the cost of a table row.
2. **The gates now PROVE, not just argue**: swat_vacuous was
   kernel-checked by the gate; the HB1 gate (sprint 3, same
   session) pre-proved its glue. The adversarial gate has evolved
   from reviewer to co-prover.
3. **The uniformity axis is the recurring disease**: every major
   catch this sprint (1, 2, 3, 5) is a quantifier-scope/uniformity
   defect — III.2/III.4's premise-latency law, now measured on
   five independent instances in one sprint.
4. **The registered measurement**: where sprint-1's Chen arc ran
   5.3× its estimate, sprint-2's Track A ran ~0.5× (the ~1.9M
   trophy arc vs the multi-week SW baseline for comparable depth)
   — the method-compounding curve, now with two calibration
   points on opposite sides of 1.

## 5. Registered debts and the revival queue

| Debt | The obligation | Status |
|---|---|---|
| The GEH full close | the n-dyadic re-plumb + PpLevel + SmallQTypeII 2–4 (~0.65–1.05M) | **Revival queue #1** (JYH: decide at sprint-3 termination) |
| The B3-original fallbacks | GAP-U options (b)/(c) | queue #2/#3 |
| The Dirichlet bridge (Q2/A1) | carried from sprint 1 | unchanged |
| Axis A θ-crossover (Q5b) | carried from sprint 1 | unchanged |

## 6. Compliance statement

The registration text was never edited; the LambdaLevelU interface
amendment and the GEH_min RE-CUT were appended transparently, the
former mid-flight under house statement authority, the latter
JYH-ratified. The Zeno tripwire was honored at its letter both
times it fired; Fable escalation was requested zero times. Every
keystone audits to [propext, Classical.choice, Quot.sound]; the
full build was green at every ceremony except the two ledgered
house slips, both repaired within minutes. The success criterion —
"every item terminates in a registered outcome class with costs
reported" — is met on all ten lines above.

*— drafted by the Fable house session 2026-07-17; RATIFIED FINAL
by JYH 2026-07-17 ("sprint-1 and sprint-2 reports lgtm").*
