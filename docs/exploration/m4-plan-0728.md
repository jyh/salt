# THE M4 WAVE PLAN — draft for the 0728 morning council

*Drafted overnight 7/27→28 from the M4-JOIN per-row supply join; REGIME-
CHECK and TLGATES-SCOPE folded; REFUTER PASS COMPLETE (CAPFREE-REF=HOLD,
REPAIR-REF/REGIME-REF=REPAIR-THEN-FIRE — the eight kills K1–K8 + the D3
save are banked in flags.md). **READ THE v2 SECTION AT THE BOTTOM FIRST —
it supersedes D1, D3, CFB, R1/R2's scope, and the wave sequencing.** The
clean council surface is docs/exploration/council-0728.md.*

## The headline

The freeze's ladder survives contact with the summit, re-priced
**~5150–8800** (was 3650–5800; the join's 4200–7080 plus the wave-0
repairs and the FRAME re-price), with ONE wave-redirecting discovery and
ONE consumability hole (wave 0, below):

**THE CAP-FREE NECESSITY.** The λ-datum is non-pretentious —
𝔻²(λχ̄, n^{it}; X) ≳ loglog X at every centre — so the summit's cap
branch is *unusable* for the M4 consumer: both of its landed suppliers
(`t0_band_supply`, `a2_station_supply_pointwise`) carry the same
`(1/16)loglog X` pretentiousness cap, false for λ everywhere. The wave
takes the **cap-free branch by necessity**: prove `CapFreeFloor (λχ̄) X′`
(CapFreeArm.lean:111) and enter through `a2Rows_of_capfree` +
`thm_a2'_of_rows`, bypassing `thm_a2'`'s cap glue entirely. This is
*cleaner* than the frozen picture — hStation, hCcc, the 800-threshold and
collisionGate never need supplying — at the cost of two new stones:

- **CFF** — the `CapFreeFloor` stone: 𝔻²(λχ̄, costwist v; X) >
  (1/32)loglog X + 25 uniformly on |v| ≤ X. This IS the large-|t| wall
  (decision 1).
- **CFB** — the cap-free T₀-band [C, 300–600]: mirror
  `band_sup_of_center`/`band_sup_supplied_Y` against CapFreeArm's
  non-pretentious arm, then the datum-blind `band_integral_of_sup`.

Also closed by the join: **U1** (floor-raise, not contradiction), **B₅**
(12 landed throughout), and **P-1** (S7 closed; the L-ladder no longer
gates M4-0). ⚠ REGIME-CHECK correction: `budget_head_grade_closed` has NO
free slot — the `∀ extraFloor` lives one level up
(`log_chowla_two_budget_head`, SpineFinal:751) and is *consumed* at
DoorFloor:251. U1's raise therefore needs the same re-cut as the D1a
regime constraint; they are ONE edit (see REGIME-CUT below).

## The four council decisions

**D1 — THE LARGE-|t| WALL (rules everything; rank it first).** The quality
floor must run over |t| ≤ X and the landed floors die there (k²-branch
short by ~1400 orders at W = (log H)^12; the 3-4-1 branch marginal —
exponent exactly 1; VK not in corpus/mathlib). Escapes:
  (a) **regime shape** — x ≥ exp exp(1300(log Hhi)^25).
      ⟦REGIME-CHECK: **CHEAP, ~150 ln, class B** — the REGIME-CUT stone.⟧
      Who-chooses-x resolved: WE do — `logChowla2Fails` is a fixed-scale
      Prop appearing only negated at the built R.x; no adversarial x, no
      cap on Hhi anywhere (regimeEnlargeX compiling IS the proof all
      x-constraints are one-sided-below). The lever is landed:
      `chowlaRegime_exists_param_head` (RegimeHead:271) takes an arbitrary
      g(Hhi,ω) ≤ x demand. The cut: swap SpineFinal:788 to the head
      builder at g := ω·⌈exp exp(1300(log Hhi)^25)⌉₊ (the gJoin anchor
      idiom, RegimeHead:214 — note gJoin's landed 8(log Hhi)^5 arm is
      ALSO insufficient; add a max arm, do NOT touch sieveW). Four items:
      the RegimeParam upstream twin (~50, verbatim relocation), the
      budget-head g-twin written IN SpineFinal.lean (spine_False_core_xi
      is `private` — trap 9, in-file or pay 20→330), the DoorFloor g-twin
      (~15), ledger. U1's Hlo-floor rides the same edit as a fifth max
      arm at SpineFinal:789 — acyclic (g reads only (Hhi,ω); enlargement
      moves only x), so U1 + D1a = ONE stone.
      NOTE: (a) clears only the constant-demand form; `CapFreeFloor`
      demands (1/32)loglog X, which no x ≥ f(Hhi) can supply. Backstop
      for the top sliver only, not a substitute for (b).
  (b) **the L-lower-at-height-t stone** [C, ~400] — extend
      `chi_Llower_341` past |t| ≤ 1 by swapping `LFunction_norm_le_level`'s
      growing bound for the 1-line |L(1+2it,χ²)| ≪ log(q(|t|+2)) via the
      LANDED Pólya–Vinogradov + Abel. Delivers (1/4)loglog X −
      (1/4)loglog(q(|t|+2)).
      ⚠ UNDER REFUTATION: the join claimed this beats CapFreeFloor for
      |t| ≤ X^{0.8}; the maestro's own check says the delivery at
      |t| = X^c is (1/4)log(1/c) = O(1) — NOT proportional — and the
      proportional regime is |t| ≤ exp((log X)^β), β < 7/8. If so, the
      uncovered sliver is HUGE (exp((log X)^{7/8}) < |t| ≤ X), the
      k²-branch cannot fill it against a (1/32)loglog X demand (it
      delivers ~10^{−1480}·loglog X), and CapFreeFloor over the full box
      needs either VK-strength input (a major port, NOT in corpus or
      mathlib) or a redesign of the arm's floor demand (Fable-tier).
      CAPFREE-REF is tasked with settling this arithmetic against the
      Lean bytes — the council must not rule D1 before its verdict.
  *Proposed ruling: take BOTH — (b) as a wave stone (it also removes the
  k² divisor everywhere), (a) as the regime backstop for the top sliver,
  contingent on REGIME-CHECK.*

**D2 — BRANCH POSTURE: cap-free only, or carry both?** *Proposed: cap-free
only.* The cap branch's suppliers are false for our datum; carrying it is
dead weight. The cap branch stays landed in the corpus for future
pretentious-datum consumers; the M4 wave never touches it.

**D3 — THE EVEN-χ ROUTE.** The residual, sharp: `L1LowerEffective` at real
primitive **even** χ; bites only in the Siegel-zero corner (|t| ≲ 1/log X;
elsewhere the landed zero-free regions cover). Without it M4-3's χ-sum
loses uniformity → one bad χ takes the trivial bound → the W-saving dies →
M4-7 cannot close. No per-character escape. Routes:
  - **Siegel completion** (proposed): the `∃c` shape is
    ineffective-friendly — Siegel's ineffectivity does NOT obstruct the
    statement (the pearl doctrine applies: effectivity stays a jewel,
    tracked, not a blocker). `siegel_L_one_exceptional` is half-landed;
    the ε-form's two named gaps: sharp near-line growth L ≪ log q +
    2^{ω(N)} = N^{o(1)}. Needs a price scoper before dispatch.
  - Hyperbola fallback: CONFIRMED DEAD at our pin (w = 12 ≫ 2).
  - Class-number: campaign-tier; register.
  *Proposed ruling: dispatch a SIEGEL-PRICE scoper with the wave; the
  even-χ stone rides wave 2 (M4-3/M4-7 don't consume it until close).*

**D4 — THE A2Frame BLOCKS ITEM.** ⟦TLGATES-SCOPE: **CONFLICT** — two
statement repairs BEFORE the wave (full record in flags.md).⟧
  - **R1 (critical): `thm_a2'` has NO MODELS as stated** — the
    ∀ j : ℕ binders at ThmA2Rows:627–628 are jointly contradictory
    (ramRbot → 0 vs 2 ≤ m₀ j); kernel-true, unconsumable. The trap-7
    thin-collision is real but second in line, same quantifier. Repair:
    re-cut five binder families to ∀ j ∈ ramI — 21 sites / 10 files, one
    sweep, MR-tower rebuild [B, 300–600]. The sole consumer only ever
    uses j ∈ ramI, so the narrowing is proof-transparent.
  - **R2: `A2Frame.box` unsatisfiable outright** (|t| = X endpoint; the
    ≤ X is load-bearing for CapFreeArm's pocket-centre geometry, so not
    a typo). Repair (a) widen to 3X — extends the D1 floor demand to
    |v| ≤ 3X, so it is ruled INSIDE D1 [C, 150–300]; or (b) shrink the
    window top [C, 200–400]. Proposed: (a).
  - Both repairs are frozen-statement changes: **Fable design block,
    JYH-gated (iron rule 1) — this is the council's sharpest item.**
  - The good news: TLBlockGates34 (17 conjuncts, not 16) IS satisfiable
    at door scale post-repair — witness table delivered (ramI pinned a
    singleton at P = Q; kk/Mt determined by B_j; ONE witness serves both
    frames). All scale floors astronomically clear (the sharpest, C4's
    loglog X ≥ 996.4, exceeds the stated 800 — subsumed by the ε-window's
    own 6412.6). The err assembly needs three new datum binders + the
    N = 2Xd pin + a NEW sharp p²-mass stone [err C, 250–450].
    FRAME total post-repair [C, 880–1610] — the 400–800 draft band was
    2× low.

## The wave table (the proposed dispatch order)

Wave 0 — THE STATEMENT REPAIRS (Fable design block, JYH-gated, BEFORE
everything that consumes thm_a2'):
| brief | content | class, band |
|---|---|---|
| R1 | the ∀ j : ℕ → ∀ j ∈ ramI re-cut, 21 sites / 10 files, one sweep + rebuild | B, 300–600 |
| R2 | the box widening to 3X (ruled inside D1; re-cuts CapFreeFloor's box + box_gate_le_X + no_pocket_of_floor) | C, 150–300 |

Wave 1 — independent openers, all dispatchable at fire (none consume
thm_a2', so they can run concurrently with wave 0):
| brief | content | class, band |
|---|---|---|
| M4-0 | window rewrite at `bigXiArcTight_twelve`; the phase re-basing lemma | B, 120–200 |
| M4-0′ | complex-weight `sum_Ioc_abel` copy + the drift kill (IBP) | C, 250–400 |
| M4-2 | residue split; the `blockOmega`/`MemS` dilation lemma (~120 ln new) | C, 400–650 |
| M4-3 | character expansion; the conjugation bridge ONCE, in `lamChi`'s orientation (trap 5) | C, 300–450 |
| CFB | the cap-free T₀-band | C, 300–600 |
| L-HT | the L-lower-at-height-t stone (D1b) | C, ~400 |
| REGIME-CUT | the budget-head g-twin (D1a + U1, one edit; in SpineFinal.lean per the `private` trap) | B, ~150 |

Wave 2 — after wave 1 + the D1/D3/D4 rulings:
| brief | content | class, band |
|---|---|---|
| CFF | the `CapFreeFloor (λχ̄)` stone (consumes L-HT + the D1a regime slot) | C, 300–500 est |
| M4-4 | c.o.v. + trivial cut + outer dyadic cover (NOT subsumed — thm_a2' is single-scale) | C, 350–550 |
| M4-5 | instantiate thm_a2'_of_rows at g := λχ̄ (~70 binders; the six side conditions land in the frozen shape) + trivial branch + C-S | C, 350–550 |
| FRAME | A2Frame ×2 assembly per TLGATES-SCOPE's witness table (`thin`+`blocks`+`box`+`ksGate`+`err` incl. the new p²-mass stone; gated on R1+R2) | C, 880–1610 |
| M4-6 | the quality supply wiring (χ-floor chain + the ~20-ln datum adapter 𝔻²(seamCoeff(ellLin g) 1 t₀, costwist v) = 𝔻²(g, n^{i(t₀+v)})) | C, 500–900 |
| EVEN-χ | per D3's ruling (Siegel completion, priced first) | ? |

Wave 3 — close:
| brief | content | class, band |
|---|---|---|
| M4-1 | the 1_𝒮 insert via `hsieve_of_engine` citation (window generality rides M4-8) | B/C, 200–350 |
| M4-7 | arithmetic close (`level1_term_door_decays` beats W^{5/2}) | C, 300–500 |
| M4-8 | door glue B3/B4/B6 + HS-6 half-open endpoint + log ω absorption at the 8C/δ gate | C, 550–850 |
| M4-9 | three-step exit (all suppliers landed; C_MRT arithmetic on the raised floor) | B, 180–280 |

## The trap sheet (every brief carries the relevant subset)

1. **The hMcap wall** — `t0_band_supply`/`a2_station_supply_pointwise`
   demand the (1/16)loglog X cap, FALSE for λ at every centre. Any
   executor who "supplies" one has erred. (The wave never enters the cap
   branch; these names are off-limits.)
2. **A2-4 is off-route for λ** — `M_window_bridge_inf`'s far slot is
   negative for non-pretentious data. Do not route M₀ through it.
3. **Two A2Frames** — (cq₁,T₀₁)/(cq₂,T₀₂) separate existentials; five
   binders stated twice; never identify them.
4. **hgRows₁ at C₁ vs hgRows₂ at Ccc** — different inequalities; one
   constant for both is unsound.
5. **The conjugation orientation** — mathlib's `sum_char_inv_mul_char_eq`
   emits `χ(a⁻¹)·χ(b)`; the floor chain speaks `lamChi χ` (χ̄-twisted).
   Fix the bridge once, in `lamChi`'s orientation, before M4-3 or M4-6
   land.
6. **Ccol's magnitude** — existential in `pocketSocket_of_row` vs the
   `exp exp(10⁶+75C)` threshold; cap-free posture makes this moot, but if
   D2 rules "both branches," the magnitude check is owed.
7. **thin/blocks joint satisfiability** — TLGATES-SCOPE's question; no
   dispatch of FRAME before its verdict.
8. **Three log-scales, one glyph** — loglog X (thresholds), loglog H = the
   demand scale (M4-6's constant ≈ 1.2·10⁴ at the raised floor), log W =
   12·loglog H. Every brief pins which scale at every numeral.
9. **The private-transplant tax** — grep `private` in any file you
   transplant from before pricing (the HOIST lesson: ~20 ln became 330).
10. **Half-open windows** — `hsieve_of_engine` on Ioc X (2X); the door
    window Ioc (x/ω) x; endpoint n = X uncovered (SieveGlue:77–80).

## Standing gates

- Law #244: executors NO GIT — maestro ceremonies every landing.
- Law #253: growing-quantity gates in-statement.
- The re-grep law: every brief's supply column re-grepped at dispatch
  (the census is a photograph).
- The V9c law: calP/calQK symbolic; door exponents behind defs.
- Refuter pass on THIS PLAN before the wave fires (verify-posture law).

---

# v2 — POST-REFUTATION (supersedes the marked items above)

## D1, restated honestly (the design fork)

The floor demand ((1/32)loglog X + 25, on |v| ≤ X — 3X after R2) splits
by character class and region. The COVERED region |t| ≤ exp(A(log X)^{7/8})
is the X^{o(1)} *sliver*; the rest is *the bulk* (K1 — the earlier
"top sliver" naming was inverted).

| class | sliver (small/medium |t|) | the bulk |
|---|---|---|
| χ₀ | trivial (2·loglog) | clears via chi_floor_of_order k=2 |
| real χ, odd | L1_lower_odd (landed, effective) | clears via chi_floor_of_order k=2 |
| real χ, even | **D3's Siegel corner** → the regime lever (SIEGEL-REGIME aloft) | clears via chi_floor_of_order k=2 |
| non-real χ | the pow-route (pretDistSq_pow_le k=2 + PV upper bound — CHEAPER than 3-4-1, same delivery) | **THE WALL — nothing landed or priceable clears** |

The bulk×non-real cell admits exactly three routes, all council-tier:
(A) the VK port [D-tier, the only full-box analytic route];
(B) a v-DEPENDENT floor shape (demand c·(loglog X − loglog(q(|v|+2)))
matching the delivery — requires re-grading the CASE-B pocket demand,
Fable design block);
(C) split the annulus at exp(A(log X)^{7/8}) and kill bulk pockets by a
different mechanism (far-field/energy arm — spine geometry, Fable design
block). Note Tstar2's transport excursion exp((log X)^{3/5}) stays INSIDE
the sliver (3/5 < 7/8), so only the annulus TOP is at issue.
**Proposed: a Fable design block on (B)/(C) before any CFF dispatch; (A)
is the fallback. The wave does NOT stall — waves 0–1 are independent.**

## The other superseding rulings

- **hMcap language (K2):** the datum adapter identity is exact and landed
  (pretDistSq_ellLin_eq; the twist translates frequency by t₀). But say
  "hMcap is UNSUPPLIABLE", never "false at every centre" — falsity in the
  even-real corner is Siegel-hard. Briefs pin the shifted variable
  (|t₀+v| vs |v|).
- **CFB (K3):** the T0Band mirror target does not exist (the 1/(1+|t|)
  weight IS the cap). Re-route per-frequency via center_halasz_supply_Y +
  crude 2T₀ integral; C₁' acquires (log X)^{1/30}; the in-statement gate
  **M₀ ≥ 0.1867·loglog X** (the 1/32 floor is 6× short; the band's
  (1/4)loglog X delivery clears). CFB re-priced [C, 500–900], sequenced
  AFTER the floor library and the R2 box ruling.
- **R1 (K4):** census corrected — ~116 occurrences / ≥29 signatures / 12
  files (add USetBalance; narrow TS_feed_of_thin + TSG_feed_of_thin's
  conclusions in lockstep; 3 call-site adapters). Proof-transparent at
  every leaf (verified). Re-band [B, 600–1000]. Design choice for the
  block: in-place narrow (repairs thm_a2' itself — no vacuous flagship
  theorem left standing; recommended) vs additive narrowed twins.
- **R2 (K5):** LIGHTER than billed — thm_a2'_of_rows never mentions
  A2Frame, so no frozen statement moves. Recommended: the additive mint
  (A2Frame3 box ≤ 3X + CapFreeFloor3 + a2Rows_of_capfree3 carrying the
  R1-narrowed binders); the landed cap chain untouched.
- **FRAME:** ONE frame suffices cap-free-only (the ×2 and trap 3 are
  dead weight); h-ceiling carried in-statement (log h + 30(log X)^{1−θ}
  ≤ log X); the Cq₁/cq₁ magnitudes must be read out of the supply
  proofs or the regime drawn inside their scope (K6) — the REGIME-CUT g
  must not be Cq-blind. Re-band [C, 600–1100] incl. err.
- **REGIME-CUT (K7):** fifth arm outermost-right (max extraFloor
  U1floor); duplicate ~33 ln into RegimeParam.lean (primed names), never
  relocate; two privates force in-file (spine_False_core_xi AND
  mutualInfo_window_comm'); cite SpineFinal:749–829 (RegimeHead:76 is
  stale); g := 2·ω·⌈…⌉₊. Numerology verified safe for ALL L (min
  L/log L = e). Band holds [B, ~150–200].
- **New supply lines (K8):** orderOf χ ≤ q (~5–8 ln, mathlib route — no
  landed witness); the err p²-mass stone is build-from-scratch (non-MR
  ramP2coeff has NO landed bound; fibre bound D ≤ log(2Xd)/log P);
  the ε-window's loglog X ≥ 6412.6 is the binding scale floor on BOTH
  branches (the 800 was never binding); pin exp(−M/e), (5e/2)·log W.
- **D3 (the save):** possibly FREE via the regime lever — B(Hhi) :=
  max of the X-independent per-χ constants over the FINITE {χ mod q ≤
  W(Hhi)}, absorbed by the head builder's arbitrary g. THE LAW (byte-
  confirmed twice): regime enlargement absorbs X-INDEPENDENT debits
  only. SIEGEL-REGIME verdict pending; if RETIRES-D3, the Siegel
  completion is unnecessary and D3 exits the campaign.
