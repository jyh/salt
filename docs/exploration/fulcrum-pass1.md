# THE FULCRUM — Pass 1 verdict (2026-07-18 22:10 PT; 11/11 agents; quality-angle survived 0/2, structure refuted 1/2)

## F_MIN

THE FULCRUM (quality angle, survived both refuters; frozen with repairs folded in). Parametric family — the machine consumes exactly ONE instance:

def FulcrumQualityMin (C : ℝ) : Prop :=
  ∀ Q : ℕ, ∃ (q : ℕ) (_ : NeZero q) (χ : DirichletCharacter ℂ q) (ρ : ℂ),
    Q < q ∧ χ.IsPrimitive ∧ χ ^ 2 = 1 ∧ χ ≠ 1 ∧
    LFunction χ ρ = 0 ∧ ‖(1 : ℂ) - ρ‖ * (C * Real.log q) ≤ 1

theorem fulcrum_quality : FulcrumQualityMin C⋆ → TwinPrimeConjecture
  where C⋆ := max C⁽¹⁾ (2/c₀); C⁽¹⁾ = exp exp{2A(𝔖C(α))⁻¹} is HB's effective Corollary-2 constant (source audit §1, p.223; UNFROZEN until WP2 witnesses + WP7 assembly land — F stays parametric, glue constraint 3), c₀ = 1/126848 from the landed zero_free_region_all (Salt/SW/ZeroFreeReal.lean:605-608, verified verbatim this session; the numeral is docstring-grade, the Lean form is ∃c₀ — an A-class extraction is owed before any numeral enters).

EXACTLY what is demanded: (zeros) ONE complex zero ρ per witness — NOT assumed real, NOT assumed < 1 or > 1/2, no maximality, multiplicity irrelevant; (quality) the norm ball ‖1−ρ‖ ≤ 1/(C⋆·log q) at ONE FIXED effective constant — first-power coupling, no quality → 0, no ∀c binder; (quantity) bare unboundedness ∀Q∃q>Q — no density, spacing, or growth law (source audit §7); (moduli) unrestricted beyond carrying a primitive quadratic χ ≠ 1 (which forces q ≥ 3 — mod 1, 2 carry only trivial characters — so IMSZ's `1 < q` binder is derivable, and forces the conductor shape q = 2^a·m, a ∈ {0,2,3}, m odd squarefree, which is what HB §6/(1.9) consumes; NOT literal cube-freeness — provably false at q = 8·squarefree, both refuters' repair).

Reality, β < 1, and β > 1/2 are DERIVED, not assumed: the ball at C⋆ ≥ 2/c₀ and q ≥ 3 gives |Im ρ|+2 < 3 ≤ q, hence log(q(|Im ρ|+2)) < 2·log q STRICTLY (the strictness comes from |Im ρ|+2 < 3, NOT from (log 3q)/log q ≤ 2, which is equality at q = 3 — refuter R5), so if Im ρ ≠ 0 the zero violates zero_free_region_all's conclusion while satisfying its hypothesis disjunction (χ² ≠ 1 ∨ Im ρ ≠ 0) — contradiction with no equality corner; the carve-out (χ²=1 ∧ Im ρ=0, SiegelTwin.lean:44-48) is exactly where F's witnesses live. Then β := ρ.re is real with 1/2 ≤ β, and β < 1 by mathlib's DirichletCharacter.LFunction_ne_zero_of_one_le_re (χ ≠ 1 branch; Mathlib/NumberTheory/LSeries/Nonvanishing.lean:397 — GROUNDED by the sufficiency refuter, upgrading the candidate's MEMORY-tier risk). Output: HB (1.11) with η := ((1−β)log q)⁻¹ ≥ C⋆.

Both arms of the max are binding in-architecture (minimality refuter check 5): C < 2/c₀ kills the reality derivation (complex witnesses escape the ZFR's reach); C < C⁽¹⁾ kills the Corollary-2 error budget (loglog C⁽¹⁾ = 2A/𝔖C(α) is exact). In practice C⁽¹⁾ (double-exponential) dominates. The rejected ∃C-first quantifier order stays rejected (glue constraint 3): small-C members lose both.

Relation to the frozen target: InfinitelyManySiegelZeros (SiegelTwin.lean:85-88) ⟹ FulcrumQualityMin C for every C — via a NAMED, currently unwritten B-class compactness gadget (finitely many χ of modulus ≤ Q, real zeros isolated away from 1 by analyticity + L(1,χ) ≠ 0; refuter R2, load-bearing TWICE). The converse is asserted proof-theoretically only: no derivation exists in corpus or literature; any derivation would be a fixed-to-arbitrary quality bootstrap for Siegel zeros, open (refuter R3 — the material non-implication is unprovable, since under GRH both sides are false).

The refuted structure candidate (VirtualSiegelPackage) contributes NO weakening to this statement: its zero-deletion died on the q^500-truncation-vs-zero's-reach flaw (source audit L71-73: the log F integral's bulk accrues at heights up to q^{O(η)}); its manufacturing-route insight is recorded as unharvested slack, reopenable only by an HB pp.207-210 line-grade diff.

## THE WEAKENING LEDGER

- W1 — QUALITY BINDER (the ONE genuine strict weakening vs the frozen corpus target, per the minimality refuter's consolidation R4): IMSZ demands witnesses at EVERY c > 0 (SiegelTwin.lean:85-88, ∀c∃ order); the chain consumes ONE strength — Corollary 2 needs only η ≥ C⁽¹⁾ fixed effective. LICENSE: glue audit §4 (lines 129-138: 'the ∀-quality binder ... never uses beyond one strength'; F(C) shape + IMSZ ⟹ F(C) derivation) + source audit §1/§9.1 (lines 33-36, 156-158: 'the demanded quality is a FIXED effective constant'). Strictness verified by both refuters; the IMSZ ⟹ F direction costs the unwritten compactness gadget (refuter check 1a).

- W2 — ASYMPTOTIC DROPPED (weakening vs HB's Theorem 1, not vs IMSZ): no η → ∞, no (log log η)⁻¹ error demand — only infinitude of twins is targeted. LICENSE: source audit §9.1 line 158: 'η → ∞ needed only for the ASYMPTOTIC (1.1)'; §5 line 113: 'for MERE INFINITUDE only η ≥ C⁽¹⁾ is consumed'.

- W3 — NORMALIZATION DROPPED: HB's (1.11) constant 3 (η ≥ 3) is not demanded; the ball constant is C⋆. LICENSE: source audit §9.1 line 156: 'The 3 in (1.11) is normalization'.

- W4 — ZERO-TYPE DEMANDS STRIPPED, honesty label PACKAGING NOT WEAKENING (minimality refuter 1d): literature demands a REAL β with 1 − c/log q < β < 1; F demands only a complex zero in the norm ball, with reality/β<1/β>1/2 derived. The refuter proved the ball minus the real segment is ZFR-vacuous at q ≥ 3 (F provably equivalent to its real-restricted form), so this is statement-shape convenience for the Lean intake, carried because it makes L1 one clean glue lemma. LICENSE: ZeroFreeReal.lean:605-608 hypothesis disjunction + SiegelTwin.lean:44-48 carve-out + refuter checklist 2b arithmetic.

- W5 — QUANTITY AT THE FLOOR, honesty label DEMAND-PARITY with frozen IMSZ (refuter R4): bare ∀Q∃q>Q — weaker than any density, spacing, one-per-dyadic-range, or growth statement, but IMSZ never demanded those either. LICENSE: source audit §7 lines 134-135: 'No smoothness/progression/spacing constraints on the sequence of q'; glue constraint 2: quantity cannot drop below 'infinitely many q' (one window = one twin pair), finite prefix free.

- W6 — ONE ZERO PER MODULUS, ANY QUALIFYING ZERO, honesty label DEMAND-PARITY: no uniqueness, no maximality (ordering ρ.re ≤ β₀ is WLOG-able internally — glue constraint 6; at C⋆ ≥ 2/c₀ complex zeros sit below β₀ by the verbatim ZFR, and a maximal real zero exists by analytic isolation, sufficiency-refuter L2 check), multiplicity irrelevant (Prachar count, ZeroCountNearOne.lean:98-103, C = 7200 with multiplicity). LICENSE: source audit §7 lines 129-131: 'Uniqueness NOT assumed; multiplicity irrelevant; if several qualifying zeros exist pick any'.

- W7 — NO SQUARED-LOG, honesty label ARTIFACT-BYPASS: F is not stated in SiegelSequence's (1−β)(log q)² < c form — a corpus dispatcher artifact strictly stronger than needed, already bypassed by the HB-ENGINE re-freeze. LICENSE: glue constraint 5 + s3-hb3-design.md:876-880 ('no squared-log strengthening ... SiegelSequence bridge is bypassed').

- REJECTED WEAKENINGS (recorded so Pass 2 does not resurrect them): (a) zero-deletion / (P,D,V) output package — REFUTED this pass (Flaw A: the package pins nothing on heights (q^500, q^{O(η)}] where a log-η-sized ingredient of Lemma 7's log F evaluation lives, source audit lines 71-73; reopenable ONLY via the R1 line-diff of HB pp.207-210); (b) pure L(1,χ)-smallness — killed by the structure candidate's own audit (vacuous at εlog q threshold, stronger-than-consumed at c/log q threshold); (c) sub-first-power coupling (1/√log q) — breaks window entry, (1−β)log x must stay O(1) (glue constraint 1, the floor this F does NOT weaken); (d) primitivity-drop via imprimitive→primitive descent — bridge unlanded, mathlib support unverified; candidate rider only; (e) ∃C-first quantifier order — not known sufficient (glue constraint 3).

## THE DEMAND MAP

F_min → twins, every link cited, slack budget per link. Chain verified by the sufficiency refuter against the repo; audit citations re-grounded by the judge this session.

L0 PREFIX DISCARD (free): consume witnesses only at q > Q₀ := max(q₀ ≈ 20, 3). LICENSE: glue constraint 2 (finite prefix free); wp2 audit §5 budget conditions 1-2 (s ≥ 250, q₀ folded into C⁽¹⁾). Slack: infinite (any finite prefix discardable). NOTE: the WP7 door lemma must actually perform the discard.

L1 TYPE RECOVERY (the ONE new B-class glue lemma, unwritten): ball + C⋆ ≥ 2/c₀ + q ≥ 3 ⟹ Im ρ = 0 (else zero_free_region_all, ZeroFreeReal.lean:605-608, is violated — no equality corner, strictness from |Im ρ|+2 < 3 ≤ q), β := ρ.re real, 1/2 ≤ β, β < 1 by DirichletCharacter.LFunction_ne_zero_of_one_le_re (Nonvanishing.lean:397). Output HB (1.11) at η ≥ C⋆. Slack: ~22 binary orders in c₀ (wp2 audit §1b: row-cap gate 2⁻²⁵ vs 2⁻³ — the ZFR nowhere near binding).

L2 REPULSION (WP2): the engine demands of β₀ ONLY L(β₀,χ)=0 ∧ 1/2 ≤ β₀ < 1 (dh_master_ray, TBalR8.lean:98-104, verified; the 1−1/(4(1+log q)) range is a dead M4 fossil, catch #239, wp2 audit §1a). Delivers dh_repulsion_ordered (prose contract DHRepulsion.lean:262-275; R8c assembly in flight) → per-zero damping η^{−s/680}·polylog + Prachar count (ZeroCountNearOne.lean:98-103). Slack: σ-window 16/17 → ~0.9395 free; |γ| ≤ 1 extendable; ordering WLOG (glue constraint 6). OPEN: T-BAL-BUDGET (wp2 audit §3 — the fulcrum-relevant constraint; see risks).

L3 QUALITY SPEND (Lemmas 3/7 — where ALL quality slack is consumed): PretenseSum ≪ L(log η)^{−1/4} and L′/L(1,χ) = ηL + O(·) (s3-hb3-design.md:850-853; source §3A/B pp.206-210). At FIXED η ≥ C⁽¹⁾: z₀ = A·loglog η, final relative error A(loglog η)⁻¹ ≤ 𝔖C(α)/2 exactly at threshold — this pins C⁽¹⁾ (source p.223). Refuter swept the entire error ledger at fixed η: every consumption is constant-threshold, none needs η → ∞. Slack: ZERO — the error eats half the main term at threshold; this is why no smaller constant works.

L4 TRANSFER (WP1): landed zero-free for arbitrary χ² = 1 — 39 audited theorems (All.lean:31-70). Two slots: PretenseSum (TransferFull.lean:183) filled by L3; hres of hb_lemma2 (TransferFull.lean:204-214) = HB-L2c, UNCONDITIONAL sieve/divisor arithmetic (undesigned, C+; keystone ShiuCore CLOSED, range q ≤ z^{1−1/8000}, wp1 audit item 1). Star step DISCHARGED: S2_sub_S3_honestWindow (StarWindow.lean:429). Refuter's L1-range note: at window top the transfer charges primes up to 2x+2 > q^500 — one-line Mertens-increment absorption owed. Slack: hres fully parametric; star junk √x-grade, far below need.

L5 SIEGEL-FREE MIDDLE (WP3-6): Rosser dim 4, level q^{1/3} (import-vs-rederive OPEN); Weil/Kloosterman banked (norm_kloosterman_le_two_sqrt, Weil/Descent.lean:206; Composite/CompositeTail); Lemma 5 AUDITED zero-free — 'no β₀/η in §§5-7' (source §4 line 91), with the standing-(1.11) caveat (line 97-98: audit finding, not stated claim — the Lean port must re-verify the de-scoping); WP5 congruence core UNSTARTED (~1.5-3M, the pole); WP6 consumes the zero only through L′/L(1,χ) = ηL, supplied by L3 (QuadCharSum landed). Window q^250 ≤ x ≤ q^500: Weil binds below (true need ≈ q^{240} — ~q^{10} slack, source §6 line 118), zero-reach binds above ((1−β)log x ≤ 500/η = O(1) at fixed η — THE first-power floor, glue constraint 1, the one dimension NOT weakened). Conductor shape consumed at (α_i, q/Δ)=1 steps via (1.9) (source line 96) — land as the structural lemma (a ∈ {0,2,3}), NOT cube-freeness.

L6 ASSEMBLY + POSITIVITY (WP7, unbuilt): κS₁·(L′/L)² = x𝔖C(α) — the exact (ηL)² cancellation (source §3 STRUCTURAL CANCELLATION, pp.200/210: the zero never feeds the main term's size) → Theorem 1 → Corollary 1 on [q^300, q^500] (dyadic tail O(q^250)) → Corollary 2: ≥ ½𝔖C(α)x(log x)⁻² > 0 twin pairs at x = q^300 per witness. Existence needs only the λ⁻ side (source §9.4 lines 163-165). Slack: any fixed window powers work (source §6); yield is per-triple.

L7 QUANTITY CASH-OUT (WP7): each witness q > Q yields a twin pair > q^300/2 > Q via twin_survivor_of_pos (TwinDoor.lean:204-223, LANDED, consumes NO Siegel input) + the unbuilt B-class ∀n∃x door lemma (template: Chen H-glue, Assembly.lean:425-440 → Set.infinite_of_forall_exists_gt; chen_headline is the plumbing proof-of-life, ChenTheorem.lean:32) + the carrier bridge S⁽³⁾ → p1PrimeSum. F's ∀Q∃q>Q is consumed HERE and nowhere else. The retired TwinTypeII door (TwinDoor.lean:182-184, :270) is NOT used — HB supply is windowed (glue L2 :51-53). Slack: extraction is per-window, needs exactly the floor quantity.

WALLS (non-contradiction): F is character-sighted; the TwinBar walls (WallUnconditional.lean:44-52, tolerant-Φ genre; 2log2 < 2 bar) are inapplicable by the non-contradiction triple (SiegelTwin.lean:29-48, incl. the ZFR carve-out at :44-48) — verified by both refuters at file grade.

COMPARISON PATH (not sufficiency): IMSZ ⟹ F needs the compactness gadget (refuter R2, unwritten, load-bearing twice). A T-BAL failure degrades 'strictly weaker' bookkeeping, never sufficiency of F itself — but see risk 1 for the honest coupled-failure phrasing.

## THE NEGATION BRIEF (Pass 2 foundation)

FOR PASS 2 — the precise negation and its calibration.

STATEMENT: ¬FulcrumQualityMin(C⋆) = ∃Q₀, ∀q > Q₀, ∀ primitive quadratic χ ≠ 1 mod q: EVERY zero ρ of L(·,χ) has ‖1−ρ‖ > 1/(C⋆·log q).

HONEST SPLIT: the complex part (Im ρ ≠ 0) is a near-theorem — zero_free_region_all (ZeroFreeReal.lean:605-608) delivers it with ~22 binary orders of slack once 1/C⋆ < c₀-grade (true by construction). So the ENTIRE content of ¬F is the real part: an EFFECTIVE, UNIFORM, FIRST-POWER Siegel-free gap — no real zero in [1 − 1/(C⋆·log q), 1) for any real primitive χ mod q, all q > Q₀. That is the corpus's NoSiegelZeros (SiegelTwin.lean:76-80) with the ∃c witness PINNED to the machine's own 1/C⋆, weakened only by a finite prefix; ¬F ⟹ NoSiegelZeros needs the SAME compactness gadget as IMSZ ⟹ F (refuter R2 — one named B-class node serves both directions). Because F(C⋆) is strictly weaker than IMSZ, ¬F(C⋆) is strictly STRONGER than ¬IMSZ: the fulcrum sharpens the landed dichotomy heathBrown_iff_dichotomy (SiegelTwin.lean:136-147) from 'TwinPrime ∨ NoSiegelZeros(∃c)' to 'TwinPrime ∨ effective-gap-at-the-specific-1/C⋆-beyond-a-prefix'. Per refuter R4: state this as a named corollary target, theorem-shaped, not prose.

CALIBRATION (all rerun by the refuters this session): (1) Beyond Siegel — siegel_theorem (SiegelClose.lean:841-844) gives ineffective β ≤ 1 − C(ε)/q^ε; refuter arithmetic: at ε = 1/2, C = 1 the ball is blocked at q = 10¹² but OPEN at q ≥ 10¹⁴ — a finite threshold Q₁(ε) for every ε, and the ∀ε∃C order yields no uniform floor (that would BE NoSiegelZeros). The corpus records the same sit at SiegelTwin.lean:62-66. So ¬F is NOT provable from Siegel; symmetric honesty: F is NOT refutable by Siegel — first-power log sits on the safe side (a q^{−θ}-grade ball would NOT have). (2) Beyond Landau-Page — one-exception-per-range, not zero exceptions. (3) NOT derivable from the landed ZFR — the carve-out (χ²=1 ∧ Im ρ=0, SiegelTwin.lean:44-48) is exactly where F's witnesses live; the region was PROVEN with that side condition deliberately. (4) Both horns genuinely open: F is not provably true (no zero-existence technology near s = 1 at any radius; F ⟹ ¬NoSiegelZeros at c > 1/C⋆ grade, contradicting GRH-belief), ¬F is not provably true (would be an effective first-power gap, open since Landau, cascading into effective PNT-in-AP/class-number consequences no current technology reaches).

QUANTIFIER HYGIENE FOR PASS 2: the non-implication 'F(C⋆) ⇏ IMSZ' is proof-theoretic ONLY (refuter R3): under GRH both sides are false, so the material non-implication is unprovable; the correct claim is that no derivation exists and any would be a fixed-to-arbitrary quality bootstrap for Siegel zeros, open. Do not state it materially.

HUNTING GROUNDS: (i) what unconditional artillery ¬F feeds — effective L(1,χ) lower bounds, effective prime-in-AP error terms, the classical no-exceptional-zero cascade; (ii) whether any corpus tool (siegel_theorem, zero_free_region_all, a Landau-Page port) could ever prove ¬F even for a restricted modulus family — it cannot at first power, which is precisely why both horns stay open; (iii) the honesty check that ¬F does NOT follow from the landed ZFR (the carve-out).

DO-NOT-RESURRECT LIST (killed this pass): (a) the zero-deleted (P,D,V) package — REFUTED on the q^500-truncation-vs-zero's-reach flaw; its negation-side surplus (anti-mimic/value-rigidity beyond the ZFR) is NOT part of ¬F_min and must not be claimed; reopen only via the HB pp.207-210 line diff (R1, ~0.1M). (b) pure L(1,χ)-smallness as a fulcrum — vacuous-or-too-strong dichotomy. (c) sub-first-power coupling — window entry breaks. (d) ∃C-first order — glue constraint 3.

## OPEN RISKS

- T-BAL-BUDGET is REGISTERED-OPEN (wp2 audit §3, tbal-s0-freeze.md:18 condition 4): per-zero η^{−s/680} does not alone majorize the A=2 budget; sufficiency of the corpus-grade repulsion rests on composition (count × strip × x-tuning). Corrected fallback phrasing (sufficiency refuter repair 2): an η-only shortfall moves C⋆; an η–q-COUPLED failure (uncancelled polylog(q) at fixed η) cannot be fixed by constant inflation — but it would break the IMSZ-instantiated chain identically, and the fallback is the mapped Jutila-grade rebuild (b ≈ 2+ε, ~2 orders of exponent headroom, s3-hb3-design.md:937-944). F's shape survives via literature-grade repulsion, not constant inflation alone.
- C⁽¹⁾ is UNFROZEN (WP2 witnesses + WP7 assembly pending): F must stay parametric; any fixed-numeral freeze today would be dishonest (glue constraint 3). c₀ is likewise ∃-form in Lean (numeral 1/126848 docstring-grade only) — A-class extraction owed before numerals enter (refuter R6).
- The L1 type-recovery glue is NEW and unwritten (B-class): zero_free_region_all application + LFunction_ne_zero_of_one_le_re (now GROUNDED, Nonvanishing.lean:397) + c₀ extraction; endpoint strictness must come from |Im ρ|+2 < 3 ≤ q (refuter R5).
- The compactness gadget (zeros of L(·,χ) isolated near 1: analyticity + L(1,χ) ≠ 0 + finitely many χ of modulus ≤ Q) is UNWRITTEN and load-bearing TWICE — IMSZ ⟹ F (the weaker-than claim) and ¬F ⟹ NoSiegelZeros (the dichotomy sharpening). One named B-class node (refuter R2).
- CONDUCTOR-SHAPE TRAP (both refuters, provable-falsity grade): 'real primitive ⟹ q cube-free' is FALSE at q = 8·(odd squarefree) (χ₈ at q = 8 is real primitive, 2³ | q). Must be landed as the structural lemma q = 2^a·m, a ∈ {0,2,3}, m odd squarefree, and the §6 Lean port must consume THAT via HB (1.9) — pin (1.9) verbatim against the paper before the F freeze (the PDF was staged in the source auditor's session, not currently in-repo).
- Mapped ≠ landed: ~5-9M of construction between the landed surface and consumption (glue §3 a-g): WP2 endgame (R8c in flight; crude density node; sharp-EF rebuild catch #47; Lemma 3/7 glue), HB-L2c (undesigned, C+, ShiuCore closed), WP3 Rosser decision, WP4's THREE deltas (gcd factor (k,u,v)^{1/2}, the 2-power/conductor assembly, Lemma 10's congruence-conditioned interval completion — enumerated per refuter coverage note, Siegel-free and weakening-insensitive), WP5 congruence core (~1.5-3M, the pole), WP6 leading terms, WP7 assembly + carrier bridge + door lemma. Any mapped link failing can ADD demands to F.
- Lemma 5's zero-freeness is an audit FINDING under the paper's standing hypothesis (1.11) (source audit lines 97-98), not HB's stated claim — the Lean port must re-verify the de-scoping (fossil genre #224/#239).
- Transfer range corner (sufficiency refuter L1 slip): at window top x = q^500 the transfer charges primes dividing n₊ ≤ 2x+2 > q^500 — bank the one-line Mertens-increment absorption (≤ log 2) or state PretenseSum at N = 2q^500 + 2.
- The WP7 door lemma must actually discard small q (q ≤ Q₀): the q ≥ 3 hygiene is automatic from χ ≠ 1, but q₀ ≈ 20 (budget condition 2) is not.
- UNHARVESTED SLACK (the structure candidate's verified residue): if an HB pp.207-210 line-grade diff (R1, ~0.1M: re-stage the PDF, check whether the log F integral ∫_x^∞ v^{β₀−2}/log v is a formal completion with analytic content confined ≤ x) resolves in the formal-completion direction, the zero-deletion weakening reopens as a Pass-3 candidate — the (P,D,V) package or its (T)-widened form. Until then the zero stays in F.
- Ordering WLOG (glue constraint 6) is argued, not landed: maximal-real-zero existence via analytic isolation must be part of the L1/L2 seam node.
- Sibling-audit dependency: 'glue §4', 'source audit p.223', 'wp2 §3' are wf-session outputs, re-grounded by the judge this session against the repo where load-bearing; the HB paper itself is NOT currently staged — any dispute at page grade requires re-staging.

## FORMALIZATION ESTIMATE

STATING F_min IN LEAN — cheap, available today (A-class, ~15 lines): every ingredient of FulcrumQualityMin already appears in the landed IMSZ def (SiegelTwin.lean:85-88 uses DirichletCharacter ℂ q, IsPrimitive, χ ^ 2 = 1, LFunction, exactly the same binders); the only new syntax is the norm-ball inequality ‖(1:ℂ) − ρ‖ * (C * Real.log q) ≤ 1 replacing the real-β chain. Natural home: Salt/TwinBar/SiegelTwin.lean alongside IMSZ/NoSiegelZeros/BadHyp, keeping the quantifier-discipline commentary block. State it PARAMETRIC (def taking C : ℝ); do NOT freeze a numeral C⋆ until C⁽¹⁾ freezes (WP2 witnesses + WP7 assembly) — the closing move is then a one-line instantiation.

IMMEDIATE COMPANION NODES (all pre-flagship, cheap): (1) fulcrum_of_IMSZ : InfinitelyManySiegelZeros → ∀ C > 0, FulcrumQualityMin C — B-class, requires the compactness gadget (zeros isolated near 1: finitely many χ of modulus ≤ Q, analyticity + LFunction_ne_zero_of_one_le_re at s = 1); serves double duty for the dichotomy corollary. (2) The L1 type-recovery lemma — B-class: ball at C ≥ 2/c₀, q ≥ 3 ⟹ ρ.im = 0 ∧ 1/2 ≤ ρ.re < 1, consuming zero_free_region_all (ZeroFreeReal.lean:605) + Nonvanishing.lean:397; prerequisite: the A-class c₀-numeral extraction from the existing proof (or keep c₀ existential and state L1 with ∃-threading — cleaner: restate zero_free_region_all with the explicit numeral, refuter R6). (3) The conductor-shape lemma — B-class: χ real primitive mod q ⟹ q = 2^a·m, a ∈ {0,2,3}, m odd squarefree (mathlib support for conductors/fundamental discriminants is partial — MEMORY-tier, must be scouted; fallback is a direct character-theory proof). (4) q ≥ 3 hygiene — A-class (mod 1, 2 have trivial unit groups).

UPGRADING THE FLAGSHIP TO CONSUME F_min: the target theorem becomes fulcrum_quality : FulcrumQualityMin C⋆ → TwinPrimeConjecture, with the landed HeathBrownStatement (SiegelTwin.lean:92-93) recovered as a corollary via fulcrum_of_IMSZ — strictly more theorem for the same construction. The construction itself is unchanged from the IMSZ plan: L1 converts F-witnesses to HB (1.11) triples, then the mapped mass runs as designed. Remaining build (glue §3, priced ~5-9M net of tonight's WP2 progress; campaign price ~7-13M at design:912): WP2 endgame (R8c in flight + crude density ~0.5-0.9M + sharp-EF rebuild ~0.6-1.0M + Lemma 3/7 glue + THE T-BAL-BUDGET AUDIT before the consuming node), HB-L2c design+land, WP3 decision, WP4 three deltas, WP5 core (~1.5-3M, the pole), WP6 leading terms, WP7 assembly + carrier bridge + the B-class arbitrarily-large-x door lemma (template Chen/Assembly.lean:425-440). The F-specific increment over the IMSZ-conditional plan is SMALL: nodes (1)-(4) above plus the L1 lemma — roughly one Opus session of glue once c₀ is extracted; everything else is shared with the already-ratified HB-ENGINE campaign. Dichotomy corollary for the writeup: fulcrum_dichotomy : TwinPrimeConjecture ∨ (effective first-power gap at 1/C⋆ beyond a prefix) — theorem-shaped per refuter R4, one propositional step from fulcrum_quality plus the compactness gadget.
