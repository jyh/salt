# ⟦THE L² RESTRUCTURE⟧ — freeze v1 (2026-07-30 12:45 PDT; JYH-granted: "exactly my friend! Let's go!")

*The head re-cut that closes the S11 compose. Input: the DELTA-SCOPE
dossier (banked in flags, "THE 539-BIT GIANT IS ONE BYTE"). Sequence:
this freeze → REF-L2 (two refuters, dispatched with this freeze) → on
PASS the seven-stone wave fires under the standing grant.*

## The finding (one paragraph)

K = |Ξ| (the major-arc frequency count), not energy. The door pays a
full δ per frequency only because CircleMethod.lean:403 discards the
second Fourier factor at its trivial bound H. Keeping it (real Φ:
‖dft(−ξ)‖ = ‖dft ξ‖ — a strict tightening of a landed estimate) turns
the major arm into (2C₀/log H)·Σ_{ξ∈Ξ}‖dftΦξ‖², and the sieve-insert
error — being α-INDEPENDENT — pays its total Fourier mass ONCE via
Parseval (Σ_{ξ∈ZMod H}‖(raw−sieved)^(−ξ/H)‖² = H·notMemSCount)
instead of once per arc.

## The new budget line

    2·K·Braw + δ/2 + 8·2^k/x  <  c₀·ε        (c₀ = cD3/(16C))

K multiplies the SQUARED-scale socket grade; the glue δ (the only
summand hMδ reads) is K-FREE. δ₀' ≈ c₀ε/4 ≈ 1.2e-6; b: 602 → 64 vs
the free K = 9/2 ceiling 87 — **23 bits of margin**. The socket's
Braw ceiling RELAXES (1.3e-336 → 1.15e-168). Nothing in the ledger
gets harder; the ε⁻¹⁶ halves.

## The seven stones (~1.1–1.3k, all additive; the head in-file per the S0-TOWER precedent)

1. fourier_split_sq + circle_method_estimate_sq (CircleMethod, ~200, C)
2. log_chowla_two_shell_xi_sq (Theorem23Shell, ~150, B)
3. MRTUniformityXiL2 + contradiction_of_mrtDoorXiL2 (MRTDoor, ~60, A/B)
4. spine_False_core_xi_sq + the head twin + the 9/2 twin (SpineFinal,
   in-file, ~250, B/C)
5. the Parseval-difference stone + the glue mass re-export (~100, B)
6. the L²-summed arc adapter twin (M4Window, ~90, B)
7. the exit/close re-plumb (the new three-summand grade line;
   M4DoorGates UNCHANGED; ~300, B/C)

## The refuter mandates (REF-L2, before any stone fires)

R1 ⟦THE PARSEVAL STONE⟧ — load-bearing: derive
Σ_ξ‖(raw−sieved)‖² = H·notMemSCount from dft_sum_mul_conj at the
actual defs; without it the shed is 2 bits.
R2 ⟦THE α-INDEPENDENCE CENSUS⟧ — walk EVERY debit in the door chain;
any second α-independent term rides the same treatment; any genuinely
α-dependent one keeps its K — name each.
R3 ⟦THE ARITHMETIC⟧ — re-derive the b = 64 table + the relaxed Braw
demand against the landed RSanDoorRho page + THE g2 M-GATE priced
(1787702400·a2Level1 M ≤ target/5 — never counted; it relaxes here
but must be PRICED).
R4 ⟦TAO-FAITHFULNESS⟧ — the new door is a finite sum of integrals,
no sup inside, implied by the per-α door; the MRTDoor seam warning
re-stated on the twin.

FIRE RULE: on R1+R2 PASS (R3/R4 amendments foldable), the wave fires
without further ask — JYH's grant covers the sequence.
