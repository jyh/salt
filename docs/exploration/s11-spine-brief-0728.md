# THE S11 SPINE BRIEF — the road's final act (frozen 2026-07-28 18:02 PDT, from S11-SPINE-SCOPE's fact base)

*Dispatch when THE COLLAPSE lands (the night sequence's step 3). The
full fact base is in the session record; this brief is self-sufficient.
One new file Salt/MR/M4Spine.lean, purely additive; SpineFinal is NOT
touched (the g-twin is landed; m4_exit_socket already fires the head
directly — the socket-family question is RESOLVED: no shared socket
exists, none is needed, the M4 arm fires log_chowla_two_budget_head_g
itself and MRTUniformityXi never appears in the M4 statement).

## THE CENTRAL DESIGN MOVE (state first, comment loudly)
Every class-(a) demand resolves to TWO slots: H-side → U1floor
(U1floor ≤ R.Hlo); x-side → the g-arm (g R.Hhi R.ω ≤ R.x) in the
shape x ≥ ω·Ψ(Hhi) — THE ω FACTOR IS MANDATORY on every x-side arm
(the analytic gates speak log Xd at the block bottoms; doorLadder_lower
+ doorCount give X_k+1 ≥ (x+1)/(4ω)). The apparent M-circularity
(M ≍ 24Cg/δ, δ forced by Hhi) dissolves by the FUNCTION-OF-Hhi
DISCIPLINE: define δfun/Mfun/Φ as explicit functions of Hhi BEFORE
firing the head; g reads (Hhi, ω) ONLY; instantiate after the obtain.
K6 TWICE OVER: every supplier existential (Cg/ε/δ₀ from
m4_wave_closed_of_dyadicRow; Kb; blockfree_sum_le's C) opens BEFORE
g is defined — S-0 is the non-circularity argument itself.

## THE ORDERED STEPS (S11-SPINE-SCOPE §5, verbatim)
S-0 the constants block (the obtain order — comment it).
S-1 the demand functions: δfun Hhi; Mfun Hhi := ⌈24·Cg/δfun⌉₊; Φ (the
SieveBlockGate floor at Mfun — the M-vs-X reduction: log calQK is
M-only, so hblocks ⟺ Xd ≥ Φ(M) ⟺ x ≥ 4ω·Φ); MS H := (log H)^{−56}
(the worked arithmetic: 6(1+2π)²(log H)^24(log₂H+1)·MS ≤ (log H)^{−30}
at log H ≳ 40); Braw := m4Saving.
S-2 m4SpineG (the gJoin idiom + per-arm projections): the ω-factored
max over (i) 4ω⌈exp exp(max(m4JointThr Bfun Hhi, 9.1e3, 6412.6, 996.4,
Xmin(C,ε₀), 1e4))⌉₊ (ii) 2ω(Hhi+2) (iii) 8ω (iv) 16ω/δfun Hhi
(v) 4ω·Φ Hhi (vi) the (log Xd) ≥ (log H)^{56/(θ₂₉₃−ε₀)} separation arm.
S-3 m4SpineFloor := max H0arc ⌈exp 40⌉₊ (the U1floor).
S-4 NEW LEMMA 1: H0arc + arc_gate_of_floor — ∃H0 ∀H ≥ H0,
2(log H)^12 ≤ H (~e^200; the log_scale_threshold/sq_le_log genre)
[C, ~40 — the one hard page].
S-5 NEW LEMMA 2: mrtDeliveredGrade_anti (antitone in H at e ≤ log H;
the doorGrade_anti genre + the loglog numerator on the −11/4+1 margin)
[B, ~25].
S-6 the M4DoorGates witness at k := doorCount R.ω, M := Mfun R.Hhi,
δ := δfun R.Hhi: hlogω FREE off R.hωbig (log ω ≥ 129); doorCount_gates
for hreach/hcount (hbig off arm (ii)); hpow off (iii); THE hblocks PAGE
(SieveBlockGate's four conjuncts against Φ — conjunct 2 = log X ≥ 1e4
into the (i)-max; conjuncts 3–4 the M-vs-X pages) [C, ~80 — the
second-largest item].
S-7 hdrift via qgraded_drift_price_le + the MS arithmetic [B, ~40].
S-8 hdel = sqrt_m4Saving_le_delivered at C := 2 [A, 3].
S-9 hrest via S-5 + arm (iv) [B, ~20].
S-10 the compose: **m4_door_closed** — ∃ ε R M k MS, …,
M4ChiDyadicRowMeanSq → (hnoncop) → ¬logChowla2Fails R.eps R.x R.ω —
class (a)+(b) FULLY discharged, only the analytic residues carried
(and hnoncop drops once the R2 adapter is consumed) + the False twin.

## THE TRAPS (verbatim into the executor's brief)
(1) FOUR log scales — log H / log Xd / log R.x / loglog-thresholds;
they meet only through arm (vi), never in one statement. (2) g reads
(Hhi, ω) ONLY. (3) the obtain-before-g order (K6 ×2). (4) the ω factor
on EVERY x-side arm. (5) m4_wave_closed_of_dyadicRow NOT _of_row (the
maximal-step route is refuted). (6) mrtDeliveredGrade linear in C;
C := 2 so both halves read C/2 = 1. Size ~380–450 ln.
