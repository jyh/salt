# COUNCIL TABLE — ⟦D‑1, THE χ‑SUMMED CAPSTONE⟧

*Synthesis over DEMAND / DISPERSION / SOURCES / SIEVE / NECESSITY. Every load‑bearing byte re‑read by me; disagreements recomputed and ruled. Reports are cited only where they were right.*

---

## 0. THE HEADLINE THE COUNCIL DID NOT ASK FOR — read this before the verdict line

**⟦F5 — THE `log q` SCALE WALL⟧. My own composition of two conjuncts of the SAME ∃‑bundle. If it survives one refuter, D‑1 is not the decisive question and the design block should not open.**

`DoorRowCarriedT0` (`Salt/MR/M4T0Discharge.lean:581-721`) is a single ∃‑bundle with `X` bound inside and pinned by `(Xd : ℝ) = X` (`:586`). Two of its conjuncts, at the same `(q, χ, M, Xd)`:

- `:706-709` the cap‑free floor threshold — `40·lll X + 32*((1/8)*Real.log q + (1/4)*(q:ℝ) + vkDebitConst(…) + vkMidDebit q + Kfl + 25) < Real.log (Real.log X)`
- `:712` `hgRows` — `5760 * (a2RowsSum M Xd + Ccc * (2 / (M:ℝ))) ≤ (Real.log X)^(-1/500)`

`hgRows` is an **upper** cap on `X`: `(log X)^{-1/500} ≥ 11520·Ccc/M ⟹ lnln X ≤ 500·(ln M − ln(11520·Ccc))`.
The threshold is a **lower** demand: even keeping only the `(1/8)log q` piece, `lnln X > 4·ln q` (with `vkMidDebit q`'s `(1/2)log q` and `vkDebitConst`'s `(1/4)log q` the true coefficient is ≈ 28·ln q; I use 4 to be conservative).

The register instantiates this bundle at `∀H ∈ [Hlo,Hhi], ∀q ≤ arcDen 12 H` (`Salt/MR/M4BaseNarrow.lean:1051-1057`), with the length floor in the same conjunct (`:1052`, `doorRowFloor M ≤ j ≤ Nat.log 2 H` ⟹ `M·Adoor M ≤ log₂H`). Write `v := lnln Hlo`. Then

```
ln M  ≤  v + 0.367 − ln(Adoor M)  ≈  v − 20.4      (k = 30)
ln q  =  12·lnln Hhi  =  12·v^{4.006}              (SANDWICH-REF's exact tower)
demand:  48·v^{4.006}  <  lnln X  ≤  500·(v − 29.8)
```

`48·v^4 < 500v` forces **`v ≤ 2.19`**, i.e. `ln Hlo ≤ 8.9`; the length floor forces `v ≥ ln M + 20.4 ≥ 20.4`. **Unsatisfiable at every M, every H.** At the k=30 closing point (`v = 432.5`) the two sides are `8.4·10^{12}` vs `2.0·10^{5}` — short by `4·10^{7}`, and by `10^{9}` if the full ≈28·ln q coefficient is used.

Three properties that make this worse than the M‑sandwich:

1. **It is `~4.6·10^7` times tighter than gate 12.** Gate 12 gives `lnln Hhi < Adoor M·ln2/12` (budget exponential in the anchor bit `k`); F5 gives `lnln Hhi < 2.08·ln M` (budget *linear* in `ln M`).
2. **The anchor runs the wrong way, in both directions.** A bigger `Adoor M` shrinks the legal `M` at fixed `Hlo`, hence shrinks the `lnln X` cap. At `Adoor ≡ 1` it still fails at every `v`. D‑2's proposed `2^18 → 2^30` re‑pin makes F5 *worse* (−8.3 nats of `ln M`, −4150 of `lnln X` budget) — immaterial against the gap, but the sign is the point: **the anchor does not buy F5, so D‑2 is not settled by the anchor re‑pin alone.**
3. **The tower is load‑bearing.** At `Hhi = Hlo` (no tower) the system closes for `v > 33`. F5 is precisely "the tower read through `log q`", a strictly tighter sibling of the M‑sandwich on the same object.

**Neither SIEVE nor DISPERSION composed this.** Both independently found the `q`‑dependence of the floor threshold; DISPERSION (§2) declared it "regime‑absorbable (`g R.Hhi R.ω ≤ R.x`)" — **that is REFUTED**: the regime supplies only a *lower* bound on `R.x`, while `hgRows` supplies an *upper* one, and DISPERSION's own §3 margins (`loglog X ≈ log Hhi ≥ 6.4e61`) are computed at an `X` the register forbids (`hgRows` caps `lnln X ≤ 500·ln M ≈ 1.2·10^5` at `M ≈ 10^{112}`). SIEVE's ⟦F4⟧ is the same discovery one rung short: SIEVE priced only the crude `(1/4)·q` term and proposed a repair; **the repair is necessary but not sufficient — the benign, un‑repairable `log q` terms kill it on their own.**

I verified the three same‑instance conditions SIEVE flagged as unchecked, at statement level: `X` is one variable in the bundle (`:586` pin), the register does instantiate at `q ≤ arcDen 12 H` for `H` up to `Hhi` (`M4BaseNarrow.lean:1051`), and the bundle's `M` is the register's `M` (`:1057`). What I did **not** trace is the discharge chain from `DoorRowCarriedT0` up to `m4_wave_collapsed`, and I did not re‑derive the tower law. Those are refuter questions R1 and R2 below.

---

## 1. THE VERDICT LINE

> **⟦D‑1⟧ is (c) OPEN‑AS‑STATED — and it is ALL‑OR‑NOTHING. No report dissolved it, no report reduced it, and every proposed cheap escape is refuted. But D‑1 is now *conditionally moot*: it is the decisive question only if ⟦F5⟧ falls.**

**The exact surviving statement to be proven** (in the corpus's own glyphs):

```
M4ChiSummedBlockMeanSq R M k Bcl :
  ∀ H, R.Hlo ≤ H → H ≤ R.Hhi → ∀ q, 0 < q → (q:ℝ) ≤ arcDen 12 H → ∀ i < k,
    ∑ χ : DirichletCharacter ℂ q,
      ∑ n ∈ Finset.Ioc (doorLadder R.x H (i+1)) (doorLadder R.x H i),
        (doorChiSup χ M H n)^2
      ≤ C · Bcl H · (H:ℝ)^2 · (doorLadder R.x H (i+1) : ℝ)
```
with **`C` an absolute constant — `φ(q)`, `q`, `log q` and `(log H)^ε` are all forbidden in `C`.**

Why "all‑or‑nothing", ruled from the arithmetic (NECESSITY §1d confirmed, DEMAND §1b **corrected** — see §5):

| residual ρ left in `C` | `ln ρ` at the k=30 ceiling | budget 149.7 nats | verdict |
|---|---|---|---|
| absolute constant `C` | `ln C` | `C ≤ 1.2e65` | ✅ free |
| `q/φ(q) ≍ e^γ·lnln q ≈ 47.7` | 3.86 | ✅ | free by 146 nats |
| `(lnln q)^A` | `3.29·A` | `A ≤ 45` | ✅ |
| `(log q)^A = (12·lnln H)^A` | `26.8·A` | **`A ≤ 5.58`** | ✅ |
| `(log H)^ε` | `ε·3.63e10` | `ε ≤ 4.12e-9` | ❌ dead |
| `q^θ` | `θ·4.36e11` | `θ ≤ 3.4e-10` | ❌ dead |
| `d(q)`, `2^{ω(q)}` | `1.13e10` | — | ❌ dead |

**The law: the residual may be any bounded power of `loglog H`; any positive power of `log H` is fatal.** So a `√q` saving, a `q/φ(q)` saving, a `(log q)^A` saving with `A ≤ 5` — the first is worthless, the last two are *already free* and buy nothing. **There is no partial deliverable. Strike `docs/exploration/council-0729.md:51`'s parenthetical "(A cheap partial: the large‑q trivial cut caps the prefactor at `Bcl^{-1/2}`)".** Two independent refutations agree (DEMAND §3: the trivial branch's composite `T(q) ≥ 3` against a budget `< δ₀²/4 ≈ 10^{-98}`, missing by an absolute constant `≥ 12/δ₀²` **independent of every repair**; NECESSITY §4: the register demand is uniformly `≤ G²` at every `q`, so a trivial‑grade output is unusable at any `q`). DEMAND additionally shows the repaired budget *enlarges* the useless region (`q*` moves from `> arcDen` down to `≈10^{50}`) — the cut runs backwards.

**One thing D‑1 is NOT:** it is not refuted. SIEVE §1 ("the Siegel split is DEAD, hard — the per‑χ floor `B ≥ 2.167e9·Ccc/M` is χ‑blind, so `Σ_χ B_χ ≥ φ(q)·2.167e9·Ccc/M`, short by `(log H)^{11}`") **proves only that the trivial sum of the landed per‑χ bound fails — which is D‑1's premise, not its refutation.** `2.167e9·Ccc/M` is the *current proof's* error floor (forced by `hgRows` at `ThmA2.lean:481-483` capping `log X`, then read through the `188133·(log X)^{-1/500}` summand at `:488`), not a lower bound on the truth. The truth is known and much smaller — see Route 1. SIEVE's numeral is right (`188133·5760·2 = 2.1673e9` exactly, and `×2^18 = 5.680e14`); only its framing needs correction. Its stated tolerance should read `δ₀²/(3.9e14·Ccc)`, not `δ₀/(4e14·Ccc)` — direction and fatality unchanged.

**Arithmetic reconciliation (the reports do NOT disagree).** DEMAND's `M ≥ 3.0e13·Ccc·q/δ₀²` and NECESSITY/LEVER1‑REF's `M ≥ r·1.4184e11·Ccc/G²` are **the same number**: NECESSITY's `r` carries the 53 that DEMAND folds into its numeral, and `G = δ₀/2`. `53 × 1.4186e11 = 7.52e12 = 3.005e13/4`. ✓ Verified against the bytes: `3 × m4Cmax(→10.9076 post‑lever‑1′) × Fan(=2·MSan)` = 65.45 (`M4Maximal.lean:471,487`), `× 2.1673e9` (`M4T0Discharge.lean:712` vs `:715-721`) `= 1.4186e11`; `(1+2π)² = 53.04` is the exact `qgraded_drift_price_le` constant, an **identity** in `q` (`M4BridgePhase.lean:392-410`, verified: `(1+2πA/q)²·q² = (q+2πA)²`, range `39.5A²…53.04A²` — a factor 1.344 over the whole legal `q`‑range, hence NECESSITY §2a's ruling that **F2 and F3 are each worthless standalone** is confirmed and should be banked as law). **DEMAND's "the three recounts span 10^{2.3}" is an artifact of unit choice; there is no spread. DRIFT‑SCOPE's `1.2e14` is the superseded pre‑refuter figure.**

---

## 2. THE ROUTES TABLE

| # | route | what must be proven (exact shape) | what the corpus supplies | est. cost | kill‑risks | rank |
|---|---|---|---|---|---|---|
| **1** | **THE KMT PORT** — port Klurman–Mangerel–Teräväinen, *Multiplicative functions in short arithmetic progressions*, arXiv:1909.12280 / PLMS 127 (2023) 366–446, **Cor. 1.7**, restricted to `q ≤ (log H)^{12}` | `∫_X^{2X} Σ*_{a(q)} \|Σ_{x<n≤x+h, n≡a} μ(n)\|² dx ≪ ε·φ(q)·X·(h/q)²`, S‑restricted, `doorChiSup`‑compatible. By orthogonality `= (1/φ(q))Σ_χ\|S_χ\|²`, i.e. **`Σ_χ\|S_χ\|² ≪ ε·φ(q)²(h/q)² ≤ ε·h²`** — the whole family at one character's price | Ramaré 3 791 ln; Halász 11 415 ln; the all‑χ floor stack 11 287 ln (**stronger than KMT's**: uniform for *all* χ, `CofactorSupplier.lean:347-360`, vs KMT's "all but χ₁"); VK/VMVT 13 393 ln; SW 33 632 ln + `siegelWalfisz_holds`; the 𝒯/𝒰 seam massif 17 014 ln; `char_LS` FROZEN (`Salt/LS/CharLS.lean:277`); `char_LS_max` shape‑matched to `doorChiSup` (`Salt/BV/MaxLS.lean:208`) | **8–18k ln, class C with one C/D seam** (SOURCES 7.2–13k; SIEVE 12–18k — the gap is scope: SOURCES deletes KMT's zero‑density apparatus, SIEVE counts the Halász re‑thread. Both are honest ends of one band) | **(a)** the two genuinely new stones: the **hybrid MVT** `Σ_χ ∫_{-T}^{T}\|Σ a_n χ(n)n^{-it}\|²dt ≪ (φ(q)T+N)Σ\|a\|²` (not landed; both halves are — `char_LS` + `MVHilbertUniform`, `MVHilbert.lean:134`, discharged `MVCore2.lean:575`) and the **χ‑aspect large‑value count** (`LargeValues.lean:251` → `LargeValueCount.lean:145/422/631` is t‑aspect only). **(b)** the S‑restricted analogue is inside KMT's proof, not a stated corollary — SOURCES did not read §§5–8 line by line. **(c)** `sup_K` compatibility (see Route 2) | **1** |
| **2** | **THE GAUSS‑SUM CONSUMER RE‑CUT** — the interface half of Route 1; a *prerequisite*, not an alternative | Drop the class split. At `b/q`: `Σ_m λ(m)e(bm/q) = (1/φ(q))Σ_χ τ_b(χ̄)S_χ`; Cauchy–Schwarz + `Σ_χ\|τ_b(χ̄)\|² ≤ φ(q)·q` gives `subWindowSup(b/q)² ≤ (q/φ(q))·Σ_χ (doorChiSup χ)²` | `M4Chars.lean:184`; `Salt/LS/GaussSum.lean` — **but only PRIMITIVE forms are landed** (`:54`, `:141`, `:167` all take `hχ.IsPrimitive`); `Salt/BV/PolyaVinogradov.lean:245-258` already does the additive→multiplicative expansion | **900–1600 ln, class B/C** | the imprimitive `\|τ(χ)\| ≤ √q` bookkeeping (`Salt/LS/Conductor.lean` coverage unverified). **MY RULING, and it is good news: this route needs no Parseval‑per‑fixed‑K.** ARC‑SCOPE's blocker — "Parseval does not commute with `sup_K`" — does not bite here: the CS is over χ *pointwise in K*, so per‑character sups distribute and `doorChiSup` passes through unchanged. **It kills BOTH q's at once**, where DISPERSION's alternative kills one | **1 (bundled with Route 1)** |
| **2′** | *(sub‑variant)* keep the class split; swap one line | Replace `inv_totient_sum_le` at `M4WaveClosed.lean:450` with a χ‑summed hypothesis; the same proof returns `C·Bcl·H²·A/φ(q)` | **VERIFIED EXACT** — `m4_classMeanSq_of_chiMeanSq` (`M4WaveClosed.lean:412-450`) ends in exactly that one call; `inv_totient_sum_le` is `M4Close.lean:637-650` | **~15 ln** (DISPERSION confirmed byte‑for‑byte) | kills only the `sum_const` q (`M4WaveClosed.lean:288-298`); the Chebyshev q (`:265-272`, `sq_sum_le_card_mul_sum_sq`, `card_range = q`) survives and still needs ARC‑SCOPE's ~900–1400 ln Parseval move. **Same total as Route 2, with the `sup_K` risk retained. Prefer Route 2.** | — |
| **3** | **THE Ξ_H‑AVERAGED DOOR** — SIEVE's hatch 5, **and I resolved its pivotal check** | Re‑state the door as `Σ_{ξ∈Ξ_H}(1/H)∫‖windowExpSum H n (−ξ/H)‖ ≤ K·δ` in place of the per‑ξ bound; supply it by an averaged argument | **THE CHECK RESOLVES FAVOURABLY.** `contradiction_of_mrtDoorXi` (`Salt/Entropy/Chowla/MRTDoor.lean:127-160`) consumes the door **only inside a `Finset.sum_le_sum` over `ξ ∈ bigXi`** (`:138-146`), then `hconst`/`hcard` collapse it to `card·δ ≤ K·δ`. A summed hypothesis discharges it directly — the block `:138-157` collapses to it, **~20 ln at that site**. `MRTUniformityXi` (`:109-111`) and the `hbd`→`hadapt` path (`M4Exit.lean:283-295`) are the statement surface that must move | **UNPRICED** (spine statement change, Fable/JYH‑tier); the `~20 ln` is only the collision site | **structurally live, arithmetically empty on the obvious instantiation.** My own check: the naive large sieve on the `1/H`‑spaced `{ξ/H}` gives `Σ_ξ‖S_n‖ ≤ H√(2\|Ξ\|)`, needing `√(2K) ≤ Kδ` — fails for any small `δ`. It delivers L² mass, not cancellation. **Any gain must come from a cancellation argument, and none is identified.** If it existed it would dissolve F2 *and* F3 outright | **3** (record the finding; do not open) |
| **4** | the **constant arc cap** `W₀ ∈ [10^{39.2},10^{65}]` (DEMAND §2) | `MinorArcBoundTight W₀` for constant `W₀` | `minorArcBoundTight_twelve` unconditional at `B₅ = 12` (`Salt/MR/ExitClose.lean:769`); `arcDen` **is** `B₅`‑parametric (`BigXiArc.lean:148`) and `arcDen_twelve_eq_m4W` pins it to `m4W` (`M4Close.lean:218-221`); `m4Saving = W^{-5/2} = (log H)^{-30}` (`:216,226`) — DEMAND's citations all check out | — | **REFUTED — mathematically FALSE, not merely harder.** `expSum eps H α = Σ_{p∈primeWindow}(1/p)·e(αp)` (`Salt/Entropy/Chowla/CircleMethod.lean:36-38`) — a *normalized* prime sum. At `α = a/q` with `q` a prime just above `W₀`, PNT‑in‑APs gives `‖expSum‖ ≍ (window mass)/φ(q) ≍ c/W₀`, a **constant**, while the `Ξ_H` threshold is `ε²/log H → 0`. So `MinorArcBound(W₀)` fails for every constant `W₀` and every sufficiently large `H`. The corpus's own header pin `B₅ > 10` (`BigXiArc.lean:120-124`, L6/L7) points the same way, as does NECESSITY §2c. **3‑1 plus first principles: strike DEMAND §2's "one live alternative" and §4's item 5** | ✗ |
| 5 | the Siegel / exceptional‑modulus split | — | — | — | **REFUTED as a saving** (SIEVE §1) — the binding floor is character‑blind, so a split has nothing to separate; but note the correction in §1 above: it refutes the *trivial sum*, not D‑1. SIEVE's own salvage is right and worth banking: the *quality* half `(log X)^{-0.736}` sums over `(log H)^{12}` characters for free | ✗ |
| 6 | the large sieve at the raw window | — | `char_LS_max` (`MaxLS.lean:208`), `arithmetic_LS` (`ArithmeticLS.lean:91`), `bilinear_LS_shell` (`BilinearLS.lean:279`) | — | **REFUTED — worse than trivial.** Sieved density `≈ 1 − 2/M` ⟹ `Σ‖c‖² ≈ H`, so LS delivers only the `1/q` and costs `(1+log₂H)²`; off by `13(log₂H)³`. The corpus's own `char_LS_perQ` docstring says why (`CharLS.lean:42-44`) | ✗ |
| 7 | dilation / non‑coprime reclamation | — | `m4_class_dilate_exit` (`M4BridgeDilate.lean:633`), `d0_ledger_sharp` (`M4NonCoprime.lean:259`) | ~300 ln to bank | **REFUTED** — `d₀ = gcd(r,q) = 1` on exactly the `φ(q)` classes carrying the mass; any other cut destroys multiplicativity | ✗ |
| 8 | the q‑tradeoff (re‑approximate at smaller `q`; coarsen Dirichlet quality; lower `B₅`) | — | — | — | **REFUTED three ways** (NECESSITY §2). (a) No tradeoff exists: `qgraded_drift_price_le` is an identity, factor 1.344 over the whole range. (b) Any `q' < q` gives block length `ℓ' ≤ 2q'q < 2A² < 2^{doorRowFloor M}` — **below the door's own row floor** (`M4DoorRow.door_length_gate:418-426`). (c) `B₅` is pinned from below by the minor arm. **No register corner exists**: `q` is free up to `arcDen` in both q‑carrying conjuncts (`M4BaseNarrow.lean:1040`, `:1051`) and gate 12 caps only `arcDen` at `2^{Adoor M}` | ✗ |
| 9 | pay the block‑free Mertens mass **outside** the χ‑decomposition (SOURCES' Route β — "the highest‑value single check on the board") | — | — | ~600–1400 ln *if it worked* | **RULED: it does not work as a shortcut, and it is not a separate route.** The mass does not appear as a summand; it appears as a **gate** (`hgRows`, `ThmA2.lean:481-483`) that caps `log X` from above and thereby floors the χ‑blind summand `188133·(log X)^{-1/500} ≥ 2.167e9·Ccc/M`. Invoking `thm_a2'` once per character therefore yields `φ(q)` copies of that floored summand — **the `(log H)^{12}` returns, moved from the class count to the summand count.** What SOURCES actually identified is *the structural requirement on any D‑1 proof*: the χ‑sum must be inside the analysis so the χ‑blind error terms are paid once. That is Route 1. **Merge, do not schedule separately** | merged into 1 |
| **R** | **⟦F4⟧ THE `primeDivSum` REPAIR** — not a D‑1 route; a **mandatory prerequisite on every path** | `Σ_{p∣q}1/p ≤ loglog q + M + 12/log q` in place of `≤ q` | `primeDivSum_le_modulus` (`CapFreeAssembly.lean:235-260`) is exactly as SIEVE described — `ω(q) ≤ q+1`, each `1/p ≤ 1/2`, docstring "Crude on purpose". The replacement stone is **in the kernel**: `mertens_second_sharp_real` (`Salt/Mertens/Third.lean:56-57`, `\|S(t) − (loglog t + mertensM)\| ≤ 12/log t`) | **40–70 ln (class A/B) + ~250–400 ln re‑thread across 18 sites**, every one a hypothesis weakening | none identified. Independently found by SIEVE and DISPERSION. **Do this regardless of every other decision** | **fire now** |

---

## 3. THE REFUTER QUESTIONS — the 5 checks before any route fires

**R1 — ⟦F5⟧'s discharge trace (the blocking one).** I verified at *statement* level that the floor threshold (`M4T0Discharge.lean:706-709`), `hgRows` (`:712`) and the envelope (`:715-721`) are conjuncts of one ∃‑bundle at one pinned `X` (`:586`), instantiated by the register at `q ≤ arcDen 12 H` up to `H = Hhi` (`M4BaseNarrow.lean:1051-1057`) with the register's own `M`. **Not verified: the discharge chain from `DoorRowCarriedT0` up to `m4_wave_collapsed` — is the bundle ever actually demanded at `q = arcDen 12 Hhi`, or does some intermediate weaken `q` first?** If it is, the landed register carries a second, tighter sandwich than gate 12, and `48·lnln Hhi < 500·ln M` is unsatisfiable at every `M`. **This must be answered before the D‑1 design block is scheduled.**

**R2 — the tower law, re‑derived.** ⟦F5⟧ is fatal only through `lnln Hhi = (lnln Hlo)^{4.006}` (SANDWICH‑REF). At `Hhi = Hlo` the system closes for `v > 33`. Re‑derive the tower independently (it is now load‑bearing for *two* walls, not one) and confirm the `4` is structural. If it is, F5 stands and no anchor bit buys it.

**R3 — does the F4 repair restore satisfiability?** Re‑run the two‑sided `X` squeeze with `8·(lnln q + mertensM + 12/ln q)` in place of `8q`. My arithmetic says the `(1/4)q` half closes with three orders to spare (`8·lnln q ≈ 214` vs a cap of `1.2·10^5` at `M ≈ 10^{110}`) — **but the `log q` half does not** (`≈ 28·ln q ≈ 1.2·10^{13}` vs the same cap). Confirm or refute; this is the check that separates ⟦F4⟧ (cheap, repairable) from ⟦F5⟧ (structural).

**R4 — is D‑1's demand actually `O(1)`?** SOURCES' residual risk #3, and it decides whether the literature suffices. KMT's saving is `ε ≥ (log(h/Q))^{-1/200}`, or `(log(h/Q))^{-2+o(1)}` for `f = μ` (their §3.5, via Siegel–Walfisz in place of Halász — the observation that deletes their entire zero‑density block from the port). **My ruling: with F3 landed the demand is a constant `≈ δ₀²`, and `(log H)^{-1/200} ≤ δ₀²` above an `Hlo` floor of `10^{19600}` — spine‑choosable, so KMT's strength is ample.** But **without** F3 the demand is `(log H)^{-17.5}` (ARC‑SCOPE) and **KMT is not strong enough by 17.5 exponents.** So: confirm F3 is a hard prerequisite of Route 1, not a parallel option.

**R5 — the Gauss‑sum route's `sup_K` claim and its imprimitive tail.** Confirm my ruling that `sup_K |(1/φ)Σ_χ τ_b(χ̄)S_χ(K)|² ≤ (q/φ(q))·Σ_χ (doorChiSup χ)²` holds with per‑character sups (no Parseval, no commutation), and audit `Salt/LS/Conductor.lean` for `‖τ(χ)‖ ≤ √q` at **imprimitive** χ — `Salt/LS/GaussSum.lean` has only the primitive forms (`:54, :141, :167`). This decides whether Route 2's 900–1600 ln band is honest.

*(Housekeeping, no refuter needed: strike the trivial‑cut parenthetical at `council-0729.md:51`; bank NECESSITY §2a's law that F2 is worthless standalone alongside F3's; bank the Ξ_H consumer finding at `MRTDoor.lean:138-157`.)*

---

## 4. THE DESIGN‑BLOCK DECISION, AS IT SHOULD BE PUT TO JYH

The council convened to decide whether to open a class‑D design block on ⟦D‑1⟧, and the five scopers between them settled that question cleanly: D‑1 is real, it is all‑or‑nothing (any residual above `(loglog H)^{5.6}` is fatal, and everything below it is already free — so there is no partial deliverable and no intermediate milestone), every cheap escape is refuted with byte‑grounded margins rather than slivers (the trivial cut by an absolute `10^{98}`, the constant arc cap by outright falsity of the minor‑arc bound, the Siegel split, the dilation, the raw large sieve, and the q‑tradeoff in all three of its readings), and — the one genuinely new thing on the board — **⟦D‑1⟧ is not open mathematics: it is Klurman–Mangerel–Teräväinen, PLMS 2023, Corollary 1.7, whose every side condition is either vacuous at `q ≤ (log H)^{12}` or already discharged by machinery salt has landed, and whose port price is an honest 8–18k ln of class‑C work standing on ~90k ln of reusable massif, with exactly two genuinely new stones (the hybrid mean‑value theorem in the χ‑aspect and the χ‑aspect large‑value count).** That is the good news, and it converts D‑1 from a research gamble into a priced multi‑wave campaign. **The fork is this: I do not think that campaign should be authorized this week, because in composing the reports against each other I found a wall none of them composed — ⟦F5⟧, the register's own `(1/8)·log q` floor debit read against `hgRows`' upper cap on `X`, which on my arithmetic is unsatisfiable at every `M` by seven orders and which the anchor re‑pin makes strictly worse rather than better, meaning D‑2 is not settled either.** So the honest choice is between **(A)** authorizing the 8–18k KMT port now on the bet that F5 is a discharge‑path artifact, and **(B)** spending one refuter‑hour first on R1/R2/R3 — the F5 trace, the tower re‑derivation, and the `primeDivSum` repair arithmetic — and firing only the ⟦F4⟧ repair (40–70 ln with the Mertens stone already in the kernel, mandatory on every path, and cheap enough to fire tonight) while that runs. **My recommendation is (B), unambiguously**: the port is expensive, its shape depends on what the register's demand actually is, and if F5 stands then the demand shape moves before a single line of it is written.

---

**Files touched for verification (all absolute):** `/Users/jyh/projects/claude/salt/Salt/MR/M4T0Discharge.lean` (`:581-586`, `:664`, `:706-721`), `/Users/jyh/projects/claude/salt/Salt/MR/M4BaseNarrow.lean` (`:1040-1068`), `/Users/jyh/projects/claude/salt/Salt/MR/M4WaveClosed.lean` (`:255-305`, `:412-450`), `/Users/jyh/projects/claude/salt/Salt/MR/M4Close.lean` (`:210-230`, `:637-650`), `/Users/jyh/projects/claude/salt/Salt/MR/M4Maximal.lean` (`:465-510`), `/Users/jyh/projects/claude/salt/Salt/MR/M4BridgePhase.lean` (`:388-412`), `/Users/jyh/projects/claude/salt/Salt/MR/CapFreeAssembly.lean` (`:225-265`), `/Users/jyh/projects/claude/salt/Salt/MR/BigXiArc.lean` (`:110-200`), `/Users/jyh/projects/claude/salt/Salt/MR/ExitClose.lean` (`:760-775`), `/Users/jyh/projects/claude/salt/Salt/MR/ThmA2.lean` (`:478-495`), `/Users/jyh/projects/claude/salt/Salt/MR/M4Exit.lean` (`:275-300`), `/Users/jyh/projects/claude/salt/Salt/MR/M4Spine.lean` (`:90-105`), `/Users/jyh/projects/claude/salt/Salt/MR/M4Quality.lean` (`:85-95`), `/Users/jyh/projects/claude/salt/Salt/Entropy/Chowla/MRTDoor.lean` (`:109-160`), `/Users/jyh/projects/claude/salt/Salt/Entropy/Chowla/CircleMethod.lean` (`:36-45`), `/Users/jyh/projects/claude/salt/Salt/Mertens/Third.lean` (`:56-57`), `/Users/jyh/projects/claude/salt/Salt/LS/GaussSum.lean` (`:54,141,167`).