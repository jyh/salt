# Q5b — THE PERTURBATION CLASS (Fable design pass; PENDING JYH RATIFICATION)

*2026-07-15. Status: PROPOSED. Per the registration's placeholder
protocol, this class becomes frozen only via a registration amendment
(A2) carrying explicit user ratification; no Q5b proof work before
that. Registered intent: probe the boundary of the CHEN ARGUMENT
itself (the razor + switching), the class Q6a's wall does not cover.
Anchors: `RazorClose.razor_scalar_margin` (M = 0.012151 above the
1/100 floor; the three-term shape 9779/10000 − a2hi·(log 6/4)/2 −
c̄·(3/8)·(243/100)/2), the symbolic razor (RazorClose Part B, pure
algebra over named normalised bounds), the A2Window cost ledger, the
c̄ certificate (600 panels).*

## The class: three named axes

**Axis B — the twin-target deficit (FIRST; highest signal/token).**
Point the landed machinery at P₁ instead of P₂: state the
twin-analogue of the razor (the switching weight's P₁ form) over the
LANDED carriers at the LANDED operating point, and prove its margin
is negative with an explicit kernel-checked deficit:
`twin_razor_deficit : (the P₁-razor value) ≤ −δ` with δ explicit.
Content: the strongest landed non-interface certificate — Chen's own
argument — misses the twin target by a named number. Complements the
Q6a wall (which bounds interface-reading certificates); this bounds
the specific certificate that goes THROUGH the interface. Anti-vacuity
by construction (statements about landed carriers). Expected class:
B/C, one probe executor.

**Axis A — the distribution-level crossover (θ-razor).** The landed
razor consumes BV-level-1/2 inputs baked into its numeric
coefficients. Re-derive the three razor coefficients as functions of
the level θ through the symbolic-razor layer, and bracket the
crossover: `razor_crossover : θ* ∈ [a, b]` — the level below which
Chen's argument (this razor shape) dies, kernel-checked. The landed
proof's slack at θ = 1/2 is the margin M. Q3's interface-extraction
genre applied to the razor. Expected class: C (the θ-dependence of
the main-term coefficient must be re-chased through the mass ledger);
one probe executor after a scoping recon confirms the chase is
bounded — if the recon prices the θ-chase C+/D, the axis reports as
an obstruction map (acceptable registered outcome).

**Axis C — the weight-family optimality (α, z, y).** The Chen weight's
1/2 knob generalized to α, and the operating point (z, y) =
(x^{1/8}, x^{1/3}) over a small rational grid. Deliverables: the
margin function M(α) with the in-family optimum (is 1/2 optimal?),
and the numeric (z, y)-grid map with the best margin found. Content:
the landed operating point's optimality-within-class, or a concrete
improvement (a larger margin = cheaper future consumers). Expected
class: B (symbolic algebra + numeric map); cheapest axis.

## Execution commitment (timebox honesty)

In-sprint: **Axis B**, then ONE of A/C chosen by what B reveals
(default C — cheaper — unless B's deficit shape makes the θ-question
decisive). The remaining axis is recorded debt at sprint close.
Chen-2 keeps pipeline priority; Q5b probes run in the gaps, Q5a-style
(constraint store opens each probe; R1–R4 armed; the R4 tripwire:
any probe implying "twin primes provable" from landed material is a
definitional alarm, STOP).

## Freeze discipline

Each axis's target statements above are the frozen forms; executors
may weaken to honest forms with the deviation reported (the Q6a-2
pattern), never strengthen silently. Statement changes = house/user
only. The class is CLOSED: no fourth axis without a further
amendment.
