# T-BAL S0 SYNTHESIS FREEZE (5th design; survivor: support-native, grafts applied)
Provenance: both candidates UNREFUTED by their panels. benli-faithful = the cannot-close proof for BV-fixed weights; support-native = the Selberg-weight redesign both converge on. Ledgers: s0_benli_ledger.py, s0_refuter_check.py, refuter_partB_check.py, refute_tbal5b.py, ledger.py (all in session scratchpad; re-run at execution). Ground: s0_ground.md.

## F1 — FLAGS ENTRY (grafted from benli-faithful; commit with wave 1)
BV grahamTheta weights in the Benli balance are PROVABLY unclosable at PV grade: the H7 u-free defect needs ≤ e^{-13.1} (q=3) / e^{-77.1} (q=10^6) (tightening without bound as u→0); BV truth δ_opt = +0.0473 EXACT at z=36 (adversarially certified global min over χ-patterns; the +1-extreme GROWS with z), fitted-model figures UNDERSTATE it, so deficit ≥ e^{9.7} / ≥ e^{69.5} — a power of Q vs a polylog; no tuning closes. Certifying χ-pretense at PV grade is 4-fold circular (prose-grade; no fifth route named). Benli's 4.1 is an EXACT identity for χ-BUILT weights (δ≡0) — hence the weight re-founding below. Also record: pointwise-absolute S0 dead (band primes 6–7, worst-case P1 0.96); z≫x escape dead (z–x circle).

## THE DESIGN (support-native, repairs applied)
Benli's β₀-self-balance with χ-DEPENDENT SELBERG-OPTIMAL weights (internal defs; blueprint statement edit is the ratification gate below). Support facts consumed: dhA≥0, dhA(1)=1, (†)-toolkit, prime-power geometric blocks. NO pointwise vanishing consumed.
TUNING: u:=1−β₀, σ:=ρ.re, T:=|ρ.im|+2, Q:=qT, L₂:=logQ+2, M:=√q(1+logq), P:=3M(1+‖ρ‖/σ)≤8M. Trivial split u≥1/(40L₂). Deep: z:=⌈Q¹²u^{−2}⌉, x:=Q^{104}u^{−11}, N:=⌈x⌉, R:=e^{1/u}; X₁:=770 caps x^{1−β₀}≤e^{2.6}e^{11/e}. WITNESSES b:=680, k:=14, c:=2^{−250} (∃-bound).
σ-WINDOW (why 16/17): E(β₀) u-grade is u^{−9} (z⁴=u^{−8} forced by H-defect u-carrying, ×u^{−1} single zero-completion dust); Wσ≥9+μ₁ ∧ W(1−σ)≤1−μ₂ ⟹ σ≥(9+μ₁)/(10+μ₁−μ₂); at σ=9/10 margins exactly zero (log^{14}(1/u)-divergent); δ_b u^{0.8} tail floors it ≈0.92. FROZEN σ₀:=16/17, W=11; thinnest margin 0.8−11/17=0.153.
CHAIN: (F) 3/4 ≤ 1−1/x ≤ S0+‖D_ρ‖ [floor port; 6×1/8=3/4 EXACTLY — strictness comes from any piece's ≥10^{−7} slack; state the ≤-vs-< seam for the R8 executor]. (R1) S0 ≤ N^{β₀−σ}(D₀−(1−1/x)) — EXACT termwise shift (n^{−σ}≤n^{−β₀}N^{β₀−σ}, coeffW≥0), hyp σ≤β₀ (T-BAL-UNORDERED kept); the old R4 sqrt-error mechanism is structurally ABSENT (no Abel-vs-mass anywhere). (R6@β₀) D₀ ≤ L₁·V·x^{1−β₀}/(u(2−β₀)) + E(β₀). (R4) V(λ^opt)=1/H(z) EXACT. (R5, AMENDED) H(z) ≥ (1−δ_d−δ_b)·L₁/(u(2−β₀)) — z^{1−β₀} factor DROPPED (verdict-mandatory: the sketched route proves only this form; the stronger form is plausibly false at q=3). Π := L₁Vx^{1−β₀}/(u(2−β₀)) ≤ (1+2δ)x^{1−β₀} ≤ (1+2δ)X₁; L₁V ≤ (1+2δ)u(2−β₀) ≤ 4u. (R7) D₀−(1−1/x) ≤ uX₁logx + 2δX₁ + 1/x + E(β₀). (ρ) ‖D_ρ‖ ≤ 4u·x^{1−σ}L₂/c₀ + E(ρ) [zfr_harvest]. MASTER: 3/4 ≤ 2x^{β₀−σ}[uX₁logx+2δX₁+1/x+E(β₀)] + 4ux^{1−σ}L₂/c₀ + E(ρ); contradiction-form inversion at τ=cQ^{−b(1−σ)}L₂^{−k} via monotone t^η log(e/t)^j caps (turning points ≥10^{−39.3} ≫ τ).
E-SHAPE (bracket AMENDED per verdict): E(s) ≤ C_E·P(1+‖s‖)(1+logx)³(1+2logz)^{12}z^{2+2Res}x^{−Res}·[1_{s=ρ}·L₂/c₀ + 1_{s=β₀}/u]; pole-cancelled architecture — every 1/(1−s) as ∫-form or completed to L(s,χ)=0; exactly ONE u^{−1} dust survives at β₀.
RUNGS: **R0** [B ~300] weight-generic re-founding: h(p):=1+χ(p)−χ(p)/p ∈(0,2] (exact local residue density, no degeneracy), g:=h/(p−h), H(z):=Σ_{r≤z}μ²g, selWeight λ^opt; dhWeightSqW/dhCoeffW/gcW + ports (floor, regroup, |gcW|≤3^ω, tau6W moments k=12, cpow-reality) + dhCoeff_nonneg [A ~5, grafted]. grahamTheta/dhCoeff RETAINED for existing consumers. **R1** [A ~50] tail_shift_to_beta0. **R2** [C ~250] unmoll_extraction_real (parametric y≥2; pole-cancelled EM + partial_sum_at_zero_small@β₀ + strip@1). PRE-DISPATCH: pin C_w NUMERICALLY in the frozen statement; headroom to 1.89e4 at the q=3 corner; if C_w>1.8e4 retune z:=Q^{13}u^{−2} (E-margin absorbs: −9.0→−5.9). **R3** [B ~120] L1_lower_siegel: L₁ ≥ 0.27u(2−β₀) — standalone Zeno stone (PV-grade effective Siegel lower bound); gate by deep-regime guard. **R4** [C ~380] selberg_diag + selberg_opt_eq + selweight_one + selweight_abs_le_one — pure finite multiplicative algebra. **R5** [C ~300] H_lower (AMENDED form above) via h̃=b⋆(1*χRe), b(p)=−χ(p)/p, euler_b_one (Σb(c)/c=1, eulerProduct_hasProd + B_p(1)=1 exact); δ_d ≤ 9.3C_wP(1+logz)³(1+1/u)/z; δ_b ≤ 300z^{−0.4} (0.4 structural: log2(4/3)=0.415). NAMED helpers (verdict): Hsum_ge_htilde_div [B/C]; rescale n^{−1}≥z^{β₀−1}n^{−β₀} on n≤z [A]. Zeno fallback: dh_repulsion_of_H_lower conditional. **R6** [C/D ~450 CRUX] dh_extraction_upper_W at BOTH s∈{ρ,β₀}: old-R5 main-term side unchanged; pole-cancelled; generic weights. RUNG CARD MUST CARRY VERBATIM (verdict-mandatory): the naive per-b remainder Σχ(b)b^{−β₀}O_b(1) ~ y^{1−β₀}log y swamps the budget; the executor MUST use the kernel-Abel exact form (b/y)∫₀^{y/b}[Σ_{a≤t}a^{−β₀}]dt with pole-cancellation giving E(t)=O(1+log(1/t)) pointwise, then by-parts against the zero-killed stream with the 1/y volume factor; any bare ζ(β₀) or 1/(1−s) box re-breaks the u^{−9} ledger. **R7** [B ~120] dh_balance_beta0. **R8** [C ~260] dh_repulsion_ordered: trivial split + deep master + inversion. c0 is EXISTENTIAL in Lean (zero_free_region_all; 1/126848 docstring-grade — grafted catch): keep the master existential in c0.
TARGET: dh_repulsion_ordered : ∃ b c k, 0<b ∧ 0<c ∧ 0≤k ∧ ∀ q [NeZero q] χ, IsPrimitive→χ≠1→χ²=1→2≤q→ ∀β₀, L(β₀)=0→1/2<β₀→β₀<1→ ∀ρ, L(ρ)=0→ρ.im≠0→|ρ.im|≤1→**16/17**≤ρ.re→ρ.re<1→ρ.re≤β₀→ c(qT)^{−b(1−ρ.re)}/(log(qT)+2)^k ≤ 1−β₀. Plus standalone L1_lower_siegel, selberg_opt_eq.
LEDGER (twice-scripted, q=3 | q=10^6, binding corner q=3): old sqrt-error ABSENT (R1 exact); E(β₀)-amp 10^{−9.0}|10^{−270} vs 1/8; E(ρ) 10^{−7.2}|10^{−276}; δ_d 0.43u|10^{−63} (C_w=100; survives to C_w=1.89e4); δ_b in-master at τ 10^{−10.5}|10^{−38.4} = BINDING; R3 floor 10^{−51.7}|10^{−250}; master u-side 10^{−29}; trivial 10^{−83}≤u*; master total at τ 10^{−10.52} < 3/4. Extra corners pass: T=3 both q, q=7 (χ(2)=1 tail worst case), full σ-sweep on (0,1/17].

## RATIFICATION GATE (Iron Rule 1 — flag, don't enact)
σ-hyp 9/10→16/17 vs the prose contract DHRepulsion.lean:267 (no landed Lean consumer, grep-confirmed; consumer = WP2). BLOCKS: R8 dispatch + any contract-prose edit. JYH must ratify WITH the WP2/VMVT/N-HDOM zero-density budget re-check covering σ ≤ 16/17 — fold into the already-queued T-BAL-BUDGET audit. Residual T-BAL-SIGMA-SLIVER := [9/10,16/17), recoverable only via Benli-4.3-analog u²-G-savings (class D) or subconvexity. Fallback if WP2 cannot cover 16/17: benli-faithful's SEL-NEAR re-scope (own panel mandatory; killer stone = 4.1-port with δ≡0).

## DISPATCH (staged)
WAVE 1 (σ-deviation-INDEPENDENT, dispatch now): R0, R1, R3, R4 + F1 flags entry + card update. WAVE 2 (after C_w pin): R2, then R5, R6, R7. GATED: R8 + contract edit (ratification). Reserve stones (refuter-cleared, dispatch only if R6's executor requests): inner_gcd_trisect [B ~110], coprime_char_partial_at_zero/_at_one [B ~100] (benli-faithful S3/S4, weight-free). Est ~2350 lines total.

---

# AMENDMENT 1 (2026-07-18 ~15:00 PT): THE W=14 RETUNE — VERIFIED 0/2

The R5 judge's retune, audited by two independent refuters (both
re-derived from the freeze structure, ~14k grid points, zero
failures; scripts refuter1_reledger.py + the corner hunter's, in
the session scratchpad — ship with the crush wave). SUPERSEDES the
TUNING line and the R5/δ rows above:

- **z := ⌈Q¹²·u^{−3}⌉** (a=3, crush-forced); **x := Q^{104}·u^{−14}**
  (W=14 — the UNIQUE integer with η_E = η_A at every σ; forced by
  E-grade G = 4a+1 = 13 ⟹ W ≥ 13·17/16); **X₁ := 2330**
  (conservative ~189x; honest deep-branch sup e^{2.51});
  witnesses b=680/k=14/c=2^{−250}, σ₀ = 16/17, τ UNCHANGED.
- **δ_d = δ_b = 0**: the Bb/Bd master rows are DELETED (the
  δ_b ≤ 300z^{−0.4} posit is dead — flags R5-FINISH).
- All master-row u-grades positive (η_E = η_A = 3/17, η_Eρ =
  26/17), monotone-decaying on the ray; at-τ rows vs 1/8:
  E(β₀)-amp 10^{−5.7} (q=3, thinnest, 4.75 decades spare) →
  10^{−310.6} (q=10⁶). q swept ALL integers 3..200 + spot checks
  to 10⁹; q=4 IS admissible (conductor-4 real primitive) and
  passes.
- Crush coverage margins AT THE HONEST u*-CORNER: 1.76x (q=3),
  2.44x (q=5), 5.80x (q=150), 10.63x (q=10⁶) — quote THESE, not
  the deep-limit values.
- **EXECUTION-TIME LEDGER LAW (binding, from the audit):** the
  "rows at u*" convention is DEAD — rows are capped ON THE RAY
  u < τ only (at u* some rows read 10^{+10}; that is NOT a
  failure — only u < τ is consumed by the by_contra inversion;
  E-amp's turning point 10^{−33} sits far above τ = 10^{−114.5}).
  And: stream minimizations over D are convex log-sum-exp —
  minimize EXACTLY (ternary/analytic), NEVER coarse grids (a
  22.5-nat grid produced a spurious FAIL in audit).
- The σ-sweep is stated on [16/17, 1) with max-at-the-16/17-edge
  (verified to σ = 1−10⁻⁶).
