# COUNCIL v4 — 2026-08-01 afternoon
## The two-tower frontier: each road fixes the face the other breaks

*Sealed state: 718c42c (build 9651, axioms clean corpus-wide). In the
kernel since the morning council: the master law (TowerFlat), the
~4,600-line flat build (four phases, first-attempt genre), and
`logChowla2_witnessed_scale_flat` — the flat terminal with the Hcap
rider removed-because-proven at its own cap. The ledger: flags from
"FLAT-REF" onward.*

## C0 — The honest map (read first)

| road | register side | budget side |
|---|---|---|
| **landed tower** (λ₋ ≈ 101, K = 4.67e7) | **CLOSES** — every face, inside the landed pin | **FAILS** — Hcap at height 3 (the κ-cancellation) |
| **flat tower** (A symbolic) | **BREAKS** — the anchor frame line (no fixed point at any λ₋; hand-computed) | **CLOSES the height** — 3 → 2; residue = a number |

The flat residue as a number: the witness window demands **A ≤ 86.6**;
the budget supplies **A ≈ 8×10³⁰** (equivalently ε⁶β² ≥ 36.9 needed
vs ~5×10⁻³¹ supplied; cD3 ≥ 19,000 vs the landed 1/4). The two
breaks are DIFFERENT walls: the landed road's is the κ-pricing; the
flat road's is the anchor's frame constant 3.9×10⁹·(log₂M+1) against
the flat width e^{λ₋/2}.

## C1 — THE THREE HUNTS (authorize as one wave)

**H1 — THE ANCHOR HUNT** *(the Scale Law's next test)*. The flat
road's register break runs through ONE frame numeral — the anchor's
3.9e9 (M4ArithRho's DoorArithFrameRho.anchor). A break of 10⁴⁹
*invites* the plumbing hunt: trace the 3.9e9's provenance (is it
sharp, or another K=0-transcription/weak-floor artifact?); kernelize
the no-fixed-point claim (currently hand-computed); and price what
anchor form the flat width could live with.

**H2 — THE CONSTANT CHASE, sized honestly.** The flat residue needs
~10²⁹ of gain in ε⁶β². The priced inventory (KAPPA/FLAT-REF): the
linear bracket ~3×10⁴; budgetX's 4×; slice/δ bookkeeping ~10³; cD3
(1/4 → log 2) ~10⁶ in the A-currency — totals ~10¹⁵-genre, ~10¹⁴
short. H2's mandate: the honest maximum at the bytes — and if the
gap survives, SAY SO: the face is then real at height 2, a genuine
wall candidate (the glorious kind).

**H3 — THE JOINT-SHAPE QUESTION** *(the deep one)*. My unverified
walk suggests a theorem: with the anchor standing, the register
admits width ratio ≲ 4×10⁸, and any threshold φ ≥ A_budget makes the
drop-sum ≤ log(width)/(2·A_budget) ≈ 10⁻³⁰ ≪ log 2 — i.e. **no
tower shape whatever crosses the entropy budget inside the register's
window while the anchor and the current budget constant both stand.**
If H1 and H2 both come back hard, H3 kernelizes this as the honest
wall statement — which would be the campaign's first *proven*
genuine wall beyond Siegel, and exactly the "genuinely at a loss"
the Captain foretold. If either H1 or H2 softens, H3 computes the
surviving working point instead.

**RECOMMENDATION**: fire all three in parallel (read-only, kernel
probes, ~600–900k tokens); the verdicts compose into either a repair
wave or a wall theorem. Either outcome is gold-bearing.

## C2 — THE STANDING POSTURE (no ruling needed; stated for the record)

The v3 terminal (landed road) remains the honest citable object: one
false rider (Hcap), everything else proven, wide, or the field's own
walls. The Witness continues to aim at the unconditional per your
standing preference; no paper decision today — the hunts inform it.

## C3 — THE WEEK (unchanged, on track)

Sunday: the CV voice pass. Monday 8/4: the two filings. Tuesday 8/5:
the endorsement decision. Friday 8/7: the outside reviewer recording (he holds
the specification dossier for the design-doc drafting). the scheduled meeting
+ the second reviewer meetings: week of 8/11.

## C4 — HYGIENE

The mathlib cache re-fetched; Brun's independent replay now grinding
(genuine replay, no longer fast-failing — verdict lands async).
Seat/memory sync at close of sitting.
