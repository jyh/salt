# THE M4 RESIDUE DESIGN — the wall + the class pricing (Fable block, 2026-07-28 afternoon)

*Inputs: the WALL-DOSSIER (byte-precise, session record) + flags b566fcb.
Status: FROZEN pending one refuter pass on the arithmetic, then the
closing wave dispatches. The product: the discharge route for
M4RowMeanSq — the six-item register's last open item.*

## Part W — ⟦THE WALL⟧: the err-route rewire (two moves)

**The controlling facts** (dossier): hwin is a *joint-support* claim —
relativizing it is vacuous (SeamRowWindowed:35–41 ruled this before we
asked). The corpus already retired hwin twice: RamareMR (the window
lives in the index set) and SeamRowWindowed's
`ramErr_moment_split_mr_windowed` — the SAME ramErr object, decomposed
with neither hwin nor the unrestricted hcoef, at `SeamCoefW` =
byte-shape of the repaired hcoefPin. Its seam grade 4·520 = 2080 fits
A2Frame3.err's standing 3·720 = 2160. And the three datum mismatches
(the product phase, ellLin's squarefree support, the χ-twist) are all
artifacts of the capstone PINNING b := ellLin (liouChi χ) — at a
generic 1-bounded b the door datum factorizes exactly:
cf P := λ(P)·χ̄(P), b(m) := 1_𝒮(P·m)·λ(m)·χ̄(m)·e((αP)·m) — the phase
factors at the dilated frequency, χ̄ is multiplicative, no squarefree
support is imposed.

**W1 — the rewire** [C, ~350–550]: inside the capstone's err supply,
replace the E_priced route with `ramErr_moment_split_mr_windowed`:
- the ONE missing stone: the sharp p²-mass at `ramP2domMR` (the window
  in the index filter, NO hwin) — the Σ-half is landed
  (SmallStones:462–464, 8/P); the max-half is a ~30-line port of
  ramP2coeff_norm_div_le with the by_cases replaced by the ramHonMR
  membership read. Target 16·log₂(2X)/(X·P).
- witEP2's 4/3 bump (the split's prefactor 4 vs E_priced's 3), rippling
  into its two named-binder sites + the 12·EP2 gate.
- chain-2 (the hwinBand head): the priced ladder rewire — multiply
  seam_row_number's 480·(T_ann/X_d+1)·(…) summand by 4,
  `second_window_le_first_row` the citable inequality (the delta
  SeamRowWindowed:78–98 banked in advance). Both hwin heads retire.

**W2 — the b-slot generalization** [B/C, ~200–400]: the capstone's
err-side binders re-stated at generic 1-bounded b with the pair law at
the (already-repaired) window range; ellLin-ness demanded NOWHERE on
the err side (E_priced/ramP2mass were always generic — the pinning was
M4-5's over-specification). The door datum then inhabits the binder set
with the factorization above. The T0-band side keeps its own (M4Seam)
route — untouched.

## Part C — ⟦THE CLASS PRICING⟧: the modulus recursion

**The controlling facts**: the recursion is LITERAL —
`m4_class_dilate_exit` returns an absWindowSum of
`classCoeff (memSCoeff … liouvilleC) (q/d₀) (r/d₀)` — the identical
sieved datum at modulus q/d₀ < q, window H/d₀ ± 1, frequency d₀α, with
the reduced class provably coprime and ‖λ(d₀)‖ = 1. The base q = 1 is
the plain window sum. The general-fit block lemma is ALREADY LANDED
(sum_Ioc_absWindowSum_sq_div_le at free A, B; the dilated block clears
its fits with exactly 2d₀ loss against doorLadder_fit's unconditional
X_i + H ≤ 2X_{i+1}). The honest ceiling Σ‖classes‖ ≤ H is landed and
lossless. The precedent: VdCSocket.socket_head's strong-induction idiom.

**C1 — the interval-general block predicate** [B, ~150–250]:
M4BlockMeanSqGen at free (A, B] with the fits (X ≤ A, B + H' ≤ 2X − 2d₀
slack absorbed); the doorLadder form its instance; the tight exchange's
B ≤ 2A carried as a fit.

**C2 — the q-recursion** [C, ~400–700]: by strong induction on q
(socket_head pattern): M4ClassPrice q := every class-restricted sieved
window sum at modulus q is priced. Base q = 1: classCoeff a 1 r = a —
the row mean square directly. Step: coprime classes by the landed
character expansion; non-coprime class at gcd d₀ > 1 → the dilation
(landed) → the SAME statement at q/d₀ (the IH) at window H/d₀ ± 1,
frequency d₀α, scale ~X/d₀.
**The three accountings the refuter must check:**
(i) the CAP COMPOUNDING: each level's frequency d₀α reads at the
enlarged cap arcDen·(H+d₀)/H (B-3's rule); across levels the product
∏(H_i+d_i)/H_i ≤ exp(Σd_i/H_i); with ∏d_i ≤ q ≤ W and every H_i ≥
H/W ≫ W the total enlargement is ≤ exp(W²/H) ≈ 1 — free at the door.
(ii) the LEVEL COUNT: ≤ log₂ q ≤ log₂ W levels; the per-level constants
(the drift price, the factor 2s, the class count q_i) compound to at
most W^{O(log W)}-shaped — must stay inside m4Saving's W^{5/2}-budget…
NO: the honest form — the per-level loss is (1 + 2π·arcDen)²·q_i²-shaped
in the SQUARE; the compounded total must be priced against item 4's
6·MS ≤ m4Saving with MS re-graded per level. THE REFUTER WORKS THIS
PAGE EXACTLY — it is the design's one genuine risk.
(iii) the SCALE DRIFT: the dilated scale X/d₀ must stay inside the
mean-square gates (√X ≤ X/d₀ at d₀ ≤ W — free; the loglog degradation
O(log W/log X) — free).

## The dispatch plan (post-refuter)

REFUTE-RESIDUE (one Opus refuter: the W1 grade arithmetic 2080 ≤ 2160 +
the 4/3 ripple; the C2 accounting page (i)–(iii) — kill or confirm) →
then W1, W2, C1+C2 as three executors → then the M4RowMeanSq discharge
compose → the register closes to the pricing inequality + the door
gates → the S11 spine compose.

---

# v2 — POST-REFUTATION (REFUTE-RESIDUE's verdicts; supersedes above)

- **W1 REPAIR-THEN-FIRE**: arithmetic verified (2080 ≤ 2160; Xd-glyph
  resolves; the ×4 ALREADY BANKED downstream — a2_term3_weigh saturates
  at 5760 exactly). Fold: the a2_term3_ball_weigh one-step re-proof
  (8·Sb² ≤ 5760·Sb², ~10 ln); witEP2's ripple is FOUR sites; the 12·EP2
  gate free from log X ≥ 1.6·10⁴.
- **W2 RESCOPED to the pin/err chain (P = Q) ONLY** — there the
  genericity claim verified at every link and the door factorization is
  byte-honest for ALL m. The band-chain half is FALSE (e(αpm) bilinear;
  1_𝒮(pm) level-dependent) and is NOT NEEDED under the forced order
  (below).
- **C restructured — NO INDUCTION** (depth 1: m4_class_dilate_coprime;
  the dilated frequency b/q₀ constant on classes — no second split).
  The two-case lemma: d₀ = 1 → the character expansion (1/φ cancels);
  d₀ > 1 → ONE dilation (an equality, zero loss) → the coprime branch.
- **THE q-GRADED REPAIR (the real kill, ii-b)**: M4SievedDoorSqSup +
  m4_sievedDoorSq_of_sup re-cut to keep the drift's q-grading —
  conclusion at Braw'·q²·H², the grade gate at (1+2πA/q)²q² = (q+2πA)²
  ≤ (1+2π)²·arcDen² ≈ 53·(log H)^24 vs budget 2(15−11/4) = 24.5:
  CLOSES by (log H)^{0.5} ≥ 53 at the door. The two q's are the same
  witness by construction.
- **U1**: item 4's pricing must RETAIN the decay — price summand 1 from
  cfbM0 directly ((log X)^{1/15−7/(30e)} genuinely decays); never
  through m4_quality_summand_le (g1 is unsatisfiable: 8448·C₁'² ≤ 1/5).
- **U2 — THE FORCED ORDER**: Part C is a RE-CUT of M4RowMeanSq between
  the door and the capstone (drift + split + expansion REMOVE the
  phase); the capstone instantiates at the UN-phased 1_𝒮λχ̄ datum.
- **U3**: the M4GradeGate direct-discharge sibling (√Braw ≤ delivered,
  from the weaker Braw) — mandatory; m4_gradeGate_of_pricing wastes
  24.5 exponents.
- **K3(iii)**: the endpoint-drop lemma (the dilated fit fails by ≤ 3
  units against the TIGHT doorLadder_fit; each dropped n costs
  H'²/A' — negligible but a statement obligation).
