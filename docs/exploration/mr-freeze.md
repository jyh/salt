# THE MR-GATE FREEZE (synthesis, 2026-07-18 ~17:45 PT; 10/10 agents, minimal-gate + faithful grafts, dispatch_ready)

## THE FREEZE

MR-GATE SYNTHESIS: base = MINIMAL-GATE (Xi-door + g-parametric builder), FAITHFUL engine grafted, all 4 repair-sets APPLIED, one NEW obligation neither parent priced. FAITHFUL rejected as base on a synthesis-level hole its two refuters missed: the W<=A arm — GROUNDED chowla.txt Prop 2.4 proof ("W := log^5 H ... much less than A or (log X)^{1/125}") + hierarchy 1/eps<<H-<<H<<H+<<A, log x>=log A>>H+ (chowla.txt:560-640). Faithful R8 discharges ONLY the 1/125 arm; its R5 (quality=range=A) at range q<=W=log^5 Hhi demands quality log^5 Hhi >> (1/4)loglog x ~ (1/4)log Hhi at the FIXED builder (x internal, single-exp in Hhi; RegimeParam.lean:380 exposes only R.eps=eps AND Hlo0<=R.Hlo — re-read). Repairing it = adopting this synthesis.

TARGET: MRTUniformityXi R delta + 0<=delta (MRTDoor.lean:109-111; seam contradiction_of_mrtDoorXi SHIPS :127-157, explicit hdelta — re-read) at an MR-built regime; fixed delta0 = c0*eps/(2K), c0 := cD3/(16*C) ~ 1/419 (A2-RATIFIED 2026-07-19 per the SPINE-BUDGET freeze; was eps/(2K) at the old SpineFinal.lean:309 pin), no o(1). License chowla.txt:743-750 (lambda c_p=1, major arcs only, [17] Thm A.1).

RUNGS.
S0 XI-REWIRE (B/C ~550; JYH-GATED: registered Fable spine-rewire): Xi-variants at TRUE depth — door fires in log_chowla_two_shell (Theorem23Shell.lean:~227, re-read): in-file shell_xi (private windowExpSum_norm_eq_dft :67), spine_False_core_xi + final_xi (SpineFinal.lean; private mutualInfo_window_comm' :33), conditional_xi/of_door_xi (SpineClose/TowerDischarge). hdelta free from 0<delta. Additive; originals untouched.
S1 DIST (B 600, Salt/MR/Dist.lean): D(lambda,chi n^{it};x)^2 = loglog x + Re log L(1+1/log x+it,chi) + O_Q(1); Euler k>=2 <=0.78 (p=2,k=2 = 1/4 first), Mertens shift O(1) (mertens_second_sharp Second.lean:216), tail 0.22. Chain-G additive = 5.00 EXACTLY; Mertens <=4.125 execution check (repair).
S2 ZETA-POW-LOWER (C 2000): c'/((log|t|)^{3/4}(loglog|t|)^4) <= ||zeta(1+d'+it)||, d' in [0,1], |t| >= T1 := T0+1 (repair; T0 = exp(exp 1251.23), PowRegion.lean:360-365). N1 fork: zeta'/zeta PATH INTEGRAL, cut w = eta/l, Re-only; -log||zeta|| <= 0.5+[log(1/w)+1]+w*C_L*l/eta = (3/4)l+4logl+29.2, c' = e^{-29.2}. 3-4-1 anchor at 1+eta (eta = c*L^{-3/4}l^{-3}, c=1e-9, K=8104 GrowthPow.lean:977); Landau strip via (s-1)zeta pole patch on entire_norm_logDeriv_sub_sum_scaled (Landau.lean:35); w-validity 2eta<=vkTheta/8 iff l>=1.6e-5 vacuous. EXPECT C_L ~30 corpus-literal radii (additive, survives; repair).
S3 ZETA-ALLT (B 800): all-t c''(log(|t|+3))^{-3/4}(loglog(|t|+16))^{-4}; [3,T1] via compact-min discharge (repair; nonconstructive posture registered, eps0 in c3 ZetaZeroFree.lean:235); |t|<=3 Zc_patch (ZetaInvShallow.lean:104). Classical fill 1279 ABSOLUTE. Serves MR extreme-t entry regardless of staging (POLE-2: any theta<0.98).
S4 L-CHI-POW (C 4000-8000, split a/b/c): L_pow_lower q<=Q + compact companion; CARVE-OUT (repair): ZeroFree.lean:387 hyp chi.primitiveCharacter^2 != 1 — non-real ONLY; real-chi Siegel branch priced 5.175*log q (LANDED Goldfeld-lossy SiegelFinal.lean:324/440; coeff 5.175/125 = 0.041 << 1/4, slack ~6x; repair). GAP flag: Vk-block phase-parametricity = first execution check; resists -> rescope (D-risk, no cheap fallback).
S5 NONPRET-SPLIT (B/C 900) — THE A-ARM CARRIER (new statement, strengthens BOTH parents): forall Q>=1, EX x0(Q) C(Q), forall x>=x0, q<=Q, chi mod q, |t|<=Q*x: (1/4)*loglog x - C(Q) <= D(lambda,chi n^{it};x)^2. Heights |t|<=Qx GROUNDED (chowla.txt:212-218; chi(n)n^{2ix} at :278 shows x-grade needed). Level-A form is a corollary. Worst pattern t=Qx: anchor (Q=100, x=exp(exp 1400)) pow 281.8 >= 100, classical 111.2 >= 100.
S6 OPENERS (C+B 2000): S6a dirichlet_poly_l2_mvt (gallagher_pointwise Gallagher.lean:38 + analytic_LS AnalyticLS.lean:58) [=MR-A]; S6b turan_kubilius [=MR-C]. Route-shared.
S7 ARC (C 2500; exponents STAGED-GATED): bigXi_arc: xi in Xi_H (threshold eps^2/log H, CircleMethod.lean:36-45) => xi/H near a/q, q <= log^{B5}H; contrapositive Vinogradov (LS/Vaughan + ExpSum vdC); transcribe Tao pp.24-25 FIRST. [AMENDED 2026-07-26, JYH-ratified: the exit radius is the q-DEPENDENT Dirichlet form — ∃ a q, 0 < q ≤ arcDen B₅ H ∧ |α − a/q| ≤ arcDen B₅ H/(q·H) — replacing the flat arcRadius = arcDen/H, which is a factor q too wide for MRT §4's IBP (refuter kill, s9-freeze-0726.md ⟦REFUTER VERDICTS⟧). Implemented ADDITIVELY in BigXiArc.lean (tight forms primary, old NearRat forms derived via mono); exists_large_den_of_minor already concludes the tight radius. B₅'s numeric pin PENDING the U1/B₅ design block (B₅ > 8 required by the minor-arc arithmetic; the landed B₅ = 5 provenance line is wrong).]
S8 MR-CORE (CLASS D — DECLARED; NODE FREEZE FORBIDDEN until arXiv:1501.04585 staged; 8-20k): qualitative fixed-delta MR at lambda-chi, q<=W; skeleton [M]: Parseval -> S6a -> Ramare/S6b -> Halasz-grade pointwise (quantitative sourcing OPEN: MNT-III 23.15 qualitative, montgomery3.txt:3538) -> extreme-t via S3.
S9 MRT-A1 (C/D until 1503.05121 staged, 4000): major-arc reduction, [STRIKE RETRACTED same day 2026-07-26: the refuter pass (2 independent CONFIRMED-FATALs) killed the vacuity claim — θ is the distance to a LOW-denominator rational, not to any rational; ξ = 1 ∈ Ξ_H has exact denominator H ≫ W, so the subdivision (or MRT's (4.2) IBP with a q-dependent S7 radius) is LIVE and required; see s9-freeze-0726.md REFUTER VERDICTS + flags. The subdivision below stands as designed] FLAW-1 REPAIR BUILT IN: phase-freezing subdivision — blocks H' = eps^2*delta0*H/(20pi(log H)^{B5}), per-block drift 2pi|beta|H' <= delta0/10 ADDITIVE; qualitative S8 suffices; floor Hlo*eps^2*delta0/(20pi(log Hlo)^{B5}) >= H0^MR(delta0/4) [A2: delta0 here is the NEW c0-shrunk value; formula shape unchanged, re-instantiate at the smaller delta] one-sided in Hlo. NO multiplicative Abel absorption (refuted). Preserve forall-OUTSIDE-integral (MRTDoor.lean:98-102). W = log^5 H per dyadic X in [x/2w,2x], logMeasure glue (chowla.txt:631-643).
S10 W-CAP + BUILDER (B/C 1500). S10a regime_W_headroom_of_floor (1/125 arm, landable NOW): hPHheadroom (Regime.lean:116-117) => log X_min >= 2.77eps^2 Hhi - 1.39 >= log^625 Hhi once Hlo >= T(eps); T(eps) defined by 625*loglog h + log(1/(2.77eps^2)) + 3 <= log h (+3 slack repair). S10b chowlaRegime_exists_param_head (g:N->N->N): EX R, R.eps=eps AND Hlo0<=R.Hlo AND g R.Hhi R.omega <= R.x — rerun RegimeParam:380 Sec C; ALL x-constraints one-sided lower bounds (Regime.lean:73,75,88,103,116,133,136+ — verified). g = CHOICE-JOIN (FLAW-2 repair): max(numeral anchor w*ceil(exp(exp(max(8log^5 Hhi,1e24)))), choice-extracted x0^{S5}(log^5 Hhi), C-arm exp(exp(4(M0+C(log^5 Hhi)))), sup_{H<=Hhi} X0^{MR}, omega-arm if staging demands). Numerals = sanity anchors ONLY (cannot dominate opaque C). A-ARM DISCHARGE (the new obligation): S5 at Q := log^5 Hhi, quality M0(delta0) fixed. Reading-robust: quality-fixed [17] -> Hlo-FLOOR arm ((1/4)log Hhi beats ~26 loglog Hhi + opaque const; coherent with T(eps) ~ e^5500); quality>=W [17] -> g triple-exp arm. BOTH channels ship; staging selects. Acyclic: C(Q),x0(Q) depend only on Q; g only on (Hhi,omega).
S11 COMPOSE (B/C 600): head-builder rerun of SpineFinal.lean:294-323 at the Xi terminal; ORDER (repair): eps -> K (bigXi_bounded, PRE-regime) -> delta := c0*eps/(4K) < delta0 [A2] -> S9/S10 thresholds -> floor max(H0red,H0D3,H0xi,H0^MR,T(eps),H*_{A-arm}) -> head-builder -> S0 discharge. Terminal: consume the A1-ratified SPINE-BUDGET head log_chowla_two_budget_head (compute H0door(delta0), instantiate extraFloor := H0door(delta0) — dissolves the delta0-vs-floor circularity); the old t/g/hbudget1 residual is DEAD (F0: unsatisfiable at c0=1; catch #249). H0door grows by a constant factor in log H0door.

SCOPE DIFFS (catch #224): (1) door full->Xi (licensed :743-750; drops ~2M minor-arc Katai/BSZ from D-block; if JYH refuses S0, the forall-alpha route must STILL adopt S5-split + S10b — faithful-as-frozen compose has the A-arm hole). (2) S5 range/quality split (strengthens both parents). (3) engine swap minimal->faithful (coeff 1/8->1/4; fill 3.63e22->1279; T1 exp(exp e^50)->exp(exp 1251.23)+1; anchors re-based; verified by both faithful refuters). (4) NEW W<=A obligation declared and dual-channel discharged.

EST: dispatchable-now wave (S0 gated, S1-S6, S10a, S10b signature, S11 skeleton) ~10-12k lines; staged-gated: S7 exponents, S8, S9 statement pinning. Full judge draft: scratchpad/mr_gate_synthesis_freeze.md.

## GRAFTS

- Faithful S1-S4 analytic engine: zeta'/zeta path integral with cut w = eta/l, coefficient 1/4, classical fill 1279 absolute, c' = e^{-29.2}, T1 = T0+1 — replaces the 7/8-exponent machine (fill 3.63e22, T1 = exp(exp e^50)); both refuter-verified, this one strictly better on every downstream constant and serves MR extreme-t entry via the all-t S3
- Faithful R5 statement shape (heights |t| <= Qx grounded at chowla.txt:212-218, three-range max) — re-frozen as the S5 range/quality SPLIT so it can carry the A-arm at Q = log^5 Hhi with fixed quality M0(delta0)
- Faithful R8a headroom stone regime_W_headroom_of_floor discharging the (log X)^{1/125} arm from the EXISTING hPHheadroom field (Regime.lean:116-117), with the T(eps)+3 slack repair — spares the g-builder one arm and is landable now
- Faithful R9 delta-choice order: K from bigXi_bounded PRE-regime, delta := eps/(4K) < delta0 = eps/(2K), kills the delta/delta0 circularity on sight (S11)
- Faithful terminal statement shape log_chowla_two_unconditional_mod_budget (Xi-form): residual = SPINE-BUDGET verbatim, SpineFinal.lean:261-272 minus the door lines
- Faithful refuter repairs folded: compact-min [3,T1] fill in S3; ZeroFree.lean:387 real-chi carve-out (chi.primitiveCharacter^2 != 1) in S4; C_L ~30 corpus-literal expectation; corrected anchor numbers (A2 pow 281.8)

## OPEN RISKS

- [17] A-arm reading (MRT quality-fixed vs quality>=W) unresolved until arXiv:1503.05121 staged — synthesis is robust via the dual channel (Hlo-floor arm + g triple-exp choice-join arm, both shipped), but the realized floor shape and S9's line count swing on it; NAMED stage-gate check, plus the inner-vs-outer-heights omega-arm contingency
- S8 MR-CORE is CLASS D and unpriceable until arXiv:1501.04585 staged (8-20k band); quantitative-Halasz sourcing open (MNT-III Thm 23.15 verified qualitative, montgomery3.txt:3538); node freeze forbidden pre-staging — the ~750k D-block dispatch MUST NOT fire before both papers are staged and S7/S9 statements pinned
- S0 is the registered Fable spine-rewire and needs JYH sign-off before its dispatch (Fable workflow gate); the rest of the wave is route-shared and independent — but note: if JYH refuses the Xi-rewire, the full-door faithful compose is NOT a valid fallback as-frozen (A-arm hole); the forall-alpha route would still need S5-split + S10b and regrows the ~2M minor-arc Katai/BSZ block
- S4a chi-twist of the Vk growth ladder is the single biggest below-D execution risk (D-risk, no cheap fallback: Polya-Vinogradov gives t^Theta not log-grade); first execution check, rescope trigger declared
- Nonconstructive floors throughout (choice-join g, eps0 in c3, Siegel-ineffective C(Q), exists_nat_gt thresholds): terminal is asymptotic-only, kills any effective-x claim — registered posture
- C_L (~7 design, ~30 corpus-literal) and the Mertens constant (<=4.125 needed for the -6) are design-grade: additive-only with one-sided escapes, but execution audits required (chain-G additive is 5.00 EXACTLY, zero slack on the freeze's rounding)
- chowla.txt and montgomery3.txt live only in the session scratchpad, not the repo — copy to durable staging before S7/S8/S9 freezes or the grounding chain breaks
- SPINE-BUDGET residual (t/g/hbudget1) survives untouched and is NOT owed by this campaign — the terminal is mod-budget; full unconditional log-Chowla-2 still needs that separate node

## EXECUTOR NOTES

Dispatchable wave (in order, each: lake build green, no new warnings, #print axioms <= [propext, Classical.choice, Quot.sound]): S1 (Salt/MR/Dist.lean), S2 (Salt/MR/ZetaPowLower.lean), S3 (Salt/MR/ZetaLowerAllT.lean), S6a/S6b (Salt/MR/L2MVT.lean, Salt/MR/TuranKubilius.lean), S10a (Salt/MR/DoorDischarge.lean stone regime_W_headroom_of_floor), S5 (Salt/MR/NonPret.lean, the RANGE/QUALITY SPLIT statement — do NOT weaken to the level-A form), S4 split a/b/c (Salt/MR/LChiPow.lean; check Vk phase-parametricity FIRST and stop+flag if it resists), S10b signature + S11 skeleton. S0 ONLY after JYH sign-off (registered Fable spine-rewire): in-file Xi-variants at Theorem23Shell/SpineFinal/SpineClose/TowerDischarge depth (~550 lines; private helpers windowExpSum_norm_eq_dft, mutualInfo_window_comm' force in-file placement). FORBIDDEN pre-staging: S7 exponent freeze, S8 anything, S9 statement pinning. Dispatch executors with blueprint node names as subagent_type and model: \"opus\" (memory: executor model override). Work on the MR track branch, never main. Iron rules apply: no blueprint statement alterations, 3-attempt budget then flags.md. Judge draft with full grounding ledger: scratchpad/mr_gate_synthesis_freeze.md; parent drafts: mr_gate_design_faithful.md, mr_gate_minimal_freeze.md; all four refuter verdicts in the same scratchpad. Re-grounding done this session by the judge: chowla.txt:560-640/200-218/735-755, Theorem23Shell.lean shell+closer, MRTDoor.lean:104-157, RegimeParam.lean:370-395, Regime.lean:56-140. The W<=A arm finding (faithful's hole) should be appended to docs/blueprints/flags.md by the next Fable session with a pointer to this freeze.


A2 RATIFIED (JYH, 2026-07-19 ~09:20 PDT): the three delta0 pins above re-based to c0*eps/(2K) / c0*eps/(4K), c0 = cD3/(16*C); S11 consumes the forall-extraFloor head. S8 scope/depth untouched (qualitative fixed-delta thm_A1' grade unchanged). Source: docs/exploration/spine-budget-freeze.md AMENDMENTS A2.
