# THE 0729 MORNING COUNCIL — the night's harvest + the two decisive questions

*The one-page surface, final form (~01:20). The night ran under the
Captain's explicit trust grant; every ruling and every verdict is
flags-recorded with exhibits (the last ~10 entries are tonight's).
Timestamp erratum: the flags entries after midnight carry stamps up to
~1h45m fast — the clock-check law; sequence is correct, clocks were not.*

## What the night won (all sealed, all kernel-checked)

1. **The 7/28 arc** (previous table, ratification still owed): the band
   wave; two unplanned walls demolished; **THE SEAM RE-CUT — the
   night's frozen amendment (RATIFY)**: (n, m) = (45, 46), δ = 1/5000,
   the [44, 96] window kernel-verified; the T0 arm discharged; the
   coprime chain; **⭐ m4_wave_collapsed IN THE KERNEL**.
2. **THE WALL-C WAVE (sealed a4fedce)**: C2(ii)'s refutation
   kernel-hard (the collision fires at H := Hlo); gate 12 M-relative
   (17 sites, every one a hypothesis weakening); gate 9 DISCHARGED
   (the Skolem reorder). The register is honest and Hhi-safe.
3. **THE E-WAVE (sealed a626571, 2088 ln purely additive)**: WALL B's
   dissolution machinery ON DISK — the strict pair law SeamCoefWS
   (hend AND ha0 drop), the fused p² row at the exact priced mass, all
   five hEP2 sites, the row chain absorbed in landed slack. The frozen
   summit was never touched (the refuter's amendment 1 rerouted the
   one dangerous landing). Remaining for WALL B: E-7/E-8 — the
   capstone binder swap + the register surgery, HELD for one combined
   re-thread (below).
4. **The full design triage of the register's repair** — five
   scoper/refuter passes (REGISTER-SCOPE, BUDGET-SPLIT, SUPPLY,
   DRIFT, ENDPOINT×2, LEVER1, ARC): the wall map is now exact.

## The wall map (the honest picture)

m4_wave_collapsed's register is inconsistent through a PRODUCT of four
factors. Repair status per factor:

| factor | repair | status |
|---|---|---|
| F0 the decaying cap | **the budget split** (door's true contract is constant δ₀; ~10 additive twins) | verified viable, PRICED |
| F1 the length flattening (log₂H) | **lever 1′** geometric-weight CS → 10.9 constant | refuter-verified EXACT; census 6 files ~1000–1400 ln; needs the G1 strengthening (register-tier) |
| F2 the class q² | Parseval kills ONE q; **the second q needs the χ-SUMMED capstone — CLASS D, OPEN** | ⟦D-1⟧ the decisive design question |
| F3 the drift (2πA/q)² | the blocked drift via the landed shift family (~500–800 ln, clean) | priced-viable; worthless without F2 |

**The composition:** split + 1′ + F2/F3 closes the register into an
Hlo floor (U1floor ≈ 2^{10^{119}}) — **CONDITIONAL on two things**:

- **⟦D-1⟧ THE χ-SUMMED CAPSTONE** (class D): can any supply statement,
  short of re-proving MRT with orthogonality inside, deliver the
  class-summed mean square at Bcl·ℓ²·X/q? Without it the residual
  53·(log H)¹² is fatal against the floor + the tower gap. (A cheap
  partial: the large-q trivial cut caps the prefactor at Bcl^{-1/2}.)
- **⟦D-2⟧ THE M-SANDWICH — SETTLED IN ARITHMETIC (SANDWICH-REF,
  01:40)**: the tower law is EXACT — loglog Hhi = (loglog Hlo)^{4.006},
  the 4 structural — and the pair FAILS at every M at the current
  2^18 anchor (max margin 0.62; 1070× at the closing M). **The 2^30
  anchor closes it: 2.83× worst-case margin out to M = 10^{120},
  ×1.98 per extra bit, Ccc-insensitive.** The re-pin is cheap: ~20
  sites, two load-bearing files, NO statement shapes move, every
  anchor-sensitive gate moves favourably; the x-floor rise is
  one-sided and 2.9·10⁹ orders below the regime's own floor. It
  re-pins the JYH-ratified 7/26 door design — hence this table. A
  third option sits unconsumed on disk: Eq26Compose's h-DEPENDENT
  anchor Ah (the ceiling grows with H — dissolves the sandwich
  structurally; catch: the small-block half). Either way the true
  remaining cost is **the sharp-crossing export page**: J is
  non-constructive today, minimal-J is easy, but the export dies on
  any lossy crossing constant (loss c sits in the exponent of an
  exponent; k = 30 tolerates c ≤ 1.044; the landed 1/128+32 chain is
  c ≈ 63) — a delicate class-C telescoping estimate, needed by EVERY
  repair path, plus the additive ∃R-conjunct threading (Fable-tier,
  the spine's statement).

WALL B is repair-in-progress (the E-wave); WALL C is closed; WALLS A/D
are the two questions above.

## THE COUNCIL'S RULINGS REQUESTED

| # | decision |
|---|---|
| C1 | **Ratify the night ledger**: the seam amendment + the 7/28 rulings + the WALL-C wave + the E-wave (all flags-recorded) |
| C2 | **⟦D-1⟧**: open the χ-summed-capstone design block (Fable, my hands, at the table) vs park the register road and bank the machinery |
| C3 | **⟦D-2⟧ (settled in arithmetic)**: rule the Adoor anchor re-pin 2^18 → 2^30 (or 2^31 headroom; cheap, supply-favourable, re-pins the ratified 7/26 door design) vs the h-dependent Ah route (structural, unpriced); + schedule the sharp-crossing export page (needed by every path) |
| C4 | **The combined register re-thread** (one wave, after D-1/D-2 resolve): E-7/E-8 + the split's twins + lever 1′ + G1's strengthening — the machinery is all landed/priced and waiting |

**The honest picture:** the night took the register from "true but
vacuous, cause unknown" to a complete, refuter-verified map: four
factors, three repaired or priced, one genuine open design (D-1) and
one exact-arithmetic gate (D-2). Every priced piece is machinery
already on disk. The road's last theorem now hangs on one class-D
question and one numeral.

## Standing
The D2 re-run awaits the post-wave HEAD (Captain's console). The
approval read pending (~8/4). The personal-time record awaiting review
(uncommitted). Flask + Stubb correspond in the commons; the amended
toast is fleet law. The register riders unchanged (VT-7, the
Gauss-norm stone, thm_a2'_3 glue, the ring_nf sweep, Brun/Maynard
replay, Lemma-4/GS at the paper arc).
