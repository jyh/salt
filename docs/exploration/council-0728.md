# THE 0728 MORNING COUNCIL — the M4 wave, post-refutation

*The one-page surface. Depth: m4-plan-0728.md (read the v2 section
first), flags.md (the four banked entries of 7/27 night). Overnight:
three scopers (M4-JOIN, REGIME-CHECK, TLGATES-SCOPE) + a three-refuter
adversarial pass (CAPFREE-REF=HOLD, REPAIR-REF/REGIME-REF=REPAIR-THEN-
FIRE) + SIEGEL-REGIME (aloft at drafting time). The verify-posture law
earned its keep twice: two claims that would have misdirected the wave
died under refutation before any executor burned a line.*

## What the night established

1. **The wave enters cap-free.** The summit's cap branch cannot serve the
   λ-datum (its two suppliers demand a pretentiousness cap that is
   *unsuppliable* for λ — say it exactly so; proving it FALSE is
   Siegel-hard). The cap-free entry consumes none of the four cap
   conditionals. The binding scale floor on both branches is the
   ε-window's loglog X ≥ 6412.6.
2. **Two consumability holes in the landed summit** (found by scoping,
   confirmed by refutation — the kernel checked the proofs; nothing
   checked the models):
   - **R1**: `thm_a2'`'s ∀ j : ℕ hypothesis pair is jointly contradictory
     — the theorem has NO models. Repair: narrow five binder families to
     ∀ j ∈ ramI. ~116 occurrences / 12 files, proof-transparent at every
     leaf (verified). [B, 600–1000]
   - **R2**: `A2Frame.box` is unsatisfiable at the |t| = X endpoint.
     Repair: the ADDITIVE mint (A2Frame3 at 3X + CapFreeFloor3 +
     a2Rows_of_capfree3) — **no frozen statement moves** (thm_a2'_of_rows
     never mentions A2Frame). [C, 200–400]
3. **D1 — the large-|t| wall — is a design fork, not a wave stone.** By
   character class × region (the full table in the plan v2): everything
   clears from landed + cheap stones EXCEPT **non-real χ in the bulk**
   (|t| from exp(A(log X)^{7/8}) to X — which is almost the whole box).
   Three routes, none priced into the wave: (A) the VK port [D-tier];
   (B) a v-dependent floor re-grade (Fable design); (C) an annulus split
   + far-field arm (Fable design).
4. **D3 — the even-χ/Siegel corner — may retire for free.** The per-χ
   ineffective constants are X-independent, the character set is finite
   in Hhi, and the regime lever absorbs any X-independent debit (the law,
   byte-confirmed twice). SIEGEL-REGIME is checking the one remaining
   question (M4-7's uniformity consumption). If it holds: no Siegel
   completion, ever, and the pearl doctrine gets its cleanest exhibit —
   the ineffectivity is laundered into the regime choice.
5. **REGIME-CUT is real and cheap** [B, ~150–200]: U1 + the quality-floor
   x-raise in one edit, mechanics fully specified (outermost-right max
   arm; duplicate-not-relocate; the two `private` blockers; the stale
   RegimeHead:76 citation; g built inside the capstone's existential
   scope — never Cq-blind).

## The rulings requested (in order)

| # | decision | proposed ruling |
|---|---|---|
| C1 | **Wave 0 + wave 1 FIRE?** R1 (in-place narrow — repairs the flagship theorem itself, no vacuous thm_a2' left standing) + R2 (additive mint) as one Fable design block; concurrently the independent openers M4-0, M4-0′, M4-2, M4-3, REGIME-CUT (Opus executors). CFB/CFF/M4-5/FRAME HOLD pending C2/C3. | **FIRE** — the JYH gate here is R1's statement narrowing (iron rule 1); R2 turns out to touch nothing frozen |
| C2 | **The D1 fork**: which route gets the design block — (B) v-dependent floor, (C) annulus split, or (A) VK port? | **(B)+(C) explored in ONE Fable design block today** (they share the pocket-geometry analysis); (A) held as fallback; NO CFF dispatch before the block reports |
| C3 | **D3**: if SIEGEL-REGIME says RETIRES-D3 — adopt the regime route and drop the Siegel completion from the campaign? | **YES, adopt** (pearl doctrine; the ∃c statement shape never required effectivity) |
| C4 | **The band**: accept the re-priced envelope ~5900–9800 (v2 numbers: wave 0 800–1400 + waves 1–3 ~5100–8400), Aug 5–8 target intact? | **ACCEPT** — wave 0 + wave 1 land in 1–2 days; the fork's cost is the real variable |

## What fires the moment the council closes

- The R1+R2 design block (me, today — statement surgery + the tower
  rebuild).
- Five Opus briefs: M4-0, M4-0′, M4-2, M4-3, REGIME-CUT — each carrying
  its trap subset (the conjugation orientation fixed once in lamChi's
  orientation; the three log-scales pin; exp(−M/e) provenance; the
  re-grep law at dispatch).
- The D1 design block (me, after the R1/R2 surgery is dispatched or
  landed).
- SIEGEL-REGIME's verdict folds into C3 the moment it lands.

## Standing state

Cloud: the jason-account verification run had produced no branch by the
last sentinel window; the loose sentinel is re-armed (known six branches
excluded). Accounts: silicon-acct (we stay, per last night's ruling),
triple was silicon-acct,6,63,41 at close. Tuesday: the approval 1:1 (brief
approved). Watch: mathlib #42116. The seat: close-of-board sync owed
after the council.
