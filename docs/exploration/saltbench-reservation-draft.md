# SALTBENCH — the D-1 reservation list (DRAFT for JYH ratification)
## (maestro, 2026-07-25 10:49 PDT; the pre-arXiv obligation from the 7/25 design sitting)

*The policy (ratified): the flagship paper may describe the method and name
theorems freely; the PRE-STATE COMMITS and NODE BRIEFS of the ranges below
stay private until their N = 6-month rolling release. Ratifying this list
sets the wave boundary BEFORE the paper ships.*

## WAVE-1 RESERVED (the proposed first bench wave; release ≥ 6 months
## after salt's history goes public, whenever that is)

- **E-cells (executor replay, 5 nodes)**: (1) the L14 five-stone day —
  S-A/S-B/S-C/S-D/S-E from their pre-state commits (the 7/24 arc,
  b4f215d±); (2) T1_decay_annular_tailed (the byte-verbatim tail wire);
  (3) prop_A3_T1_row_moment (the B-pin coefficient-rescale node);
  (4) chi_floor_of_order (the k-th-power route); (5) renormalise (GS 7.1 —
  incl. the κ=2 relaxation sub-task). Each cell = pre-state checkout +
  the frozen statement + kernel verdict + the honesty column.
- **S-cells (two-sided scoper)**: the windmill arm — the kernel-ramp
  dissolution (windmill #7), the zones dissolution (#8), the Perron-D
  dissolution (#9), the GS-7.1 deflation (#12); the giant arm — the R2.4
  retraction case (the wrongly-dissolved GS 7.1) + the annHead object
  finding (the hsplit vacuity) + the ball_leg divergence. Ground truth =
  the ledger entries + flags, redacted to pre-verdict state.
- **D-cells (refuter)**: the seam-freeze V1→V2 pass (the socket/supply
  conflation catch); the DINITZ-REF pass (the Full-form widening catch);
  the five refuted design freezes of the pinned-assembly era.
- **F-cells (maestro)**: the B-pin decision context (the ball-vs-line
  object choice); the S-3 route-scoping decision w/ the ρ numeric.
- **V-cells (verification)**: the two transcription-defect tasks (the
  (X/h₁)/T weight; h^{δ/25}) as graded fidelity cells; the (A.8) sign
  defect; jacobian-verify + dinitz-verify as worked exemplars (already
  public — exemplars, not eval cells).

## STAYS PUBLIC-SAFE (the paper may cite freely)

Theorem names + statements; the audit counts; the windmill COUNT and the
pattern's description; the method prose; the ledger's existence and its
entry numbering (the paper already cites ledger #N — the ENTRIES cited in
the paper are thereby burned as bench material and are excluded above).

## THE RULE GOING FORWARD

Each future campaign's maestro designates its bench-reserved nodes at the
campaign's close (the rolling pipeline); a reserved node's pre-state commit
+ brief stay out of any public history until release. Salt's public-history
purge (the fleet map's hard gate) excises the reserved ranges' briefs.

**ASK (JYH): ratify the wave-1 list as drawn, or amend.**
