# CHI-CHECK — the stone-9 kill-checks (2026-07-24 night)

*Adversarial verification of door-road-0724.md's lever. BOTH GREEN, four
corrections owed, and one finding STRONGER than the lever:* ***no D-grade
node remains in the campaign.***

## KILL-CHECK 1 (the θ-sufficiency) — GREEN on the question, RED on the 0.976 numeral

- **The demand chain confirmed and the object named**: quality consumed at
  exactly one line (MRT p.15): M(gχ̄;X′) ≥ M(g;X,W) − O(1), (2.3) ⇒
  exp(−M)M ≪ W^{−5/2}. The object is **M(g;X,W) = inf over ALL χ mod q ≤ W
  and ALL |t| ≤ X** of 𝔻(g, χn^{it}; X)². Demand = 3·log W; the (3/125)loglog x
  figure is a correct CEILING but not our demand — at the door's pin
  (W = log⁵H, H₀ = exp(9·10³⁸)) the demand is 15·loglog H ≈ **1345.5, a pure
  number**, vs supply Θ(loglog x) = Θ(9·10³⁸). Margin ~10³⁴.
- **The supply rule (1−θ) confirmed from first principles**: 𝔻(λ,χn^{it};X)² =
  loglog X + log|L(1+1/logX+it,χ)| + O(1) + O(Σ_{p|q}1/p); a θ-power region
  ⇒ (1−θ)loglog X − 5·logloglog X − C(q) at the worst corner |t| ≍ X.
  θ=2/3→1/3 (MRT (1.12) ✓), θ=3/4→1/4 (matches the landed floors' exact shape ✓).
- **⚠ Corpus thinness**: pretDistSq is character-ready by f-instantiation
  (zero refactor), but NO χ-twisted distance surface exists; and **the
  𝔻 ↔ log‖L‖ identity is NOT landed even at q=1** — lambda_nonpret_of_bridge
  carries the λ-Euler bridge as a named hypothesis (flags: the prime
  partial-sum→full-sum bridge at σ=1, oscillating case — BLOCKED, the single
  residual). The χ-twisted bridge INHERITS this q=1 blocker — shared
  critical-path debt with S5, not new D-risk, but it belongs ON the ladder.
- **⚠ The "10.4×" ledger entry is the same computation, not independent
  confirmation.**
- **The q-aspect**: zero_free_region_all' landed (every χ≠1, c₀=1/126848,
  classical shape); the real-zero branch via siegel_zero_free_exceptional /
  siegel_L_one_exceptional. **⚠ The "5.175·log q" numeral has NO corpus
  witness or derivation** (grep hits only mr-freeze:14); the actual Lean gives
  ≤ (3+13.5(1−β₁))·log q + an OPAQUE INEFFECTIVE additive C(q₁,β₁) ≈
  3.7–4.7·log q. Conservative, conclusion survives; flag as design-grade.
  **⚠ W at the door is (loglog x)⁵, NOT (logloglog x)⁵** (the triple-log came
  from pass3_t2's different hierarchy); overservice is ONE iterated
  exponential, not two.
- **The three ledgers**: L1 generic corner ⇒ θ < 122/125 STRICT (equality
  fails on the logloglog slack); L2 with the Siegel debit SUBTRACTED ⇒
  θ < 0.9346 (θ=0.97 FAILS here); **L3 the door's actual regime (S10a
  headroom log x ≥ 2.77ε²H) ⇒ θ ≤ 1 − 4.1·10⁻³⁶ — ANY fixed θ < 1, margin
  ~10³⁴ at θ=0.97.** The road must cite S10a, not 1/125. The region IS
  genuinely required at |t| ≍ X (classical θ=1 gives coefficient 0).

## 🔑 THE BIGGER FINDING — stone 9 needs NO χ-power region at all (ROUTE A)

**(H1) |t| ≥ 1/(2·ord χ)**: take k = 2·ord(χ); f^k = χ₀·(λ^k=1-part),
g^k = n^{ikt}; the multiplicative pretentious triangle (landed
pretDist_triangle, full 1-bounded generality; the per-prime identity is EXACT
incl. p | q) gives 𝔻(λ,χn^{it};X)² ≥ (1/16W²)·[𝔻(1,n^{ikt};X)² − Σ_{p|q}1/p]
— discharged by the ALREADY-LANDED ζ-side dist_one_floor_pow at b = kt (no
upper cap). Demand met once loglog X ≥ 240(log H)^{10}loglog H — an x-floor,
and every regime constraint is one-sided lower ⇒ FREE. This is S10b's
"triple-exp arm" which mrt_extract §0 declared DEAD — **it is not dead; it is
the price of not building a χ-power region** (soften that declaration).
**(H2) |t| ≤ 1/(2·ord χ)**: log(q(|t|+2)) = O(loglog H) ⇒ θ=1 classical
suffices — served by the LANDED zero_free_region_all' + the Siegel branch.
Residual: the χ-Euler bridge at bounded height = the SAME stone as the q=1
blocker (shared debt).

**Rescope**: Route A [C, no D-risk, **1200–1800 ln**]: triangle clone 250–400
+ χ^{ord}=χ₀ bookkeeping 150–250 + Σ_{p|q}1/p 80 + H1 assembly 300 + H2
400–600 + the shared bridge + the S10b anchor bump [A]. Route B (the full
χ-power region at θ≈0.97) if ever wanted: **C not D, 4000–6500** — the
"phase-parametricity" D-risk resolves FAVOURABLY via residue-class splitting
(log(qm+r) = log q + log(m+r/q) is a shifted log phase; j-th derivative =
the ζ phase at effective length N/q; triangle over q classes costs NO q-loss;
Vk/Taylor's phi_taylor_block already takes a real base point; Salt/Vmvt's
5722 ln reused verbatim; the region+lower-bound legs are clones, simpler for
χ≠1 — no pole patch). **ROUTE A is the design block's primary.**

## KILL-CHECK 2 (the W^{1/4} ledger) — GREEN, mechanism corrected

- **φ(q₀) is never paid — it CANCELS**: orthogonality's 1/φ(q₀) against
  Σ_χ 1 = φ(q₀), because the per-χ bound is UNIFORM in χ — supplied by the
  quality floor being an infimum over ALL q ≤ W, χ (that is WHY Q = W). The
  cost actually paid is Σ_b = q ≤ W (triangle over residues) + the (4.2)
  drift factor. **⚠ door-road:92-94's mechanism sentence wrong twice**: MRT
  pays q not √q (a Gauss-sum route would be CHEAPER, failing instead on
  (n,q)>1 — hence the d₀-descent); and the failure mode of the naive route is
  using the composed L¹ grade (logH)^{−1+o(1)} as the per-χ input instead of
  the quality-driven W^{−5/4} (then W^{−5/4}·q ≤ W^{−1/4}).
- **The exponent bookkeeping reproduced exactly** to (4.1) ≪ HX/(dW^{1/4}) +
  HX/(d²W^{1/4}); the three A.2 terms each ≪ W^{−5/2}, the binding sub-term
  exp(−M)M at 10⁻⁵⁸¹ vs required 10⁻⁴⁸⁷. **Enrichment for mrt_extract:284**:
  1/125's SECOND structural provenance — (1/50)÷(5/2) = 1/125 is what makes
  the (logX')^{−1/50} term ≤ W^{−5/2}; the §4 proof uses this one.
- **The δ₀ ledger closes by construction, one-sided ✓**: H₀door is DEFINED by
  (logH)^{−5/4} = δ₀ ⇒ log H = exp(0.8·112.13) = 8.99·10³⁸ (the road's
  numeral reproduced); monotone ⇒ ∀H ≥ H₀ ✓; the extraFloor:=0 cross-check
  reproduces "~47 orders" ✓; W-window and P₁ < Q₁ checks pass with ≥10³⁶
  headroom. **⚠ implied-constant-free — quote exp(9·10³⁸) as a SHAPE**
  (C_MRT = 10⁶ shifts it to exp(1.1·10⁴⁴); harmless, one-sided).
  **Robustness**: even the full Prop-2.4 grade (if the ∀α guard ever forces
  it) closes at H₀ = exp(5.9·10⁵⁰) — the major-arc route buys ~12 orders,
  not existence.
- **B5 consistency**: Thm 2.3's window has a LOWER bound (logH)⁵ ≤ W — B5=5
  sits exactly ON it (if S7 delivers B5 < 5, inflate W to log⁵H — harmless;
  if B5 > 5, re-instantiate S10a's 625 = 125·B5). H > W^{203} ✓ at H₀door.
  Prop 5.1/5.3's W ≥ log²⁰H not on the path (Thm 2.3/Prop 2.4 only).

## Bottom line

**No D-grade node remains in the campaign.** Stone 9 = plain C on either
route; Route A (1200–1800) makes it among the CHEAPEST stones. The one item
the road missed: the χ-Euler bridge inherits the ALREADY-BLOCKED q=1 λ-Euler
bridge (the flags "single residual") — shared critical-path debt with S5;
put it on the ladder explicitly.

## ⟦CORRECTION (maestro, same night — EULER-SCOPE)⟧

The "shared critical-path debt" claim (:58, :106) is STALE: the q=1 λ-Euler
bridge CLOSED 2026-07-19 (`euler_osc_bridge_unconditional` + `lambda_nonpret`
unconditional; the flags "BLOCKED" entry was superseded in-file). Route A's H2
χ-Euler bridge re-prices to **~165–230 ln, all [B]** — mathlib holds the χ
log-Euler form (`DirichletCharacter.LSeries_eulerProduct_exp_log`), the peel
and tail stones are χ-free or trivial clones, and `euler_osc_truncation` is
already general-g (plug g = χ·costwist). Route A total drops accordingly.
