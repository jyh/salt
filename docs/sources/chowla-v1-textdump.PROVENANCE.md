# chowla-v1-textdump.txt — provenance stamp (sidecar; the .txt is BYTE-UNMODIFIED)
- sha256: 21ffaf5d00a7cdbebca486b85fbce5d1b8e5c6169e1230e12b02263248fba3ea
- What it is: an UNSTAMPED plain-text rendering of Tao, "The logarithmically averaged
  Chowla and Elliott conjectures for two-point correlations" — content-verified as the
  **v1** rendering of arXiv:1509.05422 by math's item-18 version axis (equation and
  reference numbering v1-exclusive; QUEUE item-18 record, tranche 6).
- Where it came from: .claude/worktrees/*/docs/sources/chowla.txt (three byte-identical
  copies, all untracked, all one `rm` from gone). mr_map_sources.md:39 named it "THE
  primary source" — part of the corpus was authored against THIS rendering.
- Ruling: Captain 2026-08-23 16:4x, "ratify all recommendations" (item-18 residue A) —
  executed with one gate-required adjustment: the pre-public-release gate (.gitignore:17)
  bars tracking external paper text in public salt, so this copy is LOCAL+gitignored like
  its PDF siblings; the TRACKED durable copy lives in the private seat repo (sources/).
- The stamp is a SIDECAR so the .txt stays byte-identical to what the corpus cites.
