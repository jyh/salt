> Provenance: the merged verdict of the 2026-08-17 pre-submission priority re-run, copied from the
> private campaign register into salt for public citation, following the same convention as
> `priority-survey-2026-08-11.md` (the baseline this delta is measured against). The merged verdict
> is preserved whole and unedited, with one declared exception: the closing line's machine-local
> working-artifact paths are replaced by this header's in-repo pointer. The five per-lane working
> reports remain in the campaign register. Method: five independent read-not-grep lanes + merge;
> two verdicts rest on our own local kernel checks (`lake build` + `#print axioms`) of the
> competitor's public bytes, not on their claims. Cited by papers/flagship/main.tex; the survey
> re-runs again at submission (eval-board surface on a cadence of days).

# PRIORITY AUDIT — DELTA VERDICT (state as of 2026-08-17; baseline: docs/priority-survey-2026-08-11.md)

Five lanes merged: lane-bv (rows 1/2/7/8, two local kernel builds), lane-primegaps (rows 2/10/11), lane-mathinc (rows 5/9 + decoys), lane-institutional (rows 3/4/6/12 + PNT+/AFP/eval), lane-open-web (scale meta-claim, owed searches, arXiv/press). Two verdicts in this table rest on OUR OWN `lake env lean` kernel checks of the competitor's public bytes, not their claims.

**FOUR VERDICTS CHANGED: rows 2, 6, 7, 9. Two claims FELL (7, 9). One fell retroactively — row 7 was already false at the 08-11 stamp.**

## 1. THE DELTA TABLE

| # | Claim | 08-11 → 08-17 | Movement + decisive evidence | URL |
|---|---|---|---|---|
| 1 | Siegel–Walfisz | **STANDS → STANDS** | No movement. `axiom siegel_walfisz` present on ALL 4 branches of the canonical repo (main HEAD 18569f20); README unchanged ("would be a larger project"); claude-opus's `SiegelWalfisz.lean` is a 59-line consumer of the axiom, not a proof. Corroboration widened: atlas-lean's SW ends `by sorry` (Meta, dormant); erdos-696 line 67 `axiom siegel_walfisz`; gersh's is a self-described scaffold. SW is now the SINGLE wall (see row 2). | github.com/amellendijk/lean-bombieri-vinogradov/blob/main/BV/Axioms.lean |
| 2 | Unconditional BV → bounded gaps | **STANDS → THREATENED — VERDICT CHANGED** | The survey's cited evidence (MainResults `sorry`) is DEAD on the public **claude-opus** branch (pushed 2026-08-12, post-survey): `theorem bombieri_vinogradov` proved with explicit constant. **Our kernel check** (local build, `#print axioms`): depends on `[C_LS, C_SW, large_sieve, propext, siegel_walfisz, Classical.choice, Quot.sound]` — no sorryAx. Large sieve is discharged on the sibling branch (row 7); merging the two leaves SW as the only wall. The survey's trigger ("the day its two axioms discharge, claims 1/2/7 all move at once") is HALF-FIRED. Salt's "unconditional BV-grade level" survives only because SW is unproved everywhere; PrimeGapsLib's own formalization.yaml still declares BV an unproven literature dependency on all three main results (corroboration from the consumer side). | github.com/amellendijk/lean-bombieri-vinogradov/tree/claude-opus |
| 3 | Chen I + Chen–Goldbach | **STANDS → STANDS** | No movement. Eval board `chen_theorem` "Not yet solved"; cross-checked at source — all 42 `results/*.json` in lean-eval-submissions (HEAD 7077536, 08-17) swept, zero solvers. Citation caution: the board's solved "Chen theorem for Markoff graphs" is W. Chen — name collision, not our row. | lean-lang.org/eval/problems/chen_theorem/ |
| 4 | Rosser–Iwaniec linear sieve | **STANDS → STANDS** (fence intact) | No movement. `SelbergSieve.lean` still exactly 405 lines, every headline one-directional, Λ² upper only; last substantive change 2026-06-11; no new sieve files in NumberTheory walks; `"Rosser-Iwaniec"`/`"linear sieve"` language:lean = 0 with `SelbergSieve` as passing positive control. Never-"first-sieve" fence remains mandatory. | github.com/leanprover-community/mathlib4/blob/master/Mathlib/NumberTheory/SelbergSieve.lean |
| 5 | VK 3/4-power ZFR | **THREATENED → THREATENED** | No movement. math-inc org has pushed NOTHING since 2026-04-16 (all 10 repos' dates read); strongpnt bytes re-read — still classical dlVP (`1 - A/log(|t|+2)` at line 5153). The survey's OWED camelCase `ZeroFreeRegion` search now closed: 4 hits, none VK. `Korobov language:lean` = 0 on two spellings. smccolm's new ZFR is also classical dlVP — no row-5 impact. Fence unchanged: never "first machine-checked zero-free region" (occupied since Sept 2025). | github.com/math-inc/strongpnt |
| 6 | Vinogradov Mean Value Theorem | **STANDS → THREATENED — VERDICT CHANGED** | Event dated 2026-08-16, found independently by two lanes: lean-lang.org eval marks `vinogradov_mean_value` **SOLVED by @ZhengyangZhang06 with Humanifa + GPT 5.6, Aug 16 2026**. Statement read at the exact judged commit (lean-eval @ 8cdf39e): genuine full BDG/Wooley main-conjecture strength (real Diophantine counting quantity, arbitrary s, k). Verification grade: leanprover/comparator (same-statement, axioms in the standard three, kernel-accepted). The SOURCE is private (`submission_public: false`, 404 repo), publication announced **2026-08-31** — almost certainly before Pi review completes. "No public artifact" is literally true today but one click from a dated kernel-grade counter-attestation. | lean-lang.org/eval/problems/vinogradov_mean_value/ |
| 7 | Large sieve inequality + BDH | **THREATENED → FALLEN — VERDICT CHANGED, retroactively false at the 08-11 stamp** | PR #1 "Lean proof of the large sieve inequality" (dkuthoore, Devin-agent branch) was PUBLIC from **2026-07-09**, merged 08-04. On the `large-sieve` branch: `theorem large_sieve` proved (formerly the axiom), `C_LS = 8π²+2`, Gallagher-1967 route, ~3,600 lines, zero live sorries. **Our kernel check**: `[propext, Classical.choice, Quot.sound]` — completely clean. Their PROGRESS.md asserts "never formalized in any proof assistant." Both salt public dates (Nature 08-14, flip 08-16) POSTDATE 07-09: on public evidence, the first large-sieve inequality belongs to this PR. Statement scope: primitive characters (the all-characters form is false — their issue #4). Sub-check zeta-23: no movement past MVDiag 13; its companion paper is now arXiv 2608.13637 (08-13, Alpöge & Furman) + Forbes press — the MV-Hilbert footprint hardened. | github.com/amellendijk/lean-bombieri-vinogradov/pull/1 |
| 8 | Vaughan's identity | **FALLEN → FALLEN** | No movement. `Lambda_decomp` still sorry-free at BV/Defs.lean:368 on main (re-read at bytes); gersh untouched since 07-27. Strike-from-firsts ruling permanent. | github.com/amellendijk/lean-bombieri-vinogradov/blob/main/BV/Defs.lean |
| 9 | Kloosterman Weil 2√p via Stepanov | **THREATENED → FALLEN — VERDICT CHANGED** | The 08-11 residue (Kloosterman-specific object) is now public. NEW ENTRANT **smccolm/Lean** (created 08-02, agent-driven): `kloostermanSumZMod_prime_weil : ‖S‖ ≤ 2√p` (incl. char 2) + full composite Weil–Estermann bound + genuine sum defs, method explicitly Stepanov (Harcos's self-contained version) — the exact object AND method of salt's claim. Cleanliness verified by read: 0 sorry, 0 axiom decls, 0 native_decide tree-wide; the landing commit's CI log shows the three Kloosterman modules built on the public runner 2026-08-14T06:47 UTC. Landed **2026-08-14 06:43 UTC — same-day as salt's Nature timestamp, 2 days before the flip**. Load-bearing (consumed by their DFI eq-(25) files). math-inc/RiemannHypothesisCurves remains frozen (artifact date corrected to 2026-01-16); HautevilleHouse decoys DELETED, not replaced. | github.com/smccolm/Lean |
| 10 | Maynard–Tao bundled / ≤12-EH residue | **FALLEN as bundled → FALLEN as bundled; residue STANDS, strengthened** | Delta = 3 commits, all 08-17, all yaml/CI — **zero Lean changes, nothing below 246, no new records**. The residue is now re-confirmed from the competitor's own machine-readable declaration: their formalization.yaml lists exactly three main results, each with `literature_dependencies: "The Bombieri-Vinogradov Theorem"` — BV-conditional by self-declaration; salt's ≤12 is EH-strength. ANOMALY: the current repo object was created 2026-08-14 (the 08-08 repo was replaced); our 08-11 survey record is eyewitness FOR their earlier date — do not use the recreation against them; symmetrically their 08-08 has the same evidentiary shape as salt's pre-flip dates. PHRASING WATCH: the 156ae320 edit demoted conditionality from `relationship:` to a `source_note:` — registries rendering only `relationship:` will display them as unqualified. | github.com/AxiomMath/PrimeGapsLib (formalization.yaml @ 156ae320) |
| 11 | Parity wall / twin-bar as kernel objects | **STANDS → STANDS** (no "first") | Zero commits anywhere since 08-11 (master tip 2026-05-29; all 58 branches enumerated; both post-master branches diffed and read). The §7 "parity barrier (heuristic; not formalized)" block is byte-identical; the salt-shaped reformulation was NOT attempted. Salt remains alone in the reference class; the no-"first" phrasing fence stands. | github.com/gotrevor/bounded_gaps |
| 12 | MRT/Halász; log-Chowla-2 | **STANDS → STANDS** | No movement. `Halasz` = 0 (control passed); Radziwiłł hits are author-citation collisions; one NEW statement-only file — ImperialCollegeLondon/AnnalsChallenge's Sawin–Shusterman Chowla/twin-prime over F_q[T], sorries at lines 128/148, statements not proofs; its eval row unsolved (all 42 results files). mathlib walks clean. | github.com/AlexKontorovich/PrimeNumberTheoremAnd/wiki/Terence-Tao's-personal-log |

## 2. CONFLICTS BETWEEN LANES — RESOLVED READ-BEATS-GREP

Unlike 08-11 (6 of 12 contested), this cycle has **no unresolved factual contradiction**; every CHANGED verdict is grounded in bytes read, two with local kernel checks. The frictions and their resolutions:

1. **Row 2**: lane-primegaps reported "STANDS, strengthened" while lane-bv reported "treat as THREATENED." Not a contradiction — different surfaces. lane-primegaps read only the consumer side (PrimeGapsLib's yaml declaring BV unproven); lane-bv built the producer's claude-opus branch and kernel-checked `bombieri_vinogradov` proved-modulo-axioms. The kernel read of the canonical repo governs: **THREATENED**, with the consumer-side declaration retained as corroboration that unconditional BV still exists nowhere.
2. **VMVT search counts**: three lanes ran nominally overlapping code searches and got 0, 0, and 11 hits (`VinogradovMeanValue`). The GitHub code-search index is demonstrably inconsistent — lane-bv proved it blind to the canonical BV repo (`bombieri_vinogradov language:Lean` = 0 against a repo containing the string). All zeros carry no evidential weight; the row-6 verdict rests entirely on page/file reads (eval board bytes + benchmark statement at the judged commit), where both reading lanes agree exactly.
3. **Row 7 vs the 08-11 table itself**: a lane-vs-prior-survey conflict. The 08-11 verdict "the inequality itself is unproved everywhere" was false when written — PR #1 had been public 33 days. Resolved by lane-bv's clone-build-and-`#print axioms` of the public bytes. The 08-11 survey's own lesson ("absence-by-grep lost every time it was tested") claimed one of its own rows this cycle.
4. **Kloosterman greps**: `Kloosterman language:lean` = 0 while `extension:lean` = 125 with a genuine kernel-built Weil bound among them — same index pathology; the row-9 faller was found by clone-and-read, and would have been invisible to the survey's original search grammar.

## 3. CROSS-CUTTING ITEMS RE-EVALUATED

### (a) "First on a private tree" — re-cut for the post-flip world

The 08-11 defect ("a private repository cannot establish priority against a public artifact") is now bounded, not gone. Salt is public since **2026-08-16** with a consolidated history; the earliest third-party-verifiable date is the **Nature submission timestamp 2026-08-14**. Pre-flip internal dates (2026-07-*) remain third-party-unverifiable — lane-primegaps stated the symmetry precisely: PrimeGapsLib's recreated repo gives their 08-08 "the same evidentiary shape as our pre-flip dates." Neither side may use the other's consolidation against them; neither side's pre-consolidation dates establish public priority.

**The honest template for every surviving claim**: "machine-checked in salt (public 2026-08-16; Nature submission timestamp 2026-08-14); as of 2026-08-17 no public artifact in any proof assistant proves X." Per row:

- **SW (1), Chen (3), linear sieve (4), MRT/Halász/Chowla (12)**: assertable in template form, with the row-4 never-"first-sieve" fence.
- **BV (2)**: "no public artifact proves *unconditional* BV; the canonical Lean BV project proves BV modulo a declared Siegel–Walfisz axiom statement and says so in its own source." The old plural ("takes SW *and the large sieve* as axioms") is dead — large sieve is discharged on their public branch.
- **VK ZFR (5)**: template form for the narrow VK claim only; the exponent-convention flag (salt's "3/4-power" vs the standard 2/3 in the VK exponent) remains OPEN — no lane addressed it; a referee reads an undefined convention as an error.
- **VMVT (6)**: "no public artifact as of 2026-08-17; the lean-eval board records a comparator-verified private solution dated 2026-08-16, publication announced 2026-08-31." Never unqualified again. Note the symmetry: their instrument (third-party-verified claim, private tree) is exactly what salt's was pre-flip.
- **Large sieve (7)**: STRUCK from every absence/firsts sentence, everywhere, permanently. On public evidence priority belongs to PR #1 (2026-07-09). Honest form: independent formalization; if compared, note their theorem is primitive-character-scoped via Gallagher.
- **Vaughan (8)**: struck (unchanged ruling).
- **Kloosterman (9)**: STRUCK from firsts. Honest form per the lane: "independent; salt's proof dated 2026-07 on a tree private until 2026-08-16, Nature timestamp 2026-08-14; a public independent artifact (smccolm/Lean) landed 2026-08-14 06:43 UTC, same-day." Any "as of <date>" no-public-Kloosterman-Weil sentence is true only for dates strictly before 2026-08-14.
- **≤12/EH (10)** and **parity/twin-bar (11)**: residues stand in template form; no "first" on 11; on 10, cite their yaml's BV-conditionality (machine-readable, self-declared).

### (b) The scale meta-claim — FALLEN AS A NUMBER, twice over

The 08-11 ruling ("restate domain-specific, never raw line count") is now doubly mandatory. EconCSLib is **1,417,485 lines** at HEAD 633a868 (08-17) — it outran its own paper's 986,391, so citing the arXiv figure as current is stale in BOTH directions. A second ~million-line corpus appeared inside the window: Qiuzhen-CFSG/CFSG ("FormaTheoria," arXiv 2608.10894, 08-11; 988,089 lines counted at clone, sorry-free outside deliberate comparator stubs, authors incl. Shing-Tung Yau). "Largest formal *analytic number theory* corpus" survives untouched; any raw-line-count sentence is falsified twice.

### (c) Differentiator erosion + landscape addenda

- Differentiator (a) "one-human + consumer-subscription configuration" is **no longer unique in kind**: Mellendijk's own formalization.yaml declares Claude Code + "$20 Claude PRO subscription." Scale and the fabricated-silicon vertical remain the differentiators; the epistemics/economics documentation remains uncontested.
- **Brun landmine**: the eval board shows `brun_constant_converges` solved by SIX systems, earliest 2026-06-22 (all pre-survey, absent from the 08-11 table; no readable public artifact today). "No machine-checked Brun's theorem" must NEVER be asserted — this touches salt's own Brun track.
- **A new claims surface exists**: the mathlib-initiative `formalization.yaml` registry (Palomar schema; zeta-23, amellendijk, PrimeGapsLib all registered within the window). `relationship:` fields there will function as public priority assertions. Recommend evaluating registering salt.
- **Eval-board cadence**: the Humanifa + GPT 5.6 solver is mass-sweeping the board with private-until-published sources. Re-check cadence for that surface: **days, not weeks**, through Pi submission.
- Watch list: amellendijk branch merge (would leave SW the single wall on a public unconditional-BV push), Humanifa VMVT publication 08-31, smccolm/Lean (live Guth–Maynard/DFI program), RexHannes/twin-prime-proof-search (self-declared non-proof, but the name), ImperialCollegeLondon/AnnalsChallenge (created 08-13, statements-only).
- Declared coverage gap (both relevant lanes): Lean Zulip — public mirror stale since late Feb 2026, live instance auth-gated. This is a GAP, not a clean absence.

## 4. THE TWO BOOKKEEPING FIXES FROM 08-11

- **(a) Repo-handle pin — RESOLVED this cycle.** Canonical handle pinned at the API: **amellendijk/lean-bombieri-vinogradov** (`full_name`, repo id 1092520002); FLDutchmann URLs 301-redirect today. All citations in this delta use the canonical handle. STILL OWED: the 08-11 table's URLs and any main.tex citation still carry `FLDutchmann/...` — a rename away from 404; edit at next docs touch.
- **(b) AFP `PNT_with_Remainder` artifact date 2024-05-05 (vs "Song–Yao 2025") — STILL OWED.** No lane re-verified or applied it this cycle; carry it forward as an open docs edit wherever the brief's phrasing survives.

## 5. SUBMISSION-BLOCKING — changes required in main.tex before the Pi submission

The headline sentence and every derived claim must be reissued. Blocking items, in severity order:

1. **Strike the large sieve inequality from the headline absence sentence and every firsts list** (row 7 FALLEN; the clause was false even at the sentence's own 2026-08-11 stamp — public PR since 07-09, now kernel-clean by our own check). Restate as independent formalization only.
2. **Strike / re-date the Kloosterman–Weil clause** (row 9 FALLEN 2026-08-14, same-day as the Nature timestamp). Any absence claim is valid only strictly before 2026-08-14; use the "independent, same-day" form.
3. **Qualify the VMVT clause** (row 6 THREATENED): "as of <date>; the lean-eval board records a comparator-verified private solution dated 2026-08-16, publication announced 2026-08-31" — or strike. The unqualified clause will be publicly falsifiable before review completes.
4. **Fix the "two live external formalizations take SW *and the large sieve* as axioms" sentence** → "take Siegel–Walfisz as an axiom" (large sieve discharged on the public branch; BV proved modulo axioms there — row 2 THREATENED).
5. **Replace all "private until this publication" priority framing** with the post-flip form: public 2026-08-16, Nature submission timestamp 2026-08-14 as the earliest third-party date; pre-flip internal dates presented as internal, never as public priority.
6. **Scale claim**: remove any raw line-count comparison (986,391 is stale; two ~million-line corpora exist); domain-specific form only.
7. **Differentiator (a)**: soften any uniqueness claim for "one-human + consumer-subscription" (Mellendijk's yaml is a public counterexample in kind); keep scale + vertical + epistemics.
8. **If main.tex or Brun-track prose anywhere implies no machine-checked Brun's theorem, strike it** (six eval-board solvers since 2026-06-22).
9. **VK exponent convention** (3/4 vs standard 2/3): define the convention explicitly or restate — open since 08-11, unaddressed, referee-facing.
10. **Citation hygiene**: repoint FLDutchmann → amellendijk URLs (item 4a); stamp every absence claim "as of 2026-08-17" and re-run the eval-board surface within days of actual submission.

Baseline document: docs/priority-survey-2026-08-11.md (this repository). Lane working artifacts: the private campaign register.
