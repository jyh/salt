# THE ARC COMMISSION — blueprint v2 (post-refuter)

**Chartered by the Captain at council 2026-08-25** (QUEUE.md:893-897, verbatim: *"THE ARC IS
NOT DROPPED — IT IS CHARTERED AS ITS OWN COMMISSION TODAY. A Fable design block writes its
blueprint from gate-verified statements; math's waves execute the A/B nodes it classifies."*).
Author: the helm (Fable), 2026-08-25, from six structured surface-reads. **v2 folds a 6-refuter
adversarial pass (REPAIR-THEN-FIRE ×6, incl. two CONFIRMED-FATAL — the pad flip in §1 and the
A.1 truth defect in §5/H-1); verdicts at the run record `wf_44c71c16-f51`.** Statement changes
are Captain/Fable-tier; node volatile fields follow the standard blueprint workflow.

## 0 · What this commission is, in one paragraph

Item 15 (the MRT port) SEALED on `m4_exit_socket_split_sq_trivial`
(`Salt/MR/MRTPortTrivialSplit.lean:48`, rooted, `[3 axioms]`) — a landed conditional whose
antecedent is **the named α gap** and whose consequent is `¬ logChowla2Fails` at the socket's
∃-chosen regime (Tao (2.4) at the Liouville model, `ChowlaFailure.lean:59`). This commission's
object is the antecedent side: **supply the arc**. Its inheritance is three named gaps
(QUEUE.md:866-870, :970-975): **GAP α** (the door datum is PHASED — `doorCoeffPhase c α` —
where A.1 has no frequency), **GAP A.1** (`MRTThmA1` is a `def … : Prop` with **no producer**
— and see H-1: the def as transcribed is FALSE as stated and must be repaired before any
producer campaign), and **GAP X** (the output-side scale quantifier: conclusions are delivered
at an `∃ R`-chosen regime — unboundedly many scales, NOT all large scales; mechanism at
register:122-132). GAP SIEVE was deleted by D9 **on the soundness axis only** — its economics
are D-1's open question. Nothing in this commission is unconditional until all three gaps
close; every wave carries that sentence.

## 1 · The consumption pad (landed) and the one-way chain above it

- **THE PAD — door currency, PAD A.** The antecedent of `m4_exit_socket_split_sq_trivial`,
  sed'd from `MRTPortTrivialSplit.lean:55-58`:
  `∀ H : ℕ, ∀ [NeZero H], R.Hlo ≤ H → H ≤ R.Hhi → ∀ α : ℝ, NearRatTight (arcDen 12 H) H α →
  (∫ n, ‖absWindowSum lamCoeff H n α‖ ^ 2 ∂(logMeasure R.x R.ω)) ≤ c * (H : ℝ) ^ 2`
  with `c` ∃-bound at :49. Producing this byte-string discharges the seal's ONLY hypothesis;
  the conclusion it yields is `¬ logChowla2Fails R.eps R.x R.ω` **at the ∃-chosen R**
  (GAP X open), and nothing is unconditional. The producer's lever: the `∀ (U1floor) (g)`
  binder (`MRTPortTrivialSplit.lean:50`) lets the supply demand `R.x`, `R.Hlo` arbitrarily
  large — E4 spends the same lever at `MRTPortExitLamGated.lean:87-97`.
- **The row currency (PAD B) is STRICTLY UPSTREAM, not an alternative.**
  `M4RowMeanSqLam R (doorCount R.ω) MS` (`MRTPortRowLam.lean:49`) plus the sed'd pricing line
  `∀ H : ℕ, R.Hlo ≤ H → H ≤ R.Hhi → 6 * MS H ≤ c` (`MRTPortExitLamGated.lean:78`; note `MS`
  is `∀`-bound at :77 — the producer picks it) feed E4. **The chain is ONE-WAY: PAD B ⇒
  PAD A** via `m4_doorSq_of_rowMeanSqLam` (`MRTPortRowLam.lean:59`, modulo the three ladder
  gates and the `4·2^k·H/x / Z` tail, absorbed by E4). **The converse does not exist:**
  `M4RowMeanSqLam` has ZERO producers (9-occurrence census, all def/binder/docstring/audit)
  and no DOOR→ROW lemma is landed; L¹⇒L² is false, not unproved.
- **Blueprint choice: PAD A** — it is the seal the Captain's ruling names (QUEUE.md:888-889)
  and it is what Wave 1's lane A produces directly: W2's conclusion is byte-identical to
  PAD A's antecedent (frozen brief :156-158, the brief's own :165), and W3 composes into
  PAD A's consumer. PAD B stays named as the stronger upstream currency, reachable only
  through a row-side supply this blueprint does not route.

## 2 · D-1, the road fork — THE COMMISSION'S FIRST DESIGN DECISION (Captain-tier, apparatus attached)

**The fork D9 left unpriced** (register:20-25, the gate's verbatim *"I have NOT priced which
side is cheaper"*): supply the arc **unsieved at λ** (the trivial split's demand: full
unsieved major-arc strength on `lamCoeff`) **vs** supply it **sieved** (MRT's own route:
less analysis, but the register drags `M4DoorGates` + `M4SievedDoorSq` + the class price +
the `q²` drift). The sieved road's pad is a third object this file deliberately does not
freeze: the four supply legs of `m4_exit_socket_split_sq_arc` at a non-trivial split
(`S11ExitL2.lean:121-140`) — the pricing wave states it if that road is priced viable.
Two facts move the price, carried with their fences: (i) `M4ClassPrice.lean` — 1,114 lines,
**41 theorems + 5 private lemmas, ZERO sorry** (re-measured at tip; QUEUE:196 recorded 42 on
08/23) — answers the class pricing **in the kernel**; QUEUE:196's own fences ride along:
it serves the UNPRIMED residue (`M4RowMeanSq_L` zero mentions) and it is **NOT claimed that
the descent is routine**. (ii) The sieved row predicate `M4RowMeanSq` has **no producer in
either lane** (QUEUE:196) — neither road has a supply today.

**Decision apparatus (spike standing-law, decision-spike form; the rule is TOTAL):**
- *Unit:* per road: class-D node-count, class-C node-count, and landed-lemma coverage,
  measured by the pricing wave. *Baseline:* the frontier map in §4 (v2 of the Wave-A
  critic's map — this file's own, so the baseline names its blind spots).
- *Rule (total by construction):* **(a)** if exactly one road prices at ≤1 class-D node AND
  no class-C node outside a landed template's reach, recommend that road; **(b)** if both
  do, recommend the one with fewer C-nodes (tie → unsieved, the shorter statement);
  **(c) in every other case — including any road with C-nodes of unknown reach — the
  verdict is "sequence Wave 1 lanes behind W0's report and re-price after"; this is the
  DEFAULT and reachable from every input.**
- *Timebox:* one pricing wave (readers + gate, no executors). *Kill:* a pricing wave that
  starts DESIGNING a road has left its recon mandate — stop and escalate.
- *Who rules:* the Captain, on the wave's report. **This blueprint does not assume the
  trivial split is cheapest — assuming that is assuming what D9 explicitly did not settle.**

## 3 · WAVE 1 — the α-eliminator (pre-built, frozen, adopted with its delta stated)

**Adopted from `seat/briefs/2026-08-25-item15-WAVE-A-ARC-IN-SCOPE-FROZEN.md`** — six frozen
tier-B statements, two-lane structure, kill conditions, name-collision table, do-not-claim
list, all verbatim (statements SED'D from the brief, never retyped).
**ADOPTION DELTA (refuter R3):** the brief's :5 firing condition has TWO conjuncts.
(a) *"the arc is a proving task"* — satisfied by QUEUE.md:893 (the commission charter).
(b) *"Ratified route = … the δ₀ SPLIT road"* — **NOT inherited: it is precisely what D-1
reopens.** Wave 1 is stated at `lamCoeff` throughout and W3 consumes the trivial-split
socket; **it is an investment in the UNSIEVED road only** (brief :269 — "false about other
roads by default"). Its only road-neutral node is W4 (stated at a general `a`), and W4 sits
behind W0. **Dispatching lane A before D-1 rules is a priced 3-node bet on the unsieved
side** — taken only at the Captain's word or after D-1's pricing report (§7).
- Lanes: **A = W1→W2→W3** strictly sequential (the BLOCKED α-eliminator; each node's
  `hgrade` carries the absolute `4 * (1 + 2 * Real.pi) ^ 2 * Bblk H ≤ c` — brief :135/:154/
  :177); **B = W4/W5→W6** (W5 strictly before W6; W4 independent, consumed at the same
  place). One file per node.
- **W0 — THE BINDING PRE-DISPATCH GATE (critic defect 4, unchanged):** before lane B
  dispatches, read `M4BridgeResidue.lean:383, :407, :424` and report whether the LOSSLESS
  landed route supersedes the χ-SUMMED route. W0's report goes to the helm first.
- **Deliverable, sed'd from brief :252 (the paraphrase the refuters caught is struck):**
  *"a REDUCTION of the split road's residual, in one direction only: from 'for every
  tight-major α' to 'for every EXACT RATIONAL b/q with q ≤ arcDen 12 H = (log H)^12, in L²
  over the base, at block length `blockLen H q`'"* — the wave **relocates** the open object
  from a frequency-indexed door integral to an exact-rational **blocked** one
  (`blockSupSq`, an integrated sum of squared sub-window sups — a strictly stronger object
  than the window L² it replaces); it does not shrink it. And do-not-claim #4 rides: the
  `4(1+2π)²` is absolute but the grade `4(1+2π)²·Bblk H ≤ c` is **unpriced** — `δ₀` and `K`
  are unpinned; composability was checked, satisfiability was not. **Wave 1 does not close
  GAP α and must never be reported as closing it.**
- Elaboration risk priced: the brief is READ-ONLY; the six tiers are estimates; a B that
  turns out gate-heavy is treated as C — a finding, not a failure.

## 4 · The frontier after Wave 1 (named, classified, NOT scheduled)

- **F-1 [D] The χ-summed supply at λ** — the analytic core of GAP α: bound `∑_χ mod q` in
  square of `∑_{m ∈ Ioc n (n+K)} liouChi χ m`, uniform in base `n`, over the door's measure,
  `q ≤ (log H)^12`. First genuinely new analysis in the commission; Fable design block when
  its time comes. Fence kept: the M4ChiSummedFreeRow routing is FACTUALLY WRONG and struck
  (QUEUE:919-922; its integrand runs at `doorChiCoeff χ M` — sieved; refuter-verified in
  three hops at the bytes).
- **F-2 [C] The lane glue** (`blockSupSq` from the coprime head) — deliberately NOT stated
  as a node; stating it now would be inventing a statement to fill a link (brief fence kept).
- **F-3 [C] The non-coprime classes at λ** — W4's second RHS term; whether the sieved
  d₀-dilation survives sieve-removal is an open design question.
- **F-4 [D] GAP A.1 — the primary's producer. PRECONDITION: H-1 (the A-appendix
  fidelity-and-truth pass) resolves first — F-4's target statement is currently FALSE as
  transcribed (see H-1).** The corpus's named chain (`MRTParsevalConstantMatch` at 𝒥 = ∅ →
  `MRTThmA2` at 𝒥 = ∅ → `MRTThmA1`, `MRTPropA3.lean:3821-3822, :3898-3901`) is a
  **definitional RESTATEMENT, not a reduction**: at 𝒥 = ∅, `gJ = 1` and A.2's RHS is
  byte-identical to A.1's, so the antecedent is equivalent to `MRTThmA1Statement`. **The
  route with content is the source decomposition** A.1 = A.2 + MR-Thm-3 + density Lemma 2.2
  at η=1/12, P₁=(log h)^480 (mrt_extract.md:194-196, PDF pp.20-21). Carried fences: only
  ζ-VK zero-free input at extreme t (extract:232); the θ-mismatch (MRT θ=2/3 vs landed
  θ=3/4) bars copying quantitative exponents (extract:326-329); the PUBLISHED Prop A.3
  proof is incorrect for slowly-growing M — only the arXiv-v3 corrected proof ports
  (extract:212-217, re-verified at the PDF); A.1's file already holds proof-route
  arithmetic (`MRTThmA1.lean:159-173`). **F-4 is NOT on the seal's chain** (frozen brief
  :255 — A.1 cannot serve the phased arc datum): it is a separate campaign against the
  ratified primary, not the next link.
- **F-5 [B/C] GAP X — the investigation node.** The register measured the mechanism (the
  ∃-lever is a lower bound; `regimeEnlargeX` cannot apply post-hoc) and explicitly did not
  claim a `∀ X ≥ X₀` threading cannot be built. One bounded node: attempt the threading
  from gate-verified statements; report the exact obstruction if it fails. No landing may
  be read as closing GAP X until this node closes it.

## 5 · WAVE H — housekeeping and record fidelity (H-1 is a PRECONDITION, not optional)

- **H-1 [faithfulness + TRUTH, math's hand, pen rule, Captain informed — in flight, mandate
  delivered 08/25 ~14:0x]:** ONE source session over the A-appendix defs, four defects:
  **(1)** `MRTThmA1` as transcribed is **FALSE for every C** — refuter counterexample
  `f ≡ 1`: `mrtM 1 X = 0` (t = 0 gives `pretDistSq = 0`), so `exp(−M)·M = 0` and the RHS
  goes small while the LHS ≈ 1. The source's own p.21 remark (*"exp(−M(f;X))M(f;X) may be
  replaced by exp(−M(f;X))"*) is the form that is TRUE at degenerate M; the repair takes
  the remark form (or restricts f) under the 46b7a5a9 protocol. **(2)** the Lean A.2's
  middle term does not match the source (`(log h)^{1/3}/P₁^{1/6−η}` + η/P₁/Q₁/X(η)
  hypotheses vs A.1's term with none — extract:200-201, PDF p.21). **(3)** the sift is the
  **WRONG SIDE**: `gJ` (Sec9Glue.lean:134-135) indicates NO prime factor in the 𝒥-blocks;
  the source's S demands AT LEAST ONE in EVERY block (`MemS`, Sec9Glue.lean:118-119) — for
  nonempty 𝒥 the Lean LHS averages the complement of A.2's set, while `MRTPropA3.lean:
  3795-3796` asserts the identification. **(4)** `MRTThmA1.lean:14` — the "verbatim" source
  block still carries the UNSQUARED denominator the 46b7a5a9 repair fixed everywhere else
  (sole surviving instance, repo-wide grep). The 𝒥 = ∅ implication direction is
  kernel-true regardless — D9/E4 and the seal are UNAFFECTED (A.1 occurs only in
  hypothesis position; measured).
- **H-2 [A, one commit]:** three stale NOT-ROOTED docstrings — `MRTPortExitLam.lean:32`,
  `MRTPortExitLamGated.lean:52-53`, `MRTPortA1Const.lean:53-54` — all three ARE rooted
  (`All.lean:396-399`, audit :8541-:8548). Repair in the `MRTPortRowLam.lean:29-32`
  append-only idiom.
- **H-3 [A, QUEUE amendment]:** the "absolute factor 3, no log and no loglog debit"
  one-liner (QUEUE:788-791) over-compresses: the 3 arrives via `door_count_le_three_mul_norm`
  (`M4Door.lean:648`) under `4 ≤ log ω` (+2 gates), and the composed conclusion carries a
  second term `4·2^k·H/x / Z`. The landed absolute-3 conclusion is
  `m4_doorSq_of_rowMeanSqLam`. Amend to name the gate and the term.
- **H-4 [A]:** `M4Join.lean:99-100` still calls the class pricing "NOT attempted here"
  while `M4ClassPrice.lean` answers it in the kernel. Status-correct in the file's own
  STATUS-CORRECTED idiom (`M4Join.lean:30-32`).

## 6 · Method law (inherited, binding on every wave)

Gate-as-author (8 prose design blocks → 7 kills → 0 landings; gate-written statements 15/15
first-attempt across D-wave + Wave B). One serious attempt per mechanical node, then flag
and stop. Statements frozen; sed, never retype. The name-collision table (two "Prop 2.4"s;
`q` twice) rides every executor brief. `pdftotext` drops `\left|…\right|` bars in
1503.05121v3 — source reads use page images beside text. Build law: `../saltbuild.sh` only,
no pipes on its line, judge by the printed EXIT line (never the harness's word); EXIT=75 =
lock-wait abort; **address audit-log runs by PATH, never position — the log is
machine-global** (math's migration finding ③). Do-not-claim inheritance: nothing here is
unconditional; no wave "closes the major arc"; consumer-less sockets are sockets.

## 7 · Sequencing and success

**Now:** Wave H fires (H-1 in flight at math; H-2/3/4 any A-capable seat) · the D-1 pricing
wave fires (readers + gate, recon-fenced). **Lane A of Wave 1 waits for D-1's pricing report
or the Captain's explicit acceptance of the unsieved bet — whichever comes first**; lane B
waits on W0 regardless. **Then:** the Captain rules D-1; the ruled road's frontier gets its
Fable design block (F-1 or the sieved twin); F-5 runs as a bounded probe in parallel.
**Success shape:** PAD A produced under the ruled road ⇒ the seal fires and
`¬ logChowla2Fails` follows **at the ∃-chosen regime**, with GAP X the remaining gap on
this chain; GAP A.1 is NOT on this chain — F-4 is a separate campaign against the ratified
primary, gated on H-1. **Kill shape:** if the D-1 pricing wave finds both roads ≥2 class-D
after Wave 1, the commission reports that finding with the frontier map rather than burning
executor waves — a recorded stall is the cascade working.
