# The P0/P1 rung — the both-sided Brun sieve + almost-prime twins (`p0`)

*Fable, 2026-07-12. The possibility-side keystone (parity-frontier P0/P1,
as design-corrected — errors #15 and #16 both averted pre-freeze: #15 =
fixed-depth Bonferroni cannot serve P1; #16 = the recon's sketched error
factor missed an `e²` in the denominator (the true feasible-λ window is
HALF the sketch's, and the natural λ=1/4, b=1 twin window is EMPTY).
Constants below are transcribed from the PRIMARY source read at
page-image level: Halberstam–Richert, "A new look at Brun's sieve",
Mém. SMF 25 (1971) 97–106 (numdam 10.24033/msmf.39), Théorème 2 p. 99,
proof pp. 100–106, twin example p. 99; independently reproduced from the
proof's own derivation (2.11)–(2.17).*

## The frozen mathematics (H-R Théorème 2, exact)

Setting: `S(𝒜;𝒫,z)` the sifted count; `ω` multiplicative nonneg,
`W(z) = ∏_{p<z}(1 − ω(p)/p)`; remainders `R_d`. Hypotheses:
**(Ω)** `ω(p) ≤ A` (A ≥ 1); **(Ω₁)** `ω(p)/p ≤ 1 − 1/A₁`;
**(R)** `|R_d| ≤ ω(d)` for `d | P(z)`.
Ladder: `z_r = 2`, `log z_n = e^{−nΛ} log z`, `r` minimal with
`e^{rΛ} ≥ log z/log 2`. ⚠️ CORRECTED (catch #19): H-R's (2.18) sets
**`Λ = (2λ/A)·(1 + B₁/loglog z)⁻¹`** — z-DEPENDENT, `< 2λ/A`, with
"z large" required for `0 < Λ ≤ 1`; `2λ/A` is only the asymptotic
scale (and the `e^{−nλ}` form only its A=2 limit). B0's machinery is
Λ-generic, so B5/P1 instantiate per (2.18). Blocks (ν = 1 upper / ν = 2 lower) — ⚠️ CORRECTED (catch #17, by the
B0 EXECUTOR): the operative form is the RESTRICTED COUNT
`χ_ν(d) ⟺ ∀n ≥ 1: #{p ∣ d : p ≥ z_n} ≤ 2b − ν + 2n − 1` (formalized as
`chi` via `bigOmegaGe`); the compressed "all-of-d/min-prime-factor"
reading is NOT divisor-closed (violates the paper's (2.2) — concrete
counterexample in Defs.lean's docstring) and the previously-claimed
single-condition equivalence is FALSE (one direction only:
`chi_imp_windowed`). Condition **(1.2): `λ·e^{1+λ} < 1`**
(λ < 0.2784…; this is the true window — the sketch's `λe^λ < 1` was the
#16 error).

**The pair (freeze verbatim, O-free per doctrine — the remainder as the
explicit (2.12) product, from which the paper's `O(z^c)` follows):**
- UPPER: `S ≤ X·W(z)·(1 + 2λ^{2b+1}e^{2λ}/(1 − λ²e^{2+2λ})) + Σ_{d|P(z)} χ₁(d)|R_d|`
- LOWER: `S ≥ X·W(z)·(1 − 2λ^{2b}e^{2λ}/(1 − λ²e^{2+2λ})) − Σ_{d|P(z)} χ₂(d)|R_d|`
- REMAINDER (2.12): `Σ_{d|P(z)} χ_ν(d)ω(d) ≤ (1 + A·π(z))^{2b−ν+1} · ∏_{n=1}^{r−1} (1 + A·π(z_n))²`
  (with (R), this bounds `Σχ_ν|R_d|`; the level-exponent corollary
  `= O(z^{2b−ν+1+2/(e^Λ−1)})` is a consequence, NOT the frozen form —
  "support ⊆ {d < z^c}" was a mis-description, do not state it).

NOTE the asymmetry is real: upper `λ^{2b+1}`/depth `2b`; lower
`λ^{2b}`/depth `2b−1`. Numerator `e^{2λ}`, denominator `e^{2+2λ}` — the
`e²` is load-bearing (#16).

⚠️ SIDE-CONDITION (catch #19): H-R's p. 99 statement hides "z large"
inside its `O(·)` remainder (plus a `2 → 2.01` level-exponent fudge we
don't inherit — we keep the exact (2.12) product). The O-free pair
above holds only once (2.16) does, i.e. `z ≥ z₀(λ,b,A)`. Fable
decision: **B5 is stated hypothesis-parameterized** on B3b's
`hMert : log(W(z_n)/W(z)) ≤ n·κ` + `hkappa : κ ≤ 2λ` interface (no
hidden z₀); **P1 discharges `hMert` at the twin instance** via the
corpus' product-form Mertens, where the threshold is concrete.

## P1 operating points (margins PRE-COMPUTED from the true constants)

For twins: `𝒜 = {n(n+2) : n ≤ x}`, `ω(2) = 1, ω(p) = 2` — **(Ω) A = 2**
(so Λ = λ), **(Ω₁) A₁ = 3**, (R) ✓ via the landed M2/CongruenceCounting.

| target | b | λ | main-term margin `1 − 2λ^{2b}e^{2λ}/(1−λ²e^{2+2λ})` | level u-gate `3−ν+2b−… = 2b−1+2/(e^λ−1)` | u | per-element K | pair K |
|---|---|---|---|---|---|---|---|
| **PRIMARY** | 2 | 1/4 | **+0.946** (huge slack) | > 10.08 | 10.1 | **10** | **20** |
| stretch | 1 | 253/1000 | +0.014 (razor) | > 7.982 | 7.99 | 7 | 14 |

Freeze P1's headline at the PRIMARY point (`Ω(n(n+2)) ≤ 20`, stated in
`ArithmeticFunction.cardFactors` = Ω with multiplicity): the 94.6% margin
survives any Lean constant-loosening. The {7,7}/K=14 stretch is a
SEPARATE optional node afterward — its 1.4% margin demands near-exact
constants; do NOT let it gate the rung. Chebyshev input for the
remainder numerics: mathlib's π(z) bounds (the `log 4` upper, as used in
`Salt/LS/BDHPrep`).

## Node DAG (recon-refined; namespace `Salt.BrunLower`, files `Salt/BrunLower/`)

| id | content | class |
|---|---|---|
| B0 ✅ | defs: ladder (Λ = 2λ/A), blocks χ_ν (decidable), W(z); structural rules (2.1)–(2.4) as lemmas | B |
| B1 ✅ | `IsLowerMoebius` + `siftedSum_ge_sum_of_lowerMoebius` (the mirror of mathlib's upper plumbing — 35-line dualization, mathlib-worthy) | B |
| B2 ✅ | the pointwise block-Brun inequalities — **THE keystone, LANDED first design, no floor**: H-R's actual mechanism is Lemma 1 + (2.5), a SINGLE smallest-prime peel (`Σ_{d∣n}μχ = Σ_{t∣m}μ(t)(χ(t)−χ(pt))`), NOT an ω(n)-induction and NOT per-block Bonferroni — the sign is (2.3) contraposed (`μ(t) = (−1)^{ν−1}` on the boundary). Bridges to `IsUpper/LowerMoebius` for ALL n via the radical (B2.4 folded in). NOTE: the pointwise pair is fully combinatorial — `hprime`/z play no role; `b ≥ 1` is load-bearing only at n = 1 | C |
| B3 ✅ | the truncation main-term (2.11)–(2.17) — LANDED with the #16-critical core (`blockTail_le`: Stirling floor at the TIGHT `2e⁻²`, geometric close at `λ²e^{2+2λ}`, page-image-confirmed) fully proved; endpoint `mainSum_le_of_upper`/`mainSum_ge_of_lower` at the frozen constants. ⚠️ **catch #18**: the paper's chain consumes **(2.16)** `W(z_n)/W(z) ≤ e^{2nλ}` (whence `Σ_{z_n≤p<z}ω(p)/p ≤ 2nλ`) — H-R discharge it pp. 104–106 from a Mertens estimate + (Ω₁) + the Λ-choice (2.18) *valid only for z large*; the freeze's hypothesis list omitted it. Landed as explicit interface hypotheses `hWr`/`hps` (+ the (2.11) elementary-symmetric reduction as `hdecomp`); NO statement improvised (Iron Rule 1). → B3b | C |
| B3b ✅ | **(2.16) discharged** — LANDED (WRatio.lean): carriers `windowPrimes`/`windowSum`/`Wratio`; `windowSum_le_log_Wratio` (H-R's p. 104 one-liner, needs only ν < 1); `Wratio_le_exp` (= (2.16), feeds B3's `hWr`) and `windowSum_le` (feeds `hps`) from the single Mertens interface `hMert : log(Wratio) ≤ n·κ` + `hkappa : κ ≤ 2λ` — type-checked to plug into MainTerm with no glue. Transcription findings = **catch #19**: (2.18) is a z-dependent Λ-REDEFINITION `(2λ/A)(1+B₁/loglog z)⁻¹` AND the O-free freeze needs the z-threshold (folded above); p. 99's `1+λ²e^{2+2λ}` denominator is a known H-R typo for `1−` (the derivation (2.17) is unambiguous) | C |
| B3c (optional) | general-ω discharge of `hMert`: raw Mertens `Σ_{w≤p<z} ω(p)log p/p ≤ A(log(z/w)+1)` → (Ω₁) + partial summation → `W(w)/W(z) ≤ (log z/log w)^A(1+O(1/log w))` → the p. 106 "whence". NOT on P1's critical path (P1 discharges `hMert` directly at ω(p)=2 via product-form Mertens); polish node for the verbatim H-R Thm 2 | C |
| B3a ✅ | the (2.11) elementary-symmetric bound + endpoint composition — LANDED (BlockDecomp.lean): `esymm_le` (`e_k(x) ≤ (Σx)^k/k!`, from-scratch on `sum_pow_eq_sum_piAntidiag` + `multinomial_spec` — no mathlib Maclaurin exists; mathlib-worthy), `block_esymm_le`, `mainSum_le/ge_blockForm` (discharge B3's `hdecomp` — compiler-verified plug), composed `mainSum_le_of_upper'`/`mainSum_ge_of_lower'` (drop-ins for B5; only (2.16)-shaped + `hcorr` hypotheses remain). Lemma 3 transcribed verbatim from re-fetched p. 102 (pymupdf beat the broken numdam XRef) | C |
| B3a′ ✅ | **`hcorr` DISCHARGED, both sides, no floor** (Lemma3.lean, 911 lines): `master_signed` (Lemma 3 as a reusable EQUALITY, `mainSum = W + (−1)^{ν−1}Σ_p ν(p)W(p)·blockInner`), `hcorr_upper`/`hcorr_lower`, and the composed `mainSum_le/ge_blockForm'` drop-ins (only the (2.16) density hyps remain for B5). Mechanism: everything reformulated over SUBSETS of `prodPrimes.primeFactors` (divisors↔powerset), telescoping by strong-induction min-prime peel, the `d = δpt` reindex as ONE `Finset.sum_nbij'`, `prod_one_add` collapses the δ-slice to `W(p)`, `(2.10)` forcing via violating-level location, `(p,U)↦insert p U` over-count injective via `p = min`. Only hypothesis beyond B0 + frozen set: `hzprimes` (all sift primes < z — H-R's own `P(z)` setup). Original spec: — the p. 104 display; the LAST combinatorial node of the P0 chain: the χ telescoping `χ(d) = 1 − Σ_{p∣d}{χ((d,P(p⁺,z))) − χ((d,P(p,z)))}` into (2.7), reindex `d = δpt`, Euler `Σ_{δ∣P(p)}μ(δ)ω(δ)/δ = W(p)`, Lemma 1 (= B2's peel), Corollary grouping (`W(p) ≤ W(z_n)`, sign into `\|θ\| ≤ 1`), (2.10) forcing `Ω(d) = 2b−ν+2n, d ∣ P(z_n,z)` → the powersetCard form. ~800-line class-C obligation, B0/B2 carriers only, nothing outside the frozen set | C |
| B3 | the truncation tail: block sum `Σ e^{2nλ}(2nλ)^{2b−ν+2n}/(2b−ν+2n)!` bounded via `(2b−ν+2n)! ≥ (2n)!(2n)^{2b−ν}`, `(2n)! ≥ (2n/e)^{2n}` (Stirling floor — mathlib `Nat.factorial` bounds; the geometric close at ratio `λe^{1+λ}` — THE #16-critical arithmetic) | C |
| B4 ✅ | the remainder product (2.12) — LANDED FULL (Remainder.lean): `remainder_prod_bound` at the exact `(1+Aπ(⌊z⌋))^{2b−ν+1}·∏_{n∈Ico 1 r}(1+Aπ(⌊z_n⌋))²` (`Nat.primeCounting`, corpus-aligned) + the (R)-corollary `remainder_prod_bound_of_R` (B5's shape). The cumulative caps COUPLE the blocks (per-block counts are NOT independently ≤ cap): handled by the F-lemma `capSum_le` (`Σ_{S⊆B,\|S\|≤e}(∏ω)x^{e−\|S\|} ≤ (x+Σω)^e` — under-used top budget flows down the ladder) + `capSum_master` (level induction with the shifted-ladder recursion, budget `e−\|S₁\|+2`). The per-rung `zLev` product is load-bearing for the level gate (a flat `(1+Aπ(z))^{2r−1}` would blow u) | B |
| B5 | the frozen pair `brun_lower`/`brun_upper` | B |
| PM1 ✅ | **windowed Mertens (upper)** — LANDED FULL (MertensWindow.lean): `sum_inv_prime_window_le` at **C₃ = 19, w₀ = 2** (decaying `19/log w`, M never defined) + the PM2-consumable subset forms; Mertens-1 LOWER via corpus Λ⌊N/d⌋ identity + mathlib Stirling `le_log_factorial_stirling` (C_low = 7/2); prime-power strip by (p,k)-reindex + geometric sum (C_pp = 5/2, no rpow); two-sided C_R = 6; Abel as a difference of two full-range corpus-template passes. First Mertens lower bound + windowed second theorem in Lean anywhere. Original spec: — Fable-designed 2026-07-12 after the tools recon (NOTHING windowed/lower exists in corpus OR mathlib; mathlib has NO Mertens at all): `Σ_{w≤p<z} 1/p ≤ log(log z/log w) + C₃/log w` for `w₀ ≤ w ≤ z` (C₃, w₀ explicit; w₀ = 2 ideal). Constituents: Mertens-1 LOWER `Σ_{p≤x} log p/p ≥ log x − C_low` (log-factorial route: `Λ ∗ ζ = log`, `Σ_n Λ(n)⌊N/n⌋ = log N!`, Stirling floor; prime-power strip by convergent comparison) + corpus upper (`Salt/Maynard/Mertens.lean` `sum_log_div_prime_le`) + the Abel windowed pass (corpus `mF = 1/log t` machinery, `integral_inv_tlog/tlogsq`) — M cancels in the window, so no Mertens constant needs defining. Full-range `w` small also delivers P1's `W(z) ≥ c/log²z`. Mathlib-worthy | C |
| PM2 | **the twin `hMert` discharge**: at TwinSieve (`ν(2)=1/2, ν(p)=2/p`), choose `Λ(z) = λ·(1+B₁/loglog z)⁻¹` (the (2.18) shape; B₁, z₀ explicit from PM1's constants), absorb the quadratic tail `Σ_{w≤p}[−log(1−2/p)−2/p] ≤ C/w`-shape + the bottom-rung constants (p=2 at n=r contributes log 2; small odd primes covered by the window bound) into `2n(λ−Λ)`; land `hMert_twin : z ≥ z₀ → ∀ n ∈ Icc 1 r, log(Wratio) ≤ n·κ(z)` with `κ(z) ≤ 2λ` — feeds B3b/B3 verbatim. Level-gate note: `u(Λ) = 2b−1+2/(e^Λ−1) → 10.08⁺` as z→∞, so K=10/pair-20 survives with an explicit z₁ | C |
| P1.1–P1.7 | instantiate at TwinSieve (M2/CongCount/Sieve.lean verbatim reuse — instance `Salt.TwinSieve.sieve`, `rem_abs_le` is (R)); (Ω₁) at A₁=3; W(z) ≥ c/log²z via PM1 full-range; remainder numerics vs the level gate at (b=2, λ=1/4, u→10.08⁺, z ≥ z₁); survivor ⇒ `Ω(n(n+2)) ≤ 20`; **headline `twin_almost_prime : {n | Ω (n*(n+2)) ≤ 20}.Infinite`** | B/C |

PB-floors: B1+B2-at-fixed-depth (the Bonferroni pair) is an honest
standalone artifact if the block induction stalls; P1 floor = any fixed
explicit K ≤ 30 (the `loglog` failure shape does NOT meet the contract).
The {4,4} record and the fundamental-lemma dressing stay OUT.

Primary-source page images cached in the session scratchpad; the PDF at
the session tool-results dir (re-fetch from numdam if absent).
