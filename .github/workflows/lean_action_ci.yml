name: Lean Action CI

# Council 2026-08-26 (item 6): the Lean job is gated at PR/merge for branches and
# runs per-push only on the default branch. Measured cause: this workflow takes
# 99-110 min against a ~20-min working-push cadence, so with cancel-in-progress
# the last EIGHT runs on a working branch were all *cancelled* — never a red,
# never a green, nothing alarmed for 4.5 hours. Push discipline is not the fix
# ([skip ci] on the two eligible pushes bought 109 min against a 99-110 min run,
# a coin flip), and `schedule` fires only on the default branch so it structurally
# cannot cover a working branch. One run even wore conclusion=failure with a
# CANCELLED job and zero failed steps — a cancel in a red costume, which alarms a
# human reader while carrying no information. A branch's kernel referee is the
# local saltbuild audit (CLAUDE.md), which is green on every landing; use
# workflow_dispatch below to fire a full branch run on demand at a seam.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  schedule:
    # Nightly backstop build (09:00 UTC) so the badge never goes >24h stale.
    # Routine ceremony pushes carry [skip ci]; rung/campaign-end pushes omit it.
    - cron: '0 9 * * *'

# Rapid ceremony bursts supersede each other: cancel the stale run, bill ~1.
#
# ⛔ EXCEPT ON THE DEFAULT BRANCH, AND THE NUMBER IS THE ARGUMENT (measured 2026-08-29/30, after the
#   541-commit math/w1-e3-port merge landed). A full build of this tree took 2 h 34 m wall
#   (run 33293655100, 04:58:44 -> 07:33:00 UTC). With cancel-in-progress, a verdict on `main`
#   therefore requires a 2.5-HOUR GAP IN PUSHES — and on 08/29 main saw FIVE consecutive Lean runs
#   CANCELLED (the merge's own verification among them, plus a deliberate workflow_dispatch killed
#   by the next seat's push). The one green arrived only after every seat had banked and exited.
#   ⇒ ON AN ACTIVE DAY, `main` NEVER GREENS AND NEVER REDS: the gate reads "in progress" forever,
#     and an absence of red is not a pass. A GREEN THAT STRUCTURALLY CANNOT HAPPEN IS WORSE THAN NO
#     GATE, because nobody knows they are unprotected.
#   ⇒ SO: BRANCHES KEEP cancel-in-progress — that is what it was for, and a stale branch build is
#     genuinely worthless. THE DEFAULT BRANCH DOES NOT: an in-progress `main` verification now runs
#     to a verdict instead of being killed by the next push. ⚠️ THAT IS NOT THE SAME AS "every main
#     push is verified", and the earlier draft of this comment claimed it was: GitHub still cancels
#     an existing PENDING run when a newer one queues ("any existing pending job or workflow in the
#     same concurrency group will be canceled"), so only the NEWEST PENDING survives and
#     intermediate main commits can still go unverified. Nor do the runs go concurrent — this group
#     SERIALISES them, so the price is latency and coverage, NOT concurrent billing. (`queue: max`
#     would verify every push, at a real billing cost. Deliberately not taken here.)
#     This file's own header already prescribes the local `saltbuild`
#     audit as A BRANCH's referee; main had no such prescription and no working CI either.
#
#   ⛔ THE TEST IS `github.ref` AGAINST A LITERAL — NOT `github.event.repository.default_branch`,
#     which is what this change first shipped as (evidence's review of PR #4, 08/30). A `schedule`
#     run HAS NO WEBHOOK PAYLOAD (GitHub's trigger reference lists it "Not applicable"; GITHUB_REF
#     is populated, `github.event` is not). The payload form therefore yields `refs/heads/` on the
#     nightly run, compares TRUE, and leaves THE CRON as the single run still carrying
#     cancel-in-progress: true — and the newcomer is the canceller, so it would kill an in-progress
#     main build. IT FAILS OPEN, SILENTLY, ON EXACTLY THE RUN THIS BLOCK EXISTS TO PROTECT.
#     Measured: schedule run 33209460323 (08-28 20:43:39Z) was cancelled 25 s in by push run
#     33209469976 (20:43:47Z) — the push direction is what this change fixes; the cron direction is
#     what the literal fixes. And this cron does not fire at its nominal 09:00: observed 09:41 /
#     14:11 / 14:17 / 19:27 / 20:43 UTC, an 11-hour spread, so it lands inside an active-day build
#     sooner or later. The literal has no payload dependency, and it is not a new hardcode —
#     `on.push.branches: [main]` sits eleven lines above this block.
concurrency:
  group: lean-ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read # Read access to repository contents
  pages: write # Write access to GitHub Pages
  id-token: write # Write access to ID tokens

jobs:
  build:
    runs-on: ubuntu-latest
    # Billing cap. The 2026-07-17 diagnosis: mathlib cache engages fine, but
    # salt's own ~9k-job corpus needs ~2-2.5h cold on a 4-core runner. 240 min
    # lets a seeding run COMPLETE so the .lake/build cache below gets saved;
    # cached runs then finish in minutes. (~$1.20 per cold run at Linux rates.)
    # ⛔ 300, NOT 240 (raised 2026-09-01). 240 is INSIDE the runner-variance band
    #   rather than comfortably above it. Measured the same day on the identical module
    #   span `Salt.SW.ShiftTrivChar -> Salt.MR.M4Gauss`, two runners differed by 1.43x
    #   (123.9 min vs 176.9 min) — and the faster one did slightly MORE work (639 vs 633
    #   modules). A cold build finishes in ~215 min on the fast half of the fleet and does
    #   NOT finish inside 240 on the slow half, so 240 is a coin-flip gate on a branch
    #   whose greens people merge on.
    #   This compounds with the restore/save split above: a run cancelled at the wall
    #   SKIPS its cache save, so a single overrun makes the NEXT run cold too. That is
    #   what turned one slow build into a self-sustaining fixed point (3 runs, 720 billed
    #   minutes, nothing produced).
    # ⚠️ A CAP MUST STILL EXIST, and 300 is not merely "more": the same morning run
    #   33460951379 went SILENT for 3h09m at [8980/9795] — a COMPLETE archived log with
    #   zero output of any kind in the window — and only the wall ended it. No cap value
    #   fixes a stall; the cap is what bounds one. 300 clears the measured cold build
    #   with margin and still bounds a hang.
    timeout-minutes: 300

    steps:
      # Disk headroom: the corpus (~9,400 jobs) + mathlib cache (~5.7GB) +
      # docgen outgrew the runner's ~14GB free disk (2026-07-22: runs died
      # at 9,267/9,351 with "no space left on device"). Reclaim ~25-30GB of
      # preinstalled cruft before anything heavy runs.
      - name: Free runner disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/.ghcup /usr/share/swift /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force 2>/dev/null || true
          df -h /
      - uses: actions/checkout@v5
      # Salt's own build cache (mathlib comes via lake exe cache get inside
      # lean-action). restore-keys lets any prior seed serve incrementally.
      #
      # ⛔ RESTORE/SAVE ARE SPLIT, AND THE SAVE IS DEFAULT-BRANCH ONLY (2026-09-01).
      #   Measured that morning: the repo's 10 GB cache cap was consumed by entries on
      #   refs/pull/10/merge totalling 8,589,028,190 B — a MERGED PR's ref that nothing
      #   is eligible to read. (Only the SIZE is quoted, hand-summed from `gh cache list`.
      #   The entry COUNT disagreed between `gh cache list` and the repo usage endpoint
      #   and was never explained, and the usage endpoint was later shown to be the stale
      #   half — it still reported deleted entries 13 minutes after `cache list` read 0.
      #   `gh cache list` is the instrument here.) Those saves evicted main's own pair between 00:59Z and
      #   02:01Z; main and PR #11 then ran cold, blew `timeout-minutes: 240`, and were
      #   CANCELLED — and a cancelled run SKIPS ITS CACHE SAVE (step `Post Restore salt
      #   build cache` read `skipped` in both, `success`/22 s in the warm control).
      #   ⇒ cold -> overrun -> cancelled -> nothing saved -> cold. A fixed point.
      #   A PR cannot restore another PR's cache, but it CAN evict main's, so a PR save
      #   is all cost and no benefit. Restoring stays on for everyone: a PR reads the
      #   base branch's entry, which is the one the seed workflow writes.
      - name: Restore salt build cache
        uses: actions/cache/restore@v4
        with:
          path: .lake/build
          key: salt-lake-${{ runner.os }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-${{ github.sha }}
          restore-keys: |
            salt-lake-${{ runner.os }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-
      - uses: leanprover/lean-action@v1
        with:
          # Explicit (not auto): the 2026-07-17 run 29599988363 spent 2h11m and
          # died starved — the signature of building mathlib from source.
          use-mathlib-cache: true
          # ⛔ THE DOMINANT TERM IN THE CAP, AND IT IS NOT OURS BY DEFAULT (2026-09-01).
          #   lean-action wraps actions/cache over `path: .lake` — the WHOLE tree, so it
          #   CONTAINS the .lake/build we cache above — keyed per `github.sha`. Measured:
          #   3,381,278,129 B per sha against a 10 GB repo cap, i.e. 79% of everything a
          #   run stores, and only ~2 shas of headroom for the whole repository.
          #   Proven redundant by ORDER, not by reading: in warm run 33456610698 the
          #   salt entry restored at 00:56:00 (869 MB) and lean-action unpacked 3,223 MB
          #   OVER it at 00:57:07, both from the same sha.
          #   Off the default branch we drop both halves. The cost is real and small:
          #   `lake exe cache get` measured 4m04s (seed run 33521573204) against a 53 s
          #   restore of this entry — ~3 minutes on a job whose warm total is ~5 min.
          # ⚠️ WHY main KEEPS IT: this cache is also the insulation against mathlib's own
          #   CDN being unavailable for the pinned revision, which is when lake builds
          #   mathlib FROM SOURCE — the 2h11m death recorded two lines above. Keep the
          #   insulation where a failure is most expensive; drop it where it destroys
          #   the cap. `use-github-cache` gates BOTH the restore and the save in
          #   lean-action's action.yml (its only two uses of the input).
          use-github-cache: ${{ github.ref == 'refs/heads/main' }}
      # Blueprint lint: docs↔code consistency + repo-wide axiom audit (CLAUDE.md
      # iron rule 3). Runs AFTER lean-action so the oleans it built are on disk:
      # the lint's phase-1/phase-3 `lake env lean` invocations elaborate a tiny
      # `import Salt` scratch against those cached oleans (no full rebuild), and
      # elan/lake are already on PATH from lean-action.
      - name: Blueprint lint (docs↔code + axiom audit)
        run: python3 scripts/blueprint_lint.py
      # The save half of the split above. Default branch only, and AFTER the build so
      # it stores what the build produced. `actions/cache@v4` has no conditional save,
      # which is the whole reason restore and save are separate steps.
      - name: Save salt build cache (default branch only)
        if: ${{ github.ref == 'refs/heads/main' }}
        uses: actions/cache/save@v4
        with:
          path: .lake/build
          key: salt-lake-${{ runner.os }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-${{ github.sha }}
      # ⛔ REMOVED 2026-08-28: `- uses: leanprover-community/docgen-action@v1`
      #    IT WAS GREEN ONLY WHEN IT DID NOTHING. The action sees this repo's `docs/`
      #    folder, sets DOCS_EXISTS=true, and tries to build a JEKYLL site from it —
      #    but salt has no Gemfile, no _config.yml and no docs site, so the build dies
      #    with "Could not locate Gemfile or .bundle/ directory" (exit 10).
      #    ⇒ MEASURED ACROSS RUNS RATHER THAN INFERRED FROM ONE:
      #        fbad5d1a  cache MISS -> texlive/ruby/pages sub-actions RUN   -> RED
      #        fcfb3cf2  cache HIT  -> texlive, ruby/setup-ruby, upload-pages-artifact
      #                               and deployment ALL `conclusion=skipped` -> GREEN
      #      The step's GREEN means the docs cache was hit and nothing was built; its
      #      RED means it actually ran. It has never once built or deployed docs.
      #    ⇒ ***A CHECK THAT IS GREEN ONLY WHEN IT SKIPS IS NOT A CHECK*** — and the
      #      consequence was a RED CHECK OVER A GREEN KERNEL on every salt PR, the same
      #      shape as the duplicate Lean job retired at council item 19.
      #    NOT NEUTRALISED (no `continue-on-error`, no `if: false`): a step that cannot
      #    fail is decoration. If salt is to publish API docs, that is a PROJECT item —
      #    a Gemfile, a docs site and a Pages deployment on a repo that is private now
      #    and public later behind a hard gate — and it is the Captain's to open, not a
      #    red-check repair. The Pages permissions above are left untouched for it.
