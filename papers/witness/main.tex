% ============================================================================
% THE WITNESS (paper #2) -- v0.1 skeleton, opened 2026-08-02.
%
% Authorized by the C2 ruling (docs/exploration/council-0802.md) and the two
% christenings in docs/blueprints/flags.md: THE PEARL
% (logChowla2_witnessed_scale_flat_L_v2 and its lineage) and THE TOLL (the
% entropy-decrement width law of the flat tower).
%
% TODO(title): the working title below is a placeholder chosen only so that
% both named theorems appear in it. The title is the author's to settle.
%
% House conventions, inherited verbatim from papers/flagship/main.tex:
%   * amsart, same preamble;
%   * every stated result carries its Lean declaration name via \leanname{};
%   * Appendix A is the statement -> declaration -> axiom audit table;
%   * voice: problem-first; every strong adjective paid by a number; every
%     conditional named at the statement, never in a footnote;
%   * author/thanks block and Acknowledgments as in the flagship.
% Two words are banned from the prose of this paper by house rule: "honest"
% (and "honestly") and "load-bearing". Em-dashes are permitted.
%
% SECTION STATUS at v0.2 (2026-08-04, after the word on
% logChowla2_ineffective_v6)
%   1  Introduction ........ DRAFTED (conservative; every hypothesis named);
%                            abstract + 1.2 reconciled with v6 on 08-04
%   2  The Toll ............ DRAFTED IN FULL (the chapter this draft exists for)
%   3  The flat tower ...... DRAFTED IN FULL (the technical heart of section 2)
%   4  The Pearl ........... DRAFTED IN FULL against the FINAL objects: the
%                            landed terminal (v2), the two 08-02 defects and
%                            their repairs, the re-cut terminal at an
%                            arbitrary admissible lever
%   5  The classical limit . DRAFTED IN FULL: containment, hoist, the v1->v6
%                            chain, v6 with its five riders by nature
%   6  Method .............. STUB -- contents listed, prose not written
%   A  Statement index ..... Toll rows FILLED; Pearl rows FILLED, with the
%                            v2/v6 signature transcriptions
%
% Compiled with pdfLaTeX (TeX Live 2026); box-clean (0 overfull, 0
% underfull) as of 2026-08-06.
% ============================================================================
\documentclass[11pt]{amsart}
\usepackage[margin=1.1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage[colorlinks=true,linkcolor=blue!60!black,citecolor=blue!60!black,urlcolor=blue!60!black]{hyperref}
\usepackage{microtype}
\usepackage{xcolor}

% --- Appendix A transcribes Lean source, exactly as papers/flagship/main.tex
% does. `alltt' is base LaTeX's verbatim-like environment that still expands
% \-commands, so the unicode of the declarations can be mapped character by
% character below. On a modern LaTeX (UTF-8 input by default) nothing further
% is needed; on an older one add \usepackage[utf8]{inputenc}. The list is
% exactly the characters occurring in this paper's transcriptions.
\usepackage{alltt}
\newcommand{\lchar}[1]{\hbox to \fontcharwd\font`\M{\hss$#1$\hss}}
\DeclareUnicodeCharacter{00AC}{\lchar{\neg}}
\DeclareUnicodeCharacter{03B2}{\lchar{\beta}}
\DeclareUnicodeCharacter{03B4}{\lchar{\delta}}
\DeclareUnicodeCharacter{03B5}{\lchar{\varepsilon}}
\DeclareUnicodeCharacter{03C9}{\lchar{\omega}}
\DeclareUnicodeCharacter{2080}{\lchar{{}_0}}
\DeclareUnicodeCharacter{208A}{\lchar{{}_+}}
\DeclareUnicodeCharacter{2115}{\lchar{\mathbb{N}}}
\DeclareUnicodeCharacter{211A}{\lchar{\mathbb{Q}}}
\DeclareUnicodeCharacter{211D}{\lchar{\mathbb{R}}}
\DeclareUnicodeCharacter{2192}{\lchar{\to}}
\DeclareUnicodeCharacter{2200}{\lchar{\forall}}
\DeclareUnicodeCharacter{2203}{\lchar{\exists}}
\DeclareUnicodeCharacter{2227}{\lchar{\wedge}}
\DeclareUnicodeCharacter{2264}{\lchar{\le}}
\DeclareUnicodeCharacter{2308}{\lchar{\lceil}}
\DeclareUnicodeCharacter{2309}{\lchar{\rceil}}

\newcommand{\leanname}[1]{\par{\noindent\small\ttfamily\raggedright #1\par}}
\newcommand{\leaninline}[1]{{\small\texttt{#1}}}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\title[The Pearl and the Toll]{The Pearl and the Toll: a Witnessed-Scale
Chowla Regime and the Width Law of the Entropy Decrement}
\author{Jason Hickey}
\thanks{This work was developed in collaboration with Claude
(Anthropic's Fable~5 and Opus models); see \S\ref{sec:method} and the
repository's commit ledger, in which every commit is co-authored. It was
written in a personal capacity.}
\date{August 4, 2026}

\begin{document}

\begin{abstract}
Every theorem in this paper is verified by the Lean~4 kernel over
\textsf{mathlib}, with axiom base exactly
$\{\texttt{propext}, \texttt{Classical.choice}, \texttt{Quot.sound}\}$;
nothing rests on informal argument. The mathematics---the designs, the
proofs, and their formalization---is joint work of the author and Claude
(Anthropic's Fable~5 and Opus models), carried out under the author's
direction and ratification; the commit ledger records the collaboration
line by line.

We prove two theorems about the entropy-decrement route to the
logarithmically averaged two-point Chowla statement. The first, the
\emph{toll}, is unconditional: along the flat tower of scales
$H_{j+1} = H_j\lfloor 2A\log H_j\rfloor$ at design constant $A \ge 1$, the
telescoped entropy decrement equals a potential difference to within
$5\%$, and consequently a tower long enough to exhaust the one-bit entropy
budget $\log 2$ must multiply the doubly-logarithmic width
$\log 2A + \log\log H$ by a factor in the bracket
$[\,4^{(20/21)A},\ (21/20)\,4^{A}\,]$ --- exponent pinned within $4.8\%$,
constant within $5\%$, with the continuum value $4^{A}$ inside. Crossing
the entropy budget is thus a conservation law rather than an optimization:
the price is fixed, and a design chooses only the coordinate in which to
pay it. The second theorem, the \emph{pearl}, is conditional and we name
its conditionality at the statement: at every design constant $A$ above an
explicit floor there is a Chowla regime whose window base is pinned at
$\lceil e^{e^{3.2A}}\rceil$, and for that regime the logarithmically
averaged two-point Chowla correlation does not fail at the witnessed
scale, on a list of hypotheses given in full in \S\ref{sec:pearl}: one
analytic constant of the band lane, eight numeric riders on constants the
theorem itself produces, and two carried predicates. Of that list, one
rider was found unreachable along the proof and one predicate false at the
regime the theorem itself produces --- the second refuted by the first
theorem's own lower bound --- and both were repaired at the level of the
statement, which is the episode \S\ref{sec:pearl} is written around. A
chain of six corollaries then removes, in order, the two Siegel-genre
riders by the classical limit, the band-lane constant by its own window,
three numerals at their leaves, the base-scale cap at a raised lever, the
outer-scale ceiling by threading, and the co-factor supply by repairing
its register; the last of them has no outer hypothesis and no
conclusion-side predicate, and of its five surviving hypotheses three are
met by objects the development exhibits and two are the Siegel
obstruction. Making the classical limit available required a quantifier
hoist that the formal statement, and only the formal statement, made
visible.
\end{abstract}

\maketitle

% ============================================================================
\section{Introduction}\label{sec:intro}
% ============================================================================

The logarithmically averaged two-point Chowla statement,
\begin{equation}\label{eq:chowla}
\frac{1}{\log x} \sum_{n \le x} \frac{\lambda(n)\lambda(n+1)}{n}
\;\longrightarrow\; 0 ,
\end{equation}
is a theorem of Tao \cite{TaoChowla}, proved by an entropy decrement: if
the correlation fails at a scale, the per-symbol entropy of the Liouville
window drops by a definite amount when the window length is multiplied,
and the drops telescope. Entropy per symbol of a $\pm1$-valued window lies
in $[0, \log 2]$, so the total available decrement over any tower of
scales is one bit. The proof therefore has a budget, and the budget is
spent by a tower of scales: build the tower long enough that the summed
decrement exceeds $\log 2$, and the failure hypothesis dies.

What does that tower cost? The question is not rhetorical in a formal
development, where the tower is an actual sequence of natural numbers, its
length an actual index, and its terminal scale an actual bound that
downstream lemmas must accommodate. In the campaign behind this paper the
answer was, three separate times, the difference between a route that
closed and a route that did not. The first of our two theorems answers it
exactly.

It is worth stating why the flat threshold does not appear in the
original argument. The theorem of \cite{TaoChowla} is qualitative,
meaning the correlation tends to zero, with no rate claimed. It is
proved, as it happens, by an argument that is effective in principle ---
and the tower's cost is invisible to the statement, because a terminal
scale is a rate-shaped quantity and no rate is asked for. The threshold
$\kappa = H/(\log H\log\log\log H)$ is the one the argument's internal
bookkeeping produces when nothing bills the terminal scale. The growing
factor is not idle there --- it buys the decrement step its hypotheses
cheaply at every scale --- but the formal rebuild shows how little of it
the budget itself consumes: in our development the budget chain
degenerates from requiring $0 < \log\log\log H$ to requiring $0 < A$,
positivity and nothing more (\leaninline{budgetFloor} against
\leaninline{budgetFloorFlat}, \S\ref{sec:toll}). Forfeiting the growing
threshold is therefore possible, but not free: at a constant threshold
the decrement step must be re-established against a fixed design, and
the price appears in this paper as the floor $A \ge 162$ and the window
base $\lceil e^{e^{3.2A}}\rceil$ of the second theorem
(\S\ref{sec:pearl}). The trade is pointless for the limit statement and
decisive for a budgeted one: it converts the triple-exponential terminal
scale into a single exponential, at the cost of constants that a
qualitative proof has no reason to purchase.

\subsection{The toll}
Fix a real design constant $A \ge 1$ and an integer base $B$, and let the
\emph{flat tower} be $H_0 = B$, $H_{j+1} = H_j\lfloor 2A\log H_j\rfloor$;
its telescoped decrement at length $J$ is
$S_J = \sum_{j<J} \bigl(2A\log H_j\bigr)^{-1}$. Write $L = \log H$,
$\ell = \log L$, $c = \log 2A$.

\begin{theorem}[The toll; \S\ref{sec:toll}, unconditional]\label{thm:toll-intro}
Let $A \ge 1$, $B \ge 4\cdot 10^{6}$, and let the base satisfy the flat
floor $100(c + \ell_0) \le L_0$. Then any tower length $J$ at which the
decrement crosses the one-bit budget, $\log 2 < S_J$, forces
\[
c + \ell_J \;\ge\; 4^{(20/21)A}\,\bigl(c + \ell_0\bigr),
\]
and at the least such length the width does not overshoot:
\[
c + \ell_{J} \;\le\; \tfrac{21}{20}\,4^{A}\,\bigl(c + \ell_0\bigr).
\]
\leanname{Salt.Entropy.Chowla.towerFlat\_width\_ge,
Salt.Entropy.Chowla.towerFlat\_width\_le}
\end{theorem}

The two halves bracket the width multiplier in
$[\,4^{(20/21)A},\ (21/20)\,4^{A}\,]$, which contains the continuum value
$4^{A}$: the exponent is pinned to within $20/21 = 0.952\ldots$ of the
truth and the constant to within $21/20$. Both halves come from one law
(Theorem~\ref{thm:master}): the decrement $S_J$ \emph{is} a potential
difference, $S_J = (2A)^{-1}(w_J - w_0)$ with $w = \log(c+\ell)$, to
within a factor $21/20$. The whole content of the floor function in the
tower's definition is the $20/21$.

We read Theorem~\ref{thm:toll-intro} as a conservation law. Crossing the
entropy budget costs a fixed amount, $4^{A}$ up to $5\%$ in each direction,
and no design decision changes the amount; a design decides only which
coordinate is billed. The flat threshold $\kappa = H/(A\log H)$ pays in
doubly-logarithmic width, with $A$ a constant of the designer's choosing.
The threshold of the original argument, $\kappa = H/(\log H\log\log\log H)$,
pays the same invoice with $\log\log\log H$ in the role of $A$ --- and
since that is not a constant, the bill arrives on the scale axis instead,
as the triple-exponential floor $\lceil e^{e^{e^{X}}}\rceil$ that the flat
road replaces by the single exponential
$\lceil e^{\max(4X,\,2\log A + 2)}\rceil$
(\leaninline{budgetFloor} against \leaninline{budgetFloorFlat}). \S\ref{sec:toll}
makes this two-axis reading precise and states exactly which part of it is
kernel-checked. The width half is not special to a constant threshold: for an
arbitrary threshold schedule $\varphi_j \ge A_b$ driving the same step, crossing
the budget still forces a factor at least $4^{(20/21)A_b}$, and the constant
schedule at $A_b$ pays at most $\frac{21}{20}4^{A_b}$
(Theorem~\ref{thm:shapetoll} and Corollary~\ref{cor:extremal}).

\subsection{The pearl}
The second theorem is conditional, and we state its conditionality here
rather than later. Here is the form the corpus carries.

\begin{theorem}[The pearl; \S\ref{sec:pearl}, conditional]\label{thm:pearl-intro}
For every $A_0 \ge 162$ there exist a tolerance $\varepsilon > 0$, a
design constant $A \ge \max(162, A_0)$, and fourteen further constants
such that, \emph{given} one analytic constant hypothesis of the band
lane, eight numeric riders on those constants, and two carried
predicates --- each listed in full in \S\ref{sec:pearl} --- there is a
Chowla regime $R$ with window base pinned at the design base,
terminal scale beyond any prescribed function of the regime's own data,
and
\[
\lnot\,\mathrm{logChowla2Fails}(R.\varepsilon,\ R.x,\ R.\omega),
\]
that is, the logarithmically averaged two-point correlation at the
witnessed window does not exceed $\varepsilon\log\omega$ in absolute
value.
\leanname{Salt.MR.logChowla2\_witnessed\_scale\_flat\_L\_v2}
\end{theorem}

The sixteen constants are the point: they are what a classical proof
would sweep into ``for suitable constants,'' and the formal statement
of \S\ref{sec:pearl} cannot --- it names each one at the binder, and
Table~\ref{tab:dials} labels them. A reader needs two of them now:
$\varepsilon$ is the conclusion's tolerance, and $A$ is the design
constant --- the toll's dial. The hypotheses sort in
\S\ref{sec:pearl-hyp}: the eight riders are six ceilings on constants
the theorem itself produces and two windows of Siegel genre, and it is
the Siegel pair that survives every removal \S\ref{sec:limit}
performs.

The eight hypotheses and two predicates are listed in full in
\S\ref{sec:pearl}; none is hidden, and the paper claims nothing
unconditional about \eqref{eq:chowla}. Two of the eight are of Siegel
genre --- windows on constants that the theory does not effectively bound.
Two other items of the list did not survive inspection: the rider
$T_0 \le e^{e^{100}}$ is unreachable along the proof, and the base-scale
cap predicate is false at the regime the theorem produces, refuted by the
lower bound of Theorem~\ref{thm:toll-intro}. \S\ref{sec:pearl} states both
findings, their dates, and the two statement-level repairs --- a raised
lever for the cap, the consumer's own tolerance for $T_0$ --- and gives the
re-cut terminal that carries them (Theorem~\ref{thm:pearl-recut}).

\S\ref{sec:limit} then runs the chain of six corollaries that ends at
\leanname{Salt.MR.logChowla2\_ineffective\_v6}
which has no outer hypothesis and no conclusion-side predicate, and carries
exactly five: $e^{-100} \le c_s$ and
$T_0 \le \exp(\tfrac12\sqrt{\lceil e^{e^{3.2A}}\rceil})$ and a condition on
the caller's own outer-scale request, all three met by objects the
development exhibits; and $e^{-100} \le K_s$ together with a cushion on a
constant whose third leg is the extreme-value minimum of $\|L(s,\chi)\|$
uniform in $q$ --- the Siegel obstruction, named twice.

\subsection{Effective, ineffective, and the order of quantifiers}
The two statements stand in the anatomy familiar from the theory of the
exceptional zero: an \emph{effective} statement conditional on a bound
relating a fixed constant to a growing window contains an
\emph{unconditional-in-that-constant} statement whose own constant is then
ineffective. Here the growing windows are double exponentials in the
design constant $A$ ($x_0 \le e^{e^{3.2A}/10}$ and
$H_{\mathrm{opq}} \le \lceil e^{e^{3.2A}}\rceil$), the constants they
window are fixed, and choosing $A$ above both discharges them; what is
lost is effectivity, since the choice is made by
\texttt{Classical.choice}. This is exactly the caveat the analytic
literature attaches to Siegel's theorem.

The formalization contributed one thing to that argument which prose would
not have surfaced: at the quantifier order in which the conditional
theorem was first proved, the classical limit is \emph{not available}. The
statement reads $\forall A_0 \ge 162,\ \exists (\ldots, x_0,
H_{\mathrm{opq}}, A, \ldots)$, so every constant is chosen after the
caller's $A_0$ and nothing in the statement forbids $x_0$ and
$H_{\mathrm{opq}}$ from moving when $A_0$ does; two applications of
\texttt{Classical.choose} at two different $A_0$ are choices from two
different propositions, and proof irrelevance does not bridge them. The
constants are in fact $A_0$-free at the level of the proof, and making
that a fact of the \emph{statement} required re-cutting seven theorems
along the road with the quantifier hoisted (\S\ref{sec:limit}). We record
this as a methodological finding: a formal statement carries its
quantifier order as data, and an argument the prose would perform without
comment can be blocked by it.

\subsection{What is in this paper}
\S\ref{sec:toll} states and proves the toll and its exports.
\S\ref{sec:flat} develops the flat tower --- the construction, the floor
invariant, the per-step bracket --- which is the technical content of
\S\ref{sec:toll}'s proof. \S\ref{sec:pearl} states the pearl with its
complete hypothesis list. \S\ref{sec:limit} gives the classical limit and
the quantifier hoist. \S\ref{sec:method} describes the method, which is
treated at length in a companion paper \cite{companion} and in the
program's first paper \cite{fulcrum}. Appendix~\ref{app:index} maps every
numbered statement to its Lean declaration and its axiom audit
(Table~\ref{tab:index}).

Conditionality is tabulated in one place, Table~\ref{tab:conditional};
every conditional statement in the paper names its exact residual
hypothesis at the point of statement.

% ============================================================================
\section{The toll}\label{sec:toll}
% ============================================================================

\subsection{The budget and the tower}
The entropy-decrement argument runs on a tower of window lengths. At
window length $H$ let
$e(H) := \mathbb{H}\bigl[\lambda\text{-window of length } H\bigr]/H$
be the per-symbol entropy of the Liouville window under the logarithmic
measure of the regime. Two endpoint facts fix the budget: $e(H) \le \log
2$ for every $H \ge 1$, since the window is $\pm1$-valued
(\leaninline{Salt.Entropy.Chowla.\allowbreak entropy\_per\_symbol\_le}), and $e(H) \ge
0$ (\leaninline{Salt.Entropy.Chowla.\allowbreak entropy\_nonneg\_per\_symbol}). The
decrement step says that if the correlation fails at level $j$ then
$e(H_{j+1}) - e(H_j) \le -\,1/(2A\log H_j)$ at the flat threshold
$\kappa = H/(A\log H)$; summing over $j < J$ gives
\[
e(H_J) \;\le\; e(H_0) - S_J, \qquad
S_J \;:=\; \sum_{j<J} \frac{1}{2A\log H_j},
\]
\leaninline{Salt.Entropy.Chowla.tower\_telescopeFlat}, and with the two
endpoint facts, $S_J \le \log 2$. So the argument closes exactly when the
tower is long enough that $S_J > \log 2$: one bit, in nats, is the entire
capital of the method.

The tower cannot be extended for free, because $H_j$ grows and the
summands $1/(2A\log H_j)$ shrink. The question the toll answers is what
the terminal scale must be.

Convention for \S\ref{sec:toll} and \S\ref{sec:flat}: every declaration
cited below lives in \texttt{Salt/\allowbreak Entropy/\allowbreak Chowla/\allowbreak TowerFlat.lean} of the
repository unless a different file is named at the citation, and all are
in the namespace \leaninline{Salt.Entropy.Chowla}.

\begin{definition}[The flat tower and its decrement]\label{def:flattower}
For a real $A$ and naturals $a$, $B$, set $H_0 = a\,B$ and
\[
H_{j+1} \;=\; H_j \cdot \bigl\lfloor 2A \log H_j \bigr\rfloor ,
\]
and $S_J := \sum_{j<J} \bigl(2A\log H_j\bigr)^{-1}$. Throughout this paper
$a = 1$, so $H_0 = B$.
\leanname{Salt.Entropy.Chowla.chowlaTowerFlat,
Salt.Entropy.Chowla.towerDropSumFlat}
\end{definition}

The multiplier $\lfloor 2A\log H_j\rfloor$ is chosen so that the tower
funds its own threshold: the step's two error terms fit inside half the
per-step drop $1/(2A\log H_j)$ for every $\varepsilon \le 1/2$, $A \ge 1$
and $\log H \ge 15$
(\leaninline{Salt.Entropy.Chowla.flat\_hkey}). The name \emph{flat} marks
the contrast with the original threshold $H/(\log H \log\log\log H)$: the
third logarithm has been replaced by a constant the designer picks.

\begin{definition}[The potential]\label{def:potential}
Write $L_j = \log H_j$, $\ell_j = \log L_j$, $c = \log 2A$, and
\[
w_j \;=\; \log\bigl(c + \ell_j\bigr).
\]
\leanname{Salt.Entropy.Chowla.towerLF, towerLamF, flatC, towerWF}
\end{definition}

The potential is forced by the arithmetic of the step, not chosen for
convenience. One step adds $d_j = \log\lfloor 2A L_j\rfloor$ to $L$, and
$d_j = c + \ell_j$ exactly up to the floor function; so
$\Delta\ell_j \approx (c+\ell_j)/L_j$ and
$\Delta w_j \approx 1/L_j$. That is: the summand $1/(2AL_j)$ of $S_J$
\emph{is} $\Delta w_j/(2A)$. The next theorem says so with explicit
two-sided constants.

\subsection{Hypotheses}
Three hypotheses are in force for every statement of this section, and
each is named at every statement that uses it.
\begin{itemize}
\item[(H1)] $A \ge 1$: the design constant is at least one.
\item[(H2)] $B \ge 4\cdot 10^{6}$: the house regime floor, which supplies
  $\log B \ge 15$ and, along the tower, $L_j \ge 15$ and $\ell_j \ge 2$
  at every level.
\item[(H3)] the \emph{flat floor} $100\,(c + \ell_0) \le L_0$, i.e.
  $100\,(\log 2A + \log\log B) \le \log B$.
\end{itemize}
(H3) is what makes the second-order factors of the step $1/100$-small, and
it propagates up the tower: $x \mapsto x - 100(c + \log x)$ is increasing
past $x = 100$, so the invariant survives every step
(\leaninline{Salt.Entropy.Chowla.towerFlat\_inv}). It is a mild demand. At
$A = 10^{30}$, where $c = 69.7\ldots$, it asks $\log B \ge 8\cdot10^{3}$
(a numerical remark; the formal statement is the displayed inequality). In
the use made of the tower in \S\ref{sec:pearl}, where the design law
$3.2A \le \log\log B$ holds with $A \ge 26$, the flat floor is not a
hypothesis at all but a theorem:
\leanname{Salt.Entropy.Chowla.flatFloor\_of\_design}

\subsection{The master law}

\begin{theorem}[The master law: the decrement is a potential
difference]\label{thm:master}
Assume (H1), (H2), (H3). Then for every $J$,
\[
\frac{1}{2A}\bigl(w_J - w_0\bigr)
\;\le\; S_J \;\le\;
\frac{21}{20}\cdot\frac{1}{2A}\bigl(w_J - w_0\bigr).
\]
\leanname{Salt.Entropy.Chowla.\allowbreak towerDropSumFlat\_ge\_log\_ratio,
Salt.Entropy.Chowla.\allowbreak towerDropSumFlat\_le\_log\_ratio\_mul}
\end{theorem}

The two-sided constant is $21/20$: the telescoped decrement of the flat
tower is the potential difference $w_J - w_0$, divided by $2A$, to within
$5\%$. The lower half is exact --- no relative loss at all --- because both
prices charged by the floor function point the same way in that direction;
the $1/20$ of the upper half pays three items, two second-order factors of
the step ($L' \le (101/100)L$ and $c + \ell' \le (c+\ell) + 1/100$,
both from (H3)) and the floor price $1/500$.

\subsection{The toll}

\begin{theorem}[The toll, necessity --- the lower half of Theorem~\ref{thm:toll-intro}, at the working hypotheses]\label{thm:tollge}
Assume (H1), (H2), (H3). For \emph{every} tower length $J$ at which
$\log 2 < S_J$,
\[
\bigl(c + \ell_0\bigr)\, 4^{(20/21)A} \;\le\; c + \ell_J .
\]
\leanname{Salt.Entropy.Chowla.towerFlat\_width\_ge}
\end{theorem}

\begin{theorem}[The toll, no overshoot --- the upper half of Theorem~\ref{thm:toll-intro}]\label{thm:tollle}
Assume (H1), (H2), (H3), and let $J_{\min}$ be the least $J$ with
$\log 2 < S_J$ (it exists, by Proposition~\ref{prop:crossing}). Then
\[
c + \ell_{J_{\min}} \;\le\; \frac{21}{20}\,4^{A}\,\bigl(c + \ell_0\bigr).
\]
\leanname{Salt.Entropy.Chowla.towerFlat\_width\_le}
\end{theorem}

\begin{proposition}[The crossing is reached, at a named
level]\label{prop:crossing}
Assume (H1), (H2), (H3), and set
$J^{\star} := \bigl\lceil \exp\bigl(2(c+\ell_0)4^{A}\bigr)\bigr\rceil$.
Then $\log 2 < S_{J^{\star}}$; in particular the set of crossing lengths
is nonempty and $J_{\min}$ is well defined and positive.
\leanname{Salt.Entropy.Chowla.\allowbreak towerFlatJstar,
towerFlat\_crossing\_at\_Jstar, towerFlat\_crossing\_exists,
towerFlatJmin, towerFlatJmin\_spec, towerFlatJmin\_pos}
\end{proposition}

Together, Theorems~\ref{thm:tollge} and \ref{thm:tollle} bracket the width
multiplier of a minimal crossing tower:
\begin{equation}\label{eq:bracket}
4^{(20/21)A} \;\le\; \frac{c+\ell_{J_{\min}}}{c+\ell_0}
\;\le\; \frac{21}{20}\,4^{A},
\end{equation}
and the continuum value $4^{A}$ --- what the same computation gives with
the floor function and the discreteness removed --- lies inside. The
bracket's own width is a factor $4^{A/21}\cdot(21/20)$: the exponent base
is determined, the exponent to within $4.8\%$, the constant to within
$5\%$.

To see the size of the toll, take the working point of \S\ref{sec:pearl},
$A = 162$, where the base is pinned so that $\ell_0 \ge 3.2A = 518.4$
and $c = \log 324 = 5.78\ldots$. Then \eqref{eq:bracket} puts
$\ell_{J_{\min}}$ between $4\cdot10^{95}$ and $1.9\cdot10^{100}$: the
terminal scale of the tower is $H_{J_{\min}} = \exp\exp(\ell_{J_{\min}})$,
a doubly exponential object whose doubly-logarithmic width has been
multiplied by about $10^{93}$. (Numerical illustration; the formal content
is the displayed inequalities.)

\begin{corollary}[The $A$-free export]\label{cor:export}
Let $A \ge 26$, $B \ge 4\cdot10^{6}$, and let the design law
$3.2A \le \log\log B$ hold. Then, at the minimal crossing length,
\[
\log\log H_{J_{\min}} \;\le\; \exp\!\left(\frac{\log\log B}{2}\right).
\]
\leanname{Salt.Entropy.Chowla.towerFlat\_width\_export}
\end{corollary}

Corollary~\ref{cor:export} is Theorem~\ref{thm:tollle} with the design law
substituted and $A$ eliminated: under $3.2A \le \ell_0$ the whole right
side $\frac{21}{20}4^{A}(c+\ell_0)$ is below $e^{\ell_0/2}$, with the
step from the design point $\ell_0 = 3.2A$ to the whole half-line
$\ell_0 \ge 3.2A$ carried by a single application of $1 + x \le e^{x}$
(\leaninline{Salt.Entropy.Chowla.flatWidth\_coef},
\leaninline{flatWidth\_engine\_at}, \leaninline{flatWidth\_engine}). The
$A$-free form is what \S\ref{sec:pearl}'s road consumes; stating it
$A$-free is what keeps the design constant out of a hundred downstream
statements.

\subsection{The proof}
All four statements come from one per-step bracket, proved in
\S\ref{sec:flat} as Lemma~\ref{lem:step}: under the invariant (H3) at
level $j$,
\begin{equation}\label{eq:stepbracket}
\frac{20/21}{L_j} \;\le\; w_{j+1} - w_j \;\le\; \frac{1}{L_j}.
\end{equation}
Given \eqref{eq:stepbracket} the rest is bookkeeping in three moves.

\emph{Telescoping.} Multiply \eqref{eq:stepbracket} by $1/(2A)$ and
compare with the summand $1/(2AL_j)$ of $S_J$ term by term; summing over
$j < J$ and telescoping $\sum_{j<J}(w_{j+1}-w_j) = w_J - w_0$ gives
Theorem~\ref{thm:master}, the upper half acquiring the factor $21/20 =
(20/21)^{-1}$ and the lower half none.

\emph{Exponentiating, necessity.} If $\log 2 < S_J$, the upper half of the
master law gives $\log 2 < \frac{21}{40A}(w_J - w_0)$, i.e.
$w_J - w_0 > \frac{40}{21}A\log 2 = \frac{20}{21}A\log 4$. Since
$w = \log(c+\ell)$, exponentiating is Theorem~\ref{thm:tollge}. The
$\log 4$ is where the base $4$ of the toll comes from: the budget is
$\log 2$ and the drop carries a factor $\frac{1}{2A}$, so the exponent
carries $2\log 2$.

\emph{Exponentiating, no overshoot.} At $J_{\min} = k+1$, minimality gives
$S_k \le \log 2$, and the lower half of the master law converts that into
$w_k - w_0 \le 2A\log 2 = A \log 4$. One further step costs at most
$1/L_k \le 1/300 \le 1/21 \le \log(21/20)$, using $L_k \ge 300$ from (H3)
and $c + \ell_k \ge 3$. Hence
$w_{J_{\min}} - w_0 \le \log\frac{21}{20} + A\log 4$, which is
Theorem~\ref{thm:tollle}. Proposition~\ref{prop:crossing} is the crude
direction: each step adds at least $1$ to $L$, so $L_j \ge L_0 + j$, and
at $J^\star$ the potential has grown past $\log 2 + A\log 4$, which the
master law's lower half converts into a crossing.

\subsection{What the toll means}
We record three readings, in decreasing order of how much of each is
kernel-checked.

\emph{First: the price is a conservation law, not an optimization
target.} Theorem~\ref{thm:master} is two-sided. A cleverer tower of the
same shape cannot cross the budget more cheaply, because the decrement it
accumulates is its potential difference to within $5\%$, and the potential
difference is exactly what the crossing consumes. There is no slack to
find: the entire price of crossing $\log 2$ at design constant $A$ is
$4^{A}$ in doubly-logarithmic width, and both directions of that sentence
are theorems.

\emph{Second: the invoice has two axes, and a design chooses only the
axis.} The two thresholds and their two budget floors were set against
each other in \S\ref{sec:intro}; what this section adds is the kernel's
half of each claim. On the flat road the toll is a factor $4^{A}$ on the
width (Theorems~\ref{thm:tollge}--\ref{thm:tollle}). On the original
road the same telescoping, run with potential $\log\log\log\log H$,
yields not a factor but a \emph{power}:
$(\log\log B)^{3} \le \log\log H_{J_{\min}} \le (\log\log B)^{5}$
(\leaninline{Salt.Entropy.Chowla.tower\_loglog\_ge},
\leaninline{tower\_loglog\_le}). A power on the doubly-logarithmic axis
is a height on the scale axis, and the two budget floors of
\S\ref{sec:intro} record exactly that
(\leaninline{budgetFloor} against \leaninline{budgetFloorFlat}), with
the design constant's own demand
$A \ge 2304\log 4/(\varepsilon^{6}\beta^{2})$
(\leaninline{budgetAFlat}) absorbing what the third logarithm used to
absorb. Two roads pay one invoice on two axes. This paragraph's individual
claims are each kernel-checked. The sentence that fuses them --- that
\emph{every} threshold pays the same toll on one axis or the other --- is
proved below on the width axis, for every threshold schedule feeding this
step (Theorem~\ref{thm:shapetoll}); the further reading that the two axes
carry the same invoice remains a reading and not a theorem.

\emph{Third: the shape of the threshold does not change the price.} It is
natural to ask whether some other threshold shape evades the toll. Let
$\varphi : \mathbb{N} \to \mathbb{R}$ be an arbitrary \emph{schedule} --- a
different design constant at every level, chosen with full knowledge of the
tower --- and let the schedule tower and its telescoped decrement be
\[
H_0 = B, \qquad H_{j+1} = H_j\bigl\lfloor 2\varphi_j \log H_j\bigr\rfloor,
\qquad
S_J = \sum_{j<J} \frac{1}{2\varphi_j \log H_j}
\]
(\leaninline{Salt.Entropy.Chowla.chowlaTowerShape},
\leaninline{towerDropSumShape}). Fix a \emph{budget floor} $A_b \ge 1$ with
$\varphi_j \ge A_b$ at every level, and measure the width at that floor:
$c_b = \log 2A_b$, $\ell_j = \log\log H_j$, $w_j = \log(c_b+\ell_j)$
(\leaninline{towerLS, towerLamS, towerWS}).

\begin{theorem}[The shape-free toll]\label{thm:shapetoll}
Let $A_b \ge 1$, let $\varphi_j \ge A_b$ for every $j$, let
$B \ge 4\cdot10^{6}$, and let the flat floor hold at the budget constant,
$100(c_b + \ell_0) \le L_0$. Then for \emph{every} $J$ at which the
schedule's decrement crosses the one-bit budget, $\log 2 < S_J$,
\[
\bigl(c_b + \ell_0\bigr)\,4^{(20/21)A_b} \;\le\; c_b + \ell_J .
\]
\leanname{Salt.Entropy.Chowla.towerShape\_width\_ge}
\end{theorem}

\begin{corollary}[The flat tower is extremal]\label{cor:extremal}
Take $A_b = A \ge 1$ and the hypotheses of Theorem~\ref{thm:shapetoll}.
Every schedule with budget floor $A$ multiplies the doubly-logarithmic
width by at least $4^{(20/21)A}$ when it crosses the budget; the constant
schedule $\varphi \equiv A$, which is the flat tower of
Definition~\ref{def:flattower}, multiplies it by at most
$\frac{21}{20}4^{A}$ at its minimal crossing length. The two bounds differ
by the factor $\frac{21}{20}\,4^{A/21}$.
\leanname{Salt.Entropy.Chowla.chowlaTowerShape\_const,
Salt.Entropy.Chowla.towerShape\_flat\_le}
\end{corollary}

Three observations make the schedule tower cost little beyond
\S\ref{sec:flat}. First, the per-step bracket \eqref{eq:stepbracket} is
proved over abstract reals (\leaninline{flatStep\_dw\_le},
\leaninline{flatStep\_dw\_ge}), so it is available at any step size.
Second, the floor price transfers in one direction only: $\lfloor\cdot\rfloor$
and $\log$ are monotone, so $\varphi_j \ge A_b$ gives
$\log\lfloor 2\varphi_j L_j\rfloor \ge c_b + \ell_j - \frac{1}{500}$
(\leaninline{shapeStep\_logMul\_ge}), while the companion upper price
$\log\lfloor 2\varphi_j L_j\rfloor \le c_b + \ell_j$ fails as soon as
$\varphi_j > A_b$. It is not needed: the necessity direction consumes only
the lower half. Removing it from the per-step law costs one monotonicity
step --- $w_{j+1} - w_j$ increases with the step, so truncating the step at
$\min(d_j,\, c_b + \ell_j)$, which does satisfy both flat hypotheses,
only decreases it (\leaninline{shapeStep\_dw\_ge}). Third, the invariant
(H3) propagates for every schedule (\leaninline{towerShape\_inv}), and here
the schedule tower is the easier case: $\Delta\ell_j \le d_j/L_j$ and
$100\,d_j/L_j \le d_j$ already at $L_j \ge 100$, so a larger multiplier
only helps.

Theorem~\ref{thm:shapetoll} quantifies over thresholds, not over step laws.
Its step is $H \mapsto H\lfloor 2\varphi\log H\rfloor$, the step that the
entropy decrement of \S\ref{sec:toll} funds; a decrement lemma whose
concatenation error is not of the form $\varepsilon^{2}/m$ would fund a
different step, and whether such a lemma exists is open. The exploratory
pass that preceded this theorem (nineteen probes, recorded in the
repository's ledger) also closed two escape routes that are not step-law
changes, cheap levels and $Q$-ary steps; those two closures are probe
records, not kernel-checked statements, and nothing above rests on them.

The practical corollary of the toll is one the campaign paid for three
times: an apparent wall in this method --- a triple-exponential floor, a
frame constant at a door, a window on the design constant --- is worth
re-reading as an invoice in the wrong coordinate before it is treated as a
height. In all three episodes the obstruction moved when the coordinates
changed, though not in the same way: \S\ref{sec:method} records the
distinction between the two that dissolved on re-reading and the one
that was proved impassable as posed and had to be routed around. What
did not move, in any of them, is $4^{A}$.

% ============================================================================
\section{The flat tower}\label{sec:flat}
% ============================================================================

This section proves the per-step bracket \eqref{eq:stepbracket} on which
\S\ref{sec:toll} rests, and records the furniture that makes it available
at every level of the tower. Everything here is unconditional given (H1),
(H2), (H3).

\subsection{The tower does not stall}
The first thing to check about Definition~\ref{def:flattower} is that it
is a tower at all: if the multiplier were $1$ the sequence would be
constant and the decrement would never accumulate. Under (H1) and (H2),
$\lfloor 2A\log H\rfloor \ge 2$ for every $H \ge 4\cdot10^{6}$, since
$\log H \ge 15$ and $A \ge 1$
(\leaninline{Salt.Entropy.Chowla.flatMul\_ge\_two}). Hence every level
stays above the base floor,
$B \le H_j$ and $4\cdot10^{6}\le H_j$
(\leaninline{chowlaTowerFlat\_base\_ge}, \leaninline{chowlaTowerFlat\_base\_floor}),
and therefore $L_j \ge 15$ and $\ell_j \ge 2$ at every level
(\leaninline{towerLF\_ge\_fifteen}, \leaninline{towerLamF\_ge\_two}). The
step in the glyph $L$ is exactly additive,
\[
L_{j+1} \;=\; L_j + d_j, \qquad d_j := \log\bigl\lfloor 2A L_j\bigr\rfloor ,
\]
\leaninline{Salt.Entropy.Chowla.towerLF\_succ}.

\subsection{The floor price}
The floor function in the multiplier is the only inexactness in the whole
construction, and it is priced on both sides.

\begin{lemma}[The floor price]\label{lem:floorprice}
Assume (H1), (H2). At every level, $d_j \le c + \ell_j$. If moreover
the invariant $100(c+\ell_j) \le L_j$ holds at that level, then
\[
c + \ell_j - \tfrac{1}{500} \;\le\; d_j \;\le\; c + \ell_j .
\]
\leanname{Salt.Entropy.Chowla.flatStep\_logMul\_le,
Salt.Entropy.Chowla.flatStep\_logMul\_ge}
\end{lemma}

The upper half is $\log\lfloor x\rfloor \le \log x$ with
$\log(2AL) = c + \ell$ (\leaninline{flatLog\_prod}). The lower half is
$\log x - \log\lfloor x \rfloor \le (x - \lfloor x\rfloor)/\lfloor
x\rfloor \le 1/\lfloor x \rfloor$, and the invariant forces
$c + \ell \ge 3$ (\leaninline{flatLevel\_s\_ge\_three}), hence
$L \ge 300$, hence $2AL \ge 600$ and $\lfloor 2AL\rfloor \ge 599$: the
floor costs at most $1/599 \le 1/500$ in the logarithm. That $1/500$ is
the only place where the discreteness of the construction is paid for, and
it is one of the three items inside the $21/20$ of
Theorem~\ref{thm:master}.

\subsection{The invariant}

\begin{proposition}[The flat floor propagates]\label{prop:inv}
Assume (H1), (H2) and the base hypothesis (H3). Then
$100(c + \ell_j) \le L_j$ at every level $j$.
\leanname{Salt.Entropy.Chowla.towerFlat\_inv}
\end{proposition}

The induction is one line of arithmetic once Lemma~\ref{lem:floorprice} is
available: the step adds $d_j \ge 1$ to $L$ and at most $d_j/L_j \le
1/100$ to $\ell$, so the slack $L - 100(c+\ell)$ does not decrease.
The proposition is what allows every subsequent statement to assume the
invariant at an arbitrary level while hypothesizing it only at the base.

\subsection{The per-step bracket}
The heart of the toll is a statement about four real numbers and has
nothing to do with towers; the corpus states it that way, over abstract
reals, and then applies it at each level.

\begin{lemma}[The per-step bracket]\label{lem:step}
Let $L, L', d, c \in \mathbb{R}$ with $L' = L + d$, and set
$\ell = \log L$, $\ell' = \log L'$, $w = \log(c+\ell)$,
$w' = \log(c+\ell')$. Then:
\begin{enumerate}
\item[(i)] if $L > 0$, $c + \ell > 0$, $0 \le d \le c + \ell$, then
  $w' - w \le 1/L$;
\item[(ii)] if $c + \ell \ge 3$, $100(c+\ell) \le L$, and
  $c + \ell - \tfrac{1}{500} \le d \le c + \ell$, then
  $(20/21)/L \le w' - w$.
\end{enumerate}
\leanname{Salt.Entropy.Chowla.flatStep\_dw\_le,
Salt.Entropy.Chowla.flatStep\_dw\_ge}
\end{lemma}

Both halves are two applications of an elementary logarithm estimate, one
per glyph level. For (i) it is $\log x \le x - 1$ in the form
$\log b - \log a \le (b-a)/a$: first at the $L$ level,
$\ell' - \ell \le d/L$, then at the $c+\ell$ level,
$w' - w \le (\ell'-\ell)/(c+\ell)$; multiplying and using
$d \le c + \ell$ gives $w' - w \le 1/L$ with no error term at all. For
(ii) it is the dual $1 - 1/x \le \log x$, giving
$d/L' \le \ell' - \ell$ and
$(\ell'-\ell)/(c+\ell') \le w' - w$; the two second-order factors
$L' \le \frac{101}{100}L$ and $c + \ell' \le (c+\ell) + \frac{1}{100}$
(both from the invariant) and the floor price
$d \ge (c+\ell) - \frac{1}{500}$ then give the constant. At
$c + \ell \ge 3$ the constant this computation actually yields is
$0.986$; the lemma states $20/21 = 0.952\ldots$, which is what the
downstream arithmetic uses.

Applying Lemma~\ref{lem:step} at level $j$ with $d = d_j$, using
Proposition~\ref{prop:inv} for the invariant and
Lemma~\ref{lem:floorprice} for the floor price, gives
\eqref{eq:stepbracket} at every level
(\leaninline{Salt.Entropy.Chowla.towerStepF\_dw\_le},
\leaninline{towerStepF\_dw\_ge}), which is what \S\ref{sec:toll}'s proof
consumes.

\subsection{Reaching the crossing}
Proposition~\ref{prop:crossing} names a level at which the decrement has
crossed the budget. The estimate behind it is deliberately crude: since
$d_j \ge 1$ at every level, $L_j \ge L_0 + j$
(\leaninline{Salt.Entropy.Chowla.towerLF\_ge\_add}), so at
$J^\star = \lceil \exp(2(c+\ell_0)4^{A})\rceil$ one has
$\ell_{J^\star} \ge 2(c+\ell_0)4^{A}$, hence
$w_{J^\star} - w_0 \ge \log 2 + A\log 4$, and the master law's lower half
turns that into $S_{J^\star} > \log 2$. The named level is superexponentially
larger than necessary --- the toll's own bracket \eqref{eq:bracket} shows
the crossing happens once the width has been multiplied by about $4^{A}$ ---
but it is explicit, which is what a regime builder needs: a Chowla regime
must exhibit a tower length, not merely know one exists.

The minimal length $J_{\min}$ is then the infimum of a nonempty set of
naturals (\leaninline{towerFlatJmin}), positive because $S_0 = 0 < \log 2$
(\leaninline{towerFlatJmin\_pos}), and every shorter tower stays under the
budget (\leaninline{towerDropSumFlat\_le\_log\_two\_of\_lt\_Jmin}) --- the
minimality fact that Theorem~\ref{thm:tollle} runs on.

\subsection{What the tower is for}
The flat tower was not built to prove the toll; it was built because the
original tower's third logarithm put a triple exponential in the budget
floor, and the toll is what the flat tower's audit produced. The regime
structure that consumes it carries the design constant $A$, the design law
$3.2A \le \log\log H_-$, a tower length, and the crossing
(\leaninline{Salt.Entropy.Chowla.ChowlaRegimeFlat}); the design law alone
discharges the flat floor (H3), the width export
(Corollary~\ref{cor:export}), and the height-1 budget floor. That
consumption is the subject of \S\ref{sec:pearl}.

% ============================================================================
\section{The pearl}\label{sec:pearl}
% ============================================================================

\subsection{The target}
A \emph{Chowla regime} is the corpus's record of one working scale of the
entropy-decrement argument: an outer scale $x$, a logarithmic window width
$\omega$ (the correlation sum runs over $n \in (x/\omega,\,x]$), a stride, a
tolerance $\varepsilon \le 1/2$, an admissible window pair $H_- \le H_+$ with
$H_- \ge 4\cdot10^{6}$, a tower ratio and a tower length, together with the
side conditions those data must satisfy --- among them that the tower fits
below $H_+$ and that its telescoped decrement crosses $\log 2$.
\leanname{Salt.Entropy.Chowla.\allowbreak ChowlaRegime (Salt/\allowbreak Entropy/\allowbreak Chowla/\allowbreak Regime.lean:56)}
The flat extension adds the design constant $A \ge 26$, the design law
$3.2A \le \log\log H_-$, a flat tower length, and the flat tower's own fit and
crossing (\leaninline{ChowlaRegimeFlat},
\texttt{Salt/\allowbreak Entropy/\allowbreak Chowla/\allowbreak TowerFlatRegime.lean:122}); it is the structure
whose consumption \S\ref{sec:flat} announced.

Failure of the two-point statement at a regime is a Prop about that regime's
own data:
\begin{equation}\label{eq:fails}
\mathrm{logChowla2Fails}(\varepsilon, x, \omega)
\quad:\Longleftrightarrow\quad
\varepsilon\,\log\omega \;<\;
\Bigl|\sum_{x/\omega < n \le x} \frac{\lambda(n)\,\lambda(n+1)}{n}\Bigr| ,
\end{equation}
with the right-hand side unnormalized: this is (2.4) of \cite{TaoChowla} at the
Liouville model $a=1$, $b=0$, $h=1$, $g_1 = g_2 = \lambda$.
\leanname{Salt.Entropy.Chowla.logChowla2Fails
(Salt/Entropy/Chowla/ChowlaFailure.lean:59)}

Denying \eqref{eq:fails} at one regime is not \eqref{eq:chowla}, and this
paper claims no more than it proves: the denial says that the correlation on
the window $(x/\omega,\,x]$ of that regime does not exceed
$\varepsilon\log\omega$ in absolute value. What the pearl adds to a single
denial is depth. The regime's window base is pinned --- exported as an
equation, not a bound --- at
\[
\mathrm{flatDesignBase}(A) \;=\; \bigl\lceil e^{e^{3.2A}} \bigr\rceil
\]
\leanname{Salt.Entropy.Chowla.flatDesignBase
(Salt/\allowbreak Entropy/\allowbreak Chowla/\allowbreak TowerFlatBuilder.lean:95)}
\noindent with $A$ above any floor the caller prescribes, and its outer scale exceeds
$g(H_+, \omega)$ for a function $g$ the caller supplies. Both quantifiers are
inside the statement.

\subsection{The statement}
We state the theorem in the form the corpus carries it, with every hypothesis
at the statement.

\begin{theorem}[The pearl; conditional, hypotheses in full]\label{thm:pearl}
Assume the band-lane hypothesis
$\mathrm{S16BandLaneCBoundedL}(32\cdot10^{6})$ of \S\ref{sec:pearl-hyp}, and
let $A_0 \ge 162$. Then there exist $\varepsilon \in \mathbb{Q}$, reals
$C_g, K_c, \delta_0, C_t, A, \beta$, naturals $x_0, H_{\mathrm{opq}},
M_{\mathrm{fl}}$ and reals $C_q, c_s, T_0, K_q, K_s, C$ such that:
\begin{enumerate}
\item[(a)] \emph{(sign and size)} $0 < \varepsilon$, $1 \le C_g$, $0 < K_c$,
  $0 < \delta_0$, $0 < C_t$, $1 \le M_{\mathrm{fl}}$, $0 < C_q$, $0 < c_s$,
  $3 \le T_0$, $0 < K_q$, $0 < K_s$, $0 < C$, $\log C \le 40$,
  $C_g \le 2\cdot10^{12}$, $\tfrac{1}{500} \le \varepsilon$,
  $\tfrac{1}{838400} \le \delta_0$, $M_{\mathrm{fl}} \le 2^{355}$,
  $0 < \beta$;
\item[(b)] \emph{(the design constant)} $162 \le A$, $A_0 \le A$, and
  $\mathrm{budgetAFlat}(\varepsilon,\beta) \le A$;
\item[(c)] \emph{(the arm census)} if
  $H_{\mathrm{opq}} \le \mathrm{flatDesignBase}(A)$ then\\
  $\mathrm{flatWitFloor}(\varepsilon,\beta,A,H_{\mathrm{opq}})
   = \mathrm{flatDesignBase}(A)$;
\item[(d)] \emph{(the conditional)} under the eight hypotheses
\[
K_c \le 2^{539},\quad C_t \le 2^{23},\quad
x_0 \le e^{e^{3.2A}/10},\quad
H_{\mathrm{opq}} \le \mathrm{flatDesignBase}(A),
\]
\[
e^{-100}\le c_s,\quad T_0 \le e^{e^{100}},\quad K_q \le e^{100},\quad
e^{-100}\le K_s,
\]
for every $g : \mathbb{N}\to\mathbb{N}\to\mathbb{N}$ there is a Chowla regime
$R$ with
\[
R.\varepsilon = \varepsilon,\quad
R.H_- = \mathrm{flatWitFloor}(\varepsilon,\beta,A,H_{\mathrm{opq}}),\quad
g(R.H_+, R.\omega) \le R.x,
\]
\[
50 \le \log\log R.H_- \;\Rightarrow\;
\log\log R.H_+ \le \exp\bigl(\tfrac{1}{2}\log\log R.H_-\bigr),
\qquad
3.2A \le \log\log R.H_-,
\]
\[
\log\log R.H_+ \;\le\; 2\,e^{1.6A},
\]
and such that
\[
\begin{aligned}
&\mathrm{S16CofactorSupply\_L\_gk}(32\cdot10^{6}, C_q, R,
  \mathrm{flatDoorM}(A))\\
&\quad\Rightarrow\;
\mathrm{S16BaseScaleCap96\_L\_gk}(32\cdot10^{6}, R, \mathrm{flatDoorM}(A))
\end{aligned}
\]
\[
\Rightarrow\;
\lnot\,\mathrm{logChowla2Fails}(R.\varepsilon, R.x, R.\omega).
\]
\end{enumerate}
\leanname{Salt.MR.logChowla2\_witnessed\_scale\_flat\_L\_v2
(Salt/MR/S16FlatFinal.lean:136)}
\end{theorem}

Table~\ref{tab:dials} labels the sixteen constants, each traced to the
declaration that mints it. They arrive in five groups: the spine's own
dials; the exported witnesses of the conditional hop, each carrying a
pin even where no rider consumes it (the theorem reports the sizes of
its internal constants); the opaque Siegel pair; the four constants of
the crossing supply; and the co-factor gate with the coprime-tail
export.

\begin{table}[h]
\centering\scriptsize\setlength{\tabcolsep}{4pt}
\begin{tabular}{llll}
\toprule
Constant & What it is & Pin --- and how tight & Spent at \\
\midrule
$\varepsilon$ & the spine's window parameter & $\tfrac{1}{500}\le\varepsilon$, attained exactly & the tolerance $R.\varepsilon$ \\
$A$ & the design constant, symbolic & $162\le A$, binding (via $M_{\mathrm{fl}}$'s pin) & the base, door, width \\
$\beta$ & the decrement bracket parameter & positivity only & $\mathrm{budgetAFlat}(\varepsilon,\beta)\le A$ \\
$\delta_0$ & the spine's budget decrement & $\tfrac{1}{838400}\le\delta_0$, attained exactly & exported witness \\
\midrule
$C_g$ & the sieve-mass constant & $\le 2\cdot10^{12}$, true at $1.6\cdot10^{12}$ & exported witness \\
$K_c$ & the large-spectrum bound & $\le 2^{539}$, true at $2^{538.42}$ & rider 1 \\
$C_t$ & the constant-pool constant & $\le 2^{23}$, true at $2^{22.78}$ & rider 2 \\
$M_{\mathrm{fl}}$ & the graded door-modulus floor & $\le 2^{355}$, 49 orders of margin & exported witness \\
\midrule
$x_0$ & the band supplier's threshold & inherits Siegel's ineffectivity & rider 3's window \\
$H_{\mathrm{opq}}$ & the road's opaque floor arm & carries $K_{\mathrm{Chen}}$ (Siegel) & rider 4, census (c) \\
\midrule
$c_s$ & the decay constant & rider met with 33 orders of room & rider 5 \\
$T_0$ & the socket floor & the unreachable rider (\S\ref{sec:pearl-defects}) & rider 6 \\
$K_q$ & the below-floor comparison & rider met with 46 orders of room & rider 7 \\
$K_s$ & the port's one ineffective constant & unbounded from the needed side & rider 8 \\
\midrule
$C_q$ & the co-factor grade gate & positivity only & inside the co-factor predicate \\
$C$ & the coprime-tail export & $\log C\le 40$, true at ${\approx}28$ & a pure export \\
\bottomrule
\end{tabular}
\caption{The dials of Theorem~\ref{thm:pearl}, each traced to the
declaration that mints it. An ``exported witness'' is a constant the
$\exists$-prefix reports with a pin although no rider consumes it. The
constant $C$ here is the coprime-tail export; it is \emph{not} the
band-lane constant of the outer hypothesis, whose trace
\S\ref{sec:pearl-hyp} discusses --- the two share a numeral and nothing
else.}
\label{tab:dials}
\end{table}

Three of the conclusion's lines are the tower's, and \S\ref{sec:toll} is where
they come from. The line $3.2A \le \log\log R.H_-$ is the design law, which the
witness floor satisfies by construction
(\leaninline{Salt.MR.flatWitFloor\_design}); under the arm-census hypothesis
that floor \emph{equals} the design base $\lceil e^{e^{3.2A}}\rceil$, by (c),
which is how the base comes to be exported as an equation. The implication
$50 \le \log\log R.H_- \Rightarrow \log\log R.H_+ \le
\exp(\tfrac12\log\log R.H_-)$ is Corollary~\ref{cor:export} in the regime's own
variables --- the $A$-free width export, which is what lets the terminal state
a width certificate without carrying the design constant into it. The line
$\log\log R.H_+ \le 2e^{1.6A}$ is that export evaluated at the pinned base:
$\log\log H_- \le 3.2A + \log 2$, so
$\exp(\tfrac12\log\log H_-) \le \sqrt{2}\,e^{1.6A} \le 2e^{1.6A}$
(\leaninline{Salt.MR.flat\_L\_width\_priced}). This is the toll arriving at the
pearl: the width the tower must buy, priced in the coordinate the downstream
lemmas read.

\subsection{The hypothesis list, item by item}\label{sec:pearl-hyp}
Theorem~\ref{thm:pearl} is conditional in three separate ways, and they are
different in kind.

{\emergencystretch=2.5em
\emph{The outer hypothesis.} $\mathrm{S16BandLaneCBoundedL}(32\cdot10^{6})$
is the band lane's constant hypothesis
(\leaninline{Salt/\allowbreak MR/\allowbreak S16FlatTerminalLinear.lean:49}). Everything in it beyond a landed unconditional theorem
(\leaninline{Salt.MR.\allowbreak mmuChiRate\_holds\_gated}, carried through
\leaninline{S11HoistLinear}) is the single conjunct $\log C_{\mathrm{band}}
\le 40$. It is not of Siegel genre: the constant is effective, and a trace of
every existential hop that mints it puts it at
$\log C_{\mathrm{band}} \approx 22\,661$, dominated by a literal
double-exponential threshold in the zero-free-region lane. So the numeral $40$
is not payable as stated. \S\ref{sec:limit} records what happened to it.\par}

\emph{The two Siegel-genre riders.} $x_0 \le e^{e^{3.2A}/10}$ and
$H_{\mathrm{opq}} \le \lceil e^{e^{3.2A}}\rceil$ are windows, not bounds: each
compares a constant the theorem produces against a double exponential in the
design constant. $x_0$ is the band lane's opaque threshold; $H_{\mathrm{opq}}$
is the road's opaque floor arm, whose own constituents ride the
Siegel-ineffective constant of the Chen-type input. Neither constant is
effectively bounded anywhere in the corpus or in the literature.
\S\ref{sec:limit} removes both, at the price named there.

\emph{The six numeric riders.} $K_c \le 2^{539}$ and $C_t \le 2^{23}$ are
ceilings on socket constants; $e^{-100} \le c_s$, $T_0 \le e^{e^{100}}$,
$K_q \le e^{100}$ and $e^{-100} \le K_s$ come from the crossing supply. All six
are spent on constants \emph{this theorem existentially produces}: they are
conjuncts the $\exists$-prefix could in principle export and does not, because
every hop that mints such a constant packs it into an
$\exists\,C,\ 0 < C \wedge \ldots$ that exports positivity alone.

{\sloppy
\emph{The two carried predicates.} The conclusion's implication asks for
$\mathrm{S16CofactorSupply\_L\_gk}$ --- a supply statement about the co-factor
block at the linear door and socket --- and
$\mathrm{S16BaseScaleCap96\_L\_gk}$, the base-scale cap\par}
\[
\forall\ \text{sockets } (H,L,q,j,A',s):\quad
\log\log(A'+s) \;\le\;
\frac{\log \mathcal{P}\bigl(\mathrm{AdoorL}(M), \mathrm{s13GK}(K,M), 2\bigr)}
     {9.60000096} ,
\]
both at \leaninline{Salt/MR/S13CapGateLinear.lean:891,897}. A theorem of the
form $P \to Q \to \lnot F$ says nothing about $F$ when $P$ or $Q$ is false at
the objects the theorem itself produces. That observation is the subject of the
next subsection.

\subsection{Two defects, found and repaired}\label{sec:pearl-defects}
On 2 August 2026 two of Theorem~\ref{thm:pearl}'s hypotheses were shown to be
unsatisfiable at the objects the theorem produces. Both were found by
instruments the corpus had built for other purposes, and both were repaired the
same day at the level of the statement. We record them because a paper that
lists only what survived its own checks is not evidence that the checks work.

\emph{The base-scale cap is false at the produced regime, and the width law of
\S\ref{sec:toll} is what refutes it.} The cap bounds $\log\log(A'+s)$ at every
socket by a quantity fixed by the lever $K$, which Theorem~\ref{thm:pearl}
pins at $K = 32\cdot10^{6}$; the right-hand side is then about $10^{9.6\cdot
10^{6}}$. But the socket's own conjuncts pin the block scale $A'+s$ at the
window endpoint --- the corpus's own inhabitation witness for the socket sits
at $A'+s = 2R.x$ exactly --- and at the endpoint the regime's outer scale is
governed by the tower, whose width Theorem~\ref{thm:tollge} bounds from
\emph{below}: crossing the entropy budget forces the doubly-logarithmic width
up by $4^{(20/21)A}$, which at $A = 162$ puts $\log\log(A'+s)$ at or above
$10^{1.76\cdot10^{95}}$. A fixed ceiling of $10^{9.6\cdot10^{6}}$ against a
demand of $10^{1.76\cdot10^{95}}$ is not a numeral that can be adjusted. The
consequence for Theorem~\ref{thm:pearl} as stated is exact and we state it
plainly: its inner implication has a false antecedent at the regime it
produces, so that implication carries no information about
\eqref{eq:fails} until the lever is changed. The finding was reached twice
independently (through the caller's $g$, and by arithmetic at $g \equiv 0$);
the arithmetic is probe arithmetic against kernel-checked inputs, not itself a
kernel-checked theorem, and we mark it as such. What \emph{is} kernel-checked
is the lower bound that forces it, Theorem~\ref{thm:tollge}.

Two features of this episode are worth naming. First, the refuter was the
paper's own unconditional theorem: the toll was proved on 1 August and refuted
a hypothesis of a terminal on 2 August. Second, the defect is invisible to an
axiom audit and to the kernel, both of which certify Theorem~\ref{thm:pearl}
as a true statement, which it is.

\emph{The $T_0$ numeral is mis-sized.} The rider $T_0 \le e^{e^{100}}$ is
unreachable along this proof. $T_0$ is a maximum of two thresholds; the
twisted row meets $e^{e^{100}}$ with equality, but the $\zeta$ row inherits the
threshold of the power-saving zero-free region, which is $e^{e^{A'}}$ with
$A' \ge 1100$ --- so $T_0 \ge e^{e^{1100}}$, and at the corpus's own lever
$T_0 \approx e^{e^{1251}}$. Again this is not refutation of a Prop but
unreachability along the proof, and again the repair is at the statement: the
sole consumer of the rider has a sharp sibling that asks only
$T_0 \le e^{\sqrt{H_-}/2}$, which was already landed. At $A \ge 162$ the pinned
base gives $\sqrt{H_-} \ge e^{e^{518.4}/2}$, so the sharp rider admits
$T_0$ up to $e^{\frac12 e^{6.4\cdot10^{224}}}$ against a witness at
$e^{e^{1251}} = e^{10^{543}}$: two exponential levels of clearance. (Numerical
illustration; the formal content is the displayed inequality.)

\subsection{The repaired terminal}\label{sec:pearl-repair}
Both repairs are statement-level and neither weakens a landed declaration:
Theorem~\ref{thm:pearl} stands as proved, marked in the repository's registry
with the vacuity reason, and the road was re-cut beside it.

The cap repair turns on the observation that the lever is a parameter, not a
constant of the mathematics. Set
\[
\mathrm{KlevF}(A) \;=\; \bigl\lceil k_{\mathrm{cap}}\, e^{1.6A}\bigr\rceil,
\qquad k_{\mathrm{cap}} := 4 .
\]
\leanname{Salt.MR.kcap, Salt.MR.KlevF (Salt/MR/KLever.lean:142,147)}
\noindent Two inequalities decide it. \emph{Admissible:}
$\mathrm{KlevF}(A) \le 1.7\cdot10^{8}\cdot\mathrm{flatDoorM}(A)$ for every
$A \ge 26$, the ceiling the door and the selection register both carry, with a
margin of $137$ uniform in $A$
(\leaninline{Salt.MR.KlevF\_le\_wideCeiling}). \emph{Sufficient:} the cap's
demand is $\log\log H_+ \le 2e^{1.6A}$, the width line the regime already
exports, and the supply at lever $K$ is $K\log 2 = k_{\mathrm{cap}}(\log 2)
e^{1.6A} = 2.7724\,e^{1.6A}$ --- a margin of $1.386$, which is why
$k_{\mathrm{cap}}$ is a named dial and not an inline numeral. At that lever the
cap is a theorem, on the regime's own exported ceilings plus
$\varepsilon \ge 1/500$:
\leanname{Salt.MR.s16\_baseScaleCap96\_L\_at\_klevF (Salt/MR/KLever.lean:427)}
The two levers are separated by exactly the coefficient comparison
$2 < k_{\mathrm{cap}}\log 2$.

The lane's terminal is therefore stated at an arbitrary admissible lever, so
that the instantiation $K := \mathrm{KlevF}(A)$ is available to its consumer,
and it is stated with the two 2~August repairs in place.

\begin{theorem}[The pearl, re-cut at an arbitrary
lever]\label{thm:pearl-recut}
Let $A_{\mathrm{win}} \in \mathbb{R}$ and assume the $A$-windowed band-lane
hypothesis $\mathrm{S16BandLaneCBoundedL\_winU}(A_{\mathrm{win}})$. Then there
exist $\varepsilon \in \mathbb{Q}$, reals $C_g, K_c, \delta_0, \beta$, naturals
$x_0, H_{\mathrm{opq}}, M_{\mathrm{fl}}$ and reals
$C_q, c_s, T_0, K_q, K_s, C$, satisfying the sign and size conditions of
Theorem~\ref{thm:pearl}(a) other than those on $C_t$, and with
$M_{\mathrm{fl}} \le \mathrm{flatDoorM}(A)$ at every admissible $A$ in place of
$M_{\mathrm{fl}} \le 2^{355}$, such that: for every lever $K \in \mathbb{N}$
there is
$C_t > 0$ such that for every design constant $A$ with $162 \le A$,
$A_{\mathrm{win}} \le A$, $\mathrm{budgetAFlat}(\varepsilon,\beta) \le A$ and
$K \le 1.7\cdot10^{8}\cdot\mathrm{flatDoorM}(A)$, the arm census
of Theorem~\ref{thm:pearl}(c) holds, and under the five hypotheses
\[
x_0 \le e^{e^{3.2A}/10},\qquad
H_{\mathrm{opq}} \le \mathrm{flatDesignBase}(A),\qquad
e^{-100} \le c_s,
\]
\[
T_0 \le \exp\Bigl(\tfrac12\sqrt{\mathrm{flatWitFloor}
(\varepsilon,\beta,A,H_{\mathrm{opq}})}\Bigr),
\qquad
e^{-100} \le K_s,
\]
for every $g$ obeying the caller-side rider
$\mathrm{XCeilRiderStrict}(\varepsilon, g)$ there is a Chowla regime $R$ with
the exports of Theorem~\ref{thm:pearl}(d), together with the outer-scale
ceiling $\log R.x \le (31/\varepsilon)\,R.H_+$, such that
\[
\begin{aligned}
&\mathrm{S16CofactorSupply\_L\_gk}(K, C_q, R, \mathrm{flatDoorM}(A))\\
&\quad\Rightarrow\;
\mathrm{S16BaseScaleCap96\_L\_gk}(K, R, \mathrm{flatDoorM}(A))
\end{aligned}
\]
\[
\Rightarrow\;
\lnot\,\mathrm{logChowla2Fails}(R.\varepsilon, R.x, R.\omega).
\]
\leanname{Salt.MR.\allowbreak logChowla2\_witnessed\_scale\_flat\_L\_v2\_%
\allowbreak uniform\_win\_xceil\_cqhoist
(Salt/\allowbreak MR/\allowbreak RegisterCompose.lean:209)}
\end{theorem}

Three differences from Theorem~\ref{thm:pearl} carry the content. The lever is
universally quantified, which is what makes $K := \mathrm{KlevF}(A)$ legal and
the base-scale cap a theorem rather than a hypothesis. The $T_0$ rider is the
sharp one. And the regime now exports its own outer-scale ceiling
$\log R.x \le (31/\varepsilon)R.H_+$, at the cost of a condition on the
function the caller supplies: $\mathrm{XCeilRiderStrict}(\varepsilon,g)$ asks
that on the gate the builder's own regime satisfies, $\log g(H_+,\omega) +
\varepsilon^{2}H_+ \le (31/\varepsilon)H_+$
(\leaninline{Salt/MR/XThread.lean:82}). The trade is in the consumer's favour:
Theorem~\ref{thm:pearl} asked for a property of the regime it produces, which
its consumer cannot inspect; Theorem~\ref{thm:pearl-recut} asks for a property
of the function its consumer writes down. The budget is
$(31/\varepsilon)H_+ \ge 15\,500\,H_+$ and the builder's own scale sits at
$\log x \approx 1.386\,\varepsilon^{2}H_+$, a ratio $22.4/\varepsilon^{3} \ge
2.8\cdot10^{9}$ at $\varepsilon \le 1/500$.

What Theorem~\ref{thm:pearl-recut} still carries, and \S\ref{sec:limit}
discharges, is the co-factor supply predicate and the two Siegel-genre windows.

% ============================================================================
\section{The classical limit}\label{sec:limit}
% ============================================================================

\subsection{The containment}
Two of Theorem~\ref{thm:pearl}'s eight hypotheses are windows rather than
bounds:
\[
x_0 \le e^{e^{3.2A}/10}
\qquad\text{and}\qquad
H_{\mathrm{opq}} \le \bigl\lceil e^{e^{3.2A}}\bigr\rceil .
\]
Each relates a constant produced by the theorem to a window that grows
with the design constant $A$, and $A$ is not bounded above: the theorem
produces, for every $A_0$, some $A \ge A_0$. That is the classical shape.
If the constants $x_0$ and $H_{\mathrm{opq}}$ do not themselves move with
$A_0$, then choosing
\[
A \;=\; \max\Bigl(\max(A_0, 162),\ \max\bigl(A_{\mathrm{budget}},\
\max(4x_0,\ H_{\mathrm{opq}})\bigr)\Bigr)
\]
discharges both windows outright, because the windows are double
exponentials and the constants are not: each falls to two applications of
$x + 1 \le e^{x}$. What is lost is effectivity --- the resulting $A$ is
bound by \texttt{Classical.choice} and no bound on it is claimed --- which
is precisely the caveat the analytic literature attaches to every
consequence of Siegel's theorem. The effective-conditional statement
contains the ineffective statement; the containment is the standard one,
and Step~1 of \S\ref{sec:chain} is its instance.

The device applies once, at the first corollary of \S\ref{sec:chain}, and is
then reused verbatim for every constant that is fixed while its window grows in
$A$: the band-lane constant, and the two Siegel-genre arms. What it never
touches is a constant on which the road's own conclusion depends --- those are
the riders that survive to the end.

\subsection{The hoist}
The containment argument above has a hypothesis we underlined: \emph{if
the constants do not themselves move with $A_0$}. At the quantifier order
in which Theorem~\ref{thm:pearl} is stated, they may. The statement reads
\[
\forall A_0 \ge 162,\ \exists\,(\varepsilon, \ldots, x_0,
H_{\mathrm{opq}}, A, \ldots),\ \Phi(A_0, \varepsilon,\ldots),
\]
so every constant is chosen after $A_0$. Nothing in the statement asserts
that the witness for $A_0 = 162$ and the witness for $A_0 = 10^{6}$ share
their $x_0$; and a consumer cannot recover it, because two applications of
\texttt{Classical.choose} at two values of $A_0$ are choices from two
different propositions ($A_0$ occurs in $\Phi$, via $A_0 \le A$), so proof
irrelevance does not identify them. The classical limit is unavailable
until the order is corrected.

At the level of the proof term the constants are $A_0$-free: the caller's
$A_0$ enters the whole road at exactly one line, where the design constant
is chosen as $A := \max(A_0, A_{\mathrm{budget}})$, and
$\varepsilon$, $K_c$, $\delta_0$, $\beta$ and $H_{\mathrm{opq}}$ are all
minted strictly above it, while $x_0$ and the band constant come from a
hypothesis with no $A$ in it at all. Turning that reading of the proof
into a property of the \emph{statement} is the hoist: seven theorems along
the road were re-cut, each as the landed statement with $A$ moved out of
the existential and the corresponding $\forall A$ moved inward to the
point where the landed proof chose it, so that the road reads
\[
\exists\,(\text{constants}),\ \ldots \ \wedge\ \forall A,\ 162 \le A
\to A_{\mathrm{budget}} \le A \to \exists\,H_{\mathrm{cap}},\ (\text{the
same body}).
\]
Every body is the landed body verbatim.
\leanname{Salt.MR.flat\_head\_uniform, flat\_socket\_uniform,
flat\_doorL2\_uniform, flat\_road\_uniform, flat\_capstone\_uniform,\\
flat\_conditional\_uniform,
logChowla2\_witnessed\_scale\_\allowbreak flat\_L\_v2\_uniform}

We draw one methodological conclusion, and it is not about this theorem.
The step ``the constants are fixed, the window grows, so take the
parameter large'' is performed silently in analytic arguments hundreds of
times, and it is almost always right. Whether it is right in a given
instance is a question about quantifier order, which prose records only in
the reader's head, and which a formal statement records as data. Here the
step was blocked, the blockage was visible in the statement, and the
repair --- seven verbatim re-cuts --- was mechanical once identified.

\subsection{The chain}\label{sec:chain}
With the order corrected, the classical limit is available, and it was taken.
What followed was six statements, each the previous one with one hypothesis
removed and the removal paid for. We give the chain because the arithmetic of
the payments is the content: at each step the item that left the statement left
for a stated reason, and the reasons are of four kinds --- a limit, a window, a
numeral proved at its leaves, and a predicate proved outright.

\emph{Step 1: the Siegel pair, by the limit.} The two windows of
\S\ref{sec:pearl-hyp} fall to the $\max$ of the containment argument. The
resulting statement carries the band-lane hypothesis, six numeric riders and
the two predicates, and no Siegel item of any kind.
\leanname{Salt.MR.logChowla2\_ineffective (Salt/MR/S16Uniform.lean:1061)}

\emph{Step 2: the band lane, by an $A$-window.} The numeral
$\log C_{\mathrm{band}} \le 40$ of \S\ref{sec:pearl-hyp} is unpayable, and it
is also not what the consumer needs: the constant is spent at exactly one site,
to put a graded constant under a pin which a later step charges against
$\mathrm{flatDoorM}(A)$. Re-cutting the band lane's hypothesis as the
$A$-window $\log C_{\mathrm{band}} \le 0.64A - 20$ makes it a consequence of
the landed hypothesis-free existential, and the same $\max$ device then
discharges it. The band constant is $A$-free and $K$-free, a property that
Step 4 will need in the stronger form of one witness
$A_{\mathrm{win}} \approx 3.5\cdot10^{4}$ serving every lever. The outer
hypothesis list is now empty: the caller supplies only the depth $A_0$.
\leanname{Salt.MR.logChowla2\_ineffective\_v2 (Salt/MR/S16Uniform.lean:1707)}

\emph{Step 3: $K_c$, $C_t$, $K_q$, by conjunct-carry.} Each of the three was a
rider only because the hop that mints it exports positivity alone. Pricing them
at their leaves gives $K_c \le 2^{228.2}$ against a rider of $2^{539}$ (more
than $300$ bits of room), $C_t = 6e^{14} = 2^{22.78}$ against $2^{23}$
(occupying $86\%$ of its budget), and $K_q = 1.26848\cdot10^{-3}$ against
$e^{100}$ ($46$ orders); carrying the three conjuncts up the lane spends them
where the proof needs them.
\leanname{Salt.MR.logChowla2\_ineffective\_v3 (Salt/MR/S16Compose.lean:1112)}

\emph{Step 4: the base-scale cap, at the raised lever; and the $T_0$ reshape.}
This is \S\ref{sec:pearl-defects} arriving in the corollary line: the lever is
raised to $\mathrm{KlevF}(A)$, where the cap is a theorem, and the predicate is
deleted from the statement; the $T_0$ numeral is replaced by its consumer's
tolerance. What appears in their place is one new conclusion-side antecedent,
the outer-scale ceiling $\log R.x \le (31/\varepsilon)R.H_+$ --- the builder's
own law, which the lane did not yet export.
\leanname{Salt.MR.logChowla2\_ineffective\_v4 (Salt/MR/S16ComposeV4.lean:1002)}

\emph{Step 5: the outer-scale ceiling, by threading.} The builder's law is
carried down every hop of the uniform lane, so the antecedent becomes an
export. The cost is the caller-side rider
$\mathrm{XCeilRiderStrict}(\varepsilon,g)$ of \S\ref{sec:pearl-repair}. One
conclusion-side predicate remains, the co-factor supply.
\leanname{Salt.MR.logChowla2\_ineffective\_v5 (Salt/MR/XThread.lean:1367)}

\emph{Step 6: the co-factor supply, by repairing its register and
instantiating its supplier.} The predicate had a landed reduction: a named
seventeen-conjunct register implies it, uniformly in the lever. On 3 August
that register was proved \emph{false} at every socket the terminal produces ---
its own conjuncts, at the trivial dilation ladder $D \equiv 1$, chain into a
fixed constant standing above an unbounded function of the scale. Unlike the
cap refutation of \S\ref{sec:pearl-defects}, this one is a theorem:
\leanname{Salt.MR.cofkL\_bulk\_false\_at\_socket
(Salt/MR/RegisterInhabit.lean:260)}
The route died and the object it fed did not; Step 5's statement is
unaffected and remains citable. The repair re-instantiates the supplier at the
ladder the exponent count demands, $D = \lceil \log X\rceil$, and inhabits all
seventeen conjuncts --- two of which were false at the bottom of the $T$-range
until the socket's own forcing was found, and two of which turned out to be
theorems rather than the carried gates a previous reading had recorded. One
prerequisite had to be cleared first, and it was again a quantifier order: the
threshold that absorbs the supply reads $C_q$, and the design constant must
clear that threshold, so $A$ must be chosen after $C_q$ --- while the lane
chose $A$ first. Since $C_q$ is independent of the lever, moving $\forall K$
inside the existential of the $K$-free constants closes the circle.
\leanname{Salt.MR.cofkR\_cofactorSupply\_L\_gk (Salt/MR/RegisterRepair.lean:476)}

\subsection{The corollary}
\begin{corollary}[The classical limit; ineffective in $A$]\label{cor:v6}
For every $A_0 \in \mathbb{R}$ there exist $\varepsilon \in \mathbb{Q}$, reals
$C_g, K_c, \delta_0, C_t, A, \beta$, a natural $M_{\mathrm{fl}}$, reals
$C_q, c_s, T_0, K_q, K_s, C$, and a function
$K_{\mathrm{vt}} : \mathbb{N}\to\mathbb{N}\to\mathbb{R}$, with the sign and
size conditions of Theorem~\ref{thm:pearl}(a), except that
$M_{\mathrm{fl}} \le \mathrm{flatDoorM}(A)$ stands in place of
$M_{\mathrm{fl}} \le 2^{355}$, and with $162 \le A$, $A_0 \le A$ and
$K_{\mathrm{vt}} \ge 0$ everywhere, such that, under the three riders
\[
e^{-100} \le c_s,
\qquad
T_0 \le \exp\Bigl(\tfrac12\sqrt{\mathrm{flatDesignBase}(A)}\Bigr),
\qquad
e^{-100} \le K_s ,
\]
for every $g : \mathbb{N}\to\mathbb{N}\to\mathbb{N}$ obeying
$\mathrm{XCeilRiderStrict}(\varepsilon, g)$ there is a Chowla regime $R$ with
\[
R.\varepsilon = \varepsilon, \qquad
R.H_- = \mathrm{flatDesignBase}(A) = \bigl\lceil e^{e^{3.2A}}\bigr\rceil,
\qquad g(R.H_+, R.\omega) \le R.x,
\]
\[
50 \le \log\log R.H_- \Rightarrow
\log\log R.H_+ \le \exp\bigl(\tfrac12\log\log R.H_-\bigr),
\qquad
3.2A \le \log\log R.H_-,
\]
\[
\log\log R.H_+ \le 2e^{1.6A},
\]
such that the cushion
\[
32\,K_{\mathrm{vt}}\bigl(\mathrm{KlevF}(A),\,
  \lceil (\log R.H_+)^{12}\rceil\bigr)
\;+\; 32\bigl(2\log \mathrm{flatDoorM}(A) + \log 4 + 50\bigr)
\;\le\; \tfrac{1}{4}\log R.H_+
\]
implies $\lnot\,\mathrm{logChowla2Fails}(R.\varepsilon, R.x, R.\omega)$.
The caller supplies only $A_0$: there is no outer hypothesis, and no named
predicate is carried --- the cushion is an explicit inequality in the theorem's
own data, stated where it is used.
\leanname{Salt.MR.logChowla2\_ineffective\_v6
(Salt/MR/RegisterCompose.lean:345)}
\end{corollary}

The price of the whole chain is stated in the name. $A$ is produced by
constants bound through \texttt{Classical.choice}, so no numeral for the
witnessed scale can be read off the theorem; that, and nothing else, is what
``ineffective'' means here, and it is the price the analytic literature pays
for every consequence of Siegel's theorem.

\subsection{The five riders, by nature}
Corollary~\ref{cor:v6} carries five hypotheses. Three of them are met by
objects the corpus exhibits.

\begin{itemize}
\item $e^{-100} \le c_s$. The constant is minted in closed form; every leaf on
  its chain is a literal. Its value is $c_s = 3.716\cdot10^{-11}$ against
  $e^{-100} = 3.72\cdot10^{-44}$: $33$ orders of room, and the inequality at
  the closed form is kernel-checked.
  \leanname{Salt.MR.cs\_closed\_form\_ge\_exp\_neg\_hundred
  (Salt/MR/RiderTrace.lean:132)}
  It is carried rather than discharged because the constant travels through
  eleven pass-throughs between its mint and this statement, and rethreading
  them is a separate wave.
\item $T_0 \le \exp(\tfrac12\sqrt{\mathrm{flatDesignBase}(A)})$. At
  $A \ge 162$ the rider admits $T_0$ up to $e^{\frac12 e^{6.4\cdot10^{224}}}$,
  and the corpus's own witness sits at $e^{e^{1251}} = e^{10^{543}}$: two
  exponential levels of clearance (\S\ref{sec:pearl-defects}).
\item $\mathrm{XCeilRiderStrict}(\varepsilon, g)$. This is a condition on the
  function the caller writes down, not on any object the theorem produces. The
  constant request $g \equiv 0$ satisfies it, as does the request the
  composition itself supplies; the budget is $(31/\varepsilon)H_+$ against a
  builder scale of $1.386\,\varepsilon^{2}H_+$.
\end{itemize}

\noindent The other two are one obstruction, appearing twice.

\begin{itemize}
\item $e^{-100} \le K_s$. This is a fixed numeral against a fixed
  constant whose content is Siegel-ineffective: nothing in the corpus or in
  the literature bounds it from the side the rider needs.
\item The $K_{\mathrm{vt}}$ cushion. The constant has three legs. Two are
  closed form, of sizes about $44.8$ and $2.7\cdot10^{43}$. The third bottoms
  out at the minimum of $\|L(s,\chi)\|$ on a compact box, taken uniformly over
  the moduli $q \le \lceil(\log H_+)^{12}\rceil$ that the socket's own arc
  bound admits. That minimum exists by the extreme-value theorem and is not
  effective in $q$ --- and the declaration that supplies it says so at the
  statement.
  \leanname{Salt.MR.LFunction\_band\_lower (Salt/MR/SiegelArm.lean:161)}
  This is the single point at which Siegel's ineffective constant enters, and
  the cushion is therefore carried by name, with the trace behind it rather
  than a bound. At the terminal's own regime the cushion admits
  $K_{\mathrm{vt}}$ up to $e^{518}/128 \approx 10^{222}$.
\end{itemize}

\subsection{What the corollary does not say}
Three readings are excluded by the statement itself, and we exclude them here
so that no reader has to reconstruct the exclusion. It does not say that
\eqref{eq:chowla} is proved: the conclusion is a denial of failure at one
witnessed window of one produced regime, at every depth. It does not say
``unconditional'' without qualification: five hypotheses stand, and they are
listed above. And it gives no numeral --- not for $A$, not for the window base,
not for the outer scale --- because the design constant is chosen classically.

What it does say is that the road from the entropy decrement to a witnessed
Chowla regime, with the Siegel windows removed by the classical limit, the band
lane's constant removed by its own $A$-window, three numerals removed at their
leaves, the base-scale cap removed at the raised lever, the outer-scale ceiling
removed by threading, and the co-factor supply removed by repairing its
register, now ends at one place. The wall the campaign met in July, and did not
pass, is the only hypothesis left standing inside the statement: Siegel's
ineffective constant, named twice.

% ============================================================================
\section{Method}\label{sec:method}
% ============================================================================

This section records the working mode that produced the results,
because part of what this paper reports is that the mode exists. The
mathematics was developed by AI agents (Claude: the Fable~5 and Opus
models) working under the author's direction, with the Lean kernel as
the referee of record; every step below is a dated entry in the
repository's ledger or a commit in its history, and both are part of
the archived sources.

\emph{Statements are frozen before proofs are attempted.} Every theorem
in this development began as a design block that fixed its statement,
its hypotheses, and the shapes of its constants before any proof search
was allowed to touch it. A frozen statement is then attacked by
independent refuters --- agents whose standing instruction is to refute,
each carrying assigned kill-checks --- and executors are forbidden to
alter a frozen statement to make a proof go through: an executor that
discovers a statement unprovable is required to stop and report, and
the report is a ledger entry, not a failure. The pearl is the worked
example. Its hypothesis list survived three adversarial passes --- a
non-vacuity pass, a satisfiability pass at the objects the theorem
itself produces, and a delta pass over the final supply chain --- and
the passes found two defects that an axiom audit could not have seen:
the rider $T_0 \le e^{e^{100}}$, true as a proposition but unreachable
along the corpus's own proof, and the base-scale cap, false at the
regime the theorem produces, refuted by the toll's own lower bound.
\S\ref{sec:pearl-defects} records both; both were repaired at the level
of the statement, on the day they were found.

\emph{The toll was found by an audit, not transcribed from the
literature.} On 31~July~2026 the campaign faced what its ledger then
called a single mountain: the entropy budget demanded
$\log\log\log H \ge 6.5\cdot10^{19}$ while the register permitted
$\log\log\log H \le 6.24$. The demand audit that priced this deficit
produced the master law (Theorem~\ref{thm:master}) the same evening ---
the ledger's verdict, dated 19:43 that day, reads ``the mountain is a
pricing choice'' --- and the flat tower with it. The shape-free toll
(Theorem~\ref{thm:shapetoll}) was produced two days later by a
different instrument: a hunt commissioned to kernelize the wall
statement should two parallel hunts come back hard, and it returned the
width law as a theorem instead. The claim we make here is about the
mode and not about the novelty of the computation: a two-sided law came
out of a demand audit that a prose development has no reason to
perform, because a prose development is never billed.

\emph{The three walls have names and dates.} Between 17:17 on 31~July
and 12:49 on 1~August the development met three apparent walls, and for
roughly fifty minutes on the morning of 1~August all three were open at
once. The first was the triple-exponential budget floor above. The
second was a frame constant at the door: a numeral $3.9\cdot10^{9}$
against which the flat road's width demand had no fixed point, with a
computed deficit near $10^{49}$. The third was a window on the design
constant: the surviving arm of the first wall, re-expressed in the flat
coordinate, demanded $A \le 86.6$ while the budget demanded
$A \approx 8\cdot10^{30}$. Two of the three dissolved on re-reading,
and the ledger's verdicts say so in their own words: the budget floor
because ``the mountain is a pricing choice,'' and the frame constant
because it was not a frame constant at all --- the numeral is
$\tfrac{1}{12}A(M)\log 2$ rounded down by $1.8\%$, so ``the anchor is
the door,'' and the $10^{49}$ deficit was an artifact of the witness
window. The third did not dissolve. As posed it was proved impassable
--- the kernel holds a floor $A \ge 1.7\cdot10^{6}$ against the
window's $86.6$ --- and it moved only when a frozen base pin, chosen
for the old road, was unfrozen and the door re-cut, at the cost of a
rebuild measured in thousands of lines. Its coordinate is still visible
in Theorem~\ref{thm:pearl}: the floor $162 \le A$, the design law
$3.2A \le \log\log R.H_-$, the pinned base
$\lceil e^{e^{3.2A}}\rceil$, and the two double-exponential windows.
The first and third walls are the same wall in two coordinates; the
second was independent. The general lesson is the one
\S\ref{sec:toll} stated: an apparent wall in this method is worth
re-reading as an invoice in the wrong coordinate before it is treated
as a height --- and the size of a deficit is evidence of its nature,
since the true walls of this development are tight, while a deficit of
$10^{19}$ in a triple logarithm is bookkeeping. The ledger's entry for
that law, dated 31~July, carries the author's own remark, which we
preserve because it is the section's thesis in one sentence: ``do you
not see the irony, the largest giant is but a windmill.''

\emph{Some walls stayed walls.} A method section that lists only its
successes is not evidence, so we state plainly what this method did not
get past. The Siegel obstruction is carried exactly where
\S\ref{sec:pearl} and \S\ref{sec:limit} name it: the threshold $x_0$
and the floor arm $H_{\mathrm{opq}}$ ride as double-exponential windows
in Theorem~\ref{thm:pearl}, and after every removal \S\ref{sec:limit}
performs, the pair that survives in the final corollary is of the same
genre. The band-lane constant is carried as a hypothesis whose stated
numeral, $\log C_{\mathrm{band}} \le 40$, is not payable as stated:
the trace of every existential hop that mints it puts it near
$22\,661$, and \S\ref{sec:limit} records what the window that replaced
it costs. Two constants are exported with positivity alone ($\beta$ and
$C_q$), and their numeric traces are open. The method found these walls
quickly and priced them exactly; it did not move them.

\emph{The development keeps an error ledger.} The ledger is an
append-only record of every design error caught, numbered and dated,
and the file is part of the archived sources. Its headline measurement
is that no design error of the campaign was first discovered by the
kernel: every one was caught by a refuter, an audit, or a neighboring
theorem --- once by this paper's own Theorem~\ref{thm:toll-intro} ---
and the kernel's role was to certify the repairs. The kernel is the
floor of this method, not its net; what the floor guarantees is that an
error which survives every instrument above it still cannot become a
theorem.

% ============================================================================
\section*{Acknowledgments}
% ============================================================================
This work was conducted entirely in the author's personal capacity, on
personal time and personal equipment, and used no confidential or
proprietary information of any employer. The views, findings, and
conclusions expressed here are the author's own and do not represent
the views, positions, or endorsement of the author's employer or of any
other organization. The collaboration with Claude (Anthropic's Fable~5
and Opus models) is described in \S\ref{sec:method} and recorded
commit-by-commit in the repository; it likewise implies no position or
endorsement on the part of Anthropic. The author directed and ratified
every design decision and bears sole responsibility for the claims. The
formalization builds on \textsf{mathlib}, and this work stands on the
shoulders of that community.

% ============================================================================
\appendix
\section{The formal-statement index}\label{app:index}
% ============================================================================
% At release: the axiom column is regenerated from the All.lean
% #audit_axioms blocks, exactly as in the flagship. ``The three'' below
% records the corpus-wide audit at the sealed state (build 9674/9675,
% exit 0). Pearl rows filled 2026-08-04 against the tree at b36f53a; the
% two headline signatures are transcribed in Appendix B.

\begin{table}[h]
\centering\small
\begin{tabular}{lll}
\toprule
Statement & Declaration & Axioms \\
\midrule
\multicolumn{3}{l}{\emph{The toll} (\S\ref{sec:toll}, \S\ref{sec:flat});
file \texttt{Salt/Entropy/Chowla/TowerFlat.lean} unless noted} \\
\midrule
Def~\ref{def:flattower} & \leaninline{chowlaTowerFlat} & --- \\
Def~\ref{def:flattower} & \leaninline{towerDropSumFlat} & --- \\
Def~\ref{def:potential} & \leaninline{flatC, towerLF, towerLamF, towerWF} & --- \\
Thm~\ref{thm:master} (lower) & \leaninline{towerDropSumFlat\_ge\_log\_ratio} & the three \\
Thm~\ref{thm:master} (upper) & \leaninline{towerDropSumFlat\_le\_log\_ratio\_mul} & the three \\
Thm~\ref{thm:tollge} & \leaninline{towerFlat\_width\_ge} & the three \\
Thm~\ref{thm:tollle} & \leaninline{towerFlat\_width\_le} & the three \\
Prop~\ref{prop:crossing} & \leaninline{towerFlatJstar} (def) & --- \\
Prop~\ref{prop:crossing} & \leaninline{towerFlat\_crossing\_at\_Jstar} & the three \\
Prop~\ref{prop:crossing} & \leaninline{towerFlat\_crossing\_exists} & the three \\
Prop~\ref{prop:crossing} & \leaninline{towerFlatJmin} (def), \leaninline{towerFlatJmin\_spec} & the three \\
Prop~\ref{prop:crossing} & \leaninline{towerFlatJmin\_pos} & the three \\
\S\ref{sec:toll} minimality & \leaninline{towerDropSumFlat\_le\_log\_two\_of\_lt\_Jmin} & the three \\
Cor~\ref{cor:export} & \leaninline{towerFlat\_width\_export}
  (\texttt{TowerFlatExport.lean}) & the three \\
Cor~\ref{cor:export} engine & \leaninline{flatWidth\_coef, flatWidth\_engine\_at,} & the three \\
 & \leaninline{flatWidth\_engine} (\texttt{TowerFlatExport.lean}) & \\
(H3) is a theorem & \leaninline{flatFloor\_of\_design}
  (\texttt{TowerFlatRegime.lean}) & the three \\
Lem~\ref{lem:floorprice} & \leaninline{flatStep\_logMul\_le, flatStep\_logMul\_ge} & the three \\
Prop~\ref{prop:inv} & \leaninline{towerFlat\_inv} & the three \\
Lem~\ref{lem:step} & \leaninline{flatStep\_dw\_le, flatStep\_dw\_ge} & the three \\
\eqref{eq:stepbracket} at level $j$ & \leaninline{towerStepF\_dw\_le, towerStepF\_dw\_ge} & the three \\
\S\ref{sec:flat} tower floor & \leaninline{flatMul\_ge\_two,
  chowlaTowerFlat\_base\_floor} & the three \\
\S\ref{sec:flat} glyph step & \leaninline{towerLF\_succ, towerLF\_ge\_add} & the three \\
\S\ref{sec:flat} level bounds & \leaninline{towerLF\_ge\_fifteen,
  towerLamF\_ge\_two,} & the three \\
 & \leaninline{flatLevel\_s\_ge\_three} & \\
\S\ref{sec:toll} threshold funding & \leaninline{flat\_hkey} & the three \\
\S\ref{sec:toll} budget endpoints & \leaninline{entropy\_per\_symbol\_le,}
  & the three \\
 & \leaninline{entropy\_nonneg\_per\_symbol} (\texttt{Endpoints.lean}) & \\
\S\ref{sec:toll} telescoping & \leaninline{tower\_telescopeFlat}
  (\texttt{DecrementFlat.lean}) & the three \\
\S\ref{sec:toll} the other axis & \leaninline{tower\_loglog\_ge,
  tower\_loglog\_le} (\texttt{TowerExport}) & the three \\
\S\ref{sec:toll} the two floors & \leaninline{budgetFloor}
  (\texttt{BudgetCore}), \leaninline{budgetFloorFlat,} & --- \\
 & \leaninline{budgetAFlat} (\texttt{BudgetFlat.lean}) & \\
\midrule
\multicolumn{3}{l}{\emph{The shape-free toll} (\S\ref{sec:toll});
file \texttt{Salt/Entropy/Chowla/TowerShape.lean}} \\
\midrule
Thm~\ref{thm:shapetoll} schedule & \leaninline{chowlaTowerShape,
  towerDropSumShape} (defs) & --- \\
Thm~\ref{thm:shapetoll} glyphs & \leaninline{towerLS, towerLamS, towerWS,
  towerMulS} (defs) & --- \\
Thm~\ref{thm:shapetoll} & \leaninline{towerShape\_width\_ge} & the three \\
Thm~\ref{thm:shapetoll} telescope & \leaninline{towerShape\_dropSum\_le} & the three \\
Thm~\ref{thm:shapetoll} invariant & \leaninline{towerShape\_inv} & the three \\
Thm~\ref{thm:shapetoll} floor price & \leaninline{shapeStep\_logMul\_ge\_abs,
  shapeStep\_logMul\_ge} & the three \\
Thm~\ref{thm:shapetoll} per-step & \leaninline{shapeStep\_dw\_ge,
  towerStepS\_dw\_ge} & the three \\
Cor~\ref{cor:extremal} tie & \leaninline{chowlaTowerShape\_const, towerLS\_const,}
  & the three \\
 & \leaninline{towerLamS\_const, towerWS\_const, towerDropSumShape\_const} & \\
Cor~\ref{cor:extremal} & \leaninline{towerShape\_flat\_le} & the three \\
\S\ref{sec:toll} schedule floor & \leaninline{shapeMul\_ge\_two,
  chowlaTowerShape\_base\_floor,} & the three \\
 & \leaninline{towerLS\_ge\_fifteen, towerLamS\_ge\_two, towerLS\_succ} & \\
\midrule
\multicolumn{3}{l}{\emph{The pearl} (\S\ref{sec:pearl}, \S\ref{sec:limit});
namespace \leaninline{Salt.MR}, file \texttt{Salt/MR/} unless noted} \\
\midrule
\S\ref{sec:pearl} target & \leaninline{Salt.Entropy.Chowla.logChowla2Fails}
  (def) & --- \\
 & (\texttt{Salt/Entropy/Chowla/ChowlaFailure.lean:59}) & \\
\S\ref{sec:pearl} regime & \leaninline{Salt.Entropy.Chowla.ChowlaRegime},
  \leaninline{ChowlaRegimeFlat} & --- \\
 & (\texttt{Entropy/Chowla/Regime:56}, \texttt{TowerFlatRegime:122}) & \\
\S\ref{sec:pearl} base & \leaninline{Salt.Entropy.Chowla.flatDesignBase},
  \leaninline{flatWitFloor,} & --- \\
 & \leaninline{flatDoorM} (defs; \texttt{TowerFlatBuilder:95}, \texttt{S15SelLinear:201}) & \\
Thm~\ref{thm:pearl} & \leaninline{logChowla2\_witnessed\_scale\_flat\_L\_v2}
  & the three \\
 & (\texttt{S16FlatFinal.lean:136}) & \\
Thm~\ref{thm:pearl} width & \leaninline{flatWitFloor\_design,
  flat\_L\_width\_priced} & the three \\
\S\ref{sec:pearl-hyp} predicates & \leaninline{S16BaseScaleCap96\_L\_gk,
  S16CofactorSupply\_L\_gk} & --- \\
 & (defs, \texttt{S13CapGateLinear.lean:891,897}) & \\
\S\ref{sec:pearl-hyp} band lane & \leaninline{S16BandLaneCBoundedL} (def,
  \texttt{S16FlatTerminalLinear.lean:49}) & --- \\
\S\ref{sec:pearl-repair} lever & \leaninline{kcap, KlevF} (defs),
  \leaninline{KlevF\_le\_wideCeiling} & the three \\
 & (\texttt{KLever.lean:142,147,172}) & \\
\S\ref{sec:pearl-repair} cap & \leaninline{s16\_baseScaleCap96\_L\_at\_klevF}
  (\texttt{KLever.lean:427}) & the three \\
Thm~\ref{thm:pearl-recut} &
  \leaninline{logChowla2\_witnessed\_scale\_flat\_L\_v2\_} & the three \\
 & \leaninline{uniform\_win\_xceil\_cqhoist}
  (\texttt{RegisterCompose.lean:209}) & \\
\S\ref{sec:pearl-repair} rider & \leaninline{XCeilGate, XCeilRiderStrict}
  (defs, \texttt{XThread:68,82}; & --- \\
 & ns.\ \leaninline{Salt.Entropy.Chowla}) & \\
\S\ref{sec:limit} hoist (7) &
  \leaninline{flat\_head\_uniform \ldots
  logChowla2\_witnessed\_} & the three \\
 & \leaninline{scale\_flat\_L\_v2\_uniform} (\texttt{S16Uniform.lean}) & \\
\S\ref{sec:chain} step 1 & \leaninline{logChowla2\_ineffective}
  (\texttt{S16Uniform.lean:1061}) & the three \\
\S\ref{sec:chain} step 2 & \leaninline{logChowla2\_ineffective\_v2}
  (\texttt{S16Uniform.lean:1707}) & the three \\
\S\ref{sec:chain} step 3 & \leaninline{logChowla2\_ineffective\_v3}
  (\texttt{S16Compose.lean:1112}) & the three \\
\S\ref{sec:chain} step 4 & \leaninline{logChowla2\_ineffective\_v4}
  (\texttt{S16ComposeV4.lean:1002}) & the three \\
\S\ref{sec:chain} step 5 & \leaninline{logChowla2\_ineffective\_v5}
  (\texttt{XThread.lean:1367}) & the three \\
\S\ref{sec:chain} refutation & \leaninline{cofkL\_bulk\_false\_at\_socket}
  (\texttt{RegisterInhabit.lean:260}) & the three \\
\S\ref{sec:chain} repair & \leaninline{cofkR\_cofactorSupply\_L\_gk}
  (\texttt{RegisterRepair.lean:476}) & the three \\
Cor~\ref{cor:v6} & \leaninline{logChowla2\_ineffective\_v6}
  (\texttt{RegisterCompose.lean:345}) & the three \\
\S\ref{sec:limit} riders & \leaninline{cs\_closed\_form\_ge\_exp\_neg\_hundred}
  & the three \\
 & (\texttt{RiderTrace.lean:132}) & \\
\S\ref{sec:limit} Siegel leg & \leaninline{LFunction\_band\_lower}
  (\texttt{SiegelArm.lean:161}) & the three \\
\bottomrule
\end{tabular}
\caption{``The three'' $=$ \leaninline{propext, Classical.choice,
Quot.sound}. Toll rows and pearl rows are both complete as of commit
\leaninline{b36f53a} (2026-08-03); line numbers are as of that commit and the
source is the arbiter wherever a rendering here and a declaration could be read
apart. The two headline declarations are transcribed in
Appendix~\ref{app:signatures}. The full index is generated at release.}
\label{tab:index}
\end{table}

\begin{table}[h]
\centering\small
\begin{tabular}{lll}
\toprule
Conditional statement & Residual hypothesis & Status \\
\midrule
Thm~\ref{thm:pearl} & band-lane constant ($\log C \le 40$); & landed;
  cap arm false \\
 & 2 Siegel windows $+$ 6 numeric riders; & at the produced \\
 & 2 carried predicates & regime \\
Thm~\ref{thm:pearl-recut} & band-lane $A$-window; 5 riders; the
  $g$-rider; & landed \\
 & 2 carried predicates, at a free lever & \\
\S\ref{sec:chain} step 1 & band-lane constant; 6 riders; 2 predicates
  & landed \\
\S\ref{sec:chain} step 2 & 6 riders; 2 predicates & landed \\
\S\ref{sec:chain} step 3 & 3 riders; 2 predicates & landed \\
\S\ref{sec:chain} step 4 & 3 riders; $x$-ceiling; co-factor supply & landed \\
\S\ref{sec:chain} step 5 & 3 riders; $g$-rider; co-factor supply & landed \\
Cor~\ref{cor:v6} & $c_s$, $T_0$, $K_s$; $g$-rider; $K_{\mathrm{vt}}$ cushion
  & landed \\
\bottomrule
\end{tabular}
\caption{Every conditional in this paper appears here with its exact
named residual.
Nothing in \S\ref{sec:toll} or \S\ref{sec:flat} is conditional: the toll
is unconditional given its three stated hypotheses (H1)--(H3), which are
hypotheses on the tower's parameters, not on the theory. Theorem~\ref{thm:pearl}
is a true statement whose inner implication has a false antecedent at the
regime it produces (\S\ref{sec:pearl-defects}); its row is kept because the
repair line runs through it. Of Corollary~\ref{cor:v6}'s five residuals, three
are met by objects the development exhibits and two are the Siegel
obstruction.}
\label{tab:conditional}
\end{table}

% ============================================================================
\section{The two headline signatures}\label{app:signatures}
% ============================================================================
% Transcribed from the tree at commit b36f53a (2026-08-03): each block is the
% declaration's source lines with the proof body cut. Widths are <= 87
% characters, inside the 93 that fits the measure at \footnotesize. Do not
% hand-edit; re-read the source to refresh.

The two declarations that \S\ref{sec:pearl} and \S\ref{sec:limit} are written
around are given here in full, so that the renderings above can be checked
against them. Each block is the declaration's source with the proof body cut.
Both close over the three axioms.

\medskip\noindent\textbf{Theorem~\ref{thm:pearl}} (the pearl). Source
\leaninline{Salt/\allowbreak MR/\allowbreak S16FlatFinal.lean:136}; the audit is at
\leaninline{Salt/\allowbreak MR/\allowbreak All.lean:7205}. The eight riders are the eight arrows of
the second parenthesised block; the two carried predicates are the last two.
\leaninline{flatDesignBase A} is $\lceil e^{e^{3.2A}}\rceil$ and
\leaninline{flatWitFloor} is the road's witness floor, which the conjunct
before that block pins to it.
{\footnotesize\begin{alltt}
theorem logChowla2_witnessed_scale_flat_L_v2 (hband : S16BandLaneCBoundedL 32000000)
    (A₀ : ℝ) (hA₀ : 162 ≤ A₀) :
    ∃ (ε : ℚ) (Cg Kc δ₀ Ct A β : ℝ) (x₀ Hopq Mfl : ℕ) (Cq cs T₀ Kq Ks C : ℝ),
      0 < ε ∧ 1 ≤ Cg ∧ 0 < Kc ∧ 0 < δ₀ ∧ 0 < Ct ∧ 1 ≤ Mfl ∧
      0 < Cq ∧ 0 < cs ∧ 3 ≤ T₀ ∧ 0 < Kq ∧ 0 < Ks ∧ 0 < C ∧ Real.log C ≤ 40 ∧
      Cg ≤ 2 * 10 ^ 12 ∧ 1 / 500 ≤ ε ∧ 1 / 838400 ≤ δ₀ ∧ Mfl ≤ 2 ^ 355 ∧
      0 < β ∧ 162 ≤ A ∧ A₀ ≤ A ∧ budgetAFlat (ε : ℝ) β ≤ A ∧
      (Hopq ≤ flatDesignBase A → flatWitFloor ε β A Hopq = flatDesignBase A) ∧
      (Kc ≤ 2 ^ 539 → Ct ≤ 2 ^ 23 →
        (x₀ : ℝ) ≤ Real.exp (Real.exp (3.2 * A) / 10) →
        Hopq ≤ flatDesignBase A →
        Real.exp (-100) ≤ cs → T₀ ≤ Real.exp (Real.exp 100) → Kq ≤ Real.exp 100 →
        Real.exp (-100) ≤ Ks →
        ∀ g : ℕ → ℕ → ℕ, ∃ R : ChowlaRegime,
          R.eps = ε ∧ R.Hlo = flatWitFloor ε β A Hopq ∧ g R.Hhi R.ω ≤ R.x ∧
          (50 ≤ Real.log (Real.log (R.Hlo : ℝ)) →
            Real.log (Real.log (R.Hhi : ℝ))
              ≤ Real.exp (Real.log (Real.log (R.Hlo : ℝ)) / 2)) ∧
          3.2 * A ≤ Real.log (Real.log (R.Hlo : ℝ)) ∧
          Real.log (Real.log ((R.Hhi : ℕ) : ℝ)) ≤ 2 * Real.exp (3.2 * A / 2) ∧
          (S16CofactorSupply_L_gk 32000000 Cq R (flatDoorM A) →
            S16BaseScaleCap96_L_gk 32000000 R (flatDoorM A) →
              ¬ logChowla2Fails R.eps R.x R.ω))
\end{alltt}}

\medskip\noindent\textbf{Corollary~\ref{cor:v6}} (the classical limit). Source
\leaninline{Salt/\allowbreak MR/\allowbreak RegisterCompose.lean:345}; the audit is at
\leaninline{Salt/\allowbreak MR/\allowbreak All.lean:7736}. There is no hypothesis to the left of the
colon but the depth \leaninline{A₀}, and no predicate in the conclusion: the
five residuals are the three arrows \leaninline{cs}, \leaninline{T₀},
\leaninline{Ks}, the caller-side \leaninline{XCeilRiderStrict ε g}, and the
final \leaninline{Kvt} cushion. \leaninline{arcDen 12 R.Hhi} is
$(\log H_+)^{12}$, the modulus ceiling the socket's arc bound admits.
{\footnotesize\begin{alltt}
theorem logChowla2_ineffective_v6 (A₀ : ℝ) :
    ∃ (ε : ℚ) (Cg Kc δ₀ Ct A β : ℝ) (Mfl : ℕ) (Cq cs T₀ Kq Ks C : ℝ) (Kvt : ℕ → ℕ → ℝ),
      0 < ε ∧ 1 ≤ Cg ∧ 0 < Kc ∧ 0 < δ₀ ∧ 0 < Ct ∧ 1 ≤ Mfl ∧
      0 < Cq ∧ 0 < cs ∧ 3 ≤ T₀ ∧ 0 < Kq ∧ 0 < Ks ∧ 0 < C ∧ Real.log C ≤ 40 ∧
      Cg ≤ 2 * 10 ^ 12 ∧ 1 / 500 ≤ ε ∧ 1 / 838400 ≤ δ₀ ∧ Mfl ≤ flatDoorM A ∧
      0 < β ∧ 162 ≤ A ∧ A₀ ≤ A ∧ (∀ K Qm : ℕ, 0 ≤ Kvt K Qm) ∧
      (Real.exp (-100) ≤ cs →
        T₀ ≤ Real.exp (Real.sqrt ((flatDesignBase A : ℕ) : ℝ) / 2) →
        Real.exp (-100) ≤ Ks →
        ∀ g : ℕ → ℕ → ℕ, XCeilRiderStrict ε g → ∃ R : ChowlaRegime,
          R.eps = ε ∧ R.Hlo = flatDesignBase A ∧ g R.Hhi R.ω ≤ R.x ∧
          (50 ≤ Real.log (Real.log (R.Hlo : ℝ)) →
            Real.log (Real.log (R.Hhi : ℝ))
              ≤ Real.exp (Real.log (Real.log (R.Hlo : ℝ)) / 2)) ∧
          3.2 * A ≤ Real.log (Real.log (R.Hlo : ℝ)) ∧
          Real.log (Real.log ((R.Hhi : ℕ) : ℝ)) ≤ 2 * Real.exp (3.2 * A / 2) ∧
          (32 * Kvt (KlevF A) ⌈arcDen 12 R.Hhi⌉₊
              + 32 * (2 * Real.log ((flatDoorM A : ℕ) : ℝ) + Real.log 4 + 50)
            ≤ Real.log (R.Hhi : ℝ) / 4 →
            ¬ logChowla2Fails R.eps R.x R.ω))
\end{alltt}}

\subsection*{Availability of the formal sources}
The Lean sources for every declaration cited in this paper are
archived as a curated, self-contained snapshot at Zenodo
(DOI: \texttt{10.5281/zenodo.TBD}); the same archive accompanies the
arXiv submission as ancillary files. The archive pins the toolchain
(Lean~4, \texttt{v4.32.0-rc1}, with mathlib at the same tag), builds
with two commands (\texttt{lake exe cache get; lake build}), and
carries the build-time axiom audit whose whitelist is exactly
\leaninline{propext, Classical.choice, Quot.sound}. Declaration names
in the text are the stable identifiers; file:line citations refer to
the archived snapshot.

\begin{thebibliography}{99}
\bibitem{TaoChowla} T.~Tao, \emph{The logarithmically averaged Chowla and
Elliott conjectures for two-point correlations}, Forum Math.\ Pi
\textbf{4} (2016).
\bibitem{MR2016} K.~Matom\"aki and M.~Radziwi\l\l, \emph{Multiplicative
functions in short intervals}, Ann.\ of Math.\ \textbf{183} (2016).
\bibitem{MRT} K.~Matom\"aki, M.~Radziwi\l\l, and T.~Tao, \emph{An
averaged form of Chowla's conjecture}, Algebra Number Theory \textbf{9}
(2015).
\bibitem{Chowla} S.~Chowla, \emph{The Riemann hypothesis and Hilbert's
tenth problem}, Gordon and Breach, 1965.
\bibitem{mathlib} The mathlib community, \emph{The Lean mathematical
library}, CPP 2020.
\bibitem{fulcrum} J.~Hickey, \emph{Twin primes and Siegel zeros: a
fulcrum}, in preparation.
\bibitem{companion} J.~Hickey, \emph{The Salt method}, in preparation.
\end{thebibliography}

\end{document}
